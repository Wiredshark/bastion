# Project Bastion — DESIGN STATUS (the living design-frontier map)

The "what's designed / designing / still `[LEDGER]`" map for the **GENERAL DESIGNER** workflow
(`readme/GENERAL-DESIGNER-prompt.md`). Kept current so an amnesiac or parallel design session resumes
correctly. Companion to `readme/DESIGN_PASS_LOG.md` (the append-only claim/done ledger) and
`readme/df-feature-gap-ledger.md` (the full DF-* inventory). Build frontier: `BASTION_ARCHITECTURE.md §6`.

> ## ⛔ DESIGN FRONTIER GENUINELY COMPLETE — LANE HELD (updated 2026-07-10, architect-confirmed)
> The whole design frontier is COMPLETE: **~21 systems + the caves investigation + the 4-pass CONTROL SURFACE**
> (UI-1 invoke · UI-2 see · UI-3 do · UI-4 read/inspect). Nothing near-frontier is left to claim. The lane is
> **HELD** — the architect RE-FIRES only on: **(a)** Ben brings the next frontier · **(b)** the play-tester starts
> surfacing verb/UI gaps (→ feed UI-3 as the living list) · **(c)** DF-FLUID gets attempted (→ design **DF-MAGMA**
> alongside it then, same engine/lava kind). Meanwhile: **bookkeeping/consolidation only, no autonomous topic-
> picking.** (Earlier queue items — the 5 Ben-routed + the UI epic — all DONE + accepted [flipped DESIGNED].)
> **Rule going forward:** build-once INFRASTRUCTURE engines were in-scope; Tier-2/3 LEAF features DEFER to their
> build era (JIT — cheaper w/ fresh context). **The control surface (UI-1/2/3/4) is the flagged POST-B5.8 build
> priority, verbs+systems built PAIRED, TIME CONTROLS (pause/fast-forward) first.** DF-FLUID = a dedicated careful
> pass WITH Ben (risky $$$), NOT autonomous churn. Available on request: reconcile future-work §-sections
> CONSUMED-BY (the one open consolidation, per DESIGNER-SUGGESTIONS §6).

**Resume point (2026-07-09):** design frontier advancing fast (multiple sessions). **DESIGNED so far:**
DF-PRODUCTION, DF-DIG-VERBS, DF-QUALITY(+DF-ARTIFACT), DF-ZONES, DF-BURROW (this session, clean pause on context
budget — backlog NOT exhausted) + DF-HIST(+DF-LOG), DF-RELIGION (parallel). Two schema locks landed (Quality →
frameworks §2b; ZoneKind → §2 recommended). The **Manage tier is now complete** — DF-ZONES (places, soft-attract)
+ DF-BURROW (places, hard-restrict) + **DF-POLICY (quotas & rules — DF-ORDERS + DF-STANDING, DESIGNED
`DF-POLICY-design.md`)**. **MIND-PAYOFF LOOP NOW FULLY DESIGNED** (DF-ROOMS + DF-QUALITY + DF-FOCUS + B-AG3
= build/make/provide → feel AND perform; pending B7 as shared substrate). **NON-DEFERRED NEAR-FRONTIER BACKLOG ESSENTIALLY EXHAUSTED (2026-07-09).** This designer added 14 passes this
session (DF-ROOMS, DF-FOCUS, BUILD-FRAMEWORK, HAZARD-EVENTS, DF-MECH/TRAP/OPERABLE, DF-LIVESTOCK, DF-MIGRATION
+DF-PRESTIGE, DF-ROT, DF-SYNDROME, DF-JUSTICE, DF-MISSION, DF-POWER + the earlier PRODUCTION/DIG-VERBS/QUALITY/
ZONES/BURROW/CAVERN cluster). **What REMAINS:** (a) **DF-FLUID** — the $$$ physics keystone (unblocks DF-PUMP/
DF-MAGMA/DF-HYDRO/floodgate/flood-hazard/DF-POWER-pump) — needs a DEDICATED careful pass, **architect decision**;
(b) **Tier-3 epics under the DEFER guard** — do NOT design without architect greenlight; (c) systems gated on
in-flight work (DF-MEDICAL on DF-WOUND). **AWAITING ARCHITECT STEER** (see DESIGN_PASS_LOG "BACKLOG STATE" note):
stop / greenlight DF-FLUID / lift a Tier-3 guard / redirect. IN FLIGHT: DF-TAVERN, DF-WOUND — skip.
**DF-CAVERN+DF-GEOLOGY, DF-ROOMS, DF-FOCUS DESIGNED** this session. **DF-WOUND** in
flight (parallel subagent). **DF-TRADE
DESIGNED** (`DF-TRADE-design.md`; TRADE-0..6 — DESIGNED-downstream of the production/founding frontier). Always
check `DESIGN_PASS_LOG.md` for live CLAIMs before starting (parallel sessions run concurrently).

---

## DESIGNED (has a buildable design doc with Done-when sub-blocks)
| Topic | Doc | Sub-blocks | Notes |
|---|---|---|---|
| Founding / Embark (B11) | `FOUNDING-EMBARK-DESIGN.md` | B11.0–B11.8 | pre-existing pass |
| God-Powers (B13) | `GOD-POWERS-CATALOG.md` | verb menu | pre-existing pass |
| **DF-PRODUCTION** (DF-WORKSHOP + DF-CHAIN + DF-FARM + DF-COOK) | **`DF-PRODUCTION-design.md`** | **PROD-0..PROD-5** | this session. Behind B6; farm partly precedes. Refines ledger costs. |
| **DF-HIST** (+ DF-LOG consolidated) | **`DF-HIST-design.md`** | **HIST-0..HIST-6** | Chronicle/Legends — the legibility organ. rtsim data+event-bus = substrate; net-new = persistent player sink + the `record()` capture API (lock first) + feed (DF-LOG) + browser. v1 = HIST-0..2. No 3D/anim. |
| **DF-RELIGION** (temples/worship/prophets/faith seam) | **`DF-RELIGION-design.md`** | **REL-0..REL-5** | Best-fit topic (you ARE the god). Splits: colony tier = mostly WIRE (tavern gather-loop retargeted at a temple + a `worship` field on `Needs` + a `Priest` `Profession` arm + a devotion accumulator) behind **B7**; world faith-politics (convert/rival gods/holy war/festivals) = **NOT DF-RELIGION**, it's Divine-Politics DP2–DP4 (LATE). Direct tier deliberately empty. REL-0 (buildable `faith` zone) precedes B7. Cheapest animation in the ledger (v1 NATIVE). |
| **DF-DIG-VERBS** (stairwell/ramp/channel/shaft-ladder) | **`DF-DIG-VERBS-design.md`** | **DIG-0..DIG-4** | this session. Vertical excavation vocab. Mostly wiring on B5 (worldgen ships ramp/staircase/spiral primitives + Ladder sprite + Climb state). Net-new = designation verbs + reachability-safe top-down decomposer (solves pit-trap) + top-down painting UX. KEY: ramp verb == B5.8 auto-carve-steps (one `carve_ramp` lib). HARD PAIR w/ B5.8. Zero new animation debt. |
| **DF-QUALITY** (+ DF-ARTIFACT apex) | **`DF-QUALITY-design.md`** | **QUAL-0..QUAL-3** | this session. LOCKED canonical quality enum = engine `item::Quality` (frameworks §2b). Net-new = skill→quality craft stamp (fills DF-PRODUCTION S6) + per-instance `craft_quality` field + B-AG3 quality→thought hook + strange-mood event (DF-ARTIFACT faithful: artifact-or-death). HARD dep DF-PRODUCTION S1; reuses B-AG3 (DONE). |
| **DF-ZONES** (typed activity/building zones) | **`DF-ZONES-design.md`** | **ZONE-0..ZONE-2+** | this session. Umbrella schema for §2 activity-zone half (canonicalizes it, load-bearing w/ B5.6b-2). Net-new = locked `ZoneKind` vocab + ONE soft-magnet mechanism (bias-not-command) + thin per-kind wires (most NEEDS-gated on their behavior system). v1 proves w/ Refuse+Gather. DF-BURROW kept separate (hard vs soft policy). |
| **DF-BURROW** (movement-restriction zones) | **`DF-BURROW-design.md`** | **BURROW-0..BURROW-3** | this session. Hard-policy cousin of DF-ZONES (completes the zone-policy pair). Two filters on B4 (job-claim + idle-clamp); the real work is the pillar reframe: a burrow = the god's "Call to Shelter" directive WITH a survival escape valve (critical hunger breaks confinement — the DF safe-room-death-trap designed OUT). Standing mode v1; siege shelter-alert gated B8; survival valve gated B7 (never ship confinement without ≥ a stub override). Zero animation debt. |
| **DF-TRADE** (caravans / depot / haggling — colony↔world economic seam) | **`DF-TRADE-design.md`** | **TRADE-0..TRADE-6** | this session. Veloren ships almost all of it — barter engine (`common::trade`), a supply/demand economy sim (`world::site::economy`, but **frozen at worldgen** = the seam), drivable `Body::Cart`/`Carriage` caravans (vehicle DONE), roaming `Profession::Merchant` AI. Net-new = a **cheap live colony economic node** (surplus/deficit `GoodMap` over DF-PRODUCTION stocks; NPC sites stay frozen, LOD-safe) + `ZoneKind::TradeDepot` (DF-ZONES + B6-haul) + a **two-tier caravan** (§3t real-vs-abstract) + autonomous clear (`SitePrices::balance` + Sentiment) + god tilt (bless-caravan=Buff / curse-roads=Hazard-Events) + trade-route overlay (§3s). Guardrail: god **tilts**, never runs trades (price/dispatch/fleet screen = AVOID). Colony tier near-term; **world faction trade-network = Divine-Politics DP1/DP3/DP5** (TRADE-6 = seam spec only). ALL animation NATIVE. **DESIGNED-downstream** — sits on DF-PRODUCTION+founding+DF-ZONES+B6, sequence after the production cluster. |
| **DF-POLICY** (DF-ORDERS + DF-STANDING) | **`DF-POLICY-design.md`** | **POL-0..POL-4+** | the colony *policy* layer (Manage tier for **quotas & rules** — completes the Manage trio w/ DF-ZONES places-soft + DF-BURROW places-hard). Both halves UNIFY into ONE `ColonyPolicy`: an ORDER = a quota rule ("keep ≥N" → engine emits work to close the gap), a STANDING RULE = a behaviour toggle (forbid-on-death, auto-haul-refuse). **RECONCILED w/ DF-PRODUCTION PROD-1 — does NOT duplicate:** PROD-1's minimal production-pull is the FIRST INSTANCE of this pass's generalized, verb-agnostic order engine; this pass adds the conditional gate + manager panel + the STANDING-RULE half PROD-1 never touched. ONE real net-new substrate = a colony **STOCK CENSUS** (count item-kind across `BastionPile`s), **SHARED with PROD-4's economy readout** (build once, pays twice). ZERO new 3D assets, ZERO new animations (UI-in-code, like DF-HIST). **Guardrail (ledger's #1 4X-drift risk): zero-policy = complete healthy game — asserted in POL-0's load-bearing Done-when; policy = optional thermostat, never mandatory.** Near-frontier (rides B6 + PROD-*). |
| **DF-CAVERN + DF-GEOLOGY** (layered caverns/danger tiers + ore veins/stone layers) | **`DF-CAVERN-GEOLOGY-design.md`** | **CAVERN-0..4 / GEO-1** | this session. Completes the mining-framework §6 line w/ DF-DIG-VERBS. HEAVY reuse: `world/src/layer/cave.rs` already ships `LAYERS=5` depth-layered tunnel-connected caverns + per-depth/temp `Biome` + per-biome `apply_entity_spawns`; ores/gems = mineable sprites w/ drops+hardness. Net-new = legible danger tiers (3 named over the 5 layers) + PROSPECTING/colony-knowledge (discovered≠omniscient — the pillar+tension core) + depth-graded ore (feeds DF-PRODUCTION) + the flagship BREACH EVENT + danger-scaled life. v1 = CAVERN-0+GEO-1 (buildable on shipped worldgen NOW). Near-zero animation debt. **DESIGNED-downstream:** the Breach sits on 3 UNBUILT systems — the hazard-event engine (NOT started), B8, DF-DIG-VERBS. DF-MAGMA DEFERRED (needs DF-FLUID). |
| **DF-ROOMS** (rooms & room-quality from furniture) | **`DF-ROOMS-design.md`** | **ROOM-0..ROOM-3** | this session. The construction→mind bridge. HEAVY reuse: furniture ships (`Furniture` sprite category); B-AG3 thought pipeline DONE; enclosure BFS proven in asset-lab (`castle_verify.py`). Net-new = room detection (BFS enclosure over built walls, event-driven) + role classification (from furniture) + the 4-stat IMPRESSIVENESS model (Wealth=DF-QUALITY craft-quality · Beauty=décor · Space · Cleanliness=DF-ROT) w/ the RimWorld WEAKEST-STAT-DOMINATES rule (the whole legibility+anti-gaming design) + ownership + room→B-AG3 thought hook. **First consumer of DF-QUALITY**; **shared enclosure detection** w/ DF-ZONES/DF-PRODUCTION/DF-RELIGION (build once). Cleanliness soft-gated on DF-ROT. Passive-consequence = clean pillar fit (god provides materials/blessing, never places a rug). ZERO animation debt. Décor asset batch → ASSET_REQUESTS. Closes a leg of the mind-payoff loop; DF-FOCUS is the remaining leg. |
| **DF-FOCUS** (personal needs → focus → work) | **`DF-FOCUS-design.md`** | **FOCUS-0..FOCUS-3** | this session. CONSOLIDATION of agency-bible §5b.2 (the mechanic was already designed there — grounded + made buildable, not re-derived). The loop that makes minds MATTER. Net-new = personal-needs model + a **LOCKED `Need` enum** (Pray/Socialize/Drink/Craft/Family/SeeAnimals/AdmireArt… derived from facets) + the FOCUS scalar (met:unmet → ±50% work, SEPARATE from mood) + self-need-jobs (low yields/high doesn't) + `satisfies(Need)` venue binding. KEY = the locked `Need` vocab is the INTERFACE every venue plugs into (temple→Pray, tavern→Drink, art→AdmireArt…) — DF-FOCUS is WHY the venue corpus matters. Purest indirect-influence fit (god provides venues/blesses, never orders; Answer-a-Prayer miracle). ZERO new assets (consumes venue batches), near-zero anim. FOCUS-0 enum-lock buildable NOW; the ±50% loop is B7-downstream. **CLOSES the mind-payoff loop** (DF-ROOMS + DF-QUALITY + DF-FOCUS + B-AG3 — now fully designed, pending B7). Fold DF-RELIGION worship-need → `Need::Pray`. |
| **BUILD-FRAMEWORK** (the Construction Framework — advanced/autonomous building) | **`BUILD-FRAMEWORK-design.md`** | **BUILD-0..BUILD-6** | this session (architect-directed; sibling to the mining framework §6). UNIFYING INSIGHT: **building UP (additive) + mining DOWN (subtractive) = ONE engine** (place a templated VOLUME → PREP site cut/fill → work-tick → materialize; only the sign differs). HEAVY reuse: structure prefabs RUNTIME-ADDRESSABLE (`structure.rs StructuresGroup::load_group`) — §3c open unknown resolves favorably; asset-lab already produces the catalog; race-keyed sites; B5 Build/Mine; B1.8 sampler; B5.6b volume paint; ClimbSkill. Net-new = the place→prep→work spine + placement solver (POINT/AREA/PATH/LINE-ENCLOSE/VOLUME-DOWN) + site-prep planner (§3x) + VOLUME model + depth modes + **VERTICAL/build-UP construction** (floor-by-floor, inverted pit-trap solved by the SHARED reachability decomposer w/ DF-DIG-VERBS) + catalog tiers (fixed→param→composed piece-pools) + autonomous build/mine. ENRICHES §3c + §3x; FOLDS IN both Ben inputs (autonomous mining zones → BUILD-6; zone-mgmt UI + depth modes → BUILD-3, near-frontier via B5.6b). Piece-pool asset batch → ASSET_REQUESTS. Shared schema: VOLUME model + depth-mode enum. No new anim debt. |
| **HAZARD-EVENTS engine** (§1a — radius-effect + NPC-reaction) | **`HAZARD-EVENTS-design.md`** | **HAZ-0..HAZ-3** | this session. The BLOCKLESS shared engine the future-work finding flagged as highest-leverage — unblocks DF-CAVERN Breach + DF-TRAP effects + DF-FLUID flood + timber/rockfall (all CALLERS/costumes of ONE engine). HEAVY reuse: Veloren `Explosion`/`RadiusEffect` (TerrainDestruction=rockfall, ReplaceTerrain(Lava) ships, Attack/Entity=damage/burns) = the radius-effect primitive; B-AG3 (reaction) DONE; B8 (threat). Net-new = the `HazardEvent{pos,radius,kind,attribution}` façade + THE KEY HALF: the MIND-REACTION hook (kind×proximity×injury×ATTRIBUTION → B-AG3 fear/trauma/grief/grudge/awe — a hazard is a radius-effect AND a mind-event; the same rock = fear vs grief vs grudge-against-the-god by attribution). Fit=PASS (losing is fun; some hazards ARE god wrath ① Miracle, others god mitigates). ZERO new anim debt. Shared schema: `HazardKind` enum + `Cause` attribution. **DF-MECH FIRES this** (effect-engine-before-trigger). KEYSTONE #2 — build EARLY (HAZ-0/1 buildable now on Explosion+B-AG3). Aftermath asset batch → ASSET_REQUESTS. |
| **DF-MECH / DF-TRAP / DF-OPERABLE** (trigger→link→effect) | **`DF-MECH-design.md`** | **MECH-0..MECH-3** | this session. 3 ledger items, ONE build-once engine (§1b+§E consolidation). Trap = trigger→link→(HazardEvent); operable gate = trigger→link→(ScheduledBlockChange). EFFECT half DE-RISKED: traps→HAZARD-EVENTS (this session); operable→`ScheduledBlockChange`+`BlockChange`+sprite-toggle (ship). Build-not-wrap only for the trigger/link layer (Veloren has dungeon traps as CONTENT `HaniwaTrap`, not a place-and-wire system). Net-new = trigger sensing (plate/proximity/tripwire/lever) + the LINK graph (abstract v1) + operable state machine + place-and-wire UX + friend/foe filter. HARD GUARDRAIL: NO real-time fire verb — traps are pre-placed policy firing autonomously on condition (= B8 autonomous defense); god designates/wires + optional divine override. MECH-0 (operable) = safe cheap first proof. Legibility (the link overlay) = make-or-break. Near-zero skeletal anim. DF-POWER = separate pass. Mechanism&trap asset batch → ASSET_REQUESTS. |
| **DF-LIVESTOCK** (animal husbandry) | **`DF-LIVESTOCK-design.md`** | **STOCK-0..STOCK-3** | this session. WIRING-HEAVY on strong substrate: taming/pets ship (`Pet`+`tame_pet`+`SetPetStayEvent`); product chains ARE recipes (hide→leather@TanningRack, wool→cloth, milk→cheese); §3y.A population model designed; DF-ZONES Pasture (this session). Net-new = husbandry jobs (milk/shear/butcher→hide+meat drops feeding the existing chains) + grazing + the population model wired to a herd. KEY (UO lesson): a herd is a bounded POPULATION (logistic growth to pasture capacity + slaughter pressure), not a spawn-timer — SAME model as wild fauna §3y.A (build ONCE, two callers). Lights up DF-ZONES Pasture (was NEEDS:DF-LIVESTOCK) + feeds DF-COOK/DF-PRODUCTION/DF-FOCUS-SeeAnimals. Fit=PASS (autonomous herding; god provides pasture/blesses). Near-zero new anim. Herd asset batch → ASSET_REQUESTS. Consolidates §3y.A. |
| **DF-MIGRATION** (+ DF-PRESTIGE driver) | **`DF-MIGRATION-design.md`** | **MIG-0..MIG-3** | this session. The world grows the colony. SUBSTRATE-strong: rtsim ALREADY migrates NPCs toward a per-site `wanted_population` (`rule/migrate.rs`) — DF-MIGRATION drives it from PRESTIGE. DF-PRESTIGE designed here as the driver (= f(wealth[DF-ROOMS+DF-PRODUCTION+DF-ECON], pop, favor)). Net-new = prestige rating + wire to migration target + petitions/residency (migrant→colonist via B3) + waves + emigration (B-AG3 low-mood colonists LEAVE, not deleted) + THE PAIRED THREAT face. GUARDRAIL: prestige has TWO inseparable faces (migrants IN + bigger threats B8) — ship BOTH or it's a 4X growth dial. Fit=PASS (purest influence-not-command: you never recruit, you make the colony worth joining). NEAR ASSET-FREE + zero anim. Prestige = a shared metric (nobles/threats/DP read it). Ties Divine Politics. |
| **DF-ROT** (decay & hygiene) | **`DF-ROT-design.md`** | **ROT-0..ROT-3** | this session. The build-once DECAY engine ("every accumulation needs a decay"). Unblocks the DF-ROOMS Cleanliness stat (its 4th, was soft-gated on DF-ROT) + gives DF-ZONES Refuse its point + spoilage for DF-COOK/DF-LIVESTOCK. KEY (§0): rot is a TRANSFORM not a delete — `freshness` decays → fresh/spoiled/rotten/inert, CONSERVED (fixes the DeleteAfter silent-delete class); the rotten stage emits MIASMA (a lingering AURA, distinct from one-shot HAZARD-EVENTS) → B-AG3 bad thought + lowers DF-ROOMS Cleanliness. Hygiene loop = auto-haul refuse (DF-ZONES) + bury/burn (DF-RELIGION) + cellar. Substrate: B5.5 piles + DeleteAfter + buffs + B-AG3. Fit=PASS (slow-burn losing-is-fun). Near-zero anim. Rot&hygiene asset batch → ASSET_REQUESTS. Reference decay engine for the LOD law. Seams DF-SYNDROME/DF-TEMP/DF-POLICY. |
| **DF-SYNDROME** (status-effect engine) | **`DF-SYNDROME-design.md`** | **SYN-0..SYN-3** | this session. Build-once affliction engine (sibling to HAZARD-EVENTS: HazardEvents=acute radius-effect, Syndrome=chronic life-cycle). HEADLINE REUSE: Veloren's buff system IS a status-effect engine (`BuffKind`+`BuffEffect` over-time/immunity/conditional) — symptoms ARE buffs. Net-new = syndromes as DATA (RON trigger/incubation/symptoms/transmission/recovery — DF moddable-reaction signature) + the EPIDEMIOLOGY life-cycle (incubate→contagion[bounded R0]→progress→recover+immunity). Behavior syndromes: were-transform = body-swap to existing creature body (NATIVE anim), vampire/madness = B-AG3. CRITICAL SEAM: DF-WOUND infection = a Syndrome CALLER (flag to in-flight DF-WOUND — co-lock not fork). DF-NIGHT (Tier-3) rides SYN-2 — engine de-risks the epic, content stays deferred. Near-zero assets/anim. Seams DF-ROT/DF-MEDICAL/DF-BURROW. Small affliction asset batch → ASSET_REQUESTS. |
| **DF-JUSTICE** (nobles/positions/mandates/law) | **`DF-JUSTICE-design.md`** | **JUST-0..JUST-3** | this session. The "god ≠ ruler" governance layer (frameworks §1 + divine-politics §7 made concrete). SUBSTRATE-strong: rtsim ships `faction.leader` (emergent mortal governor) + `ReportKind::{Theft,Death}` (crime already reported); B-AG3 = crime motive; DF-BURROW = jail; DF-MIGRATION/DF-PRESTIGE = nobles; DF-POLICY = the steward. Net-new = positions/hierarchy + MANDATES (noble demands → satisfy/unrest) + justice response (crime→sheriff→jail/fine/exile → B-AG3 reactions) + law-as-policy + divine reach-ins. FIT=PASS on §0 reframe: god is NEVER the magistrate — colony judges itself; god only influences who leads + reaches in DIVINELY (Pardon/Smite/Depose/Legitimize). Near-asset-free. Reference for "god ≠ ruler". Ties Divine Politics. Authority&justice asset batch → ASSET_REQUESTS. |
| **DF-MISSION** (off-map expeditions) | **`DF-MISSION-design.md`** | **MISS-0..MISS-2** | this session. The god directs the colony OUTWARD. SUBSTRATE-strong: rtsim SHIPS a `Quests` system (`data/quest.rs` — register→create→resolve, actor-related) = the off-map resolver; B3/B10 demote-persist = departure (colonist leaves loaded, lives rtsim-side, returns); targets exist (DF-CAVERN dungeons, B8 threats, DF-ARTIFACT). Net-new = the outward mission designation (type+target+commitment) + selection (B-AG3 courage/loyalty) + mission→rtsim Quest + outcome ripple-back (loot/rescue/reveal/casualties→grief+DF-HIST legend). Fit=PASS (§0): god SETS premise + provisions + blesses; colonists go autonomously; rtsim resolves; god does NOT play the crawl (that's B12's opt-in embody). NEAR ASSET-FREE + zero anim. CO-SEAMS DF-CAVERN adventure-mine (dig-to-reach + delve = one arc); ties B12 mortal-RPG. Types: raid/rescue/retrieve/explore/delve. |
| **DF-POWER** (mechanical power) | **`DF-POWER-design.md`** | **POW-0..POW-2** | this session. The DF-MECH continuous-flow sibling (triggered link vs continuous axle carrying power units). SUBSTRATE: WeatherGrid ships a REAL server-simulated wind field (`Weather.wind`) → a WINDMILL WORKS NOW (buildable ahead of DF-FLUID); worldgen rivers = water-wheel input; DF-MECH = shared linkage; DF-PRODUCTION = powered machines. Net-new = the power GRAPH (supply≥demand over a connected axle network) + generators (windmill/water-wheel/treadmill) + power-gate on machines (millstone→flour). Fit=PASS (designate infrastructure, runs autonomously; god provides mill + blesses wind). Near-zero skeletal anim (gears = operable rotation). **DESIGNED-downstream of DF-FLUID** for the pump/water depth (wind/river/treadmill core is NOT — POW-0 buildable now). Build on DF-MECH's linkage + share the gear asset family. Power asset batch → ASSET_REQUESTS. |
| **UNDERGROUND-LIGHTING** (Ben-routed) | **`UNDERGROUND-LIGHTING-design.md`** | **LIGHT-0..LIGHT-2** | this session. CRITICAL SURVEY ANSWER: darkness is AMBIANCE today (visual + global `day_period.is_dark()`), NOT a local-light mechanic — NPCs already carry+auto-light a lantern (`agent/action_nodes.rs:226`) but sight ignores local luminance. THE DECISION: Option A = LIGHTING-AS-LEGIBILITY (recommend — extend the night-lantern toggle underground + auto-lamp the dig plan [lamplighter drive]; makes the mine WATCHABLE, reuse-heavy) vs Option B = DARKNESS-AS-MECHANIC (optional depth — a local light-level → work-speed/mood; a real subsystem; only if the dark is a PRESSURE). Rec A now, B flagged for Ben. Lovely loop: dwarven Velorite-lamp = the glow-ore you mine IS the light you carry. Near-zero anim. Underground-light asset batch → ASSET_REQUESTS. |
| **DF-STRUCT** (cave-in + mine supports) | **`DF-STRUCT-design.md`** | **STRUCT-0..STRUCT-2** | this session (Ben-routed). THE DECISION resolved: Option A=accept floating (free, current — voxel terrain self-supports) / Option B=DF-grade structural PHYSICS (DF-FLUID-class cost, REJECTED) / **Option C=LIGHTWEIGHT HEURISTIC (RECOMMEND)** — the collapse EFFECT is already free (`HazardKind::CaveIn` on HAZARD-EVENTS), so net-new = just a bounded TRIGGER RULE (unsupported span / deep DF-CAVERN tier → cave-in chance) + supports (dig-plan mitigation). DF cave-in drama WITHOUT DF structural-physics cost. 3 distinct "dig too deep" faces kept separate: Breach / cave-in / flood(DF-FLUID). Supports REUSE the Mine batch. Near-zero anim. |
| **SHIPS-NAVAL framework** (Ben-routed) | **`SHIPS-NAVAL-design.md`** | **NAVAL-0..NAVAL-3** | this session. FLUID REALITY nailed: buoyancy/FLOAT EXISTS (`fluid_dynamics.rs` — hulls float NOW), ship-VEHICLE substrate EXISTS (`body/ship.rs` SailBoat/Galleon + rig), AI-movement precedent EXISTS (`airship_ai`). THE GATE = NAVAL-MOVEMENT (crew+directed sailing, adapt airship AI to water) — a genuine $$-$$$ build, NOT buoyancy. Flowing-fluid = DF-FLUID, SEPARATELY deferred (boats float on static water). Vessel catalog (skiff/cog/barge/warship, extend Body::Ship, race-keyed) + harbor + loaded↔abstract-route split (§3t = DF-TRADE naval tier). ANIMATION debt (the 1 recent pass w/ real new anim): `anim::sail_billow/oar_cycle/flag_flutter/rudder` (NEEDS:animation-code, separable parts, airship-rig ref). Assets viable NOW but INERT until naval-movement. Vessel&harbor asset batch → ASSET_REQUESTS. |
| **HAND-CURSOR / THE GOD-HAND** (Ben-NEW, queue #4) | **`HAND-CURSOR-design.md`** | **HAND-0..HAND-2** | this session. The B&W divine-hand cursor — the god's PHYSICAL presence + signature UX. Substrate: grab-drag pan=B1.5 (the hand grabs the ground), SELECT=B2a, the CARRY=`mounting.rs` `Link`/`Role` attach (grabbed colonist attaches to the hand-anchor), drop=physics+fall-damage, reaction=B-AG3, power+favor=GOD-POWERS. THE PILLAR LINE (§0): SELECT/drag-world = FREE everyday cursor; GRAB/LIFT a colonist = a COSTED, FELT god-power (spends favor; the colonist REACTS — awe/fear/resentment; a high drop HARMS; NOT routine move-order or it's puppet-master micro/AVOID). Animation-forward: 5 named NEEDS:animation-code (`anim::hand_idle`(set)/`grab_ground`/`grab_npc`/`carry`/`release`, one expressive rig). Extends GOD-POWERS-CATALOG (the hand casts the powers). God-hand asset (HIGH PRIORITY, the signature cursor) → ASSET_REQUESTS. |
| **DF-FLUID** (Ben-GREENLIT capstone) | **`DF-FLUID-design.md`** | **FLUID-0..FLUID-3** | this session, FINAL. Real flowing-fluid SIM (flow/fill/seek-level/drain/flood) as an EXPERIMENTAL-FLAG subproject + STATIC-FALLBACK. Survey: bodies-in-fluid physics EXIST (`fluid_dynamics.rs`, boats float) but NO flowing sim (water=static). THE ARCHITECTURE (§0, Ben's "try, else static" = frameworks §10): static water = the FLOOR (base game NEVER at risk, flag off = current game); real fluid = a separate flagged path over a BOUNDED ACTIVE-REGION (small/loaded — NOT the world = the LOD/perf/sync containment); TRY-REAL-FALL-BACK-STATIC (over-budget/unstable → revert gracefully). COST=$$$, MAY NOT SHIP (accepted — fallback loses nothing; every consumer degrades to static). Build the SAFETY (FLUID-0) before the sim (FLUID-1). Unblocks DF-POWER/DF-CAVERN-aquifer/DF-STRUCT-flood(HazardKind::Flood)/DF-FARM-irrig/DF-MECH-floodgate/DF-MAGMA-lava(sibling)/B13-terraform. WITH-BEN careful pass, not autonomous churn. Near asset-free (VFX). |
| **WORLDGEN-CAVES-ORE-DENSITY** (investigation) | **`WORLDGEN-CAVES-ORE-density-investigation.md`** | recommendation | this session, the FINAL investigation. Ben: "a lot of rock, not a lot of mini caves". Survey (cave.rs): caves = 5 deep BIG-TUNNEL layers SUPPRESSED near sites (`chunk.sites.is_empty()` — the #1 culprit); ore = depth-graded but ONLY on cave walls + sparse → solid rock has NO veins (digging strikes nothing). RECOMMEND: TUNE (cheap — lift near-site cave suppression + raise shallow-cave & ore density + starter veins + richer deep) = the near-frontier 80%; ADD (the one real lever) = ore-veins-IN-SOLID-ROCK = the already-designed **DF-CAVERN-GEOLOGY GEO-1** (this is its BUILD JUSTIFICATION). Capability-survey, not a new system. Feeds DF-CAVERN-GEOLOGY tuning-data. |
| **UI-1 GOD-POWERS-ACCESS** (Ben-UI frontier) | **`UI-GOD-POWERS-ACCESS-design.md`** | **UI1-0..UI1-2** | how the player INVOKES god-powers. Reuse: Veloren ships the action bar (`hud/skillbar.rs`+`hotbar`). KEY: powers come in 3 SHAPES (① Miracle=cast/bar-slot+hand-target · ② Blessing=SET/paint-mode · ③ Passive=ambient/status-readout NOT a button) + THE REUSE INSIGHT: the SAME hotbar MORPHS god-powers↔mortal-skillbar on EMBODY (B12) = one UI, dialed involvement. Favor-costed/gated bar + catalog panel/radial + hand for physical powers. Divine-icon asset batch → ASSET_REQUESTS. |
| **UI-2 MISSING-UI-ELEMENTS** (audit) | **`UI-MISSING-ELEMENTS-audit.md`** | audit+recommend | Veloren's HUD = a SOLO-ADVENTURER HUD; has almost NONE of the colony-sim legibility the corpus needs. Gap collapses to 4 shared PLATFORMS (not 40 panels): overlay-layer framework (extends B5.6b) · Chronicle reader (DF-HIST, zero UI built) · dashboard/inspector shell (tabbed; inventory is the ONLY inspector) · alert system (chat≠it). Build platforms FIRST → ~40 readouts plug in. Reuse-first (conrod NOT egui). HUD-icon set → ASSET_REQUESTS (coordinate w/ DF-HIST glyphs). |
| **UI-3 MISSING-PLAYER-VERBS** (audit) | **`UI-MISSING-VERBS-audit.md`** | audit+recommend | the DO to UI-2's SEE. Almost every verb beyond B2a's 4 designations is DESIGNED-but-UNWIRED. ~50 verbs route through 5 surfaces: palette (ships) · POLICY/ORDER panels (MISSING — the whole Manage tier) · god-bar+hand (UI-1/HAND-CURSOR) · context menu · META (TIME CONTROLS + embody + notes MISSING). Build the 2 missing surfaces (policy panel + meta/time-controls) + extend the 3 shipped. TIME CONTROLS = the #1 cheapest highest-play-value verb. Pillar guard: Direct tier stays minimal. Near asset-free. UI-2+UI-3 = the whole control surface (SEE+DO) = the corpus made PLAYABLE. |
| **UI-4 DIALOGUE-AND-UNIT-SELECTION** (Ben-UI) | **`UI-DIALOGUE-SELECTION-design.md`** | **DLG-0..DLG-2** | this session. Two linked UX: (1) replace god-mode's giant over-head bubbles w/ a DF-style DIALOGUE/EVENT BOX — KEY FIND: `comp/chat.rs` `to_bubble()` ALREADY routes bubble-vs-not per `ChatType`, so the split EXISTS (extend it). RULE: flavor stays a SCOPED bubble at the unit (suppressed at overseer scale), miss-able events/announcements/dialogue → the BOX (= the DF-HIST HIST-2 feed, don't fork). (2) PER-AGENT HISTORY ON SELECT = the B-AG4 inspector w/ tabs Overview/Mind/Needs/HISTORY/Skills/Relationships; the History tab = DF-HIST per-figure via a FILTERED QUERY (REUSE not re-derive). The box + panel ARE the first concrete instances of UI-2's Chronicle-reader + dashboard-shell platforms. Near asset-free (2 panel frames; portraits=reused icons v1). |
| **UI-5 GOOD/EVIL HAND + DIVINE EFFECTS** (Ben-UI, B&W) | **`UI-HAND-ALIGNMENT-DIVINE-EFFECTS-design.md`** | **ALIGN-0..ALIGN-2** | this session. The B&W signature: the god-hand reflects ALIGNMENT (GOOD-luminous ↔ neutral[HAND-CURSOR base] ↔ EVIL-clawed) + divine cast-VFX per power×alignment. THE SPINE (§0): alignment is EARNED by DEEDS not chosen (nurture→good / cruelty→smite/plague/careless-drop→evil; each power carries a weight; worship style↔alignment secondary) — NO good/evil toggle (kills the B&W soul + pillar). The hand = the MIRROR: drifts GRADUALLY (material+geometry: radiance/gilt ↔ claws/dark-veining), legible bands, REVERSIBLE. REUSE-HEAVY: `outcome.rs` Outcome bus (+`reagent`=VFX selector) + ParticleMode + glow = the divine-VFX substrate (NO new particle system); HAND-CURSOR rig = neutral base. Purest legibility (you see who you've been in your own hand). Extends HAND-CURSOR + adds an alignment-weight column to GOD-POWERS-CATALOG. Hand-two-faces + divine-VFX asset batch → ASSET_REQUESTS. **[CONSOLIDATED into `GOD-HAND-design.md`.]** |
| **THE GOD-HAND** (definitive research+requirements) | **`GOD-HAND-design.md`** | **GH-A..GH-F** | this session. THE authoritative god-hand doc — CONSOLIDATES HAND-CURSOR + UI-5 **BY REFERENCE** (both STAND as sub-designs, architect-confirmed; this ties+fills the verb/anim/uniqueness gaps, doesn't redo/supersede). Research (B&W anchor + From Dust/Populous/Reus). Q1 the COMPLETE 16-verb set (grab-ground-pan/point/select→CHRONICLE/box-select/grab-lift-carry-drop/THROW/STROKE-reward/SLAP-punish/TAP-wake/SCULPT-terrain/paint-designation/cast-Miracle/paint-Blessing/point-direct/EMBODY — control-spectrum-grouped, reuse-tagged). Q2 the COMPLETE ~15-anim set (named NEEDS:animation-code, one rig, all inherit the alignment morph — the corpus's largest single anim debt but COMPLETE up front). Q3 THE UNIQUENESS (beyond B&W BECAUSE our hand governs a DF-DEPTH REMEMBERING colony: designates mining/building · IS the whole control-spectrum incl. embody→mortal-skillbar · select opens a colonist's WHOLE recorded life · every touch remembered by a mind + weighed into the drifting alignment face · every B&W verb deepened by the sim). Reuse: B1.5/B2a/`mounting.rs`/`states/throw.rs`/`outcome.rs`/GOD-POWERS/B-AG3/HAZARD-EVENTS/DF-HIST/B12/B13. Authoritative RIG+anim+VFX asset spec → ASSET_REQUESTS (supersedes the 2 prior hand requests). |
| **AUTONOMOUS-COLONY-OPERATION** (capstone synthesis) | **`AUTONOMOUS-COLONY-OPERATION-design.md`** | validation + GAP list | this session. Validates the ~21-system corpus COMPOSES into a colony that runs w/ ZERO player input (the pillar made concrete). 3 tiers: rtsim (the shipping autonomous NPC brain `npc_ai` — choose/goto/profession/idle) + the Bastion job board + THE DRIVES that self-generate the job stream (needs/orders/growth/mining/farm/hygiene/defense/mind). Self-prioritization (needs→food→production→build→mine→defense) EMERGES from drive urgencies, not hard-coded. THE GAP LIST (the build deliverable — a zero-input colony must NEVER stall): **G1 B7 needs-decay = THE keystone** (no hunger→no need-jobs→loop DEAD) · **G2 self-designation of work** (un-designated colony does NOTHING) · G3 B8 defense · G4 vertical reachability · G5-G9 (degradation/idle/seams/exhaustion/re-designate). Soak (Tier-1b) = the guard. Close G1+G2 → the god game PLAYS ITSELF, the control surface = OPTIONAL influence on top. Asset-free. VALIDATES the whole corpus coheres. Plus: GOD-POWERS-CATALOG schema lock (alignment-weight + cast-VFX preset per power, architect-approved). |
| **AGENT-CULTURE-CHARACTERIZATION** (Ben) | **`AGENT-CULTURE-CHARACTERIZATION-design.md`** | **CHAR-0..CHAR-3** | this session. ONE unified 5-axis per-agent + per-culture model (STATS·BEHAVIOR/MIND·HISTORY·RELATIONS·LANGUAGE). SYNTHESIS — 4 of 5 REUSE: STATS=`humanoid::Species`(6 races)+Stats+ColonistSkills · MIND=B-AG3+DF-FOCUS+rtsim brain · HISTORY=DF-HIST+UI-4 tab · RELATIONS=B-AG3+DF-HIST+UI-4 tab → UNIFY into the B-AG4/UI-4 inspector's character sheet (it already tabs these). NEW AXIS = LANGUAGE (per-culture name pools + dialect, reuse Veloren name-gen; chronicle reads in-culture; full conlang DEFERRED). RACE/CULTURE-KEYING (the point) = each axis varies per race+culture = distinctness beyond appearance. ONE new system piece = the INTER-CULTURE RELATIONS MATRIX (feuds/alliances → sentiment; ties migration/justice/politics). CULTURE-PRIORITY: T1 Human+Dwarven → T2 Elf+Orc+Gnarling → T3 rest (key all 5 axes together per culture). Near-asset-free. Priority list + name-pool authoring → ASSET_REQUESTS. |
| **SOFT-COLLISION** (chokepoint yielding) | **`SOFT-COLLISION-design.md`** | **SOFT-0..SOFT-1** | this session. THE chokepoint-deadlock fix (a mechanism the builder implements — the B5.8 red cases: many colonists funnel through one 1-wide vertical chokepoint → mutually block → watchdog=unreachable; recurs in B6 hauling = mechanism-level not geometry). A transient per-colonist SOFT-COLLISION state REDUCES the colonist↔colonist push ("squeeze past") triggered by (a) zero-progress>threshold [a soft-pass GRACE WINDOW inserted into the existing watchdog stuck→unreachable pipeline] OR (b) density>N [clustering relief] — NEVER relaxes colonist↔TERRAIN (hard). Reuse: `comp::phys.rs` CapsulePrism+collision_boundary (entity↔entity push is a distinct resolution) + the B4/B5 watchdog (STUCK trigger) + comp::Colonist (scope). GUARDS: colonist↔colonist ONLY / LOADED-only / DETERMINISTIC / squeeze-not-ghost. COMPOSES with B5.8 (B5.8 gives the PATH, this makes it usable by a CROWD). Closes AUTONOMOUS-COLONY G4/G5. Asset-free. Deterministic headless gate. |
| **SEASON-CLOCK** (the annual-rhythm spine) | `SEASON-CLOCK-design.md` | **SEASON-0..2** | this session (greenlit, build-once spine). A DERIVED clock over the shipping `TimeOfDay` master (`resources.rs`) — a `Season` enum + `year_phase(0..1)` + `day_of_year`, a pure fn like `DayPeriod` buckets the day (`time.rs`). Veloren's `Calendar` is REAL-WORLD-date (holiday skins), NOT in-game seasons → the annual cycle is the small net-new. + a `SeasonalSchedule` day-of-year hook (what DF-FESTIVAL fires on). GUARDS: derived-not-authoritative (one clock, no drift, deterministic) · tunable year-length (RON) · LOD-trivial (global scalar) · optional-consumption (bias not gate — zero-input law) · CLOCK-not-effects (behaviours live in each consumer). Consumed by FESTIVAL/NIGHT/FARM/ROT/LIVESTOCK + later TEMP/BIOME-FX. Asset-free. SEASON-0 buildable NOW. |
| **DF-FESTIVAL** (the colony celebrates — joy beat) | `DF-FESTIVAL-design.md` | **FEST-0..2** | this session (greenlit quick-win epic). A festival = the gather-loop (DF-RELIGION/TAVERN) + DF-FOCUS multi-`Need` burst + DF-COOK feast + SEASON-CLOCK schedule + AGENT-CULTURE rites + DF-HIST legend, fired on a trigger — mostly WIRING, not a new engine. Net-new = a data Festival event + triggers (seasonal harvest/holy-day · event [victory/wedding/great-work] · god-declared) + converge-feast-disperse behaviour. Autonomous (colony feasts at harvest unprompted); god = patron (bless the feast = good deed / evil blight). FESTIVAL décor batch → ASSET_REQUESTS. **DESIGNED-downstream of B7** (the feast/Need payoff); rides SEASON-CLOCK (build spine first). Fills the S7 downtime gap. |
| **UNDERGROUND-LIGHTING — OPTION B** (darkness-as-mechanic) | `UNDERGROUND-LIGHTING-design.md §5` | **LIGHT-2/3** | Ben DECIDED Option B (2026-07-10): the dark has TEETH. Full build appended (§5). Net-new = a SERVER light-PRESENCE model `lit_at(pos)`→LIT/DIM/DARK (a BOUNDED proximity query over active light sources incl. daylight — NOT the renderer's GI) → DARK = slower work + shorter local sight (extend `can_see`) + a B-AG3 gloom thought; light = the relief (LIGHT-1 lamplighter now reason-driven). GUARDS: loaded-only · deterministic · bounded-query · composes (DF-FOCUS ± / B-AG3 thought / existing sight gate, no new system). **Shared model DF-NIGHT rides** (build once, 2 callers). Velorite-lamp loop pays off. |
| **DF-NIGHT** (the dark has teeth — night-creatures) | `DF-NIGHT-design.md` | **NIGHT-0..2** | this session (greenlit w/ Option-B). The nightly PRESSURE that gives dark/light/shelter/defense teeth. Substrate largely built: DayPeriod/night + **LIGHTING Option-B `lit_at`** (emergence-where-DARK / ward-where-LIT — HARD dep, build once) + **DF-SYNDROME SYN-2** (the werebeast/night-madness, engine de-risked this) + B8/DF-BURROW (the shipped answers) + HAZARD-EVENTS + reskinned deep/undead fauna. Net-new = a BOUNDED night-spawn driver + the light-wards-them rule + creature-behaviour shell + night-syndrome content. THE BOUND = scales to colony (DF-MIGRATION prestige threat-pairing), capped — a tended colony survives untouched (soak law); god can bless-ward/smite or (evil) send-the-night. NIGHT-CREATURE reskin batch → ASSET_REQUESTS (coordinate w/ deeper-cavern-life, don't fork). |
| **REPUTATION** (recorded deeds → live treatment; S1) | `REPUTATION-design.md` | **REP-0..2** | this session (greenlit; the #1 cross-corpus seam). Closes the write-only-chronicle loop: DF-HIST deeds → a DERIVED per-figure standing (bounded/decaying, like prestige) → B-AG3 sentiment reads it as a prior (hero admired / thief distrusted / redeemed exile forgiven). FACETED (Valor/Craft/Generosity/Trust/Reliability — not one number). GUARDS: earned-not-assigned · decays+outweighable (the redemption arc) · deterministic+bounded (updates on record(), cached) · composes-with-AGENT-CULTURE-matrix. Deed sources = DF-JUSTICE/MISSION/QUALITY/B8. Surface = UI-4 inspector. Near-asset-free (glyphs join DF-HIST batch). Reuse-only synthesis. Co-lock DF-HIST record() + B-AG3 sentiment. Mirror-pair w/ GOD-EPITHET. |
| **GOD-EPITHET** (emergent theology; S2) | `GOD-EPITHET-design.md` | **EPITHET-0..2** | this session (greenlit; the B&W mirror of S1 at god-scale + spoken twin of the hand). The colony NAMES its god from divine deeds ("the Merciful"/"the Wrathful") — DERIVED from **UI-5 alignment** (co-lock, don't fork; the hand SHOWS it, the epithet SAYS it) + DF-HIST divine-attribution → a banded named epithet from a per-culture pool. CONSUMED: worship STYLE (fear-devotion vs love-devotion — the one new behaviour wire, a flavour on DF-RELIGION's devotion scalar) + migration desirability (who kneels/comes) + colonists SPEAK it (UI-4 dialogue box). GUARDS: earned-not-chosen · banded/drifting/reversible · derived-from-alignment (co-lock). Epithet name-pools = content (join AGENT-CULTURE authoring). Near-asset-free. HARD-deps UI-5 alignment. |
| **UNDERGROUND-EXPERIENCE** (camera · atmosphere · hand-light) | `UNDERGROUND-EXPERIENCE-design.md` | **UX-CAM-0/1 · UX-ATMO-0 · UX-HAND-LIGHT-0** | this session (Ben-routed, research+reco; the B5.8 deep-dig watchability cluster). HEADLINE FINDING: **most substrate is already BUILT.** CAMERA = the B1.6 overseer occlusion system (`voxygen/src/bastion/occlusion.rs` — ViewMode Solid/Reveal/Slice; composable SLICE/PROXIMITY/CUTAWAY/ROOF + interior relight; `CameraMode::Overseer` ortho god-cam; `CullingMode::Underground` detector) already implements every reference technique (DF z-levels/RimWorld roof/ONI cross-section/DRG). THE GAP = Reveal's cutaway+roof are stubbed OFF pending REAL TARGETS → **feed B2 selection/B3 colonists into `Occlusion.targets`** so Reveal auto-cutaways overburden + roof-reveals the chamber over the miners (the code already anticipates this). + close 2 known limits (sliced geo still shadows; interiors not relit). ATMOSPHERE = CaveDust/Drip particles + cave ambience/music + shader fog SHIP → densify in dug tunnels + depth-tinted fog + breach sting (HAZARD-EVENTS); Option-B's dark makes existing glow read as warmth. **THE RESOLVER of legibility-vs-mood = THE GOD-HAND'S LIGHT** (a cursor-attached `LightEmitter` at the work-layer via `unproject_to_world_plane`, alignment-TINTED — folds into the UI-5 hand aura/trail; god↔mortal asymmetry: colonists blind in dark [Option-B], the god carries its own light). 3 LAYERS don't trade off: camera=structure · atmosphere=mood · hand-light=local legibility-on-demand. Near-zero NEW art (mostly wiring+tuning shipped systems). VFX/tuning notes → ASSET_REQUESTS. |
| **COLONIST-EMERGENCY-RUN** (the reserved sprint) | `COLONIST-EMERGENCY-RUN-design.md` | **RUN-0..2** | this session (Ben-routed). Makes the "sprint RESERVED" half of the TRAVEL_SPEED gait decision CONCRETE: walk (`TRAVEL_SPEED=0.8`, `bastion_jobs.rs:168`) by default, RUN (a higher `NpcActivity::Goto(pos, speed_factor)` — `rtsim.rs`) only on urgency, at a COST. Penalty = drains `comp::Energy` (shipped stamina+regen — the GOVERNOR: low energy forces drop-to-walk) + a winded/tired aftermath (DF-FOCUS/buff) + optional flagged strain risk. Triggers: AUTONOMOUS urgency (flee [redefines the vanilla flee cap `MAX_FLEE_SPEED=0.65` which is oddly SLOWER than walk → a real burst], rescue, Call-to-Shelter muster, critical need — NOT routine work) + PLAYER "Emergency!" (Manage/god directive, collective energy cost). Reads = velocity-driven run anim (ZERO new anim). GUARDS: reserved-not-free (resource-enforced) · colonist-only (vanilla untouched) · deterministic core (injury-RNG optional/flagged) · rtsim-abstract. Ties DF-FOCUS/B8/DF-NIGHT/DF-BURROW/HAZARD-EVENTS/SOFT-COLLISION. Asset-free. |
| **TREE-FELLING** (chop the base, fell the whole tree) | `TREE-FELLING-design.md` | **FELL-0..2** | this session (Ben-greenlit Option b; promoted from backlog). BUG FOUND: shipped `DesignationKind::Chop` (`bastion.rs:266`) makes a job PER `BlockKind::Wood` block + matches ONLY Wood (0x40), NOT Leaves (0x41)/ArtLeaves → chop the base, the upper trunk + canopy FLOAT. FIX = treat the tree as ONE connected volume (Wood+Leaves flood-filled from the base), ONE fell-job at the reachable base, remove TOP-DOWN in a NO-FLOAT safe order **reusing the shared reachability-safe volume decomposer** (carve_ramp/BUILD-FRAMEWORK safe-emission — the top-down dual of the bottom-up dig order; a THIRD caller: dig-down/build-up/fell-down) + drop logs scaled to trunk (`CHOP_DROP_ITEM` → DF-PRODUCTION woodcutting) + a stump. GUARDS: no-float invariant (asserted) · reachability preserved (works at base) · deterministic · bounded/trunk-rooted (merged forest canopies don't chain-fell) · Bastion-scoped. FELL-2 = OPTIONAL-later faked-fall topple (HAZARD-EVENTS timber + splintered-stump/fallen-trunk assets; NO physics). Near-asset-free v1 (stump/log reuse existing). Build candidate. |
| **TOOLS-UPGRADE** (better tools = faster work; the progression) | `TOOLS-UPGRADE-design.md` | **TOOL-0..2** | this session (Ben-routed off the B5.8 dig verdict). ALSO the design home for "slow down mining" (base slow, tools = the reward). SURVEY: the Bastion work-tick `work_rate(skill)=(1+skill·0.2)/WORK_DURATION_BASE(3.0)` (`bastion_jobs.rs:51-58`) factors SKILL but NOT the tool → the gap. Veloren ships `ToolKind::{Pick,Shovel,Farming}` (`item/tool.rs:26`) + equip + the LOCKED `item::Quality` + material tiers + the anim already sets tool-per-verb. CORE = plug `tool_factor(equipped_tool)` into work_rate: `(1+skill·0.2)·tool_factor/BASE` — LOW base (deliberately slow), rises with TIER×QUALITY; skill×tool multiply. TOOL-1 = tier ladder (stone→…→steel) + material GATING (harder/deeper ore [DF-CAVERN GEO-1] needs min tier — the pull) + craft/upgrade (DF-PRODUCTION). TOOL-2 = DF-QUALITY quality-on-tools + auto-equip-best + legibility. GUARDS: base-slow-is-tuning · autonomous (colony forges own tools, god provides ore not picks) · deterministic · Bastion-scoped work-tick · reuse LOCKED Quality. Ties DF-PRODUCTION/DF-QUALITY/DF-CAVERN/TREE-FELLING(axe)/ColonistSkills. TOOL-TIER item recolor batch → ASSET_REQUESTS. TOOL-0 buildable on shipped ToolKind/equip; tier/quality downstream of DF-PRODUCTION/DF-QUALITY. |
| **DF-VILLAIN** (nemeses; saga-villains not faceless waves) | `DF-VILLAIN-design.md` | **VILLAIN-0..2** | this session (OVERNIGHT free-range; the top vision epic). PROMOTE a threat (fleeing raider / DF-JUSTICE exile / bitter emigrant / DF-NIGHT-CAVERN named horror / rival champion) to a persistent named `Actor::Npc` (rtsim ships it) that RECURS (flee-to-return via demote-persist), REMEMBERS (a B-AG3 grudge bound to the wrong), ESCALATES (bounded rtsim followers/power scaled to DF-MIGRATION prestige — the paired-threat), and ends in a DF-HIST legend. B&W CORE: the god's own cruelties BREED nemeses (ties alignment/GOD-EPITHET); god can smite/redeem/appease/create. GUARDS: bounded (soak — no doom-spiral) · closure-guaranteed (capped escapes, killable) · deterministic-ish · rtsim-tier. Reuse hostile bodies + recolor/standard (NAMED-ENEMY note). Ties B8/JUSTICE/MIGRATION/NIGHT/MISSION/HIST/AGENT-CULTURE/divine-politics. |
| **DF-ART** (art depicts history; chronicle made physical) | `DF-ART-design.md` | **ART-0..2** | this session (OVERNIGHT; the DF soul). The DEPICTION/meaning layer (DF-ARTIFACT apex already in DF-QUALITY plugs in): art carries a "depicts DF-HIST event X in culture-style Y" descriptor, PROCEDURALLY DESCRIBED (the DF "this engraving depicts the founding… figures in ecstatic poses"), + an art→mind hook (a colonist near art of an event they LIVED → B-AG3 pride/grief/awe) + `Need::AdmireArt` + DF-ROOMS Beauty. Net-new = descriptor + describer + mind-hook + art-job. Autonomous (colony carves its own history); god inspires/commissions/blesses. Near-asset-free (DF-ROOMS art batch IS the substrate; adds TEXT + mind hook). Reuse DF-HIST/DF-QUALITY/DF-ROOMS/B-AG3/AGENT-CULTURE. Ties SACRED-SITES/DF-RECLAIM/B12 (art outlives the colony). |
| **DF-KNOWLEDGE** (discover · teach · lose; the colony arc) | `DF-KNOWLEDGE-design.md` | **KNOW-0..2** | this session (OVERNIGHT; the big-bet epic). A KNOWLEDGE state GATES the shipped skills/recipes (326) — discovered≠omniscient (DF-CAVERN prospecting model, for craft); grows via practice-innovation/research/FIND(DF-MISSION-ruin/book/trade)/god-inspire-revelation; TEACHING master→apprentice; the RECORD (books/library) preserves; LOSS (a sole-master dies un-apprenticed → a technique LOST, re-discoverable, DF tragedy). GATES the TOOLS-UPGRADE ladder (iron-smithing unlocks the iron pick — ONE loop). B&W: god INSPIRES a revelation (Promethean), can't decree a tech tree. GUARDS: discovered≠omniscient · loss re-discoverable (no softlock) · autonomous · bounded graph · deterministic-ish. Near-asset-free (book+library reuse DF-ROOMS). $$-$$$ reshapes pacing → WITH-Ben, sequence w/ TOOLS-UPGRADE. DESIGNED-downstream. |
| **SACRED-SITES** (DF-HALLOW; the land remembers) | `SACRED-SITES-design.md` | **HALLOW-0..2** | this session (OVERNIGHT; REPUTATION-for-PLACES — the 3rd face of the remembering world w/ REPUTATION[people]+GOD-EPITHET[god]). Bind significant LOCATED DF-HIST events → a sacred/haunted SITE w/ derived magnitude+valence (founder's grave reverent, massacre dread); a colonist near it → B-AG3 reverence/dread (cursed = a lingering miasma-shaped dread aura); pilgrimage/avoidance; site kinds (grave/shrine/battle/massacre/miracle/founder-hall). God CONSECRATES/desecrates/cleanses (extends DF-ROOMS ROOM-3 aura; alignment_weight; reversible). GUARDS: earned-by-events · bounded/capped/fading · deterministic · autonomous. Persists beyond the colony (DF-RECLAIM/B12). Near-asset-free (reuse graves/shrines/statues + an overlay layer). Ties DF-RELIGION/DF-ART/DF-FESTIVAL/DF-NIGHT/martyrs. |
| **DF-OMEN** (signs, prophecy & revelation; the god's will interpreted) | `DF-OMEN-design.md` | **OMEN-0..2** | this session (OVERNIGHT; influence-not-command at the theological level — the deepest B&W). Omens arise (monstrous birth [DF-LIVESTOCK] / celestial [WeatherGrid/Calendar blood-moon/comet] / strange harvest / divine act) → the colony INTERPRETS via faith×culture×**GOD-EPITHET** (the lens — same sign reads warning under Wrathful, grace under Merciful) → meaning (warning/blessing/doom) → mood + propitiation (festival/sacrifice/fear). Prophets (DF-RELIGION) voice the will (heed/doubt; may misread/mislead → ties DF-VILLAIN false-prophet). God MANIFESTS a sign (reuse GOD-HAND cast-VFX as a portent — meaning still the colony's) + whispers a dream-revelation (ties DF-KNOWLEDGE inspire). GUARD: the god CANNOT dictate the interpretation (the pillar/the drama). Bounded/deterministic-ish. Near-asset-free (portent VFX reuse + omen-birth variant). Fulfilled/false → DF-HIST/REPUTATION. |
| **DIVINE-CHAMPION** (the god's chosen; the hero-pole to DF-VILLAIN) | `DIVINE-CHAMPION-design.md` | **CHAMP-0..2** | this session (OVERNIGHT; the saga's hero-pole, the DYAD with DF-VILLAIN). The god ANOINTS a colonist (the GOD-HAND STROKE reward + a favor-costed blessing) → a CHAMPION state (blessed, divine favor, a miracle-target, a rally figure, alignment-TINTED: radiant saint-hero ↔ dread-enforcer). Leads B8 defense / undertakes DF-MISSION / inspires (DF-RELIGION/REPUTATION). EARNED-TO-KEEP (a chosen coward shames the god — ties GOD-EPITHET; not a free buff). Can ASCEND (legend/saint → SACRED-SITES) or FALL (hubris/betrayal → PROMOTED to a DF-VILLAIN nemesis — the fallen-angel, reuses the villain path; god may redeem). God can EMBODY its champion (B12 — the involvement peak). GUARDS: god-chooses-but-deeds-keep · bounded (few) · autonomous. Near-asset-free (blessed colonist + the GOD-HAND aura tint). Ties DF-VILLAIN/GOD-POWERS/B12/REPUTATION/B8/MISSION/RELIGION/alignment. |
| **OBJECT-INSPECTION** (click anything → data · lore · actions) | `UI-OBJECT-INSPECTION-design.md` | **OBJ-0..2** | this session (Ben-routed; the OBJECT twin of UI-4). Every object (tree/house/trap/workstation/vein/stockpile/zone/monument/site) is SELECTABLE → the UI-4 inspector shell (REUSE, no fork) shows (1) live DATA, (2) LORE+history, (3) CONTEXT actions. SURVEY: `unproject_to_world_plane`+entity-pick (B2a selection) + `BlockKind`/`SpriteKind`+the gameplay-marker byte registry (identity) + **`ContextVerb` enum already exists** (`bastion.rs:431` — the action substrate) + UI-4 shell + DF-HIST (object stories) + asset lore fields. Net-new = a per-class `inspect()` DATA schema (tree→logs[TREE-FELLING] · trap→armed[DF-MECH] · house→occupancy/owner[DF-ROOMS] · vein→mineral+tool-tier[TOOLS-UPGRADE] · stockpile→contents · workstation→recipe/worker) + the identify + the context menu (extend ContextVerb) + the object lore/history hook (storied objects [artifact/monument/sacred-site/heirloom] get a rich DF-HIST History tab). Legibility pillar made literal. Near-asset-free (UI-4 frame + existing icons). Depends UI-4 built first; B2a ships. |
| **DF-TEMP + DF-BIOME-FX** (the climate spine; place & season w/ teeth) | `DF-TEMP-BIOME-FX-design.md` | **TEMP-0..2** | this session (OVERNIGHT cont.; the build-once climate spine, SEASON-CLOCK companion). SURVEY: worldgen already carries per-chunk `temp`+`humidity` (`terrain/mod.rs`, `world/sim/mod.rs`), and the EFFECTS already ship as `BuffKind::{Frozen,Chilled,Heatstroke,Burning,Wet}` (`comp/buff.rs`) → neither data nor effects are new. DF-TEMP = a FELT temperature = f(base × SEASON-CLOCK season × depth[deep=cool+STABLE] × local sources[hearth/fire] × exposure[clothing/shelter]) → the existing buffs + a DF-FOCUS comfort. The colony ANSWERS: fire/hearth (DF-ROOMS), furs (DF-PRODUCTION), shelter (DF-BURROW), fuel-as-a-winter-need (TREE-FELLING), + the STABLE DEEP = a refuge (a mechanical reason for the dwarven deep-colony). DF-BIOME-FX = per-biome routing (tundra-cold / desert-heat+thirst / swamp→fever[DF-SYNDROME] / heat→spoilage[DF-ROT] / season→growth[DF-FARM]). God: bless-warmth / send cold-snap (WeatherGrid). Pays off §8-S3(season)+S4(founding-site). GUARDS: reuse buffs (no new effects) · loaded/rtsim-abstract · deterministic · colony-can-always-answer (soak) · BUILD-ONCE. TEMP-0 rides worldgen temp+buffs now. Near-asset-free (fur recolors + hearth). |
| **DF-ANCESTORS** (the honored dead; ancestors, ghosts, afterlife) | `DF-ANCESTORS-design.md` | **DEAD-0..2** | this session (OVERNIGHT cont.; the AFTERLIFE face of the remembering world). A dead colonist persists as an ancestor-figure (keeps DF-HIST + posthumous REPUTATION); kin/colony grieve+venerate (B-AG3); grave→a place (DF-ROT burial→SACRED-SITES seed). Honored → ancestor-worship (DF-RELIGION) + remembrance feast (DF-FESTIVAL) + the dead's approval. Unburied/murdered/wronged → RESTLESS: a haunting (SACRED-SITES cursed aura) + a ghost (DF-NIGHT apparition, reuse undead) + an omen/dream (DF-OMEN). God's death-levers: lay-to-rest/bless-burial (good) ↔ raise/curse-grave (evil→a "Deathless"/necromancer epithet). GUARDS: bounded (notable dead) · restless=appeasable (bury/atone/lay-to-rest, NO softlock) · autonomous · culture death-customs (AGENT-CULTURE). Near-asset-free (graves + ghost VFX). Ties MARTYRS(§11)/DF-VILLAIN(wronged-dead). |
| **DF-CURSE** (divine punishment & the geas; the hand that afflicts) | `DF-CURSE-design.md` | **CURSE-0..2** | this session (OVERNIGHT cont.; the WRATHFUL pole — the two-hands mirror of DIVINE-CHAMPION's blessing). A CURSE = a divinely-caused **DF-SYNDROME** (reuse the affliction engine) + a LIFT condition (atonement/quest[DF-MISSION]/mercy — not natural recovery). A GEAS = a binding vow/prohibition on colonist/lineage (protective/disciplinary/cruel — lawful more than evil); kept→ok, broken→the curse fires (the tripwire; DF-JUSTICE-as-divine-law). Scope: colonist/lineage(a cursed bloodline→DF-ANCESTORS)/colony/place(SACRED-SITES). Cast by the GOD-HAND slap/cruel verbs, favor-costed, `alignment_weight`− → feeds a wrathful GOD-EPITHET + read as wrath (DF-OMEN) → fear-worship. GUARDS: LIFTABLE (recoverable, no softlock — hard guard) · costed/alignment-weighted · reuse DF-SYNDROME deterministic core. Near-asset-free (curse-mark VFX reuse). |
| **DF-RECLAIM** (ruins & the second colony; the god outlives its colonies) | `DF-RECLAIM-design.md` | **RECLAIM-0..2** | this session (OVERNIGHT cont.; the remembering-world LIFECYCLE capstone, §8-S5). A colony that ENDS demotes to a persistent rtsim RUIN-site keeping its legend(DF-HIST)+monuments(DF-ART)+holy/haunted ground(SACRED-SITES)+restless dead(DF-ANCESTORS)+name(COLLECTIVE-RENOWN §11), DF-ROT-weathered. The ruin = CONTENT: findable(map/DF-MISSION) + delvable(B12 embody/DF-CAVERN — a dungeon w/ a KNOWN history; a DF-VILLAIN may hold it) + RECLAIMABLE(B11 — found on the bones, inherit structures/art/sites AS heritage + restless dead/curse AS risk). META-FIT: the GOD is the continuous character — alignment/epithet/legend persist across colonies (a ruin = the god's own memory of a chapter). "Losing is fun" made world-permanent. GUARDS: rtsim-tier/bounded (decays/simplifies) · recoverable-world. DESIGNED-DOWNSTREAM (needs B11+B12+rtsim ruin-sites); late capstone. Near-asset-free (weathered own-structures). |
| **DF-BEAST** (legendary creatures & the great hunt; the wild titan) | `DF-BEAST-design.md` | **BEAST-0..2** | this session (OVERNIGHT cont.; completes the LEGENDARY-FIGURE TRIAD — DF-VILLAIN[enemy] + DIVINE-CHAMPION[hero] + DF-BEAST[wild titan]). SURVEY: Veloren ships the megafauna roster (`biped_large` Cyclops/Wendigo/Werewolf/Minotaur/Harvester/Tursus/Gigasfrost/Gigasfire — bodies+AI+attacks). PROMOTE one → a named, persistent, region-apex rtsim Actor (reuse the DF-VILLAIN promotion) w/ a name+lair(a dread SACRED-SITE)+legend(DF-HIST)+role (landmark terror + threat[B8/DF-NIGHT] + hunt-prize). GREAT HUNT = a DF-MISSION to slay it (a DIVINE-CHAMPION may lead; hunters may die — losing-is-fun) → legendary bone/hide(DF-QUALITY)+trophy(DF-ART/DF-ROOMS skull)+renown. God beast-levers: bless-the-hunt(good) / TAME (a rare divine companion — DF-LIVESTOCK Pet, god-gated) / LOOSE on a rival(evil, alignment−). A beast can BE a nemesis (beast-villain, reuse DF-VILLAIN). GUARDS: bounded(few, region apexes) · rtsim-persistent · a real risk · tame rare+costed. Near-asset-free (megafauna ship; legendary = recolor/scale + trophy). BEAST-0 near-buildable NOW (titans ship). |
| **COLLECTIVE-RENOWN** (the fortress's name; the colony as a legend) | `COLLECTIVE-RENOWN-design.md` | **RENOWN-0..2** | this session (OVERNIGHT cont.; the 4TH FACE — COMPLETES the remembering-world model: REPUTATION[people]+GOD-EPITHET[god]+SACRED-SITES[place]+COLLECTIVE-RENOWN[colony]). REPUTATION-for-the-WHOLE-COLONY: a derived collective RENOWN (facets might/wealth/holiness/cursedness/craft/hospitality from the chronicle) + a BYNAME ("the seven-times-flooded fort"/"the cursed hold"/"the hall that slew the Gigas", in-culture via AGENT-CULTURE, composes w/ GOD-EPITHET → "the holy deep of the Merciful"). Extends DF-MIGRATION `prestige` (its NAMED/storied face — NOT a forked metric). WORLD-RESPONSE (a modifier, not a gate): colours migration(who joins)/trade(who deals)/threats(renown draws bigger foes — the paired-face, ties DF-VILLAIN/DF-BEAST)/how it's spoken of(DF-HIST). GUARDS: earned-not-declared · drifting/reversible · bounded · world-response=modifier(anti-4X) · deterministic · autonomous. Near-asset-free (colony banner/heraldry reuse + text). RENOWN-0 rides prestige+DF-HIST now. |
| **DF-WOUND / DF-MEDICAL** ⚠STUB | `DF-WOUND-design.md` | (stub) | gap-audit 2026-07-10 — a ONE-PARAGRAPH STUB (not a full pass, FRONTIER+1) to resolve dangling refs (DF-SYNDROME infection / DF-CURSE / EMERGENCY-RUN strain / B8 wounds). Injury+treatment layer: wounds = a persistent `comp::Health`-debit + a status buff (the DF-SYNDROME shape; infection = a Syndrome the wound triggers); healer = a Profession+job; infirmary = DF-ZONES venue + DF-ROOMS quality→recovery; god mend = GOD-POWERS. GRACEFUL DEGRADATION: consumers no-op wounded to a bounded self-resolving state until built. **Write the FULL pass when B8 nears.** |
| **DF-TAVERN** ⚠STUB | `DF-TAVERN-design.md` | (stub) | gap-audit 2026-07-10 — a ONE-PARAGRAPH STUB (not a full pass, FRONTIER+1) to resolve dangling refs (DF-FESTIVAL gather-loop / DF-FOCUS Drink+Socialize / DF-RELIGION congregation). Recreation venue + THE shared GATHER-LOOP (FESTIVAL/RELIGION retarget it — designed ONCE here). Reuse: `Detail::Bar`/`Stage` + ale recipes + DF-ZONES venue + DF-COOK + DF-FOCUS satisfies(Need). GRACEFUL DEGRADATION: until built, FESTIVAL/RELIGION each ship their own minimal converge-behaviour + Drink/Socialize go unmet-but-bounded (a mood cost, no crash). **Write the FULL pass when the social/festival layer nears.** |
| **MINE-MODES** (selectable access style: ladder-shaft vs terraced-staircase) | `MINE-MODES-design.md` | **MINEMODE-0..2** | this session (Ben-routed, FRONTIER+1 — mining is the active build; BUILD routes to builder QUEUED AFTER the access-reliability bug batch, a feature not a blocker). Player picks the mine's access at placement (extend the b-2.1 flat/slope mode-stepper). LADDER MINE = compact vertical shaft + ladder built access-before-descent (B5.8 + anti-stuck nets); deep/small-footprint. STAIRS MINE = the dig STAGGERS into one big descending TERRACED staircase (strip-mine/open-pit) — **the steps ARE the access, no ladder/egress structure**; = **`carve_ramp` (the shared reachability-safe decomposer) scaled to a whole mine** (a 4th caller: dig-down/build-up/fell-down/stairs-mine). HEADLINE VIRTUE (Ben): STAIRS mode's access is INHERENT in the dig geometry → **no colonist can be entombed, by construction** (carve_ramp's ordering invariant) — sidesteps the whole stuck-at-exit/egress-fail-safe/access-before-descent problem class. Trade-off: STAIRS = wide+material-heavy but stuck-proof (the safe default); LADDER = compact+deep but keeps access-management. Near-asset-free (STAIRS = pure dig geometry; LADDER reuses the mine batch; a mode-toggle icon → UI-2). Ties DF-DIG-VERBS/carve_ramp/B5.8/SOFT-COLLISION/the mining framework §3v. |

## DESIGNING / CLAIMED (in progress — check DESIGN_PASS_LOG before claiming)
_(none — the 5 greenlit passes + the SEASON-CLOCK spine DONE (2026-07-10); HOLDING for steer. Other epics — VILLAIN/ART/KNOWLEDGE/ECON/GUILD/BIOME-FX — stay pitched/deferred pending Ben.)_

## HIGH-PRIORITY `[LEDGER]` — the recommended next passes (aim here)
Ordered by the selection criteria (near build frontier · unlocks content batch · Tier-1). The frontier is at
B5.6b → B6; anything riding B6 (hauling) or B5.8 (vertical mobility) is near-term real.

> **⚑ READ FIRST — future-work already holds a LOT of design (FINDING in DESIGN_PASS_LOG, 2026-07-09).**
> `future-work-and-deferred-ideas.md` pre-designs several "undesigned"/"unbuilt-dependency" items — CONSUME, don't
> re-derive. Highlights: **§1a Hazard-Events engine** (the "unbuilt hazard engine" the DF-CAVERN Breach + DF-TRAP
> + DF-FLUID-flood all need — DESIGNED but has no block → **promote to its own consolidation-pass→block**);
> **§1b Trigger→Link→Effect = DF-MECH/TRAP/OPERABLE** (top unclaimed pick — queue as a CONSOLIDATION pass from
> §1b+ledger §E, NOT from scratch); **§3c** autonomous-building = DF-WORKSHOP-autobuild/B-AG6; **§3y.A** wildlife
> lifecycle = DF-LIVESTOCK's population model; **§3z** materials = DF-QUALITY material axis; **§3r** custom-creature
> = DF-LIVESTOCK/DF-BEAST/god-companion. **Divine-Politics DP1–DP5 is FULLY designed in the bible** — the world-
> faith/trade tiers DF-RELIGION/DF-TRADE deferred there need NO new pass. Still-undesigned near-frontier from §5:
> **DF-ROOMS** + **DF-FOCUS**. Housekeeping reco: reconcile future-work vs this map (mark §-sections CONSUMED-BY).

1. ~~**DF-DIG-VERBS**~~ **DESIGNED** this session (`DF-DIG-VERBS-design.md`; DIG-0..DIG-4). Mostly wiring on B5;
   the ramp verb unifies with B5.8's auto-carve-steps (one `carve_ramp` lib — flagged so it isn't built twice).
   **HARD PAIR with B5.8** — sequence B5.8 first or co-build. Zero new animation debt.
2. ~~**DF-RELIGION**~~ **DESIGNED** this session (`DF-RELIGION-design.md`; REL-0..REL-5). Colony tier behind
   B7 (mostly wire — the tavern loop retargeted); world faith-politics handed off to Divine-Politics DP2–DP4
   (LATE). Faith-asset batch → ASSET_REQUESTS. Recommend the architect **split the ledger's DF-RELIGION line**
   (colony `$` vs faith-politics `$$$`/Divine-Politics).
3. ~~**DF-ORDERS + DF-STANDING**~~ **DESIGNED** (`DF-POLICY-design.md`; POL-0..POL-4+) — the full policy layer,
   generalizing DF-PRODUCTION PROD-1's minimal target-pull (does NOT duplicate) + the standing-rule half + the
   shared stock census. DF-LOG **DESIGNED** (folded into DF-HIST as the HIST-2 feed). ~~**DF-ZONES**~~
   **DESIGNED** (`DF-ZONES-design.md`; ZONE-0..ZONE-2+) — umbrella schema, rides B5.6b + canonicalizes the §2
   activity-zone half. ~~**DF-BURROW**~~ **DESIGNED** (`DF-BURROW-design.md`; BURROW-0..3) — the hard-policy
   cousin. **The Manage tier (zones + burrow + policy) is complete.**
4. ~~**DF-HIST** — the Legends/Chronicle browser.~~ **DESIGNED** this session (`DF-HIST-design.md`; DF-LOG folded
   in as the HIST-2 feed slice). The `record()` capture API is load-bearing — lock before emitters harden.
5. ~~**DF-QUALITY + DF-ARTIFACT**~~ **DESIGNED** this session (`DF-QUALITY-design.md`; QUAL-0..QUAL-3). Quality
   enum **LOCKED** = engine `item::Quality` (frameworks §2b). Fills DF-PRODUCTION S6. Strange-mood kept faithful.
6. **Next unclaimed near-frontier Tier-1/2 (aim here):** **DF-TAVERN** (rides B7 + social — also unblocks the
   DF-ZONES Meeting wire + DF-RELIGION's gather loop), then DF-WOUND, DF-MECH/TRAP/OPERABLE (trigger→link→effect
   cluster), DF-CAVERN + DF-GEOLOGY (the vertical world, ties DF-DIG-VERBS + mining framework). (Policy layer
   — DF-ORDERS/DF-STANDING/DF-ZONES/DF-BURROW — and DF-TRADE now all **DESIGNED**.)

## DEFER — premature (Tier-3 / sits on unbuilt world) — do NOT design ahead of substrate
deep DF-ECON, DF-HYDRO, DF-GUILD, DF-MINECART. (Flag "premature — defer" if reached; they go stale
before build.) **~~DF-BEAST~~ DESIGNED this session (its substrate — the biped_large megafauna — actually SHIPS,
so it was near-buildable, not premature).** **DESIGNED and graduated OUT of DEFER: ~~DF-NIGHT~~ + ~~DF-FESTIVAL~~ (Ben-greenlit) + ~~DF-VILLAIN~~
+ ~~DF-ART~~ + ~~DF-KNOWLEDGE~~ + ~~DF-TEMP~~ + ~~DF-BIOME-FX~~ + ~~DF-RECLAIM~~ (OVERNIGHT free-range run) + the NEW
SACRED-SITES + DF-OMEN + DIVINE-CHAMPION + DF-ANCESTORS + DF-CURSE + OBJECT-INSPECTION. The epic pitch-list
(DESIGNER-SUGGESTIONS §9) + the SPARKS batch (§11) hold promote-ready ideas for the rest — greenlight specific ones
WITH Ben. Remaining DEFER epics are genuinely substrate-gated (DF-BEAST/ECON/HYDRO/GUILD/MINECART).**

## Cross-pass seams to keep coherent (don't fork these)
- **`Quality` enum** — **LOCKED** (DF-QUALITY §0): canonical = engine `common::comp::item::Quality`; Bastion
  defers, never forks (frameworks §2b). Companion schema to lock with it: per-instance `craft_quality:
  Option<Quality>` on the item instance (the skill stamp). DF-PRODUCTION S6 ↔ DF-QUALITY ↔ DF-ARTIFACT all use it.
- **`ZoneKind` vocabulary** — DF-ZONES §3 canonicalizes the §2 activity-zone half (recommend landing in
  frameworks §2 alongside the `purpose` enum). Farm/Production zones (DF-PRODUCTION) + faith zone (DF-RELIGION
  REL-0) are `ZoneKind`s; every zoned system defers to the one list. Soft-magnet (bias) vs DF-BURROW (forbid).
- **Standing orders / the order engine** — **DF-POLICY DESIGNED** (`DF-POLICY-design.md`; POL-0..POL-4+): the
  full policy layer. **HARD cross-seam:** DF-PRODUCTION PROD-1's minimal target-pull ↔ DF-POLICY POL-0's
  generalized order-evaluation engine must be **built ONCE** (co-build, or PROD-1 writes its pull as the first
  instance of the general engine) — else the engine forks. And the DF-POLICY **stock census (S3)** ↔
  DF-PRODUCTION **PROD-4 economy readout** are the same substrate (build once, pays twice). Standing-RULE half
  (forbid-on-death / auto-haul-refuse / cook-perm) is DF-POLICY's; cook-perm behaviour is DF-COOK PROD-3's.
- **The Chronicle** — DF-HIST **DESIGNED**: the `record()` capture API (`ChronicleEvent` + importance enums,
  lock like Quality) is the one sink DF-PRODUCTION PROD-4 (economy events), God-Powers §1.2 (divine attribution),
  DF-QUALITY (masterwork/artifact = `Legendary` kind), and nature/weather all emit into. DF-LOG = the HIST-2
  feed slice. **API must land before those emitters harden their event points.**
- **Consumption/mood** — DF-PRODUCTION (produces food) ↔ B7 (Needs decay + eat) ↔ DF-COOK payoff.
- **Zone `purpose` enum** — the canonical 8-kind taxonomy (frameworks §2); every zoned system defers to it.
  (DF-RELIGION's temple = the reserved `religious→faith` kind.)
- **`Needs` schema** — DF-RELIGION REL-1 adds a `worship` field to `comp::bastion::Needs` (B7-owned; lock once
  with the B7 designer, Quality-enum style). Worship is also DF-FOCUS's first "pray" personal-need (co-design).
- **Colony devotion → DP2 faith** — DF-RELIGION S4 (the devotion accumulator) is the interface Divine-Politics
  DP2 consumes (bounded/decaying/per-faction-mappable/deity-attributable). Co-lock so it doesn't fork.
- **Faith Chronicle events** — DF-RELIGION (first temple / prophet arises / temple stood empty / prayer
  answered) emit into DF-HIST's `record()` capture API. Divine-attribution ties God-Powers §1.2 / HIST-5.
- **Colony economic node → DP faction-trade** — DF-TRADE TRADE-0 (the colony's `SitePrices` + surplus/deficit
  `GoodMap` vector, reusing the `world::site::economy` types, NOT a forked `Economy`) is the interface
  Divine-Politics DP1/DP3 consumes for the world faction-trade network; the TRADE-3 caravan actor is DP's
  physical manifestation. Co-lock the node/caravan shape (like the DF-RELIGION devotion scalar / Quality enum)
  so DP drives it without a refork. NPC-site economies stay frozen (worldgen snapshot); only the colony lives.
- **God-Powers caravan rows** — *Bless a caravan* / *Curse the roads* get a **colony-scoped near-term slice**
  in DF-TRADE TRADE-4 (Buff + Hazard-Events banditry + `Good::RoadSecurity`), split out of their DP-gated
  world-faction-route version (DP5). Update the 2 catalog rows to note the split — scope refinement, mirrors
  the DF-RELIGION colony-vs-DP faith split. Trade chronicle glyphs join the DF-HIST glyph batch (not forked).
- **DF-TRADE ↔ DF-PRODUCTION** — the colony's tradeable surplus/deficit derives from DF-PRODUCTION stocks
  (TRADE-0 HARD-deps PROD-1). DF-TRADE `ZoneKind::TradeDepot` rides DF-ZONES + B6-haul. Goods carry the locked
  `Quality` enum (`SitePrices` already quality-discounts — honor, don't refork).
