# Decisions for Ben — review-later log

> **Purpose (Ben, 2026-07-12):** the fleet makes ALL decisions autonomously and does NOT ask Ben in real time.
> This log records only **genuine forks** — decisions where a reasonable alternative exists that Ben might prefer —
> as one-liners he can review whenever and reverse. Routine calls are not logged. Each entry: the call, the
> alternative, and how reversible it is. Newest first.

| # | Date | Fork (the call made) | Alternative | Reversible? |
|--:|------|----------------------|-------------|-------------|
| 78 | 2026-08-08 | **THE DESIGN QUESTION CLOSES: BRANCH 4 CONFIRMED BY THE CENSUS — 5b's suspend design stands, with three named lines.** The census (grep-ALL, narrowed by hand — "the dangerous reader is never the one you can recall") typed all 24 substantive `claimed_by` readers: 21 correct by construction (ownership/availability — and the bed-slot clear is BETTER under suspend: the bed stays reserved for a colonist who intends to return); 3 execution-readers, of which two were already on the table and one is NEW — **DISPERSION (16982): claims repel new claims within 2 XY to spread the work crew, so a suspended self-job's position would push workers 2 blocks off a sleeping colonist's bed.** Not a correctness break (soft score, not a gate; no compounding accumulator found — the dirty condition is explicitly recorded as searched-for and absent) but behavioral, fan-visible, and determinism-adjacent. RULED per the C1 precedent: **the dispersion set FILTERS to actively-worked jobs** — its purpose is work-crew spacing, its exercised population was work claims, and a sleeping colonist jamming the spacing is an unchosen extension to a new population; if sleep-disturbance mechanics are ever wanted, they arrive as their own chosen row. FINAL DESIGN: 5b's pointer + retained-claim suspend · the claim-sweep fix to its comment's true population (with the planted despawned-claimant case) · `claims_distinct` re-derived to distinct-ACTIVE · dispersion filtered with a named-case comment · the tick-263 falsifier binding · the b4 number landing as confirmation only (mechanism-before-measurement, both directions now). | Declare the dispersion inclusion intended (a sleep-disturbance feature nobody designed, inherited through a field rename of meaning), or wait for b4 anyway (the census already answered what b4 samples). | Yes — three one-line items; the design is 5b's own with its guards chosen; every decision measured or read, none inherited. |
| 77 | 2026-08-08 | **#76 AMENDED ON A NEW FACT: the claim sweep's test is ITSELF DEFECTIVE per its own comment — which makes FIX-THE-SWEEP-KEEP-THE-DESIGN a live fourth branch, and #76's "needs neither sweep taught" was overstated (my own #72 taught the orphan sweep owner-liveness — one sweep either way).** The sweep's comment: release jobs whose "claimant VANISHED (demoted/despawned)"; its code: "has no ActiveJob" — an exact proxy in the OLD world (claimed⇒active held by invariant) and a latent bug the moment any claimed∧inactive state exists; suspension made it reachable, it didn't make it wrong. RULED: (1) **the sweep's test gets fixed to its comment's true population REGARDLESS of design choice** — entity-exists/despawn-demotion, one line, with its own planted case (a genuinely-despawned claimant must still release); (2) **branch 4 is CONDITIONALLY PREFERRED** — 5b's design + the sweep fix has fewer moving parts than the field (no new struct member on a determinism-critical type, no orphan-sweep teaching, the pointer already built) — **gated on the two measurements already cheap/in-flight:** the b4 `claims_distinct` number AND the reader census (every `claimed_by` consumer typed ownership-vs-execution); census clean ⇒ 5b design + sweep fix + `claims_distinct` re-derived to distinct-ACTIVE + the tick-263 falsifier transfers (the strip-chain dies via the sweep fix); census dirty ⇒ #72's field as #76 ruled; (3) the tree's second update is honest — it moved on a mechanism, then moved again when the mechanism was re-typed from CONSTRAINT to DEFECT, and both updates predate any rework line. 5b holds `dcbe6f17c7`, produces only the b4 number; nothing raced. | Hold #76 rigidly (designs around a bug instead of fixing it — the bug is owed a fix in every branch), or skip the census (ships the blur unmeasured, the #72 fear). | Yes — one-line sweep fix owed regardless; the census is a read; the tick-263 falsifier binds whichever branch lands. |
| 76 | 2026-08-08 | **#75's TREE RESOLVES AT REWORK — a second, INDEPENDENT mechanism refuted the design's central claim before the first measurement even landed: the CLAIM SWEEP (16138) strips the retained claim.** Its comment says "claimant vanished (demoted/despawned)"; its CODE says "has no ActiveJob" — and 5b's suspend removes active_jobs, so a suspended colonist reads NOT-ALIVE, the claim is stripped at %ARB==3, and the orphan sweep deletes the now-unclaimed self-job at %ARB==9 — **exactly explaining the fixture's consistent ~tick-263 violation that survived four fixes** (the "quantity invariant not produced by mechanism X" resolved: produced by a bulk sweep no release-path hunt could find). RULED: (1) **the design reworks to #72's separate ownership field as originally approved** — the overloaded-claimed_by path needs TWO sweeps taught about a state they were never designed to see; the separate field needs NEITHER, and that's mechanism, not taste; (2) **5b's pointer SURVIVES as the reclaim fast-path** alongside the field — its O(1) merits are real and complementary (the field is ownership truth; the pointer is the index); suspend RELEASES the claim as #72 assumed, and the sweeps behave correctly by design; (3) **the rework's own falsifier is pre-registered: the tick-263 fixture violation must DISAPPEAR** — the chain that produced it can't exist under the field design; (4) the b4 run lands as confirmation, not decider — the tree updated honestly when a MECHANISM arrived before its first measurement. Banked: the BULK-SWEEP class (a release-path hunt is structurally blind to bulk sweeps — enumerate ALL clearing sites of a field, the read-all-writers law applied to CLEARS) and the label/value pair at the alive check. | Teach both sweeps about suspension to save the overloaded field (two guards defending a blur that a separate field dissolves), or wait for the b4 number anyway (the mechanism is read and the fixture already measured it — waiting adds nothing). | Yes — the rework is scoped, the pointer survives, the falsifier is registered, and 5b's wedge work (three ENDURE fixes) stands regardless. |
| 75 | 2026-08-08 | **SITES 4+6 DESIGN DEVIATION RULED AS A DECISION TREE, not a verdict — because 5b's design may satisfy #72's constraint IN SPIRIT by a route nobody proposed.** Their build: suspend keeps `claimed_by=Some` (never releases) + an O(1) pointer; the new self-job is pre-claimed to the same uid ⇒ one colonist, two claims ⇒ the b4 `claims_distinct` board invariant plausibly breaks. Opus flagged it as #72's two-meanings blur. THE RULING'S KEY READING: #72 mandated a NEW field because the then-design RELEASED the claim (ownership needed a home that survived release); 5b's design NEVER RELEASES — so ownership stays in `claimed_by` and EXECUTION lives in `active_jobs`/the pointer: the two meanings may already be separated by the EXISTING pair, no new field needed — PROVIDED every reader asking "actively working?" reads the execution side. DECISION TREE, pre-registered: (1) the b4 run first (in flight) — `claims_always_distinct` still TRUE ⇒ the audit already counts only active claims, the question is moot, the design stands, and #72's field requirement RETIRES as unnecessary-under-never-release; (2) FALSE ⇒ the READER CENSUS runs — every `claimed_by` consumer typed as asking the ownership question (fine) or the execution question (must read active_jobs); census clean except the audit ⇒ **the INVARIANT re-derives** (its world-model changed — the acceptance-criterion law applied to a board invariant: "distinct ACTIVE claims" preserves its protective purpose, the old form retires as world-stale) and the design stands WITH the census as proof; (3) census finds real readers confusing sleep-for-work ⇒ the design reworks to #72's field. Also banked: Opus's verification-population lesson — "VERIFIED" NAMES ITS POPULATION; the gate you'd fail is rarely in the scenario you're building. And 5b's fix-#3 tell enters the ledger: a fix that changes nothing is evidence about the fix, not the bug. | Reject outright for deviating from #72's letter (discards a cleverer design that may satisfy its spirit), or accept without the census (ships the blur unmeasured — the exact #72 fear). | Yes — the tree is pre-registered; every branch decided by a measurement; nothing reworks on preference. |
| 74 | 2026-08-08 | **C3's HOLD RESOLVED: route_exhausted GATES TO WORK KINDS (per-kind at the producer, C1's shape) — the read proved the guard is a COINCIDENCE TEST, not an ownership test.** For a self-job, "which painted region contains this point" returns either nothing (an honest gap) or SOMEBODY ELSE'S REGION — including long-COMPLETED build designations, since `designated` prunes only on explicit cancellation: a finished build reported "blocked" because a colonist couldn't reach a bed inside it — the farm-paint mis-attribution horror, and the codebase's own #55 comment names the exact class 400 lines away (handled for cancellation, unhandled for self-jobs). The self-job visibility gap ("colonists repeatedly fail to reach a bed" IS a thing a player should hear) files as its own row — blocked_regions is simply the wrong store to say it in; it composes with the #47 notification work. **D5 IS WITHDRAWN and the fan returns to FULL exact-match — and the law that withdrawal produced is BANKED: BEFORE REGISTERING AN EXPECTED DELTA, ASK WHETHER THE PRODUCING CHAIN IS ITSELF CORRECT — a registration EXPLAINS a mover; it must never EXCUSE one.** Opus registered D5 for a chain that shouldn't exist; pre-registration's discipline makes movers honest only after the prior question "should this move at all." | Let self-jobs feed the store and keep D5 (launders a mis-attribution into "expected behaviour" — the exact object the registration discipline exists to prevent), or route to Ben (the class is settled by his own #55 precedent). | Yes — one gate condition; the visibility row is filed; exact-match is the stronger fan gate. |
| 73 | 2026-08-08 | **C1 RULED: THE ARRIVAL-TOLERANCE WIDENING DOES NOT APPLY TO SELF-JOBS — beds and meals keep STRICT arrival (2.5); the strikes site 6 awakens feed escalation and visibility but never remote-arrival.** Three grounds: (a) the remote-work fiction doesn't transfer — a colonist cannot sleep-at-range, and granting bed-quality rest from 6.1 blocks away is wrong at both the fiction and mechanics layers; (b) self-jobs already own a DESIGNED give-up path (the ENDURE degradation) making the widening's anti-loop rationale redundant for them — work targets loop without it, self-jobs don't; (c) the widening was tuned for Mine/Chop on awkward blocks and its economy effect on needs was never measured — Ben's FR15 constraint arriving through a side door gets DECLINED at the door, not inherited. Implementation: the tolerance growth gates per-kind at its consumer with a named-case comment (work kinds grow; self-jobs hold 2.5). C2 (escalation) APPLIES to self-jobs as designed. **C3 HELD pending a store's-unit read:** a bed is NOT a designation region and `blocked_regions`' unit is designation regions — the haul producer was REJECTED for exactly this mismatch (#66 era), so before route_exhausted records self-job targets, the read must show what region a bed resolves to; recording nothing is honest, mis-attributing to whichever region contains the bed is the farm-paint horror. Also accepted: D5 registered pre-fan (site 6 legitimately feeds the blocked-regions count; every OTHER fan field holds exact-match), and Opus's #47 finding files as a REAL row — the dedup-by-region-alone permanently suppressing the carve-planner's player message once route_exhausted lands first (source exists to distinguish producers and the dedup ignores it; fix shape pre-named: separate ENTRY from NOTIFICATION, never widen dedup to (region,source); acceptance needs BOTH producers on ONE region — the two-colonist law's sibling, same day). | Inherit the widening silently (a behavior change nobody chose, unmeasured on the needs economy), or route C1 to Ben (his designed invariant — but its extension to a NEW population is exactly the class he delegated). | Yes — one per-kind gate condition; Ben can reverse here; C3's hold lifts on one read. |
| 72 | 2026-08-08 | **UNIFICATION MID-BUILD ESCALATION RULED: the ownership field is APPROVED, the sweep discriminates on owner liveness, and the settle invariant is re-derived — because the acceptance fixture Opus was proudest of FORBADE THE DESIGN IT ACCEPTS.** The chain: 5b read site 4 before building and found the minimal deletion unsatisfiable (nothing re-claims a released self-job — the only path back is a fresh breakdown roll); Opus's sweep-read then found his own settle invariant ("no self-job unclaimed-and-present") counts EXACTLY the state suspension legitimizes — the instrument built to protect the design would have fired ON it. LAW BANKED: AN ACCEPTANCE CRITERION INHERITS THE WORLD-MODEL OF THE CODE IT WAS WRITTEN AGAINST — fixtures carried across a lifecycle change are RE-DERIVED, never carried over. Two real blockers found by reading: the sweep deletes suspended jobs in ~0.5s, and NO ownership survives release (`claimed_by` nulls ⇒ re-claim by kind alone would hand one colonist another's breakdown deadline). RULINGS: (1) the NEW ENGINE FIELD is approved — ownership surviving release (`suspended_for: Option<Uid>` or explicit suspend state; shape 5b's) with constraints: sim-event-derived only (determinism story stated), the struct-field-add law applies (workspace grep + harness build), and the doc names its distinction from `claimed_by` (actively-held vs ownership-across-release — two meanings, never blurred); (2) owner-liveness sweep approved WITH the new planted case it implies — the owner-DIES-while-suspended path must still sweep (the new legitimate state's own leak mode, covered before it exists); (3) the two-colonist identity fixture ratified (a one-colonist same-value test PASSES on a build that swaps owners — assert WHOSE, not just WHAT); (4) `until` stays absolute this row, drift filed. Site-6-first order confirmed. | Carry the old invariant (fires on the design itself), or re-claim by kind without ownership (deadline identity-swap between colonists — the horror the field exists to prevent). | Yes — field is additive with a stated determinism story; nothing shipped; 5b already unblocked and building. |
| 71 | 2026-08-08 | **#69/#70 MECHANISM RETRACTED BY THE BUILDER'S TRACE — the watchdog is INNOCENT and the pre-fix is CANCELLED before a line was written.** 5b's precision trace (the sharpened target Opus specified) showed the truth: the "occupancy" signal was a RESERVATION set at RestAt creation, not physical arrival — sdist never dropped below ~17 across the whole 10.13s window, jump-attempt spikes with zero forward progress, stuck_time climbing smoothly to exactly 10.0. **The watchdog fired CORRECTLY on a genuinely stuck TRAVEL attempt; no colonist ever slept and was interrupted; the original "sleep never starts" reading was right all along** (the un-retraction re-retracts; the full circle is the record's to keep). RULED: (1) suspend-during-occupancy is DEAD — nothing built, caught pre-code by the trace both seniors' ratified diagnosis said would merely confirm the shape; the THIRD lane corrected both above it, which is the ladder's best hour. (2) The phenomenon RE-TYPES as the TRAVEL ROW's best specimen yet: a bed ONE BLOCK ABOVE the plateau floor fails 100% of FIRST approach attempts (jump spikes, no progress) and succeeds on retries — reproducible, with a matched pair ACROSS ATTEMPTS on the same target (what differs between attempt 1 and attempt 2?). The travel row keeps absorbing everything, consistent with the campaign verdict. (3) TWO naming defects file: the engine's bed-slot "occupant" means RESERVED-not-ARRIVED (the label family, engine edition), and the fixture's `occupancy_interruptions` counted reservation-drops — both re-label, and the fixture gains a TRUE position-based arrival signal so its counter measures what it claims. (4) Step 1's machinery-proven status STANDS (the cycles did complete — via retries); the unification proceeds unchanged (never depended on the watchdog); the corrected acceptance is now "flag stays green AND first-attempt travel succeeds," pending the travel row. | Keep the watchdog fix on "it might still help" (it would patch an innocent mechanism — the exact class #67 killed), or let the mislabeled counters stand (two more fields whose names lie). | Yes — nothing was built; the trace cost one run; the travel row gains its richest specimen. |
| 70 | 2026-08-08 | **SLEEP-KILLER PRE-FIX OPENED — the registered branch fired at its extreme: rate 1.0, eleven interruptions, ZERO clean first sleeps in 7 seeds, one outright failure.** Ruled on the grounds Opus correctly separated: the MECHANISM justifies the pre-fix (the watchdog times out on stillness and sleeping is ALWAYS stillness — no geometry exists in which a sleeping colonist moves), with the fixture's 1.0 as confirming evidence rather than sole basis — the distinction that survives if another geometry later measures 0.7. FIX SHAPE, pre-analyzed: reset-on-arrival is NOT sufficient (the data shows the clock ran DURING sleep — released exactly 10s after occupancy), so the shape is SUSPEND-DURING-OCCUPANCY: the stall clock does not accumulate while the colonist is in an active restoring state — narrow, keyed on the occupancy state that already exists, no drive-awareness needed, cleanly subsumed by unification's site-2 later. One SDIST trace run FIRST confirms the clock's behavior before building (pre-stated: accumulating-during-occupancy ⇒ suspend is the fix; anything else names itself). ACCEPTANCE, two instruments honestly split: the FIXTURE is the benefit gate (counter → zero interruptions on the CORRECTED formula — 5b's was dimensionally wrong and coincidentally right at 0 — plus seed 55 completing in budget, plus the ENDURE regression assertion holding for the unreachable case); the CORPUS FAN is the harmlessness gate ONLY (b5 contains no beds — expected delta exactly ZERO, and any movement indicts the change). Also recorded: 5b caught Opus's matched-pair ask having NO POPULATION (the counter didn't exist in the run that suggested it — third falsifier-precondition failure today, first caught by the builder before the run was spent) and named their own instrumentation gap honestly (arrival lands inside a pre-existing headroom window their instrumentation doesn't watch). | Ride the unification anyway (weeks of every colonist losing every first sleep, on a mechanism already proven universal by argument AND measured at 1.0), or build full drive-awareness now (touches everything site 2 rewrites; suspend-during-occupancy is the minimal sufficient shape). | Yes — one trace run, one small fix, the fixture already measures before/after; the fan bar is exact-match. |
| 69 | 2026-08-08 | **SLEEP-KILLER DEFECT OPENED — the feature era's first fixture found a real player-facing bug on its first run: the travel watchdog interrupts WORKING sleeps.** Sleeping is standing still, and the Traveling-state stuck-watchdog has no arrived-and-restoring exemption — so a colonist in a REACHABLE bed is released at exactly STUCK_TIMEOUT (10s) with ZERO rest gain and can't retry for exactly PREEMPT_COOLDOWN (60s); 2 of 4 fixture seeds show it; the one completed sleep in the original trace WON A RACE. This partially un-retracts Opus's §4b (his retraction was right for UNREACHABLE beds — designed ENDURE — and wrong as the general claim; his own error class named: absence of the case in his sample read as absence of the case). One chain, two cases, opposite verdicts: designed degradation on unreachable beds, DEFECT on reachable ones — the composition law again. RULED: (1) the defect files with its one-line repro and two exact constants; (2) the FIX RIDES THE UNIFICATION's site-2 work (the travel machinery becoming drive-aware is the same family as the watchdog learning "arrived-and-restoring") — UNLESS 5b's per-seed interruption distribution shows it dominating everywhere, which would warrant a scoped pre-fix; (3) 5b's unprompted `occupancy_interruptions` counter is the acceptance instrument ("a fixture that passes by WAITING OUT a bug hides the bug" — the exclusion-vs-absence law applied by the builder to their own fixture) and the fixture's 160s budget is correctly scoped as proving the machinery, not fixing the watchdog. | Fix the watchdog now as a point patch (touches travel timing for every job kind pre-unification, on a mechanism the unification rewrites weeks-to-days later), or leave it unfiled as "specified" (it is specified for the OTHER case). | Yes — filed with repro; the counter keeps measuring it; the distribution decides pre-fix vs ride-along. |
| 68 | 2026-08-08 | **AUTON-2 UNIFICATION DESIGN APPROVED — FAMILY 1 (persist the self-job entry), chosen because unification makes it a CONSEQUENCE rather than a change:** the fresh-id-per-retry existed only because self-jobs bypassed the arbiter (nothing would ever re-SELECT one, so the preempt pass had to re-CREATE); with the arbiter selecting need-drives, the entry persists and strikes accumulate like Mine's — and family 2's store's-unit obligation is AVOIDED entirely (no new producer, no denominator change) rather than discharged. All five GUARD-6 consumer sites enumerated before any retirement, including a UNIT TEST asserting the old mechanism (rewritten, never deleted — a test that no longer compiles is not a passing test). Two hard riders: the orphan-sweep's premise dissolves but the build must DEMONSTRATE no self-job can become unclaimable-and-unswept; and site 4 (Despond carve-out) is read against the new ranking before "preserved by construction" is trusted. Design's own self-catch: the urgency formula made entry and exit the same condition at the threshold (oscillation by construction) — RULED: the need-drive's EXIT is owned by the job's own completion (restore to comfort+margin, already built, sits above the interrupt threshold — the hysteresis is the entry/exit gap that exists today); no third anti-thrash mechanism. ENDURE unchanged (correct and tested); the travel defect explicitly NOT fixed by this (more destinations, not fewer — named before the build). | Family 2 (new (colonist,target) accumulator — a new producer with denominator obligations) or family 3 (bespoke self-job rescue — third mechanism). | Yes — design doc; 5b builds after step 1's fixtures prove the band reachable. |
| 67 | 2026-08-08 | **§4c RETRACTED BY ITS AUTHOR — the release→sweep→cooldown→work-meanwhile chain is the DESIGNED path (specified verbatim in preempt_scenario's doc, asserted numerically, and 5b's measured rate is CONSISTENT with the designed bound). Ledger amended: the "tenth costume" line comes OUT; the bench-the-pair and cooldown-semantic fixes are DROPPED AS WRONG (they targeted a working, tested guard), not deferred.** What survives is sharper than what died: **ENDURE cannot distinguish UNREACHABLE from UNREACHED** — a correct, deliberate, tested degradation silently absorbs travel failures it was never built to cover, which is WHY displaced-colonists never surfaced as a bug across five appearances: the design catches it and calls it normal. THE DESIGNED DEGRADATION IS THE CONCEALMENT — that replaces the retracted line in the ledger. Rulings: (1) the REAL row is the TRAVEL FAILURE, seed 7 / job 33 / bed (21872,16025,250) as live repro — 5b's site survey is ON (Opus withdrew his own don't-survey), and the row's acceptance criterion is ruled: unreachable-vs-unreached becomes OBSERVABLE (a colonist who could have arrived and didn't must not render as one who couldn't). (2) AUTON-2 STEP 1 UNFREEZES — the preempt mystery is resolved (designed behavior + an absorbed travel bug); the planted case builds with its bed REACHABLE by construction, plus a second planted case discriminating unreached-from-unreachable for the travel row. (3) Template gains its third required line beside GATE FIELDS: "EXISTING TESTS/FIXTURES COVERING THIS MECHANISM: ___ / NONE FOUND" — exhaustive implementation reading cannot tell you what was MEANT; only specs and fixtures can, and one grep would have saved the whole dropped row. (4) Process note preserved: Opus's forced ordering prevented the damage with a WRONG theory behind it — the ordering laws pay even when the reasoning that invoked them is off. | Keep the bench-the-pair direction on "it might still help" (it patches a guard the tests assert as intended), or skip the survey again (the absorbed travel bug stays invisible — five appearances and counting). | Yes — nothing was built on the dropped fixes; the survey is one run; the template line is one grep per row. |
| 66 | 2026-08-08 | **#65 AMENDED: the self-rescue diagnosis closed COMPLETE, the fix gains a COMPLETENESS CLAUSE, and the row's severity honestly REVISED DOWN.** Opus found the decisive fifth gate: self-rescue is the ONLY caller whose LADDER TIER IS DARK (`AUTO_LADDER_ACCESS=false` — Ben's own B6 live-test hotfix, a ruled trade-off) — stairs-or-nothing for exactly the pit geometry stairs can't route; 0/71 emissions ever, at two commits. The absorber is measured: the teleport fail-safe fires in 44/48 seeds — nobody dies, entombment stays impossible; colonists TELEPORT instead of digging, and the carving subsystem is dead code behind a working backstop. THE CRITICAL CATCH (§3): the paint mask and the dark tier are EACH INDEPENDENTLY SUFFICIENT for zero emissions — #65's pre-registered fix could ship, green-fan, and change NOTHING (the matched-control law pointed at a repair). AMENDMENT RULED: the fix must clear EVERY sufficient blocker or ship explicitly-partial with the remaining blocker named; the completeness path exists in-tree — emergency's arm lights the ladder tier via a scoped condition (`emergency_owner`), so self-rescue can light it the same SCOPED way without touching Ben's global hotfix (the queue-fight concern was auto-pillars in general work, not rescue corridors). Value ruling unchanged: rescue outranks paint, notification required. RANKING: with the absorber measured, the row argues on LEGIBILITY (players watch colonists teleport, 92% of seeds) and DEAD-CODE grounds, not safety — Opus withdrew his own escalation — and queues AFTER AUTON-2's chain. Ledger note: all four of today's findings are COMPOSITION defects with no wrong site anywhere; our instruments are single-site; the derived-quantity check is the first compositional instrument and more of that class will be needed. | Ship the paint exemption alone (measured: changes nothing — 0/71 either way), or re-light the ladder globally (reverses Ben's live-tested hotfix; the scoped path doesn't). | Yes — nothing built; the emergency arm is the in-tree pattern; Ben can veto the scoped tier-lighting here. |
| 65 | 2026-08-08 | **THE RESCUE CARVE IS GATED ON THE PLAYER'S PAINT — discriminator approved now, and the design ruling PRE-REGISTERED so no round-trip when it lands.** Opus's read: `plan_access` may only dig inside `board.designated` (the player's paint, +1 XY, floored below but UNBOUNDED above) — so a colonist stranded ≥2 outside every painted region has no cell the planner is PERMITTED to dig, and the refusal renders identically to impossibility (the strong verdict again — no wrong site anywhere; the mask is CORRECT for proactive descent, merely shared with a question it doesn't answer: "where does the player want digging?" vs "where may I dig to save a life?"). APPROVED NOW: his one boolean at the None site — is the stranded colonist INSIDE the mask? — separating geometrically-impossible from administratively-forbidden; it also decides whether this MERGES with AUTON-2 §4c's never-arrives bed finding (fifth displaced-colonist appearance; if stalled colonists sit outside the mask, one row, not two). PRE-REGISTERED RULING for the outside-mask branch, per the codebase's own precedent (DPA-0: "EMERGENCY plans stay free unconditionally — a trapped colonist must never be material-gated out of rescue"): **RESCUE OUTRANKS PAINT — self-rescue/emergency carves get a rescue-scoped predicate (minimal-footprint corridor to the colonist), exempt from the designation mask, WITH honest player notification per the behavioral-message standard ("colonists carved a path to rescue X" — a hole the player didn't paint must never appear silently); PROACTIVE DESCENT STAYS PAINT-GATED (economy work is correctly player-authority).** The same value question was ruled once for materials; paint is not more sacred than wood. Also in the message: §4c resolved — the sleep is never interrupted because it NEVER STARTS (travel watchdog releases a colonist that never arrived; every cleanup site correct; "a complete, correct cleanup path is indistinguishable from a completed task" — tenth costume, first with no wrong site); fix direction endorsed as benched-until applied to the (colonist,bed) PAIR (both current records live on the colonist, so retries re-pick the same bed), held pending 5b's trace. | Rule the design only after the boolean (costs a round-trip on a value question the codebase already answered once), or dilate the mask globally (widens ALL digging without player intent — rejected; the exemption is rescue-scoped or nothing). | Yes — the boolean is read-only; the pre-registered ruling activates only on its outside-mask branch and Ben can reverse it in this log. |
| 64 | 2026-08-08 | **AUTON-2 RE-SCOPED — my basics were refuted by the reads: the needs machinery ALREADY EXISTS COMPLETE AND RUNNING (Needs component, per-tick decay, sleep/food restoration, personality-staggered interrupt thresholds, worst-need candidate scoring, anti-thrash guards), and Drive's own doc comment says self-jobs are deliberately not variants with "the full unification is AUTON-2's job."** My "extend Drive with Rest/Eat" would have built a second mechanism beside a complete one — the stuck_strikes duplication error at feature scale — and Opus REFUSED to spec it, which is the design-flow's falsifier discipline working exactly as intended. Ruled re-scope: (1) **CONSTANTS READ FIRST** — the tuned decay/interrupt values decide the timescale hypothesis (the playthrough's "needs inert" was likely a 4.5-min session against long-game decay — the mechanism never met its precondition, farm-z's twin; and the same hypothesis may explain BOTH expected-red scenarios, which would make them tuning artifacts rather than design deferrals). (2) **THE ROW IS UNIFICATION** — retire GUARD 6's exempt-occupancy so self-jobs compete inside the arbiter: the doc's own stated debt, the fifth pre-built door tonight's work has walked through. (3) **TUNING TARGET SET BY PRINCIPLE, not minutes: need cycles align with the GAME DAY** (rest ~once per night, eat ~2× per day, whatever the day length is) — "visible within a session" follows from that, not from an arbitrary constant. (4) **UNLOADED-CHUNK RULING: (b) catch-up-on-reload** — one deterministic decay application from elapsed sim ticks on reload (a per-colonist last-decay-tick, no wall clock, no new per-tick work — respects both determinism-by-construction and the observer-effect budget); (a) freeze rejected as inheriting a player-attention-dependent world by silence, (c) background tick rejected as new steady-state cost. Opus validates (b) in the flesh-out. | Build Drive::{Rest,Eat} as originally specced (a duplicate arbiter beside a running one), or inherit chunk-freeze silently (need state becomes a function of player attention, undeclared). | Yes — nothing built; the re-scope is the spec's foundation; (b) is one field + one application site. |
| 63 | 2026-08-04 | **#62 FARM RULING AMENDED — the P1 premise was falsified within the hour: zero-jobs was the OPERATOR'S site-pick error (5b's own, painted z=418 over ground at 415–417), not a generator bug — proven by repainting at surveyed height on the SAME live colony: 13 tills completed, 13 sows resolved. #60's harness red was NOT reproduced live; the opposite.** My registration hypothesis was falsified by evidence that already existed (one shared `board.farms` Vec, registration logged live both runs) — I ruled a mechanism from a plausible story where 5b's survey found the input error. The REPLACEMENT row is better than the retracted bug: **FARM PAINT IS SILENTLY INTOLERANT — zero height tolerance (`is_filled` at exactly plot.min.z, every column) and zero error feedback, so a player one block off gets TOTAL SILENT FAILURE indistinguishable from a patient farm.** Mine forgives via box-scan; other kinds resolve surface-relative via z_extent; Farm has neither — the exclusion-rendering-as-absence law at the PLAYER SURFACE, its eighth costume and the first a player meets. Rulings: (1) farm OFF the P1 list; (2) the FARM-PAINT row opens (Fable-basics: resolve paint surface-relative reusing the existing z_extent machinery; if zero valid columns remain, the paint REFUSES with a player message — never silent; Opus fleshes); (3) 5b's recommendation adopted — check the HARNESS farm scenario for the same z-mistake class before assuming #60's red is a deeper defect (cheapest explanation first, just demonstrated real); (4) the growth-clock-gated-on-connected-player question runs as a CHEAP CHECK, flagged-not-asserted, exactly as 5b filed it; (5) AUTON-2's opening priority is genuinely open again — the design proceeds as launched with needs-inert as its motivating evidence. The correction chain (operator surveys their own input, states it plainly; reviewer retracts his own headline) is the culture the ledger exists to build. | Keep the P1 on the retracted premise (spends a builder on a bug that doesn't exist), or file the silent-failure as polish (it's the campaign's central defect class at the surface where a real player hits it first). | Yes — the paint row is small; the harness check is minutes. |
| 62 | 2026-08-04 | **PLAYTHROUGH RULING + AUTON-2 LAUNCHED (same turn, clause 6). Headline: run one found a P0 the corpus was STRUCTURALLY INCAPABLE of finding — every real client disconnect crashed the server (double-borrow in handle_client_disconnect; harness never performs real network disconnects). Fixed, crash-then-absence reproduced, before the scored run.** Rulings: (1) **LIVE SESSIONS BECOME A RECURRING GATE** — one scored live session per milestone, permanently; an entire defect class (real network/client lifecycle) is invisible to every fixture and fan we own. (2) **FARM IS A P1 BUG ROW, NOT AUTON-2's opening priority** — 49-cell plot, ZERO jobs in 4.5 live minutes while the HARNESS farm works: that's a live-vs-harness divergence (gate-must-test-live-path class — likely the painted-designation path never registers the plot where the job generator reads), assigned to 5b NOW in parallel with AUTON-2; "whatever played worst leads" meant AUTON-2's internal priorities, and farm isn't AUTON-2 scope — it's a regression a player hits in minute one. (3) **B′'s scope correction ACCEPTED**: two unreachability paths compete and the stronger (plan_access→None, good design) catches most cases first — B′'s real population is jobs ambiguous enough to survive plan_access yet fail real walks; narrow-not-empty (harness-proven), three player-natural trap nulls with understood reasons, and 5b's stop-at-three discipline (refusing to escalate ingenuity until something fires = refusing to manufacture the population) commended. (4) Instrument fixes ratified: the LYING VERSION BANNER root-caused (build.rs had zero rerun-if-changed — an attestation that lied, now fixed worktree-correct); inspect_cell's read-lag demoted to colour; the survey tool's bottleneck-blindness named. (5) **AUTON-2 LAUNCHES NOW** — needs-as-drives per the dossier, Fable-basics→Opus-flesh flow, with needs-inert confirmed live as its motivating evidence. Scorecard at bastion-test-evidence/live-playthrough/. | Treat farm as AUTON-2's first feature (leaves a minute-one player regression waiting behind a design phase), or skip recurring live sessions (the P0 class stays invisible until the next real player). | Yes — farm row is a fix; live-session cadence is schedule; AUTON-2 is the planned next feature. |
| 61 | 2026-08-04 | **ROW B′ SHIPS — re-scoped claim: correctness + player visibility, null corpus aggregate, harmlessness PROVEN. And Ben re-affirmed full authority ("you follow your recommendations, you have full control"), so the ship call is made, not asked.** Evidence: 37/37 both independent fans (old version's 37→36 regression GONE); noise floors measured FIRST and exactly zero in both arms; zero outcome movement on all 48 seeds AND conditioned on the 11 exposed seeds; release counters zero-delta; seed 76 clean with manufactured crossings provably absent. The fix's value — colonists stop paying for proven-hopeless retries; blocked designations honestly reported — is player-facing and structurally invisible to the b5 window; the LIVE PLAYTHROUGH is its real acceptance test and runs NEXT per Ben's ordering. | Hold for corpus-measurable benefit (a bar this value class can never clear — the corpus can't see player experience), or ship without the playthrough behind it (leaves the claim untested at the level it lives on). | Yes — env-gate remains; playthrough scores it within hours. |
| 60 | 2026-08-04 | **ROW B: NET HARMFUL AS BUILT — back to DESIGN, not tuning. Both #59 escape routes closed by the two disk reads.** Exposure was 11/48 (not ~2 — the dilution hypothesis REFUTED: 9 of 11 exposed seeds unchanged, so conditioning on exposure still shows no benefit); and the −1 seed (76) is the harm branch at its worst: base never crossed the strike threshold at all (count 0), the variant's OWN per-tick overhead perturbed timing enough to push a job over the threshold, the bench then fired on the job its overhead helped manufacture, and a 27/27 PASS became a FAIL. The observer-effect law with consequences: acting isn't passive either. Tuning is closed (no threshold fixes a mechanism whose overhead creates the condition it punishes); re-scope-to-null is closed (the aggregate isn't null, it's negative where outcomes changed). Ruled: Row B returns to design behind its env-gate (in-tree, off by default, ships nothing); redesign constraint set — the bench's per-tick COST must be measured and budgeted below its benefit (candidates: event-driven on the release path instead of per-tick work; escalate far rarer; fold into the existing churn-detector cadence), and any redesign's paired A/B inherits the noise-floor discipline. Row A stands untouched (the visibility win is live). Seed 90 gained +1 and still fails — even the favourable case doesn't buy a pass. | Ship it env-gated-off and tune later (leaves dead harmful code as an attractive nuisance), or keep tuning thresholds (spends fans on a mechanism the evidence says cannot be threshold-fixed). | Yes — nothing shipped; flag stays off; the design returns to the packet with the new constraint. |
| 59 | 2026-08-04 | **Row B ship gate NOT MET at 48 seeds (37→36 pass, deltas noise-shaped, net zero) — HELD, with the diagnosis ruled before any tuning: CONDITION ON EXPOSURE first.** The n=1 green was a favourable draw (its +1 is one of exactly two nonzero seeds; the other is −1) — single-seed-is-a-lottery demonstrated on our own fix hours after writing the law. What held at scale: mechanism works (bench→graduate→clear), release counters zero-delta 48/48, Row A's counter undisturbed. Ruled next, all disk-reads before any paid run: (1) TREATMENT EXPOSURE per seed — benching plausibly FIRED on only ~1-2 of 48 seeds in this window (the delta fields say so), so the aggregate A/B was structurally diluted ~24:1 and a null aggregate is EXPECTED even if the fix works perfectly where it fires — the exercised-denominator law applied to our own trial; the real signal is the conditioned comparison on fired seeds. (2) THE −1 SEED'S MECHANISM — did benching fire there? Fired ⇒ candidate real harm (benched a recoverable job — the transient/persistent boundary wrong at threshold 3) and the knob needs tuning; never fired ⇒ the regression is the established noise floor and the "one fewer pass" is a draw artifact. (3) Only then the fork: tune (threshold sweep WITH repeat-run controls per arm — Opus's noise-floor warning adopted: 2/48 ≈ noise, naive sweeps produce favourable-draw stories) vs re-scope Row B's claim to correctness+player-visibility with null aggregate (a Ben-visible re-scope if chosen). Playthrough stays HELD per Ben's ordering. The gate telling us the knob needs tuning is the gate working. | Ship on the n=1 + mechanism-works evidence (a null result with a possible regression attached), or jump straight to a threshold sweep (burns fans reading noise as signal). | Yes — nothing shipped; wave27 is on disk; the reads are free. |
| 58 | 2026-08-04 | **Row A SHIPS on ruling (a)+(b): outcome-neutral IS the bar for a report-only row, AND the packet's claim is amended to what was measured.** The derived-set re-check passed with the #55 exception finally implemented (`--expect-move`, both-direction guards, 11 controls); declaring-not-ignoring then EXPOSED three violations the 30 had masked — in-flight snapshot fields (claimant identity, one mid-swing progress value) shifted by the sub-tick timing of the retain scan G4's analysis PREDICTED a populated store would pay. Every aggregate, verdict, and release counter is byte-identical across all 48 seeds. Amended claim: "outcome-neutral (measured, 48/48); not cycle-neutral (in-flight snapshots may shift sub-tick; mechanism understood and predicted)." Also ruled: those fields get a third holdcheck class — IN-FLIGHT SNAPSHOT (alongside deterministic and wall-clock) — so future checks compare them with that awareness instead of rediscovering this. Prediction-measurement agreement recorded as a survival. Row B prep proceeds. | Hold Row A for cycle-neutrality (a bar no report-only row can meet on a store that must be scanned, and one the packet never needed), or ship with the original overclaimed wording (a sufficiency claim with a known counterexample — tonight of all nights). | Yes — wording is a packet edit; the class is a holdcheck line. |
| 57 | 2026-08-04 | **First real hold-check: VIOLATION (exit 1) — and ruled the RIGHT kind. Disposition: complete-manifest-and-re-check APPROVED with one hardening; window closes green only if the re-check passes it.** All four pre-registered falsifiers HELD (release counters byte-identical 48/48 — report-only confirmed; `Other` did not move; the split stands), the row's thesis measured (seeds 71 and 90 — structurally unrecordable before — now report), and the 30 violations are ONE field: `blocked_by` None→cell on exactly those two seeds — Row A's intended effect propagating into a pre-existing report field whose read-path Opus had proven in his own census and still failed to enumerate. The gate caught its own author's manifest gap; had 5b's offered `--ignore` been taken, this would have GREENed silently. HARDENING on the remedy: the new `--expect-move` seed set must be DERIVED from Row A's own counter (cross-instrument: escalation-fired ∩ diag-covered), NOT copied from the violation listing, and asserted equal BOTH directions (every expected mover moved, no unexpected movers). That converts a post-hoc explanation into a cross-field consistency proof. Also noted: the #55 exception existed but the tool couldn't express it — the law-needs-mechanism lesson again, now fixed in holdcheck itself. Bonus: 5b's cancel_region wipe confirmed at corpus scale (at_end zero on 48/48). | Close the window RED and re-scope Row A (punishes an explicable, confined, mechanism-derived delta as if it were noise), or accept the post-hoc set as-listed (a story fitted to data — the exact thing pre-registration exists to prevent). | Yes — one re-check run; the fans stand; nothing re-fans. |
| 56 | 2026-08-04 | **#55's mandatory hold-check had NO BASELINE — a gate that could not fail, caught by Opus's own pre-run manifest check. Ruled: TWO fans approved (baseline at pre-Row-A tip with per-seed JSON captured, then the Row-A fan), and the capture law is ratified: A FAN THAT PERSISTS ONLY ITS VERDICT IS NOT A BASELINE.** wave24 kept fanlogs only — no `_FULL.json` — so the most recent structured baseline (wave19) predates the build tip by ten commits and NINE fields, and the "load-bearing" hold-check I ratified in #55 would have GREENed over a comparison that couldn't separate Row A's delta from prior work. Both of us own it: he made the check mandatory without a referent; I upheld it without asking "against what." The two-fan fix clears two debts at once — the baseline fan retroactively corpus-validates the ten local-pins-only commits (correct under standing rules, never fanned) including nine fields nothing has checked at scale. Every future fan persists per-seed JSON + attestation before teardown or it's a verdict, not evidence. Declined the cheap alternative (ship Row A on local pins, defer fans) because it demotes the instrument row to an ordinary trusted change — the row's whole justification is the verified baseline. Cost: ~2 fan runs (~$0.50 at observed rates). | Ship Row A on pins now and fan later (faster, but the composition argument dissolves and the four pre-registrations go untested), or fan only the Row-A tip (single fan, but the comparison stays contaminated by ten commits). | Yes — fans are minutes and cents; the law is a capture-procedure line. |
| 55 | 2026-08-04 | **Opus's window-composition criterion UPHELD OVER MINE, with one refinement.** I ruled Row A's window composition by BUILD-READINESS; Opus overrode with VERIFIABILITY: an additive-only schema window re-baselines with a free, total correctness check (every pre-existing field must hold its old value — any drift is a bug, mechanically), while mixing in ONE mutating item deletes that check exactly on the fields it touches (intended and accidental change render identically — the day's own law). Decisive: Row A's four pre-registered falsifiers are all "existing field must not move" claims, so a mutating window would trade away the verification that justifies Row A landing first. His call: Row A + the three additive baseline-emission items now; the four mutating report-fixes in their own later window; classification by PROPERTY OF THE CHANGE, not effort estimates. My refinement, adopted: the axis is a DEFAULT with a named exception — a mutating item may join any window ONLY with an exact pre-registered per-seed delta for every field it touches (the merge bar generalized to windows); absent that registration, additive-only. Post-re-baseline hold-check mandatory. | Keep my build-readiness axis (stalls nothing but re-baselines blind on mutated fields), or run every item in its own window (maximal isolation, N re-baselines). | Yes — composition is scheduling; the extra re-baseline is bounded VM minutes. |
| 54 | 2026-08-04 | **WHOLE-PIPELINE READ VERDICT (the ≥3-places directive's formal output): DESIGN DEFECT, named invariant — plus separated point/fixture members. THE MISSING INVARIANT: a repeatedly-released job is assumed transient FOREVER — the churn-release path (site 11280, `TimedOut`) has no per-job persistence detection, no escalation, and DELIBERATELY no recording, so persistent rejection is invisible and unhandled by construction.** Its own comment names the premise ("transient congestion; retries are the mechanism") and farm's job 6 (4 claims, 4 vantages, never), seed 90's holdout (NINE attempts, extra window buys nothing), and seed 71's frontier (20 TimedOut) falsify it — same emitting site, three job kinds, proven by string-match identity not similarity. The SPECTRUM is measured, not conjectured: one path, one premise, outcomes from 2.5×-slow-but-recovers (seed 61, reclassified window-artifact WITH its slowness observation attached) through recovers-with-more-time (90's other cell) to never (farm, 90's holdout). Separated non-design members: worldgen-obstruction fixture gaps (71's tree-over-3-cells, 54's buried cell — the JULY RULE's 3rd/4th instances; fixture-audit row with the screen-then-query pattern, ~2/3 screen reliability), and 61's window artifact (corpus ledger). The symptom family ended at TWO members so the directive STANDS DOWN ON COUNT — having done exactly its job: it forced the read, the read found the shared mechanism, and the mechanism's evidence stands independent of the symptom bookkeeping (memberships moved in opposite directions — that is what decoupled groupings do). FIX ROW (gated, unbuilt): two parts — (a) the general invariant: per-JOB escalation threshold, Nth same-job release hooks #55 recording, preserving the stated no-noise intent (the per-colonist churn counter already exists; per-job is the missing half); (b) the SPECIFIC root of persistent timeouts, pending 5b's step-2 classification of 90's nine-release holdout — the densest single-cell evidence on the board. Gates: holdout classification first, FR15 paired A/B, prior-art already delivered. | Treat the three legs as three point bugs (the read's evidence says otherwise: one emitting site, measured spectrum), or hook #55 unconditionally (destroys the no-noise intent the design states — the escalation threshold preserves it). | Yes — nothing built; the verdict is a record; the fix row is gated three ways. |
| 53 | 2026-08-04 | **ARB-STARVATION row OPENED (task #59's contention hypothesis SUPPORTED 6/6), investigation-first — the strongest evidence-backed candidate on the board, and the day's chased mechanism was the wrong one.** The purpose-built counters (`starvation_cycles`/`starvation_crowded_cycles`, report-only, carrying my own documented decision rule) sat unread in every fan: five of six failing-adjacent seeds show worst-cell contention ratio exactly 1.000 (sixth 0.938), my registered kill case (starved with an EMPTY field) occurs in ZERO seeds, and seed 71 — wrongly predicted access-starved, correctly refuted — is ARBITRATION-starved: 15/27 cells open+unclaimed for the entire 360-cycle run with competitors present every cycle. Mechanism supported: greedy arbitration, no cooldown after failed attempts, hard cells lose the score comparison forever. CAVEAT RULED INTO THE ROW: ratio 1.0 is NOT sufficient for failure (seeds 52/66 fully mined despite equal contention), so the row's FIRST deliverable is the discriminator — why 71 never recovers while 66 does — BEFORE any fix design. Sequencing: investigation read-only now (Opus lane, disk + specimens); fix design gated on (a) the discriminator, (b) prior-art survey per standing law (aging/priority-escalation/cooldown are textbook starvation cures; DF labor model), (c) FR15 paired A/B — an arbitration change re-rolls the whole colony economy. Fix slots behind the guard row + observability re-baseline. Also recorded: the recursive-schema lesson (161 leaf paths, 68 top-level — 93 fields invisible to a name scan, 69 varying; the answer-machine was built, ran six times, and nobody read it). | Jump the fix ahead of the guard row on evidence strength (builds an arbitration change before knowing why identical contention sometimes resolves, on top of an un-re-baselined instrument). | Yes — investigation is read-only; the fix is gated three ways and unbuilt. |
| 52 | 2026-08-04 | **Seam-bar row DOWNGRADED — #51's registered prediction was refuted on disk data twenty minutes after registration, before any build or VM spend.** Opus's "zero access-plan visibility" claim was itself wrong (a top-level name scan missed `b5_cascade_probe.access_emissions_max`, nested under a cascade-named field): emissions ARE recorded, and they refute starvation-as-harm — zero-emission seeds fail at the corpus base rate (29% vs 25%), eight seeds fire rescue + emit nothing + PASS, and the named subject seed 71 emitted THREE plans (never starved; 54/61 emitted zero and their nulls were right for the wrong reason). Ruled: the row survives as a CORRECTNESS/CONSISTENCY row (the code defect is real — gate fixed at one caller, live at the sibling, callee comment claiming global fixedness) but loses all fan priority: parked behind the three specimens, ships in a future cleanup window WITH the comment fixes, and its bar FLIPS to #46-class exact-match (it now claims corpus-neutrality; any drift = investigate). #48's "bar row may jump the guard row" branch DIES — the guard row proceeds first regardless of the trace, which stays queued for bed diagnosis only. Ratified from the same read: constant GUARD healthy vs constant DIAGNOSTIC inert (the carve-out on his own earlier rule); task #34 permanently parked with its number (n=1, wrong polarity — a matched pair impossible in principle); b55 family fields inert in the b5 corpus. The protocol note Ben should see: pre-registration + read-the-disk-first killed a compelling code-read narrative at zero cost — without it this row was headed for a fan. | Ship the bar removal now anyway on the code-consistency argument (spends a fan slot on a row with no evidence of current harm, ahead of three instrumented specimens). | Yes — row is parked not dead; if the observability row's per-caller counters later show starved cycles anywhere, it un-parks with that as evidence. |
| 51 | 2026-08-04 | **Seam-bar row gains a REGISTERED OUTCOME PREDICTION (Opus, wave-history measurement), and I kept the instrument-first order anyway.** Opus retracted his own "a green fan would be a false green" — measured across every wave on disk, the corpus is demonstrably sensitive to mining outcomes (memberships moved wave-to-wave; the exact-match bar fired on a one-line change at wave20). Registered NOW, before the change exists: removing the self-rescue bar should raise seed 71 above 5/27 and seed 54 above 16/27; seeds 61/90 must NOT move (their cells are claimed — plan starvation is not their mechanism; they are the named null). Fork: this made ship-now-without-new-instrument viable; I ruled KEEP the observability-row-first order — the outcome prediction says whether the fix helped, the per-caller counters say WHY/whether the mechanism fired at all (a null on 71 without counters is uninterpretable), and the row already rides a build in flight, so the safer order costs ~zero calendar. Bonus recorded: waves 19/22/23/24 byte-identical CLAUSE COMPOSITION across all four merge commits upgrades today's merge verdicts from "no drift observed" to "neutrality measured at full resolution." | Ship the bar removal now against the outcome prediction alone (faster by hours, but a null result would be unreadable and the mechanism unmeasured). | Yes — prediction is registered text; order is scheduling. |
| 50 | 2026-08-04 | **Opus's unilateral shared-config change UPHELD: scoped `.gitignore` negation (`!bastion-test-evidence/**/*.log`) so corpus attestation logs are versionable.** He found the blanket `*.log` rule had silently excluded every raw pool log — the files carrying the `COMMIT=` attestations that make every determinism claim citable — while the README indexing them WAS tracked (durable documentation of non-durable data). `wave19_FULL.json`, source of eight findings today, was simply never added. Fix verified side-effect-free (zero `.log` outside the evidence tree became visible), follows in-file precedent, 46 files now tracked (4.4M). Upheld over the `git add -f` alternative because -f re-creates the trap for every FUTURE wave: durability must be the DEFAULT of the evidence tree, not a per-file operator memory. Also ratified his proposed standing rule (third instance in one day: gitignored-vs-clean `git status`, the isinstance guard, the empty log): **an exclusion and an absence must never render identically.** | Veto the ignore change and use `git add -f` per file (leaves the trap armed for the next wave), or leave logs untracked (every attestation one disk failure from gone). | Yes — two-line revert; files stay in history either way. |
| 49 | 2026-08-04 | **#48's acceptance bar AMENDED — the corpus is BLIND to access planning, so an OBSERVABILITY ROW becomes the prerequisite and jumps the queue.** Opus enumerated all 75 corpus fields: not one reports access-plan state, while `b5_rescue_fired` is true in 44/48 seeds — the code path behind the colony-global bar is heavily exercised and completely unreported, which is precisely HOW the gate survived every fan (no fan could see it). Consequence: the bar-removal row's exact-match fan would be a FALSE GREEN by construction; per the acceptance framework's own ordering, the instrument must exist before the row. Ruled: CONSOLIDATE the seeing-instrument work into ONE observability row that lands first — the guard row's `benched_since_tick` limb + access-plan report fields (live `is_access` count, plan emissions, per-caller call/non-call counters at both `plan_access` call sites, `access_pending` duration) — pure read-only reporting (no world writes ⇒ safe to measure across, per the fixture-write discriminator), field-presence-guard verified (new fields ASSERTED present in real output before any result is read), then ONE re-baseline fan populates the new fields' baseline profile. Both behavior rows (guard generalization, bar removal) then measure against an instrument that can see them, ordered by 5b's trace result as #48 specified. Also ruled: 5b studies corpus seeds 52/54 INSTEAD of building a bed fixture — Opus found bed's exact discrepancy (offline probe YES / live router NO, 10 entries) deterministic across waves 19/21 in a scenario with NO beds and no plan_access involvement, so the split is general and already has a free permanent repro; a bed-specific fixture would have been built to chase a general phenomenon. Marked coincidence, hypothesis only: seed 52 carries BOTH anomalies (the guard row's green-flip AND the offline/live split) — to be checked, not assumed related. | Ship the bar removal against the existing 75-field exact-match bar (green-by-blindness — the exact false-confidence the framework exists to prevent), or build a bed fixture anyway (duplicates a deterministic repro we already own). | Yes — reporting fields are additive; the re-baseline is one fan; nothing behavioral moves until the instrument sees. |
| 48 | 2026-08-04 | **Self-rescue one-plan-at-a-time bar: row APPROVED, queue position decided by the precondition trace already running.** Opus's seam finding (code read at `460626a6e2`, not inference): the colony-global "any live access job anywhere blocks all new access planning" bar — which the codebase ITSELF declares a starvation bug and replaced with Chebyshev-8 pocket scoping at the descent caller — is STILL LIVE at the self-rescue caller (`.take(if access_pending {0} else {1})`), and the callee's M2 comment describes the fixed state as global ("disjoint pockets plan in parallel" — true for one caller, false for the other). Damage was observed once (run-17: 12 permanent unreachable leftovers) and ABSORBED by the F3 stale-access pruner — which only bounds UNCLAIMED-plan damage; a claimed long stair dig holds the colony-global bar for its whole duration. Textbook refusal-absorption. Sequencing: if 5b's probe trace shows bed's window had a live access job (⇒ bed's "fails at plan_access" is a NON-CALL, not a rejection), the bar-removal row JUMPS the guard row — it closes a red scenario and removes an absorbed wrong gate in one move; if not, it files behind the guard row as a latent design defect. Either way the row ships: pocket-scoped replacement mirroring the sibling caller's proven pattern, the callee comment re-scoped to NAME ITS CALLERS, exact-match fan with the complete pre-registered form. Also ruled: (a) the B6 fail-safe justification gap ("teleport backstop" rescues BODIES, does not grant WORK access — yet justifies AUTO_LADDER_ACCESS=false, leaving the self-rescue ladder tier dark) files as a row-candidate with a premise-check against the deep-mine ladder-access design (ladders=deep is our own design law), plus an immediate free comment fix naming its case; (b) capsule row stays banked — Opus retracted its bed-relevance himself (plan_access is a construction planner, not a body predicate; his own name→contents inference, self-caught, in the record at `9df4998ab1`); bed's walk-test order to 5b is COUNTERMANDED in favor of the trace (non-call vs rejection is upstream of any reachability question); (c) process rule adopted fleet-wide: never characterise a function you have not read the body of — semantic claims in findings are marked READ (lines cited) or UNREAD. | Fix the bar now unconditionally (one line, proven replacement — but behavior-re-rolling mid-queue with no trace yet linking it to any red), or leave it (the code keeps calling the same gate a bug at one caller while running it at the other). | Yes — one line either way; the trace is read-only probes already shipped; the fan gates the ship. |
| 47 | 2026-08-04 | **Guard-generalization row (filed by #46) APPROVED as a THREE-LIMB row — the guard ships with its visibility companion and its missing-kind coverage, or not at all.** Premise-check closed it cleanly: a stance-less benched job is retried every cycle by the fresh standability recompute and the amnesty sweep structurally cannot see it (its first line filters to flagged-unreachable only) — so NO livelock, but the class is INVISIBLE to every diagnostic (no flag, no blocked_by, no UI signal; "about to succeed" and "never will" are externally identical — third instance of the diagnosable-state gap). Limbs: (1) generalize the leave-unclaimed guard to `job.affordance != Untargeted` (the principled #64 predicate; comment names its case per the #46 rule); (2) observable benched state (`benched_since_tick` on BastionJobInspect, the #55 surfacing pattern — struct-field-add law applies: workspace grep + harness build); (3) planted Gather None-stance coverage — 5b's own scope-limit catch: b5's corpus places ZERO Gather designations, so wave20's "0 regressions across 48 seeds" is Chop-only evidence in practice and Gather rides the same path unverified (same shape as #63's coverage gap). Bar: exact-match fan with the complete pre-registered form — PREDICTED = baseline + seed 52 flip and nothing else / DISCONFIRMING = any other membership change / NULL = seed 52 doesn't flip (fixture-commit interaction; investigate before ship). Also ruled this exchange: the capsule-aware probe extension is BANKED with named triggers (bed's honest discriminator is a live colonist walk-test — ground truth beats a second proxy); 5b's probe-tool side branch approved to merge with two label corrections (base was d3235e5329 not the claimed 460626a6e2 ⇒ real merge, not FF; "harness-only" included 23 comment-only lines in bastion_jobs.rs — verified inert). | Ship the guard alone on wave20's n=1 (creates a new invisible-stall class on every kind where stance can return None), or hold everything until a live Gather failure appears (the coverage gap stays latent). | Yes — guard is one line; the field is additive; the planted test is pure addition. |
| 46 | 2026-08-04 | **The exact-match bar caught a one-line behavioural change hiding in the 'pure refactor' ON ITS DEBUT — ruled revert-and-file.** #64 silently generalized the leave-unclaimed guard from Mine-only to all targeted kinds; for Chop that benches stance-less jobs instead of claiming them blind, which flipped marginal seed 52 GREEN (n=1 improvement). A clean diff audit, dev-profile check, and two byte-identical spot-checks all missed it; only the corpus comparison caught it — and only after the baseline re-run PROVED the corpus deterministic at fixed code (first full-clause membership measurement ever: zero drift, 48/48). Ruled: revert to Mine-only (a zero-change row ships zero changes, surprises in the GOOD direction included); the generalization files as its own row with seed-52 as opening evidence and a blast-radius premise-check (how often is stance None per kind; does anything ever re-offer a benched job, or is this the livelock family with one friendly datum). Also: the offending line's comment was a sufficiency claim with unstated scope WRITTEN IN THE ROW THAT EXISTS TO STOP THEM — third instance, ours; the rule becomes a mechanical review step (grep diffs for deliberate/sufficient/reasonable-default/no-op; each hit names its case or bounces). | Ship the generalization now on its n=1 improvement (an unintended change riding a zero-change claim — the exact thing the bar was built to prevent). | Yes — one line either way; the filed row un-parks on its premise-check. |
| 45 | 2026-08-04 | **#64 narrowed to a PURE REFACTOR (Opus proposal, upheld): every kind declares TODAY'S behaviour, equivalence-verified; all stance CHANGES become separate evidence-gated rows.** Consistency forced it: we reverted Ladder's stance change at n=0 after a control refuted the physical-support predictor — Build/Bed/Farm's changes had the same n=0 on the same refuted predictor, so shipping them would be one standard applied twice in opposite directions. Farm's till/sow semantic split (real, code-verified) survives as a COMMENT with a concrete re-entry condition: ships when Farm's own control (blocked on the unexplained farm_tilled:false) demonstrates a failure the split fixes. The fan bar UPGRADES from directional to EXACT-MATCH (wave19 profile to the clause; only intended delta = b58's scenario flipping green on the evidence-backed material fix). | Ship the three stance changes on the physical-support argument (the predictor a control just refuted), or ship Farm's split on semantic merits alone (honest-but-unproven — the class this week kept reverting). | Yes — declared values are one-line edits; each stance row un-parks the moment its control demonstrates need. |
| 44 | 2026-08-04 | **#64 partial walk-back in daylight: Ladder's stance rule REVERTED (control refuted it); bed/selfgen controls now HARD-GATE the rest of #41.** The A/B control fired on the unexpected cell: WITHOUT the new ladder stance rule, all 5 rungs place (the red we'd attributed to stance was a FIXTURE MATERIAL MISMATCH — stone handed to a wood job); WITH it, 2/5 (the 'more correct' rule can return None -> middle rungs never claimable — a refusal-capable producer with no refusal-aware consumer). My own celebrated 'remote-placement explains partial completion' attribution dies with it — the code FACT stands, my story about b58's red was wrong. #64 keeps: the affordance table, required stamping, dispatcher, Farm split, material fix — value restated as 'proven behaviour declared + future assumptions become compile errors,' not 'fixes three scenarios.' New laws: refusal capability ships with refusal handling or not at all; PER-KIND CONTROLS BEFORE PER-KIND FIXES. | Keep the ladder rule and patch its refusal path (patching an unproven mechanism so it stops regressing — how a rule survives without earning its place). | Yes — revert is one deletion; the rule can return WITH its consumer if bed/selfgen controls ever prove the need. |
| 43 | 2026-08-03 | **#41 addressing REVERSED on evidence within hours: affordances are stamped ON THE JOB at creation (required field, no Default), not looked up by (kind, is_access).** Farm refuted my composite key — one designation kind reuses one job.pos whose MEANING flips by phase (till: solid ground, on-top correct; sow/harvest: the crop cell itself, on-top = the placement bug). No kind-keyed lookup can recover per-instance meaning; the creator knows and must declare. Required-no-Default makes the next silent Mine assumption a COMPILE ERROR. The per-kind table survives as vocabulary; the stamp is the addressing. Also: Farm held OUT of the row's acceptance — its till phase fails while holding the allegedly-correct stance (a counter-control), so Farm may carry a second cause; free discriminator recorded as its re-entry condition. | Keep the composite key and special-case Farm (third addressing patch in one design — the accretion disease inside the fix for the accretion disease). | Yes — the stamp subsumes the key; reverting is mechanical. |
| 42 | 2026-08-03 | **#41 implements a PER-KIND AFFORDANCE DECLARATION, not four scattered patches (design ruling at the forest-review fork).** Evidence: FOUR independent Mine-shaped assumptions confirmed in one claim path — the on-top stance default, the Mine-only claim-then-stuck guard, the arrival widening justified by 'mine-from-below', and (falsifier 2's third outcome) NO proximity gate on placement execution, so the widened arrival AUTHORIZES placing blocks ~6 away — which uniquely explains b58's PARTIAL rungs (low ones placed remotely, high ones never arrive). Ruled: one tightly-sized KindAffordance table (solid_target, stance rule, max_work_distance, claim_requires_stance) consulted at the four sites; Mine/Chop declarations must reproduce current behaviour bit-for-bit (equivalence-flag pattern); acceptance = 3 red scenarios + boundary-near-arrival-radius prediction + directional corpus bar. | Four per-site kind-check patches (faster to write, reproduces the 6-mechanism accretion disease, and the FIFTH Mine assumption bites a scenario instead of a declaration). | Yes — the table is additive; reverting to per-site checks is mechanical. |
| 41 | 2026-07-31 | **Stance fix ACCEPTED on an AMENDED bar — my pre-stated bar fired and I am overriding it with the reasoning visible, not quietly.** The chop standability fix improved the corpus (14→12 failures, chop-family clauses 14→11, seeds 51/55/69 fully fixed, 61 improved, 80 unchanged as predicted) but produced 2 seeds with NEW red clauses (90: clean→25/27 one-short at identical friction; 71: already-failing, worsened 24/27→5/27 with downstream cascade). My bar said any new red = regression. **Mode analysis shows both are KNOWN failure modes redistributing across seeds under re-rolled scheduling — the exact phenomenon the wave-1→3 terraform control established as legitimate** (seed 113 went clean→failing then and we correctly called it perturbation). This is the SECOND bar I mis-specified for this one fix ('byte-stable', then 'no new reds') — both wrong for scheduling-perturbing changes. **New standing law: for scheduling-perturbing changes, acceptance = aggregate direction + NO NOVEL MODES + target-family improvement; per-seed membership is never the bar.** | Hold the letter of the pre-stated bar and revert a fix that made the corpus strictly better (rule-following over evidence), or re-fan hoping membership settles (it will not — re-rolls are deterministic per commit). | Yes — the fix is one commit; the amended law is one memory entry; both reversible. |
| 40 | 2026-07-30 | **Aging fix REVERTED same-day (pre-stated rule fired).** The job-aging change (shipped hours earlier marked UNPROVEN) was never corpus-fanned — I accepted a mechanism argument ("inert on the mine corpus, 8/9 excluded by blocked_by") in place of a corpus run, skipping my own re-baseline rule once. First corpus measurement (5b local, then wave 15 VM on the pinned commit): **14/48 -> 16/48 all-clause failures, 2 newly broken seeds (74, 76), ZERO improved.** Net-negative from a change with zero demonstrated rescues, so the revert rule I pre-stated before the data landed fired mechanically. Clean split kept the report-only instrumentation; the score-mutating lines and constants are out. Also resolved in the same fan: NO cross-platform divergence (9/9 seeds agree Windows-vs-VM), and 5b's in-flight #56 gate was exonerated of the seed-76 flip (aging did it). | Keep aging and tune constants blind (patching an unproven mechanism), or keep it dormant behind a flag (dead code carrying a live risk). | Yes — re-land any time WITH the acceptance test it is owed: a deliberately-constructed starvation scenario where it demonstrably rescues a failure. |
| 39 | 2026-07-30 | **Blocked-designation feedback GREENLIT as a player-facing fix — reversing my own earlier parking of it (prompted by Ben asking whether we had actually FIXED anything).** Honest position: everything landed today (27/27 gate, failed_clauses, build-site terraform, chop expanding search, mine footprint air-clear) fixed the TEST HARNESS, not the game — the 33.3%%->9.3%% improvement is our ruler straightening, not colonists mining better, and a player would notice no difference. The one genuinely player-facing defect found today was parked as a 'design question': a player can designate a mine, an obstruction blocks access, and colonists silently do nothing FOREVER with no indication why. Ruled: (1) never a silent no-op — surface the unreachable state we already compute; (2) the cascade must NAME the blocking cell, not report '27 cells unreachable'; (3) auto-clear allowed ONLY for natural obstructions with an existing work type, as a VISIBLE generated job, never player-built structures. Prior art: RimWorld alerts on unreachable jobs; Dwarf Fortress lets them sit silently and it is a known source of 'why is nothing happening' — DF is the cautionary case, not the model. | Auto-clear everything (colony quietly demolishes things the player wanted kept), or keep it a pure UI signal with no auto-clear (honest but the player still has to diagnose and fix it manually). | Yes — three separable parts; the auto-clear half can be dropped independently of the visibility half. |
| 38 | 2026-07-30 | **Chop-oracle: adaptive real-tree search, NOT a planted tree (full-authority ruling).** Ground truth proved the 37.5%% ch_* cluster is 18/18 precondition-unmet, 0/18 detection misses — the largest cluster in b5 was a test that COULD NOT RUN reporting itself as a broken game. Builder proposed guaranteeing a tree at the site (the same "fully determine your geometry" rule that fixed build-stall). Ruled against: FR10 exists to detect REAL worldgen trees — real trunk heights, canopy shapes, generator edge cases — and planting one converts it into "the oracle finds the tree we built," keeping the green light while discarding the reason for it. Instead: (a) precondition_unmet is never a gate failure, removing 18/48 false reds; (b) site selection becomes an EXPANDING-RING search for a real tree, lifting engagement from 62.5%% toward ~100%% while keeping real geometry; (c) engagement rate becomes a permanent first-class metric so the gate can't decay to vacuous-green unnoticed. Genuinely treeless regions stay precondition_unmet with a recorded reason — a fact about the world, not a defect. | Plant a deterministic tree (reliable gate, but stops testing real worldgen — the one thing it was for), or accept 37.5%% non-engagement (honest, but coverage is a lottery and the gate is weak). | Yes — all three parts are localized to the chop phase; revisit if expanding search proves expensive or still under-engages. |
| 37 | 2026-07-30 | **Failsafe-teleport clause SPLIT OUT of the mine-completion gate and made REPORT-ONLY (reviewer finding, mid-fan).** Opus reviewed 5413915f71 while the fan was burning and found `locomotion.2 == 0` gates the B24 colonist-RESCUE counter — a different failure class from "did the volume get cleared". A seed whose mine is 100%% complete but where a colonist needed a rescue was being counted as a mine-completion violation, so **the headline "19/48" is likely an over-count of a conflated metric**. Two supports: the field is documented at bastion_jobs.rs:3479 as "REPORTED telemetry, NEVER GATED", and its own increment site defines success as reverting to a "RARE backstop" — a `== 0` gate demands *never* from a mechanism designed to be *rare*, failing CLOSED on healthy runs. Ruled: keep `mine_blocks_mined == 27` (your directive, sound); split the rescue clause into its own named clause, report-only until the fan measures its base rate, then set the threshold from data. | Gate teleports at 0 now (risks replacing a too-loose tolerance with a too-tight one, on a guess), or drop the rescue signal entirely (loses a real stuck-colonist indicator we already pay to compute). | Yes — it becomes pass-affecting again with a one-line change once the base rate is known. |
| 35 | 2026-07-30 | **Mine-completion tolerance RETIRED; ~40%% of seeds were failing behind it (your directive, executed).** b5 gated mining at `mine_blocks_mined >= 26/27` (96.3%%), with a source comment reading "one window short" as scheduling noise. Two already-computed signals — `mine_cleared` and `locomotion.2` (failsafe teleports, i.e. the instrumented "a colonist got stuck" flag) — were printed every run and never gated. Now: `== 27 && mine_cleared && teleports == 0`. Positive control validated BEFORE spending compute (seed 1337 reproduces B78 exactly: 25/27, 3 teleports). Local corpus: **19/48 seeds violate it** (11 fully clean, 18 fail for unrelated pre-existing reasons). The old gate passed nearly all 19. | Keep a tolerance and treat the shortfall as throughput (what we had — it was hiding a reproducible stuck-forever bug), or gate at 27 but leave teleports ungated (would still pass colonists that only finished via failsafe rescue). | Yes — the gate is one conjunction in b5; loosening is a one-line edit. But the 19 failing seeds are a real bug either way. |
| 36 | 2026-07-30 | **GCP fan sized for measurement integrity over raw seed count (full-authority spend).** You said hundreds-to-thousands; I launched 132 seeds (11 x e2-standard-16, 176/200 vCPU, ceiling $20/90m) rather than packing to ~2000. Reason: the pool runs its seeds in PARALLEL per VM and each b5 seed boots a full headless server, so overpacking 64GB VMs risks OOM — and an OOM-induced failure is indistinguishable from the mine bug in the output, which would corrupt the exact number we are trying to establish. Wave 1 also calibrates per-seed wall time, which nothing had measured yet; later waves scale on measured throughput, not a guess. | Pack ~2000 seeds now and accept some false failures (faster headline number, dirtier evidence), or stay local-only at 48 seeds (free, but too small to trust a rate). | Yes — waves are independent; scale up or stop at any time, nothing persists between them. |
| 34 | 2026-07-29 | **Durable effects are never predicted (T7.4/Decision-4 reopening, full-authority).** Classification of all 21 predicted-frame effect channels found 6 that PERSIST past their moment (buffs, auras, transforms, summoned sprites/lights, head regrowth). Ruled: these present only on server confirmation, never predictively — for transient effects (sounds/particles) the standing law is 'late beats double'; for durable effects it strengthens to 'wrong is worse than late by definition, because the lie persists.' A buff appearing ~100ms late is minor; a buff appearing then proving false is a standing falsehood needing retraction machinery that otherwise needn't exist. Per-channel revisit on playtest feel evidence, same shape as carried-entities. | Predict everything + build a retraction ledger (snappier, heavy machinery serving rare wrongness); or predict durables and accept standing visual lies. | Yes — per-channel gating; any channel graduates individually with evidence. |
| 33 | 2026-07-28 | **Quantization/divergence policy (T6.2 review + T7.3c metric, full-authority).** Law: quantization decides WHETHER client and server agree, never WHAT gets written — corrections always apply authoritative values verbatim. Discrete state (character state kind, stance, wield, mount) compares EXACTLY; continuous physics compares within named tolerances (1mm position, 1e-3 velocity/orientation — below perception and gameplay effect); NaN/infinity always diverges with its own recorded reason. Net player effect: corrections only fire when something perceptibly disagreed, and what you get corrected TO is always the server's exact truth. | Tighter tolerances (more corrections, twitchier); looser (smoother but drift accumulates); or exact-everything (correction storm from float noise). | Yes — three named consts + a guard test; retune deliberately any time. |
| 32 | 2026-07-28 | **Save-data resolution law (T4.5 step-5, full-authority): an identity is never silently substituted; loss is recorded, substitution requires declaration, refusal is last resort.** Concretely: dead references become inert TOMBSTONES (history stays true, saves never bricked); renamed assets FAIL LOUDLY unless a curated alias table declares the rename is the same thing (silent resolve would let a player's item change identity unannounced); removed content = delete-and-record via the same tombstone mechanism; changed worldgen = save incompatible with the new world-epoch UNLESS the change ships a declared terrain-migration (dev saves regenerate freely today; at release, changing the world means declaring what happens to the people living in it). Rejected: silent substitution (identity law), suspended third-state entities (a tax on every consumer). | Silent best-effort resolution everywhere (smoother, dishonest), or hard-refusal everywhere (honest, bricks worlds). | Yes — each is a policy entry in save_migration.rs's RESOLUTION_POLICIES; re-rule any question individually. |
| 31 | 2026-07-28 | **T7.1 prediction-boundary player-feel rulings (full-authority).** Carried entities: NOT client-predicted in v1 (same rule as mounts — correctness first; carrying under latency feels slightly laggier but never desyncs), with a named revisit: if playtest feel complaints emerge it becomes a measured T5.1-cohort experiment. Ability sounds: deduplicated under latency — you may hear a swing LATE but never TWICE (a double sound asserts two events happened, i.e. a false world fact). Prediction history budget: 500ms/64KiB as named consts, tuned later from T5 cohort telemetry. | Predict carries (snappier feel, desync risk); allow double sounds (punchier, dishonest); gate T7 on a measurement program first. | Yes — all three are named consts/flags + one rule each; revisit paths explicitly recorded. |
| 30 | 2026-07-28 | **Villager dark-vs-rain shelter resolution (E3-W2, full-authority): scored by REAL rain intensity vs a fixed night baseline.** The old behavior (ledger #112) always picked "go home because dark" over rain-shelter purely by source-code order. Ruled: same priority tier, rain-shelter scores the live 0..1 weather-grid rain intensity, night-shelter a named const 0.5 (midpoint placeholder until a continuous night-depth signal exists), migrate-home 10.0 (life-event, never loses to weather). Net: heavy rain sends villagers to shelter, drizzle doesn't; comparator hysteresis damps flapping. | Different weights (rain always wins / night always wins), or keep the old order-based pick. | Yes — three named consts, one-line retune each. |
| 29 | 2026-07-27 | **Despond eat/sleep carve-out overrides B7-3 (full-authority ruling, E3/T3.53).** B7-3 made Despond's hold top-tier: even critical hunger/rest couldn't preempt it, so a despondent colonist literally starved/collapsed until the timer expired. Ruled per policy #5 (survival autonomy remains): Despond stays SET the whole time (labor refusal + timer untouched — no mid-meal work window), but needs past the EXISTING NeedTuning.interrupt threshold may issue eat/sleep self-jobs through the hold. No new tuning knob; flee was never affected (vanilla Agent AI owns it). | Keep B7-3's harsher breakdown (colonists can starve mid-Despond — dramatic but lethal), or end Despond entirely on first critical need (collapses the tuned breakdown duration). | Yes — re-tighten the carve-out guard to restore B7-3, one small edit. |
| 28 | 2026-07-24 | **Ben's DIRECT call (not autonomous): live game = DETERMINISTIC BY DEFAULT.** Builder 5's step-B finding exposed that the LIVE game (server-cli/voxygen) is non-deterministic by default — rtsim `tick_rng` falls back to per-tick OS entropy unless `BASTION_DETERMINISTIC` is set; only the harness ever set it, so every prior determinism proof ran through the opt-in path and a real player's game was never reproducible (different colony each launch). Ben chose reproducibility-by-default ("for now we add randomness later"): flip the opt-in to the default so live seeds deterministically from the world seed; per-launch VARIETY returns later as proper founding-seed randomness (random seed at founding → deterministic run), NOT the current unreproducible per-tick OS draw. Routed to Builder 5 with a guardrail: must not force live to serial execution (perf) — needs the deterministic-PARALLEL path, and if that isn't proven for the FULL live agent/rtsim path (vs just harness scenarios), STOP and flag before landing. | Keep per-launch OS-entropy variety as INTENDED (roguelike default), determinism stays a testing-only opt-in — defensible, ships unchanged, but leaves live unreproducible (no shareable seeds / replays / desync-safety) against the determinism-by-construction law. | Yes — env-gated today; the default-flip is a small revert if the parallel-determinism perf cost proves unacceptable, and the "variety later" founding-seed mechanism is a separate future add either way. |
| 27 | 2026-07-22 | **Told Opus to START building its 5 campaign fixtures (EVT/PHY/TER/PER/SHD) now, rather than holding for the fixture-vs-R0D ordering call.** All three prereqs (E1, BLD-031, CLK-006) went green with real evidence. Reasoning: Opus's fixture lane doesn't touch Builder capacity at all — no actual resource conflict with R0D-resume, which is purely a Builder-capacity question (R0D stays paused behind the bug-fix backlog regardless). Fixtures are core determinism-verification work, matching tonight's stated priority ("keep nailing down determinism"). | Hold Opus until the fixture-vs-R0D ordering is explicitly settled with Ben first, even though they don't actually compete for the same resource. | Yes — Opus can pause mid-fixture at any point; nothing built here is wasted or hard to back out of. |
| 26 | 2026-07-22 | **HOLD the engine-improvements-v2 build order (263 spec-complete rows just delivered) — do not start a DONE-verification pass on it or feed it to Builder yet. Finish the current determinism campaign first: the ~140-item bug-fix backlog (v8/v12/v13 findings, Builder 4+5) and Opus's pre-hard-four campaign (Phase 0 just sized it at ~38 new test fixtures + E1 root oracle + goldens).** Consistent with the standing "finish determinism, then come back" pattern (same call made earlier for the renderer track and the GM/LLM idea). The new build order isn't going anywhere — it's a static document, no urgency to start it, and starting it now would split builder attention across three concurrent backlogs (bug-fixes, campaign-fixture-building, new-feature build order) instead of two. | Start now in parallel — either the DONE-verification trust pass (cheap, doesn't consume builder capacity) or feed the 263 spec-complete rows to Builder as soon as any capacity frees up, rather than waiting for the whole current campaign to close out. | Yes — nothing lost by waiting; the build order and its fact-check are durable files, not perishable. Revisit once the bug-fix backlog and Phase 0/campaign are further along. |
| 25 | 2026-07-21 | **DEFERRED the whole fail-closed-vs-graceful-degrade ship-policy question — broadened from DET-MIG-001 alone to the entire version/decode/migration-mismatch surface once Builder found it's one coherent policy block, not isolated findings.** Covers: MIG-001 (divergent migration, cert-lane fix already landed), the RTSim version cluster (SVC-001/003/006/008/013 — VersionMismatch→purge, RTSIM_IGNORE_VERSION env override, decode-error→silent regenerate, regenerate-vs-migrate), and the terrain/worldgen version-compat cluster (WVC-004/010/014/015/018 — LoadOrGenerate compatibility, terrain deltas vs current generator). All of these are graceful-degrade-by-default today; fixing one in isolation without a consistent ruling across the whole cluster would be incoherent (e.g. failing closed on decode errors while still silently regenerating on version mismatch). Since the game isn't live, no real player database is at risk from leaving this open — genuinely costless to defer. | Decide now, one ruling applied consistently: hard-fail-closed (safest for data integrity across every mismatch class, but could lock a real player out with no recovery path) vs graceful-degrade (player keeps playing, but loses/reinterprets the affected state silently) vs a typed-per-class policy (different rulings for different mismatch severities). | Yes — nothing built for these clusters yet; whichever policy is chosen, Builder implements it consistently across the whole surface at once rather than piecemeal. |
| 24 | 2026-07-21 | **Deferred adding Loom + Shuttle as workspace dev-dependencies for T0.64's concurrency fuzzing, rather than approving unilaterally** — Builder 4 (now Opus-default, exec control granted while Ben asleep) flagged this as above its lane and correctly didn't add the deps itself. Shipped the Bastion-specific full-engine legal-schedule fuzzer instead (no new deps, extends the already-built `--deterministic-parallel` probe with a `BASTION_SCHEDULE_SEED` knob over declared-schedule freedoms only) — that's the packet's real deliverable and needed no dependency call. Loom (small sync primitives) and Shuttle (medium components like SlowJobPool) both require rewriting the relevant primitives to use their own sync types/instrumentation, a genuine tooling-investment decision, not a mechanical add. | Approve adding loom/shuttle now (they're dev-only, technically reversible, well-known Rust testing crates) and have Builder 4 wire them into T0.50/51's already-built primitives. | Yes — dev-dependencies, trivially removable; if you want them added, say so and Builder 4 wires them into the queue/epoch/terminal-outcome types already shipped. |
| 23 | 2026-07-20 | **Shipped ENGINE-OPT-2 (A* decrease-key/reopen correctness) CLASSIFIED, with a mild net-negative colony-economy shift, rather than holding/reverting — and queued a STUCK-ECONOMY ROLL-ROBUSTNESS RETUNE as the tracked companion.** 176 is provably correct (optimality vs Bellman-Ford, falsifiers fired, determinism preserved, FASTER not slower, M3/N2 safety untouched). But an 8-seed paired A/B (FR15 methodology) showed making paths optimal reshuffles the DA dig-economy: 3 seeds worse, 1 better (777 outright fixes ladder-built), 3 same — mildly net-negative. Load-bearing fact: the DA economy was ALREADY roll-fragile PRE-change (ladder-red at 2/7 seeds, 3-6 assert variance) — the pathfinding fix didn't cause the fragility, it reshuffled it. I ruled SHIP (reverting keeps the fragility + loses correct paths = strictly worse; we don't hostage a correct fix to a tuning lucky on old paths) + a tracked STUCK-ECONOMY RETUNE targeting ROLL-ROBUSTNESS (the real disease), done ONCE after the pathfinding-correctness batch (each path fix shifts the economy → batch the retune, cleaner A/B). Every future pathfinding-correctness fix will nudge the economy the same way (ENGOPT1's M3A red was the same class) — the retune is the foreseeable companion to the whole pathfinding phase, NOT a per-fix blocker. | HOLD ENGOPT2 until the retune lands (ship both together — no interim net-negative on the dev builder line), OR revert (keep the old lucky-tuning distribution). Hold couples a correct fix to a bigger FR15 block + stalls the ledger; revert loses correct+faster pathfinding and doesn't fix the underlying fragility. | Yes — the retune is a tracked companion (this row + registry); if you'd rather ship both together say so and ENGOPT2 waits for the retune. The interim net-negative is mild, safety-preserved (colonists always exit), and dev-line-only (no players). |
| 22 | 2026-07-20 | **Merged Codex's determinism passes to bastion/builder OVERNIGHT (Ben asleep, exec authority) via fast-forward — before the sweep (pass #3) is done.** Ben directed "merge the codex stuff." I built the integration base (crate-split tip + pass#2's 4 rtsim/common commits, clean + pass#1's colonist-values fix, whose bastion_mood.rs change git rename-followed onto the MOVED bastion-server/src/bastion_mood.rs — verified no orphan, cargo check --workspace rc=0), then FF'd builder 6357c35d23→a643d8dee6. All 5 commits are Codex-byte-identical-proven + isolated to common/rtsim (no safety-critical job/traversal code). Reversible (git revert the FF). Builder 4 told to fetch+reset before its next block; Sonnet bookkeeps. | HOLD the builder merge until the full determinism batch (passes + the 18-hole sweep + the standing gate) is complete, then merge as one clean batch with your morning awareness — cleaner history, but leaves the proven passes unmerged for no real gain and blocks nothing by merging now. | Yes — trivially `git revert` the 5 commits off builder if you'd rather they'd waited; they're isolated determinism-only, so a revert is clean. Boot-cache (the other "codex stuff") is NOT merged — still pending Codex surfacing the branch. |
| 21 | 2026-07-20 | **→ RESOLVED: Ben CYCLED (chose the alternative) — spun up Builder 4 fresh for the crate-split; Builder 3 writing a handoff doc then winds down. My continue-call was the push-forward default while he was out; on return he took the cleaner-context path, which is right at this boundary.** CONTINUED with Builder 3 for the crate-split rather than cycling to a fresh session at the M3 boundary — ruled while Ben out, push-forward. Builder 3 correctly flagged the RESTART-CHECKLIST cycle point (it ran M3 end-to-end + DPA + the infra day; context is saturated with now-CLOSED M3 detail) and offered to hand the crate-split to a fresh session — everything's durable (CRATE-SPLIT packet, M3 tag package, B57/B58), zero archaeology. I ruled CONTINUE because: (a) a fresh session start is Ben-dependent while you're out, and stalling the efficiency work to wait for it violates push-forward; (b) the crate-split is PURE STRUCTURAL with a byte-identical fidelity-run acceptance gate — any context-degradation error (mis-moved symbol, dropped dep) fails the build/diverges the run and is caught, so the saturated-context risk is bounded; (c) it's fully reversible + not safety-critical. Contrast with #19 (M3), where I held for a fresh session BECAUSE M3 was safety-critical + multi-hour — the crate-split is neither, so the calculus flips. | CYCLE Builder 3 to a fresh session for the crate-split now (cleaner context for a delicate refactor of the hottest file, per the checklist's own cycle discipline) — but requires you to start the session (you're out) and stalls the efficiency lane meanwhile. | Yes — if you'd rather a fresh session do the crate-split, cycle Builder 3 at any point; the packet is fully self-contained so a fresh builder picks it up cleanly. And this is a natural boundary to cycle at regardless once the crate-split lands. |
| 20 | 2026-07-19 | **Redirected the fresh M3 builder (Builder 3) to fix the no-wood mining LIVELOCK bug FIRST, before starting M3** — ruled while Ben out, push-forward. Builder 3's first-win DPA analysis found the likely LIVE mining bug (matches Ben's "they don't construct ladders and get stuck"): in a wood-less dig site, the wood-costed rung jobs are unclaimable → the F3 pruner (ACCESS_STALE_SECS=20) kills the plan → planner re-emits → EMIT-PRUNE LIVELOCK, zero ladders built. The graceful material-hold that should catch no-wood never engages (staged-no-wood/leg-A holds cleanly; organic-no-wood/fidelity livelocks = a pruner-vs-material-hold ORDERING bug). I ruled: fix the ordering NOW (bounded, context hot, live-value — converts livelock→clean classified hold; NOT a fidelity-% recovery, which needs wood = a design matter), THEN M3. Deeper "no-material access tier" (carved stairs/dig-out ramps) + a loud client "supply wood" signal = logged design/client follow-ups (STAIR-LADDER-MINE-ACCESS-DESIGN), not now. | Proceed STRAIGHT to M3 and queue the no-wood fix behind it — keeps the sequenced block uninterrupted, but leaves the live-regime livelock unfixed for the multi-hour M3 duration (against "fix the player's actual pain first"). | Yes — small bounded fix; if you'd rather M3 go uninterrupted, say so and it queues behind M3. The design-tier question (how a wood-less mine descends at all) is fully open for your steer. |
| 19 | 2026-07-19 | **M3 gate CLEARED (R10 Opus-passed + x2 determinism-proven) but HELD M3 for a FRESH builder rather than having the context-tail current builder start it** — ruled while Ben out. The builder (correctly, per the RESTART-CHECKLIST cycle point) recommended spawning M3 fresh: it's carried STEP0→STEP1→DPA→R10 and is deep in context; M3 is a fresh multi-hour block on a now-stable substrate, fully self-sufficient (M3-BUILDER-PACKET-FINAL + bastion-block-R10 + R10-TAG-PACKAGE + R9-fold notes all committed). I CAN'T spawn a fresh session (Ben out, no sub-agents), so: current builder works the ready loose threads now (Sonnet's leg-C fix = fruitless-amnesty strike cap + block_updates re-open hook; corpus-prep remnants), and M3 STARTS when Ben returns to spawn a fresh M3 builder — FALLBACK (UPDATE 2026-07-19 ~16:00): turned OFF — the builder flagged it's context-deep with ~1 clean block (corpus-prep) left; a degraded multi-hour safety-critical M3 is worse than waiting. Revised: current builder does corpus-prep → dispatches DPA before/after data async → writes a SESSION-HANDOFF note → winds down; M3 waits for a genuinely fresh session (Ben spawns; likely within the corpus-prep window given intermittent check-ins). | Have the current builder PUSH ON and start M3 immediately (faster to M3, and the builder offered) — but risks a safety-critical multi-hour block built on degraded tail-context, against the checklist's own cycle discipline. OR truly stall M3 until Ben's back (no fallback) — cleaner context but stalls the critical path if Ben's out long. | Yes — M3 unstarted; when back, spawn a fresh M3 builder (preferred) OR tell me to have the current builder push on now. M3's committed self-sufficiency means a fresh builder can pick it up cleanly whenever. |
| 18 | 2026-07-19 | **Did NOT build the warm compile-server VM after Ben conditionally approved it ("if it shows it works")** — the data killed the premise. My earlier worry (3-min compile inner loop) was wrong: the builder measured its own numbers and corrected them — the pure `cargo check` inner loop is 11.2s, not 3 min (it had conflated checks with verify BUILDS). The parallel-frontend flag (`-Z threads=8`) gave only ~12% and busts warm caches, so it's not worth baking in. The genuinely-slow path (verify BUILD ~3 min when the server crate changes) is ALREADY offloaded to the VM async via the wrappers (§16). So a warm compile-server would duplicate existing behavior for no real gain → not built. The real structural fix (bastion_jobs.rs 14k-line file → its own CRATE) is deferred to a post-M3 reviewed block. | BUILD the warm 32-core compile-server anyway (rsync working tree → parallel-frontend check/build → stream back, idle-stop) — but at 11.2s local checks + already-VM-offloaded verify builds, it solves a problem that isn't there. | Yes — if a future measurement (e.g. `-Z threads` on cold builds, or post-split multi-crate chains) shows a real win, the compile-server is a small build away. Nothing was spent building it. |
| 17 | 2026-07-19 | **UPHELD the R10→M3 gate (build R10 first) rather than waiving it to land M3 sooner** — ruled while Ben was out, under granted exec authority. The M3 packet explicitly gates "Do not start until R10 tags" (M3-BUILDER-PACKET-FINAL.md L137-138); R10 (the `ownership_epoch` fencing token at the bastion owned-write sites) was handed to a fresh session that died in the crash and the lean restart never re-slotted it — so it's an accidental drop, not a decision to skip. Builder flagged the ordering. I ruled: build R10 first off readme/R10-FRESH-SESSION-CHARTER.md (canonical), Opus-gate at R10's tag (NEW dynamic concurrency/safety mechanism), THEN M3 on the fenced substrate. Reasoning: M3's whole safety story rides on the fenced substrate — retrofitting the epoch UNDER a live reservation queue is the exact race class R10 exists to kill (migrating in-flight reservations with no epoch to protect the migration); and the packet says waiving re-raises the Opus gate scope anyway, so waiving takes the retrofit risk AND pays the review cost = strictly worse. Revised sequence: v6 → R10 (Opus-gated) → M3 → full validation. | WAIVE the gate: land M3 first (the reservation tuple composes; R10's epoch retrofits after) — faster to M3/features, but against the packet's explicit condition + takes the retrofit-under-live-queue race risk + re-raises the Opus gate. | Yes — pre-build; both R10 and M3 are unbuilt. If you'd rather ship M3 first and retrofit R10, flip before R10 tags. (Safety reasoning strongly favors R10-first — this is the conservative call.) |
| 16 | 2026-07-18 | **M2 LADDER TAGGED (bastion-block-M2LADDER) on "owned contract works + governs normal play + seed-20 closed," with 2/6 seeds' backstop delivery as a NAMED-OPEN optimization item rather than a tag-blocker.** The overnight M2 arc solved the ladder problem ChatGPT failed on: owned single-owner contract live + governing normal-play egress, deterministic mount-snap (killed the ~50% jump-flake), your 4 failure classes certified gone on the owned path (fixture 9/10 + general-position variant), seed-20's stranded-forever case CLOSED (organic owned exit), 52% organic escape (best-ever, from 17%), never-stranded 18/18, corpus-scale deterministic. The honest imperfection: 2/6 corpus seeds (s1337, s22) — the owned contract ENGAGES (full phase-walk on tape) but the frontier-build outran the stuck window on harder geometry, so the DESIGNED backstop delivered them (not a freeze, not vanilla, never-stranded). I ruled TAG with that named-open (it's the escape-time optimization frontier you named as an ongoing goal, downstream of walkability over-selection — a performance gap, not a correctness/Ben-4-class failure). Named-open also: vanilla leak (#15), N6 interruption dead-on-live-path (filed engine finding), engine class-7 item-identity nondeterminism. | HOLD the M2 tag until 6/6 seeds exit ORGANIC-owned (no backstop deliveries) — stricter, but conflates "the ladder works" (M2's deliverable, met) with "escape fully optimized" (the ongoing iterative metric goal), and the alternative revert-and-bank would REOPEN seed-20's strand (your class) = strictly worse. | Yes — the 2/6-backstop optimization + walkability-over-selection is the queued next block; if you'd rather the tag mean 6/6-organic, the optimization block closes it and the tag re-cuts. Revert is off the table (it reopens seed-20). |
| 15 | 2026-07-18 | **M2 ladder tag scoped to "owned contract works + governs the primary path," with the ungoverned VANILLA ladder-leak named as the NEXT arc step rather than a tag-blocker.** The fixture's seal-integrity tripwire (which I added) fired in N1: when the owned route descriptor is adversarially invalidated (entry blocked), the colonist can still exit via the PRE-EXISTING taskless vanilla ladder-token climb (the physical rungs stand; he regen-cycles up unowned) — the same leak the owned contract is meant to eventually supersede. I ruled this does NOT block the M2 tag (the strict abort-semantics gate passed; the leak is pre-existing not an M2 regression; the owned contract now genuinely governs the normal auto-route path — P0: 49 owned ascends, deterministic mount-snap). Closing the leak (gate/supersede the colonist ladder-token so the owned contract is the SOLE egress) is scoped as the M2 arc's next block. TAG IS CONDITIONAL on the v4 corpus confirming normal operation goes owned (not taskless-vanilla) — if the real scenario still exits vanilla, the tag holds. Also N1B's "auto-router rebuilt access through the seal" ruled BENIGN (legitimate bounded owned-rebuild resilience, the contract working). | BLOCK the M2 tag until the vanilla ungoverned-egress leak is fully closed (owned contract as the sole colonist path), so no adversarial route-invalidation can ever produce an unowned exit — stricter, but defers the tag for a distinct design step beyond proving the contract works. | Yes — the leak is pre-existing and tracked; closing it can be the immediate next block if you'd rather not tag M2 with it open. Flip to prioritize the leak-close. |
| 14 | 2026-07-18 | **M2 ladder: continuing to the OWNED single-owner contract rather than declaring M2 done on the working vanilla-path 6/6 corpus.** The planner-fix + mount-snap cycle produced correct connected ladders colonists climb out of on ALL 6 seeds (organic escape 17%→42%) — BUT the fixture proved those climbs run on the VANILLA regen-cycle path, UNOWNED: the Stage-1 task preflight never accepts, so the deterministic mount-snap (your #4 stuck-dismount fix) is INERT live and the 4 failure-classes are uncertified against the owned contract ("guarantees ride on luck"). Full diagnosis is a named, tape-pinned chain (banked scratchpad m2-fixture-findings.md): construction-phase ownership computes no corridor + vertical shafts have no mid-prefix anchors by geometry + terrain token leaks him out vanilla. Directed the builder to continue fixes A/B/C — activate the ALREADY-EXISTING-BUT-DEAD ConstructionFrontier traversal + bottom re-entry → preflight aligns by construction → owned path live → mount-snap engages → fixture certifies. Rationale: the owned contract makes your 4 guarantees STRUCTURAL not emergent; without it the dismount flake can still bite other geometries. NOT a rathole (complete diagnosis, existing infra). | Declare M2 "ladders work" on the vanilla 6/6 corpus NOW (it IS working in practice), tag it, defer owned-contract certification to a later block as gold-plating. | Yes — the 6/6 working-ladders milestone stands either way and is being checkpoint-committed; can stop and tag the vanilla milestone anytime if you'd rather not spend more on the owned contract. |
| 13 | 2026-07-17 | **Tagged the free-climb cap with the seed-8 stuckjob anomaly PARTIALLY pinned** (cap-compliant on every sample; the exact breach-cell xy unresolved), with the xy-pin as a COMMITTED immediate post-tag follow-up, rather than holding the tag for a full mechanism pin. Rationale: the tag's subject (the cap) is PROVEN — corpus 6/6 + sample-level cap-compliant ON the anomaly; the anomaly is a separate collision/geometry seam (the already-registered arrive-through-walls class-6), NOT a cap failure (3 relocation writers excluded); A2 proven independently via discriminator C (6/6). Fully pinning xy needs a rebuild the 39-leg gate forbids (~40min+ extra for a non-cap issue). If the post-tag xy-pin implicates the cap/A2/a worse writer, the tag reopens. **→ RESOLVED: xy-pin CONFIRMED BENIGN — the seed-8 "exit" was the DESIGNED CASE-003 embed-belt lawfully relocating A off an invalid staging-drop cell, then natural water-cavity navigation to the surface; cap lawful on every sample, A2 untouched, no worse writer. Tag STANDS, not reopened. Deeper class named (staging teleports can land on invalid cells → silent scenario-premise void) + F5 staging-precondition hardening folded into the class-6 follow-up chip.** | HOLD the tag until the seed-8 breach-cell xy is fully pinned (stricter prove-everything). | Moot — resolved benign; the tag-now-with-committed-pin path was vindicated. |
| 12 | 2026-07-17 | **Trap-depth is SKILL-DEPENDENT (accepted as intended): a deeper pit needs a higher climb skill to escape, so "requires a ladder" scales with the colonist.** Discovered via the cap corpus: colonists spawn climbing skill 0..=1 (the "most settlers start poor climbers" roll); a level-1 colonist's reach is cap(1)=6 + scramble 3 = 9, so it legitimately free-escapes a depth-8 pit while a level-0 (reach 5) cannot. So a given pit depth traps low-skill but not high-skill colonists. Left as-is (natural + emergent — a better climber escapes deeper holes; the emergency ladder is needed for pits beyond the colonist's actual reach). The B5.8 fixture stages level-0 to test the cap deterministically. | Enforce an ABSOLUTE "deep pits always require a ladder regardless of skill" floor — cap-table/scramble-reach retune so no spawn level can free-escape beyond some depth. | Yes — pure cap-table/scramble tuning (consts), no structural change; retune if you want a skill-independent trap floor. |
| 11 | 2026-07-17 | **Cap-bounce XP-farming leak → RESOLVED to: keep free-climb XP + FROZEN CAP-SKILL** (Ben overruled the builder's initial ladder-only-XP fix). The corpus caught the cap leaking on 2/6 seeds (a colonist bounces below cap, earns climbing XP, levels mid-climb, cap grows 3→6, escapes). Builder's first fix moved XP to ladders-only (killed the farm but INVERTED progression — climbing should build climbing skill, not ladders). Ben's steer: free-climbing DOES build climbing skill; close the farm instead by snapshotting the cap's skill at trapped-episode start, held across bounces, reset only on real escape — leveling helps the NEXT climb, not the current escape. Routed; revised design → fast Opus check → re-corpus. | The builder's ladder-only-XP fix (simpler one-liner, but inverts the intuitive progression — colonists never gain climbing skill without ladders). | Yes — pre-tag; both are ~localized changes; revert to ladder-only if the frozen-skill lifecycle proves fragile. |
| 10 | 2026-07-17 | **Free-climb cap applies to COLONISTS ONLY (server-side, `bastion_jobs.rs` only); PLAYERS left free-climbing as today.** Smaller/safer surface (zero common/physics change) and it unblocks the colonist mechanic + revives the emergency ladder. Whether PLAYERS should also be capped (a universal rule in common/states) is a distinct game-feel call I left to you. In Opus review, not built. | Universal wiring A: a true world rule in common/states/climb.rs that caps players too. | Yes — A is the documented future path; flip if you want players capped. |
| 9 | 2026-07-17 | **Free-climb depth cap mechanism = SPATIAL height-above-anchor gate** (climb a bare wall only ≤ cap above your last foothold; descent always allowed; ladder token exempts), cap = 3·(skill+1) → 3/6/9. Chose spatial over a fatigue model because it's literally your "logical reach" (a skill-scaled distance, not a resource) and kills the measured regen-cycling exploit by construction. In Opus review, not built. | A cycle-persistent climb-FATIGUE model (Fable's other option), or different cap values. | Yes — pre-build, Opus gating; swap mechanism or values before implementation. |
| 8 | 2026-07-14 | **"Zero-input" colony bootstrap boundary: the colony currently CANNOT bootstrap food from true zero — a player must paint at least one Farm/Gather zone before AUTON-1's self-designation generators take over from there.** Architect's read: this is likely the INTENDED boundary, not a gap — "autonomous, not 4X, the god PROVIDES" is a stated pillar, and the player painting the initial food source IS that providing; a colony that bootstraps from nothing would erode the pillar (nothing left for the player to provide). Not built either direction — logged as an open scoping question, not an auto-build. | A Farm/Gather self-designation generator (AUTON-4-ish) that lets the colony find/claim wild food sources with zero player input at all, true bootstrap-from-nothing. | Yes — nothing built yet either way; whichever Ben prefers gets built when this reaches the front of the queue. |
| 7 | 2026-07-13 | **Off-screen colonist DEATH (MIND-LOD / row 41.2, deferred): CLAMPED (default).** A neglected UNLOADED colony drifts strained/unhappy but does NOT silently die or enter breakdown off-screen — the irreversible step only happens LOADED (where you can witness/intervene). "No unexplained corpses" = legibility. Designer rec in MIND-LOD-NEEDS-DECAY-design.md: coarse tend-to-equilibrium needs-decay off-screen (≈FREE — reuses B7-0's persistent mirror + vanilla's SIMULATED_TICK_SKIP tick; freeze saves nothing, so it's a DESIGN call not a perf one), death/breakdown clamped to loaded-only. | HARSHER LIVING WORLD: let a neglected unloaded colony DIE off-screen (DF-style) — more stakes, but trades the "no unexplained corpses" legibility. | Yes — 41.2 is deferred; flip before it crafts. |
| 6 | 2026-07-13 | **ARCH-003 reassigned to Bug-Tester for the FULL bit-identity bisect** (Ben-chosen when sol/ChatGPT credits ran out mid-bisect). It's unconverged (still splits at b58 tick 5636, unknown remaining seams) so this is an open-ended, token-heavy loop (15-20min release rebuild per fix iteration) while fleet credits are tight. Work saved on worktree commit d3dc9e9a2d; Opus gates merge. **UPDATE 2026-07-13: Bug-Tester's static audit (0 rebuilds) refuted the HashMap-swamp fear — vanilla wander path is deterministic; the last seam is ONE subtle variable-tick steering/path divergence needing a multi-rebuild instrumentation campaign. Re-presented the gate to Ben; he chose FULL-CONVERGENCE (authorized the campaign) over the partial-merge fallback.** | Park the chase + Opus-review the 7 converged seam-fixes for a flag-gated PARTIAL-determinism merge; or fully shelve until credits recover. | Yes — the partial-merge fallback remains available if the campaign drags; the seam-fixes are all det-mode-gated so live is unaffected either way. |
| 5 | 2026-07-12 | **Kept ONE builder juggling main-block + parallel-fill** (with a WIP-state traceability discipline) rather than spinning up a 2nd builder agent. Reason: a 2nd builder = full extra token context + shared-checkout git-conflict/worktree/merge overhead, for currently-small fill work. | Spin up a 2nd builder in its own worktree to own the fill/queued-block lane (cleaner isolation, no thread-juggling). | Yes — escalate to the 2nd builder if the juggling ever drops a thread or fill work grows into substantial parallel blocks. |
| 4 | 2026-07-12 | **CASE-004 magnet fix kept as low-pri defensive** (row 31.1), not dropped, though Opus/bug-tester confirmed the fires are transient not permanent. | Drop it (the belt covers it). | Yes — trivially de-queue row 31.1. |
| 3 | 2026-07-12 | **EMBED-WATCH belt design**: CASE-003's single-tick belt replaced by a 30-tick-persistence, 4-corner core-in-solid embed-watch. Opus-gated, tagged. | Shorter/longer window; add hysteresis (Opus noted the hard-reset counter is B14-adjacent); tighter center-cell test vs 4-corner. | Yes — tunable; the queued belt-exercise test (31.3) validates before it's the sole net. |
| 2 | 2026-07-12 | **FR15 locomotion re-spec approach** = drive-owned progress metric (physical displacement + path arc-length), input-swapped into the KEPT rescue economy. **Opus FR17: FEASIBLE-with-changes; the no-entombment teleport backstop is proven INDEPENDENT of the metric (fed by a separate `stuck_watch` field), so safety can't regress.** | A different stall model (e.g. purely time-based, or replacing the economy). | Yes — still pre-build; pivot before the builder implements. |
| 1 | 2026-07-12 | **Decimal-insert convention** for triaged safety/bug blocks in the master list (e.g. 31.1/31.2/31.3) to avoid renumbering the 105-row tail. | Full renumber, or a separate queue doc. | Yes — cosmetic. |

