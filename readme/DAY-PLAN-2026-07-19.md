# DAY PLAN — 2026-07-19 (architect synthesis; the course we follow)

Source survey: the full readme corpus (master build list frontier rows 31.4-62.2 + the mine-arc inserts, the
design backlog, MORNING-TRIAGE, all packets/designs/logs), DECISIONS-FOR-BEN, the registry, tonight's results.
Principle: the mine arc is at PROOF-strength (two tags + a formally-clean contract) — tomorrow converts that
strength into VISIBLE GAME + BREADTH, while the mine line advances one tier per natural slot, not as a grind.

## Phase 1 — MORNING: land everything in flight (mechanical, ~2-3h, mostly self-executing)
1. Builder-2's leash evidence package → architect review (credit Opus's clean-bill pre-review) → merge.
   The AUTON-2 starving-amid-plenty class closes.
2. checked_tick (WRITTEN) → serialized verify → commit. B16 Tier-2 closes.
3. The 5 chips → serialized verifies in the resume order (aura class-close first) → reviews → merges.
   B49-B53 close.
4. Codex DET-0004 closure run (box is free in the morning) → architect review → merge queue. 4th determinism
   fix (the Rat/Cat potion) lands.
5. Bookkeeping sweep (Sonnet): flip 51.7 DONE, register the merges, FABLE-004 F4-F6 items, the stale-triage
   line, B49-53 row flips.
6. bastion-model INDEPENDENT FIDELITY AUDIT (architect/Opus — the one review not yet done: model-vs-real
   semantics, the watchdog-addition, the two honest gaps).
7. Spawn R10 (the fresh-session charter, now carrying the S6 zombie-owner insight + the token-witness field).

## Phase 2 — MIDDAY: the two safety-substrate blocks
8. R10 fencing token: build → N-FENCE + full regression net → architect Opus gate → tag. (Box priority; the
   formal model says every mechanism it fences is load-bearing — this is the vaccine for the session's bug class.)
9. M3 contention: fires the instant R10 tags (M3-BUILDER-PACKET-FINAL is translated and waiting; R9 data
   structures folded; capacity-one; the queue-wait progress-exemption interaction is the flagged risk).
   → With M3 tagged, the SUBSTRATE IS COMPLETE: single-colonist bulletproof + multi-colonist fair.

## Phase 3 — AFTERNOON PIVOT: visibility + breadth (the strategic call)
The mine line does NOT grind M4/M5 back-to-back. Instead:
10. STATUS-SURFACE (small, main-lane, precedented): the four invisible colonist states become inspector lines
    + the energy meter. The safety arc becomes VISIBLE — the #1 client-surface audit finding, and cheap.
11. MERGED-PILE-EAT-CONCURRENCY (row 50.7, B38): the one KNOWN live-play death-spiral risk on the books
    (mass hunger throttles to ~1 fed per cooldown on a merged pile). Scoped block, overdue, real stakes.
12. Fire the TESTING BREADTH lane: Grok's catalog data-collection run (the 64-feature baseline, box-permitting)
    + ARENA-TEST-STAGE (51.53, cheap — resource stock + debug spawner makes the arena the real test stage)
    → feeds row 50.65 FULL-REGRESSION-AUDIT. The bot-compile + capture-leg from the automation map's ranked
    build-list slot as fills.
13. ★ START FOREST-ECONOMY (the design exists; the pivot's headline): the first big NEW gameplay feature in
    weeks — trees drop seeds, regrowth, a forestry/planting job (reuses FARM), fruit/nut food, leaves→compost.
    WHY NOW: CHOP is proven clean + felling landed + the DPA packet wants WOOD-COSTED rungs — this closes the
    loop mining↔forestry↔construction (the real colony economy Ben's wood-chopping interest points at).
    Sonnet translates the design → packet; builder takes it after 10/11.

## Phase 4 — BEN'S SESSION (the human items, ~30-60 min whenever he's up for it)
14. One live play session on the current exe (the Play-Tester keeps a copy ready) closing the banked
    eyeball items in BEN-TEST-CHECKLIST: M2LADDER (watch a colonist build+climb out), BACKSTOPOPT (the
    judge-by timings), CHOP-FELLING (51.63 — watch a tree actually fell), STOCKPILE (51.64 — confirm which
    failure mode he hit), GATHER (51.65 — confirm if he wants the snap-tolerance parity). One session closes
    5+ rows' human halves.
15. Decisions embedded in the day (defaults ship if no word): rung material = REAL WOOD COST (default per
    MINE-LOGISTICS; flip = one const) · Gather snap-parity fill = yes/no · the E:-drive consolidation plan
    (still offered, fleet-idle window needed).

## Standing lanes (all day, self-running)
- ChatGPT deep-dive loop continues; Phase-2 design fires on FOREST-ECONOMY once its engine-docs cover the
  relevant seams (code-grounded enrichment of the build packet).
- Codex: DET-0005 (re-fire the hunt prompt on its next surface).
- B16 Tier-3: re-fire the prompt (the recon packet makes it a clean start for any capable session).
- Image-gen 2D asset lane: READY to reactivate whenever Ben wants (toolbar/portrait/UI upgrade batch — the
  integration-tester absorbs; placeholder-first stays the net).
- M4/M5: specced, advance one tier per natural slot after FOREST starts (the mine ladder keeps climbing
  without monopolizing the lane).

## Guards carried forward
Build serialization THROUGH THE ARCHITECT (the thundering-herd lesson — no simultaneous idle-checks; R10 holds
box priority) · reviewer-first on everything · never-stranded > organic > teleport-free · the morning packet
(MORNING-TRIAGE-2026-07-19.md) is the checklist for Phase 1.

## Why this course (the one-paragraph argument)
Tonight ended with the hard problem PROVEN — but proof is invisible in-game, and the audit said exactly that
(four indistinguishable colonist states; features that read as broken while working). Tomorrow spends the
morning banking the in-flight work, the midday completing the safety substrate (R10+M3 — the two blocks
everything else stands on), and the afternoon converting proof into GAME: players can SEE colonist states,
the one known death-spiral risk dies, the 64-feature baseline gets measured, and the colony gets its first
new economic loop (forestry) — chosen because every prerequisite landed this week and it feeds the mine line's
own wood-costed access. The mine ladder keeps climbing (M4/M5 specced, one tier per slot) without the project
tunneling on it. Visible progress + measured breadth + substrate completeness, in that order.

## ★ AMENDMENT (Ben, ~00:50) — MINING-LIVE-FIDELITY takes Phase-3 priority over FOREST
Ben's live observation: mining is ~50/50 on completing a designated hole, and colonists run back and forth
excessively. HONEST STATE: mining MECHANICS are harness-proven; a big organic designation in live worldgen has
NEVER been verified end-to-end (the same green-gate-vs-live class as ARENA/Farm/chop-felling). New Phase-3 order:
10. STATUS-SURFACE (unchanged — visibility first, it also makes the mining investigation legible).
11. ★ MINING-LIVE-FIDELITY (new, priority): (a) a live-shaped harness scenario — LARGE organic-worldgen mine
    designation, measuring COMPLETION % (cells dug / cells designated, with per-cell abandonment reasons) and
    MOVEMENT EFFICIENCY (distance-walked per block mined, claim-churn count, haul trips vs items) across seeds;
    (b) diagnose the two symptoms: completion-half — leading suspect = the DORMANT descent-gate ladder arm
    (D16 class, the DPA packet's headline find: deep/stair-unroutable cells silently stall) → if confirmed,
    ★ PULL DPA-0/1/2 FORWARD (the re-enable IS the fix, packet ready); movement-half — measure claim
    granularity / haul batching / job flapping, THEN scope the fix (no guessing).
    (c) Ben's live session doubles as ground truth: designate a big mine, watch completion + traffic.
12. MERGED-PILE-EAT (unchanged).
13. FOREST-ECONOMY: starts AFTER mining-live is understood (design/packet prep can proceed in parallel via
    Sonnet/ChatGPT; the build waits — a colony sim whose CORE dig loop is 50/50 fixes that first).
