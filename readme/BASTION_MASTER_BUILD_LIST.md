# Project Bastion — Approved Master Build List

> **Sequence authority.** This is the single order the systems builder executes. Exactly one row is `CURRENT`.
> The builder acts from [BASTION_CURRENT_BLOCK.md](BASTION_CURRENT_BLOCK.md); the architect owns advancement here.
> Coding direction lives in the [Implementation Playbook](BASTION_IMPLEMENTATION_PLAYBOOK.md), references in the
> [Design Index](BASTION_MASTER_DESIGN_INDEX.md), and shared ownership in the
> [Shared-Engine Registry](BASTION_SHARED_ENGINE_REGISTRY.md).

## Operating rules

- One row is one revertible tag-level block. Staged ranges execute internally in their documented order.
- Decimal rows (e.g. `31.1`) are triaged safety/bug inserts slotted after their parent row without renumbering the tail; they execute in decimal order after the parent.
- Never begin a later row before the current row is green, reviewed, recorded, and tagged.
- `CHEAP` = predominantly wiring shipped Veloren behavior; `MIXED` = reuse plus a bounded mechanism;
  `NEW` = a new simulation/data engine; `SPEC` = obtain a builder-complete JIT design when reached.
- Folded IDs are not duplicated: FARM/COOK → PRODUCTION; ORDERS/STANDING → POLICY; LOG → HIST;
  TRAP/OPERABLE → MECH.

## Strict build order

| # | Block | Outcome | Status | Class | Design reference |
|---:|---|---|---|---|---|
| 1 | B0 | Fork baseline and deterministic headless harness. | DONE | CHEAP | [Architecture §2.1](BASTION_ARCHITECTURE.md) |
| 2 | B1 | Orthographic overseer camera and Z-slice. | DONE | CHEAP | [Build report §B1](veloren-colony-rts-build-report.md) |
| 3 | B1.5 | Overseer input context and god-camera controls. | DONE | CHEAP | [Architecture §2.2](BASTION_ARCHITECTURE.md) |
| 4 | B1.6 | Occlusion/reveal/slice/underground framework. | DONE | MIXED | [Architecture §2.2](BASTION_ARCHITECTURE.md) |
| 5 | B1.7 | Overseer LOD and frustum tuning. | DONE | CHEAP | [Architecture §2.2](BASTION_ARCHITECTURE.md) |
| 6 | B2a | Selection, designation paint, and authoritative interaction channel. | DONE | CHEAP | [Architecture §2.3](BASTION_ARCHITECTURE.md) |
| 7 | B3 | Colonist entity model, skills, and god-anchor colony. | DONE | MIXED | [Architecture §2.4](BASTION_ARCHITECTURE.md) |
| 8 | B4 | Designation → job board → claim/arbitration/path request. | DONE | NEW | [Architecture §2.5](BASTION_ARCHITECTURE.md) |
| 9 | B5 | Mine/Chop/Build execution, drops, and XP. | DONE | MIXED | [Architecture §2.6](BASTION_ARCHITECTURE.md) |
| 10 | B5.5 | Zone deletion and conserved item-pile aggregation. | DONE | MIXED | [Architecture §2.7](BASTION_ARCHITECTURE.md) |
| 11 | B5.6a | Draped zone visuals, visibility, and pile tiers. | DONE | CHEAP | [Architecture §2.8](BASTION_ARCHITECTURE.md) |
| 12 | B5.6b-1 | Colored zone fills, labels, and blending. | DONE | CHEAP | [Architecture §2.9](BASTION_ARCHITECTURE.md) |
| 13 | B-MAP1 | Map/navigation surface. | DONE | CHEAP | [Architecture §2.10](BASTION_ARCHITECTURE.md) |
| 14 | B5.6b-2 | Volumetric zone extent and depth UX. | DONE | MIXED | [Architecture §2.9](BASTION_ARCHITECTURE.md) |
| 15 | B5.8 | Top-down mining, climbing, and autonomous stair/ladder access. | DONE | MIXED | [Architecture §2.10b](BASTION_ARCHITECTURE.md) |
| 16 | B5.6b-2.1 | Flat-floor mining and anti-entombment/egress cluster. | DONE | MIXED | [Architecture §6](BASTION_ARCHITECTURE.md) |
| 17 | TIMECTL | Pause and variable-rate simulation controls. | DONE | CHEAP | [Architecture §2.10c](BASTION_ARCHITECTURE.md) |
| 18 | TOOL0 | Equipped-tool factor in work speed. | DONE | CHEAP | [Architecture §2.10d](BASTION_ARCHITECTURE.md) |
| 19 | B-ASSET1 | Asset-integration harness and render arena. | DONE | MIXED | [Architecture §2.11](BASTION_ARCHITECTURE.md) |
| 20 | SCCACHE | Shared compiler-cache infrastructure. | DONE | CHEAP | [Architecture §6](BASTION_ARCHITECTURE.md) |
| 21 | B6-SOFT | General colonist soft-collision and chokepoint recovery. | DONE | NEW | [Architecture §2.10e](BASTION_ARCHITECTURE.md) |
| 22 | AR2 | Soft-collision density/teleport hardening. | DONE | MIXED | [Architecture §6](BASTION_ARCHITECTURE.md) |
| 23 | LADDEROFF | Ladder and mine-oscillation live-fix bundle. | DONE | MIXED | [Architecture §2.10f](BASTION_ARCHITECTURE.md) |
| 24 | SLOPE | Slope mining plus standability-gated claiming. | DONE | MIXED | [Architecture §2.10g](BASTION_ARCHITECTURE.md) |
| 25 | CAVEIN | Bounded cave-in with entombment-safe ejection. | DONE | NEW | [Architecture §2.10h](BASTION_ARCHITECTURE.md) |
| 26 | NIGHTHORROR | Flagship creature and reusable integration pipeline. | DONE | MIXED | [Night Horror](NIGHT-HORROR-INTEGRATION-design.md) |
| 27 | CHOP | Whole-tree felling without floating canopy. | DONE | MIXED | [Chop redesign](CHOP-REDESIGN-design.md) |
| 28 | COORD | Stigmergic crew saturation/dispersion field. | DONE | NEW | [Coordination](COLONIST-COORDINATION-design.md) |
| 29 | DETRNG | Deterministic harness RNG and invariant gates. | DONE | MIXED | [Fleet status §BUILD LANE](FLEET_STATUS.md) |
| 30 | CASE-003 | Terrain-aware soft push and center-safety net. | DONE | MIXED | [Fleet status §BUILD LANE](FLEET_STATUS.md) |
| 31 | FR15-TIGHTDIG | Tight-dig locomotion via a drive-owned progress metric (displacement + path arc-length) input-swapped into the KEPT rescue economy; paired-A/B harness FIRST (controls scheduling noise — the interim measurement, not tick-determinism, which the block no longer needs). SHIPPED `bastion-block-FR15-TIGHTDIG` (`ed29c00781`): flag-gated (`BASTION_TIGHTDIG=1`), flag OFF = today's stuck-economy bit-for-bit. **Opus R13: GREEN-LIT** — no-entombment backstop zero-contact, all three steer sources correct (incl. B6's fetch), dispersed_frac judged intended-for-tight-dig. Ships FLAG-DEFAULT-OFF (staged rollout, see row 31.4). Evidence: noise floor on identical code → 4/4 paired-A/B PASS (every gating boolean agreed every pair) → 19/19 flag-OFF ladder on the tag commit. | DONE | MIXED | [LOCOMOTION-PROGRESS-design.md](LOCOMOTION-PROGRESS-design.md) + [Build Review Log §FR17/R13](BUILD_REVIEW_LOG.md) |
| 31.1 | CASE-004-MAGNET | Ladder-magnet else-nudge (bastion_jobs.rs:1599-1610) headroom gate: skip the XY nudge unless the colonist's OWN resulting cell is 2-high standable (reuse `climb_col`) — gate the write, don't relocate; also the sibling on_pillar snap (:1593). LOW-PRI defensive: R11 confirmed the fires are TRANSIENT (self-resolving), not permanent, but the code gap is a real latent B19 hazard — keep, don't drop. SHIPPED (bb858c1cf9): both writes gated on own-z headroom, skip-not-relocate; --magnet-scenario 2/2, zero-embed asserted per-tick, gate 13/13. | DONE | MIXED | [Build Review Log §BC-004/R11](BUILD_REVIEW_LOG.md) |
| 31.2 | EMBED-WATCH | CASE-003 belt rewritten as a persistent embed-watch (30-tick 4-corner core-in-solid → shared `eject_dest`, sequential/deterministic) + Build-completion occupancy check (B19-sibling) + staged_at_anchor latent-bug fix + locomotion telemetry. Ships the FR15-TIGHTDIG branch's safety content; Opus-gated R11. | DONE | MIXED | [Build Review Log §R11](BUILD_REVIEW_LOG.md) |
| 31.3 | BELT-EXERCISE-TEST | B17 follow-up (R11): inject a persistent embed → assert belt-v2 relocates by ~tick 30, standable, net>0 (the persist→relocate path was unexercised — the gate only proved "no embed occurs"). Also confirm the Build-occupancy check covers full body feet+torso. | DONE | CHEAP | [Build Review Log §R11](BUILD_REVIEW_LOG.md) (tag bastion-block-BELT-EXERCISE-TEST a3ee084346) |
| 31.4 | FR15-STAGED-ROLLOUT | Follow-up to row 31 (Opus R13 + architect, 2026-07-12): FR15's flag ships DEFAULT-OFF — the paired-A/B evidence was 4 pairs on ONE scenario, not broad coverage. Validate default-ON across chokepoint/cavein/coord scenarios + the play-tester + more seeds → flip default-ON once broadly clean → remove the flag entirely after it bakes. Tied to the play-tester BATCH-TEST-CHECKPOINT. Also carries the **dispersed_frac WATCH item**: revisit if the committed-path steer ever engages in open-area crew work (not exercised by FR15's tight-dig scenarios). NOT NOW — no urgency, blocked on the play-tester's batch checkpoint. | TODO | CHEAP | [Build Review Log §R13](BUILD_REVIEW_LOG.md) |
| 32 | LOD-0 | Save back skills, inventory, and later needs. | DONE | CHEAP | [LOD §12](LOD-PERSISTENCE-SPEC.md) |
| 33 | LOD-1 | Atomic loaded↔simulated transition and dupe guard. | DONE | NEW | [LOD §12](LOD-PERSISTENCE-SPEC.md) |
| 34 | B6-HAUL + JOB-CORE | Typed jobs, stockpiles, auto-haul, reservations. Built GROUND-FIRST (HAUL-STORE-0: JobKind Designated+Haul, ReservationTable, activate Stockpile, reuse Pickup); CONTAINERS (HAUL-STORE-1) follow after the storage-modes design is finalized + the container-store correction verified. | DONE | MIXED | [Build report §B6](veloren-colony-rts-build-report.md) + [B6-HAUL-STORAGE-MODES-design.md](B6-HAUL-STORAGE-MODES-design.md) |
| 34.1 | B6-FETCH-REQUEUE | Sonnet tag-review finding: re-claiming an abandoned material-fetch Build job (`needs_fetch` in the claim-commit block, `bastion_jobs.rs`) doesn't check whether `job.reservation` is already `Some` before scanning + reserving ANOTHER stockpiled item — `job.reservation.or(fetch_rid)` then discards the new reservation id, orphaning it in the table forever (never released — `remove_job()` only frees the id actually stored on the job). Each re-claim of a mid-fetch abandoned Build job (colonist dies/demotes before consuming material) permanently burns one extra stockpiled item of that def. LOW-PRI defensive (a slow leak, not a double-spend/entombment/crash — needs claim churn on a fetch-in-flight job to trigger). FIXED (0afcdb11b9): `needs_fetch` now also requires `j.reservation.is_none()`. | DONE | CHEAP | [Build Review Log §R-B6HAUL](BUILD_REVIEW_LOG.md) |
| 35 | B-AG1 | Loaded NPCs continue their rtsim lives. | DONE | CHEAP | [Build report §B-AG1](veloren-colony-rts-build-report.md) |
| 35.1 | B-AG1-FIXTURE-GEO | Harness follow-up (not a code gap): the B-AG1 verification scenario's densest-GROUND-civilised-cluster pick force-loads via the same `bastion_force_load_area` every other scenario uses, but zero NPCs there promote — while the densest cluster INCLUDING captains (an airship dock) promotes fine (8/8). **ROOT CAUSE FOUND (fixed, `73cd8df83d`):** rtsim's NPC table is EMPTY pre-tick (0 civilised before ticking vs 1985 sixty ticks later — population is tick-driven), which is why the original pre-tick cluster pick could only ever find the airship dock. Fixed two ways: settle-first (tick before clustering) + a GROUNDED filter (npc z within 6 of `get_alt_approx`, excludes deck riders). `--bag1-scenario` now PASSES 2/2: promoted 241/142 walking villagers, movers 215/121 (vs the dock era's 0/0/0). | DONE | CHEAP | [Build Review Log](BUILD_REVIEW_LOG.md) (Sonnet triage, this session) |
| 36 | B-AG5-CORE | Canonical world-action helpers and drive vocabulary. | DONE | MIXED | [Build report §B-AG5](veloren-colony-rts-build-report.md) |
| 37 | ZONE-0 | Lock `ZoneKind` and the one soft-magnet mechanism. | DONE | NEW | [DF-ZONES §ZONE-0](DF-ZONES-design.md) + [Build Review Log §R12](BUILD_REVIEW_LOG.md) (Opus green-light) |
| 38 | GATHER | Paint Gather, collect wild food, and haul it to stock. | DONE | CHEAP | [DF-ZONES §§2, ZONE-1](DF-ZONES-design.md) |
| 39 | HIST-0 | Lock the Chronicle store and universal `record()` API. | DONE | NEW | [DF-HIST §HIST-0](DF-HIST-design.md) |
| 40 | B-AG2 | Data-driven archetype/race behavior over one brain. | DONE | MIXED | [Build report §B-AG2](veloren-colony-rts-build-report.md) |
| 41 | B-AG3 | Personality, memory, thought, emotion, and mood. Architect-DEFERRED (2026-07-12): the build report names B7 (row 44) as B-AG3's explicit first dependency (mood→work-refusal ties to B7) + Mind-LOD is real perf-critical engineering — build after B7, not narrowed ahead of it (re-work risk if B7 reshapes needs/mood). **CLUSTER ORDER (architect-RESEQUENCED 2026-07-13, was B7→B-AG3→41.1→41.2→43.1): B7 (44) → B-AG3 (41) → FOCUS-0-DERIVE (43.1) → B-AG3.1 (41.1) → B-AG3.2 (41.2).** Reorder reason: Sonnet's tier-assessment on 41.1 found only 2 of HIST-0's 54 ChronicleKinds have any live emitter, so the event→emotion→mood pipeline has no real remaining work until HIST-1/2 lands more emitters — 43.1 is the only bounded, ready, real-work option besides the heavy perf-critical 41.2, so it jumps the queue. **UNBLOCKED — B7 landed complete (B70-B73, row 44 DONE) same day.** **THIS ROW = the NARROWED first slice ONLY (Sonnet scoping, 2026-07-13, FOCUS-0-ENUM's own precedent, architect-approved) — two prior-art finds shrink it hugely: Personality [Big-Five, `common/src/rtsim.rs`] and relationships/grudges [`Sentiments`, `rtsim/src/data/sentiment.rs`, already asymmetric-decay persistent] are BOTH already landed vanilla, already wired to every NPC — zero new schema needed for either axis; grudges/bonds build ON `Sentiments` (chronicle=event record, Sentiments=derived relationship state — the correct DF/RimWorld split), not a parallel store. The one genuine gap = a `Value` vocabulary (beliefs/priorities, distinct from personality) + wiring personality/values as a weight on B7-0's existing mood formula.** **DONE** (tag `bastion-block-BAG3V` → `199a834f57`, gate 24/24 — `Value` enum locked [Glory/Tradition/Kin/Wealth/Piety/Nature/Craft/Freedom], `values: HashMap<Value,i8>` on `BastionColonist` [personal_needs shape, empty=neutral=bit-for-bit pre-B-AG3 mood], `care_factor` as a pure multiplier on `thought_sum`'s existing term [mood_formula signature unchanged], `ValueAffinityTable` RON keying `ChronicleKind`→`[(Value,f32)]`. Both prior-art claims live-verified before building, one refinement: `Personality`'s scalars are private, so the temperament term consumes the `.is(PersonalityTrait::Neurotic)` boolean [×1.5 on negative thoughts, post-clamp] rather than the raw field — still zero touches to vanilla. Both `Personality`/`Sentiments` reachable through the SAME rtsim read-guard the chronicle already uses — zero new coupling). Self-verify+tag confirmed clean, no findings. | DONE | NEW | [Build report §B-AG3](veloren-colony-rts-build-report.md) |
| 41.1 | B-AG3.1 | The event→emotion→mood pipeline proper (build-report §B-AG3: an event + personality + values + memory → an emotion of some intensity → mood update). **CLOSED, DONE-BY-EXISTING-WORK (architect ruling, 2026-07-13): satisfied by B7-0/B7-1/B7-3 + the row-41 slice collectively — mood = base + Σneed-penalty + Σcare-weighted-thought-decay IS the causal pipeline. Not held as a placeholder. The one real remaining piece (broadening the ChronicleKind→Value affinity RON as more events start firing) is folded into row 54 (HIST-1..2) as a linked dependent note, not tracked here.** | DONE | NEW | [Build report §B-AG3](veloren-colony-rts-build-report.md) |
| 41.2 | B-AG3.2 | Mind-LOD: loaded=full-res mind model, simulated=cheap aggregate summary, promote/demote at the loaded↔simulated boundary (build-report §B-AG3, "obey or it breaks the engine"). ★ PERF-CRITICAL + LOD surface — flag architect at packet-craft for a likely Opus METHOD gate. **Builder's LOD read-grounding (2026-07-13) found the loaded-only property already holds STRUCTURALLY (the %11 recompute + decay pass join loaded ECS storages by construction; demote/promote already mirror correctly) — so remaining build content is thinner than expected: (a) a thousands-NPC soak measurement assert, and (b) ★DESIGN FORK (route to designer at craft, don't guess): unloaded/simulated colonists' needs currently FROZEN (decay runs only on loaded joins) → long-unloaded colony promotes with stale-full meters. Options: throttled coarse rtsim-tick decay vs deliberate freeze. This is LOD-2-fidelity/desync-tolerance — the fleet-designer-owned creative core per TEST-INFRASTRUCTURE-AUDIT / the C-5 "tend-to-equilibrium aggregate tick" class. When 41.2 sequences, it goes to the DESIGNER for a prior-art-first pass (DF off-map / RimWorld world-pawns / Songs-of-Syx coarse-tick), NOT a packet-craft guess.** Sequencing deferred — architect decides next-block order after 43.1 lands, not auto-sequenced. | READY | NEW | [Build report §B-AG3](veloren-colony-rts-build-report.md) |
| 42 | SEASON-0..2 | Deterministic annual clock and consumer interface. SEASON-0 shipped `bastion-block-SEASON0`; SEASON-1 shipped `bastion-block-SEASON1`; SEASON-2 (the one-interface contract: `season()`/`year_phase()`/`day_of_year()`/`season_bias()`) shipped `bastion-block-SEASON2` — fully done. | DONE | CHEAP | [Season Clock](SEASON-CLOCK-design.md) |
| 42.1 | SEASON-HUD | Overseer HUD readout of current Season + day-of-year, reading the SEASON-0/1 clock (UI-only, reads the existing derived clock, no new sim state). Near-term legibility win (Ben gap-audit 2026-07-12): the season clock ships but is invisible to the player. DECOUPLED from the row-106 TEMP climate-FX lane — not gated behind the expensive environmental-art block. SHIPPED (`93c1970d42`): a "Season · Day N" readout above the TIME-CONTROLS cluster, derived on read through SEASON-2's interface (no private season math — the contract holding at its first consumer). Visual eyeball routes to the Play-Tester's next exe rebuild. | DONE | CHEAP | [Season Clock](SEASON-CLOCK-design.md) |
| 43 | FOCUS-0-ENUM | Lock the shared personal `Need` vocabulary — enum + serde-defaulted collection ONLY (the Playbook's own scope, verified independent of B-AG3: a fixed design-time vocabulary, no facet/value data required to enumerate it). The full design doc's facet-derivation/correlation done-when is a SEPARATE follow-up — see row 43.1. | DONE | NEW | [DF-FOCUS §FOCUS-0](DF-FOCUS-design.md) |
| 43.1 | FOCUS-0-DERIVE | Follow-up to row 43: derive per-colonist need WEIGHTS from B-AG3's facets/values (religiosity→Pray = `Value::Piety`, gregariousness→Socialize = vanilla `extraversion`) + assert the correlation over a sampled roster — the full design doc's original FOCUS-0 done-when. **UNBLOCKED — B-AG3 row 41's slice landed (`Value`/`values` schema ready); does NOT need 41.1/41.2 (architect-resequenced 2026-07-13, jumps ahead of them).** Also REUSES B7-2's preemption mechanism for its own venue-need-jobs (FOCUS-1) — does not re-derive preemption. **DONE** (tag `bastion-block-FOCUS0DERIVE` → `ffd7ab1aed`, gate 25/25, new DERIVE leg — the real generation-time value-roll shipped [`BastionColonist::generate` rolls all 8 values ±50 uniform, same rng thread as skills/name/backstory], `derive_need_weight` pure fn produced+proven [Pray←Piety, Family←Kin, Craft←Craft, SeeAnimals←Nature, Acquire←Wealth, Fight←Glory exact; Socialize via the boolean-trait 3-level API; Drink/AdmireArt/Learn baseline, no forced mapping], nothing consumes the derived weight yet [FOCUS-1's job]. Verified over a REAL 12-colonist rolled roster, not a hook fixture — per-colonist exact match to the formula, directional ordering, independent-probe consistency, and a rolled-map save/load roundtrip through the live LOD boundary. **THE FOCUS-0 ARC CLOSES** (row 43 enum-lock → this derivation).) | DONE | NEW | [DF-FOCUS §FOCUS-0](DF-FOCUS-design.md) |
| 44 | B7 | Needs decay, eating/sleeping, mood, and sensible idle. DESIGN-COMPLETE (2026-07-12): [`readme/B7-NEEDS-MOOD-design.md`](B7-NEEDS-MOOD-design.md), builder-complete. Sub-sequenced: **B7-0** (meters + decay + mood) → **B7-1** (bed + closed rest loop, owned beds) → **B7-2** (preemption — the load-bearing build-once mechanism, ★OPUS-GATE) → **B7-3** (hunger/eat + breakdown, reuses B7-2 with zero new preemption code). **B7-0 DONE** (tag `bastion-block-B70` → `0aea5c63e6`, gate 20/20 — decay+mood formula, RON tunables, chronicle thought_sum layering, LOD-0 persistence mirror; the pre-existing CAVE-IN direct-mood-write conflict resolved by folding one `chronicle.record(CaveIn)` emitter into the crush path, per the reviewer's fork ruling — fear-persists assert proved the queue→drain→chronicle→thought_sum pipeline end to end). **B7-1 DONE** (tag `bastion-block-B71` → `4e56c3d8ca`, gate 21/21 — `DesignationKind::Bed` follows the Ladder placement pattern not Build's stand-in Rock, vanilla `SpriteKind::Bedroll`/frame sprites [zero asset requests], `BedSlot` follows the reservations-table shape [capacity-1, keyed by pos], pre-claimed `RestAt` rides the DepositRun precedent; ownership persists on `BastionColonist.owned_bed`; the B7-0 thought queue generalized to `(who, where, kind)` — cave-in fear and sleep quality are two emitters on one drain, `ChronicleKind` gains `SleptInBed`/`SleptOnGround`). **B7-2 DONE** (tag `bastion-block-B72` → `656c1efda8`, gate 22/22 — the self-job preemption mechanism: pre-claimed self-jobs bypass claim-loop selection entirely, so "out-tiers all work" is impossible-by-construction, not a won comparison; a NEED-CHECK pass drops work via the proven `to_release` seam and creates a pre-claimed `RestAt` after the drain; anti-livelock trio [hysteresis, unreachable→ENDURE, 60s `PREEMPT_COOLDOWN`]; zero new steer/drive code — a need-job IS a travel job, the existing watchdog + `stuck_watch` teleport apply automatically. **Opus CLEAR-TO-TAG** — all 3 safety claims [preemption composition / no-entombment-survives-preemption / anti-livelock] confirmed true by code-read, BUILD_REVIEW_LOG §R14). **B7-3 DONE** (tag `bastion-block-B73` → `1287b161b9`, gate 22/23 + one CK leg B22-flake-classified — the eat-job [hunger joins the urgency ranking] + the despondent breakdown state [sustained-low-mood self-job at own feet, additive on B7-2's NEED-CHECK pass, zero new preemption code]; Sonnet-verified both requested safety properties directly in code: no-entombment holds [Despond rides the identical `ActiveJob`/watchdog machinery as any job — no special-cased bypass — AND is separately covered by the fully job-orthogonal `embed_watch` center-net, which iterates every colonist unconditionally regardless of job/Despond state]; no-thrash holds [active-despond skips re-evaluation entirely, a sustained-window gate, the shared 60s cooldown, and a probabilistic roll — not an instant flip — all confirmed present at `bastion_jobs.rs`'s breakdown arm]). **B7 IS COMPLETE (B70+B71+B72+B73).** | DONE | NEW | [Build report §B7](veloren-colony-rts-build-report.md) + [B7-NEEDS-MOOD-design.md](B7-NEEDS-MOOD-design.md) |
| 45 | PATH-0 | Deterministic global path budget/scheduler. **RE-SCOPED (architect ruling, 2026-07-13): the spec's own "ships WITH B7 migration" precondition is VOID — B7 shipped needs/mood, not population growth, and no other master-list block was supposed to grow N and got skipped (checked: B11/MIG-0..3/B-AG6 are the only growth-shaped blocks, all far downstream with unmet deps of their own). Validated via a SYNTHETIC-N spawn scenario instead of natural growth. The real work is unchanged: decouple `chase()`/`poll()` from the parallel agent `.par_join()` (`server/src/sys/agent/mod.rs:76`) into a sequential entity-id-ordered budgeted scheduler — the genuine determinism fix, not just a counter.** ★ TIER FINAL (architect, superseding the earlier self-verify-only call): **build→gate-at-tag WITH OPUS AT TAG** — a shared-scheduler/per-tick-cap IS a real dynamic surface (Opus-gate territory), but the pattern (sequential id-ordered queue + budget) is well-trodden, not novel — so no pre-build hold, builder just builds normally and Opus verifies the FINISHED code+scenario at tag. Four LOAD-BEARING done-when asserts Opus verifies against: (a) determinism-by-construction — two synthetic-N runs aggregate-identical, no re-introduced shared-state race, no `HashMap` in the request queue; (b) no indefinite path-starvation — every enqueued request drains within bounded ticks under sustained over-cap load; (c) no-entombment/stuck-recovery preserved — `stuck_watch` still fires for a colonist awaiting a budgeted path, the 1-tick consume-last-result latency doesn't perturb stuck-economy tuning (FR15 lesson); (d) vanilla NPCs unaffected. **★ ARCH-003 OVERLAP (reconcile at merge, not urgent now):** Change 1 restructures the same pathfinding/agent-scheduling code the Bug-Tester is fixing on `codex/arch003` — PATH-0 is determinism-FRIENDLY but does NOT close that seam (proven to persist single-threaded too, a subtle entropy source not an ordering issue); expect a merge conflict later, ideally ARCH-003's fix folds into PATH-0's cleaner scheduler at that point. **DONE** (tag `bastion-block-PATH0` → `42f4eb832c` — the sequential budgeted path scheduler: colonist Goto searches lifted OUT of the agent `par_join` into a Uid-ordered cursor'd round-robin under `PATH_TICK_ITER_CAP` [3000 iter units]; `find_path`/`astar.poll` reused wholesale via the extracted `search_step` — entropy-free, no stuck-shuffle rng. **Opus CLEAR** [BUILD_REVIEW_LOG §R15]: (a) determinism-by-construction confirmed [`BTreeMap` + `sort_unstable_by_key(Uid)`, no `HashMap`, no rng]; (b) starvation-free BY CONSTRUCTION [cursor round-robin — deferral is bounded by rotation, denial is impossible by construction, not a tuned outcome; peak_wait=1 in the 18-colonist over-cap test]; (c) stuck-economy/no-entombment PRESERVED [a waiting colonist's state is byte-identical to a mid-search tick, `stuck_watch` still fires — FR15 N/A, only WHEN the route arrives changes]; (d) vanilla/combat pathing inline-unchanged [config-root gate]. TIGHTDIG stays out of scope per the row-31.4 ruling. Synthetic-N proof: 18 colonists saturate the cap exactly [peak 3000/3000], mine resolves.) | DONE | NEW | [Pathfinding §PATH-0](PATHFINDING-SCALE-SPEC.md) |
| 46 | FARM / PROD-2 | Renewable plots, seeds, growth, tend, and harvest. **UNBLOCKED (architect-picked, 2026-07-13, next after PATH-0) — confirmed it can jump the queue per the design doc's own note (depends only on job board + zones, can precede B6/PROD-0).** Prior-art: the `Growth` sprite attribute already exists in vanilla (`common/src/terrain/sprite/mod.rs:561/585`, unused/substrate-only — `WheatGreen`/`WheatYellow` carry a TODO anticipating exactly this consumer); the painted-footprint/per-cell-job pattern already exists (`DesignationKind::Gather`, row 38) to model Farm's own footprint on. **DONE** (tag `bastion-block-FARM` → `682211eac9`, gate 28/28 — first tag to include the new permanent VOXCHECK leg, green. `DesignationKind::Farm` registers a PERSISTENT footprint (Stockpile precedent — farm cells cycle, unlike Gather's complete-and-vanish); a bounded `%15==3` scan drives till→sow→grow→harvest per cell; `Growth(0)` reserved for worldgen's mature fallback, farm wheat sows at `Growth(1)` so the new render-filtered stage assets [WheatYellow gains growth-filtered configs, zero new assets] don't regress vanilla wheat's look. Seed conservation proven via an honest ground+bag item-total ledger [15=14+1 both runs], the harvest→haul→re-sow cycle closes live. Four registry finds: B31 [`job_wanted` dual-master silent churn, 6729 creations/0 completions invisible to job-count probes], B32 [vacancy reads `Some(Empty)` not bare `None`], B33 [material-fetch generalized off a 3-site `BUILD_MATERIAL` hardcode — future material kinds inherit for free], and B30 [voxygen gate gap, fixed + gate-guarded here]. Known-scope: unloaded farms freeze [D19's LOD sibling, deferred to 41.2], wheat→meal deferred to DF-COOK.) | DONE | MIXED | [Production §PROD-2](DF-PRODUCTION-design.md) |
| 47 | RUN-0..2 | Energy-governed emergency running and flee burst. **NARROWED (Sonnet scoping, 2026-07-13, per the design doc's own sub-block split): THIS ROW = RUN-0 ONLY (the gait + energy governor — depends only on `Goto`'s existing `speed_factor` + `comp::Energy`, both real/wired). RUN-1 (triggers) genuinely blocked on B8/HAZARD-EVENTS/DF-BURROW/DF-POLICY (rows 52/53+, unbuilt); RUN-2 (winded/exhaustion) blocked on DF-FOCUS's fuller behavior layer (only the enum-lock exists). Both deferred, not built now — see rows 47.1/47.2.** **DONE** (tag `bastion-block-RUN0` → `39543568ea`, gate 28/29 + B5 flake-classified [B22's 4th instance — a `SystemData` addition changed the dispatcher's dependency graph, re-drawing B5's historical flake with zero logic change; 3/3 identical-binary reruns confirmed]. `running: bool` on `BastionColonist` [the only new state]; gait choice at the existing Goto write site [`RUN_SPEED=1.0` vs `TRAVEL_SPEED=0.8`, inside vanilla's existing speed envelope]; governor drains Energy 15/s while flagged, forces `running=false` below `RUN_MIN_ENERGY=10` [resource-governed, not timer]; recovery rides vanilla's existing unconditional accelerating regen untouched. Colonist-only by construction, zero Chaser/pathing/anim/physics surface. Fixed en route: **B34** — the first drain attempt [6/s] lost the race against vanilla's regen accelerating to a 10/s CAP, so the floor was silently unreachable; 15/s beats the cap with real margin. Stuck-watchdog composition checked explicitly: RUN_SPEED sits inside the existing envelope and the watchdog keys on progress not absolute speed, so a running colonist is strictly LESS stuck-prone, not more.) | DONE | CHEAP | [Emergency Run](COLONIST-EMERGENCY-RUN-design.md) |
| 47.1 | RUN-1 | The triggers (autonomous urgency + player "Emergency!") + the flee fix. Depends: RUN-0 (row 47) + B8 (row 52)/HAZARD-EVENTS(HAZ-0..3, row 53)/DF-BURROW (autonomous triggers) + DF-POLICY/god (player trigger) — all unbuilt. Blocked, not ready. | TODO | MIXED | [Emergency Run](COLONIST-EMERGENCY-RUN-design.md) |
| 47.2 | RUN-2 | The winded aftermath + recovery + exhaustion tie. Depends: RUN-0/1 + DF-FOCUS's fuller personal-need-driven behavior layer (only the FOCUS-0 enum-lock + FOCUS-0-DERIVE exist today, not the consuming behavior) + buffs. Blocked, not ready. | TODO | MIXED | [Emergency Run](COLONIST-EMERGENCY-RUN-design.md) |
| 48 | AUTON-0 | Single activity-authority arbiter. **★ OPUS-GATE AT PACKET-CRAFT (architect-flagged before scoping): METHOD drafted (Sonnet, 2026-07-13) and held with the architect for Opus BEFORE reaching the builder — not supplied directly.** Spec already reviewer-vetted (FR3, FEASIBLE-WITH-CHANGES, "no blockers"); live-verified against current code rather than trusted from the doc: `rtsim_controller.activity` write-site count corrected from the spec's stale "7" to **9** (checked live); the claim-sweep mid-job-clear safety claim re-confirmed against current `bastion_jobs.rs:5124-5137`. Two open questions for the Opus pass: (1) Flee's urgency-scoring signal — B8 (threat data) doesn't exist yet either, defaulting to a no-op stub like Survive/SelfNeed unless a live vanilla threat signal is found; (2) a NEW composition risk neither the spec nor PATH-0 anticipated (they were written at different times) — what happens to a PATH-0-scheduled pending path request when the arbiter clears `ActiveJob` mid-Goto to preempt Work for Flee. Forward note, not a scope change: B7 is now built, so AUTON-2 (the Survive/SelfNeed payoff + E1 death-spiral gate) is no longer blocked whenever the architect sequences that far. **PACKET LOCKED (`readme/AUTON0-BUILD-PACKET.md`, 6 guards) — builder's own live-grounding surfaced two more load-bearing findings folded into the packet before build: GUARD 6 (self-jobs [RestAt/EatFrom/Despond] are an exempt occupancy the arbiter defers to, preserving B7-2/B7-3's Opus-cleared guarantees by construction — full unification is AUTON-2's job) and a substantially narrowed GUARD 1 (of 9 activity-write sites, only 2 are real writes; gate one, exempt one per an existing reviewer ruling, leave clears alone). Flee builds for REAL (not stubbed) off two existing vanilla per-colonist signals the builder found (hostile-target, below-flee-health) — no new determinism seam.** **DONE — "THE PLAYS ITSELF KEYSTONE SHIPS"** (mechanism `40aa8b2174` + a pre-tag dead-colonist-skip fix `afc175f89c`, tag `bastion-block-AUTON0` → `afc175f89c`, gate 30/30 all green on the rerun. All 6 guards verified in the tag report: Guard 1's narrowed map built exactly as adopted [one gated writer + claim-loop entry, one exempt fail-safe, harness untouched, clears self-consistent/unconditional]; Guard 6 self-job skip at the pass top preserves B7-2/B7-3 by construction; Guard 5's PATH-0 composition exercised live (peak_wait 0 post-storm); Guard 4 demonstrated TWICE unprompted (stuck_watch independence — a jobless-trench rescue and a post-recovery off-plateau rescue, both drive-independent) plus a storm-window false-trip assert (zero teleports across 480 ticks where the 60s timer structurally cannot complete); Flee built for real on BOTH signals, exercised through the real health-write path. **Root-caused fix pre-tag:** the arbiter now skips dead/dying colonists at the pass top (a corpse needs no drive; a drive write on a dying colonist raced vanilla's death processing — caught by the BED corpse-probe, root-resolved not classified). **Registry find (RUN-0's sibling):** vanilla silently restores a working colonist's health to FULL mid-scenario (a max-update-class heal) — the arbiter responded CORRECTLY, the test fixture was dishonest (a one-shot health write isn't a persistent threat); fixed by re-asserting the threat per second like a real hostile would. **★ OPUS FINAL SIGN-OFF: CLEARED (BUILD_REVIEW_LOG §R16, 2026-07-13)** — all 6 guards + the dead-colonist skip + the Flee ruling re-verified directly against the tagged commit (not the packet's claims); the two gate reds were both real, root-fixed findings, and the teleport red was live proof Guard 4's backstop is drive-independent. AUTON-1 next.) | DONE | NEW | [Autonomy §AUTON-0](AUTONOMY-ARBITRATION-SPEC.md) |
| 49 | AUTON-1 | Zero-input Mine/Gather/Farm/Haul/Build generation. **SCOPED (Sonnet, 2026-07-13): DF-POLICY (row 86, unbuilt) and B8/threat data (row 52, unbuilt) don't exist yet — generators run unconditionally-enabled v1 with an explicit future policy-hook point (not hardwired), and the defense/muster generator (needs a threat signal) is deferred; mine/haul/build generators ship. Self-verify+tag (data-generation into the existing job board, reuses `place_designation`'s dedupe discipline, not a new authority mechanism).** **CRAFT-TIME FINDING (builder, confirms tier not just claims it):** vanilla ore is SPRITE-based mining (weapon-damage collection, distinct from Bastion's block-removal Mine economy) — wiring it needs a new completion arm, out of this data-generation-only block's scope; deferred to row 49.1. v1 mine generator is DEMAND-DRIVEN instead: deficit = unfilled-plan-cells − stone-supply − pending-mine-jobs (BUILD_MATERIAL==MINE_DROP==stone), so generation QUIESCES when demand hits zero — the no-runaway invariant falls out as an exact assert, not a tuned cap. **DONE — ★ ARCHITECT LEAN SIGN-OFF: CLEARED** (gate effective 31/31, 2 B22 flakes classified; generation quiesces at demand-zero proving no-spam; G2 closes — an un-designated colony mines→hauls→builds→retires its own plan through the AUTON-0 Work-gate cleanly). Tag `bastion-block-AUTON1` → `ede5b80b1a`, gate 29/31 first draw + 2 B22 flakes classified (B5's known flake + a new PREEMPT windowed-completion instance) → effective 31/31. Demand-driven mine generator (deficit = unfilled-plan-cells − stone-supply − pending-mine, `BUILD_MATERIAL==MINE_DROP==stone`), build generator (per-plan unfilled census → capped jobs, `board.plans` frozen cell lists, intent-only at queue-time), haul reuses the existing B6 generator as-is. Zero wire enums, zero SystemData changes — generated jobs indistinguishable from painted ones by construction. Two more registry finds: D20 (a generator's own output can re-satisfy its own candidate filter — the skip-column workaround is honest-not-fully-closed for player-painted structures) and the B37/B24-4th-instance pair already filed. Scenario proves the full zero-paint→generate→claim→build→retire→quiesce loop with byte-identical exact counters across runs. | DONE | NEW | [Autonomy §AUTON-1](AUTONOMY-ARBITRATION-SPEC.md) |
| 49.1 | AUTON-1-ORE | Follow-up (builder finding, AUTON-1 craft, 2026-07-13): wire the mine generator to vanilla's SPRITE-based ore (Coal/Copper/Iron/gems, weapon-damage collection) — needs its own new completion arm, distinct from Bastion's block-removal Mine economy, so it's out of AUTON-1's data-generation-only scope. Low-pri wishlist; possibly pairs with B8/defense-muster's own eventual block. | TODO | MIXED | [Autonomy §AUTON-1](AUTONOMY-ARBITRATION-SPEC.md) |
| 49.2 | B6HAUL-UNREACHABLE-RELEASE | Follow-up (builder finding, AUTON-1 craft, 2026-07-13; registry B37): a permanently-unreachable haul job churns forever without ever dropping, so its reservation is held hostage forever — `should_merge` amplifies this, pinning an entire merged pile behind one bad job. Fix: give unreachable-haul jobs a strike-cap that DROPS the job AND releases the reservation on expiry. Deliberately NOT a rider on AUTON-1 — it changes existing B6HAUL unreachable-retry behavior, needs its own scope/verification. **DONE** (tag `bastion-block-HAULPIN` → `5d6b8a133d`, self-verify+tag. A churning unreachable Haul now DROPS at `HAUL_DROP_STRIKES=3` [the arrival-tolerance growth cap — once tolerance stops growing, further churn cannot converge] via the post-loop `remove_job` [frees the reservation]; the slot-7 generator re-emits from a FRESH scan — retry-by-RESCAN [item fetchable between tries] replaces retry-by-churn [item + whole merged pile pinned forever, exactly AUTON-1 run-2's starvation]. Designated kinds keep the unreachable economy on purpose [terrain targets legitimately un-block, the 60-tick amnesty is theirs]. Scenario: a 2-stack sealed 7-deep [beyond the 6.1-block strike-grown remote-grab envelope] cycles emit→strike→drop→re-emit exactly 3 times both runs, reservations ≤1 always, stack conserves, ×2 identical. Evidence chain honored per the earlier-agreed condition: at-commit ×2 ran clean pre-AUTON-2, the 33-leg suite green at the direct child commit, diff-confirmed AUTON-2's delta doesn't touch the release/reservation code.) | DONE | CHEAP | [Build report §B6](veloren-colony-rts-build-report.md) |
| 50 | AUTON-2 | Survive/SelfNeed drives and shortage recovery — E1, the death-spiral-prevention success criterion. **★ FULL OPUS-GATE AT PACKET-CRAFT (like AUTON-0) — METHOD drafted (Sonnet, 2026-07-13), held with the architect for Opus BEFORE reaching the builder.** Live-verified reuse survey: B7-2's NEED-CHECK preemption + B7-3's Despond already deliver much of the spec's "Survive drive" behavior via a SEPARATE Opus-cleared mechanism — recommend NOT re-unifying self-jobs into literal Drive variants (leave B7-2/B7-3 untouched), and scope AUTON-2 to the 2 genuinely-missing pieces: (1) TRAIT-STAGGER — `NeedTuning.interrupt` is a global fixed constant today (`common/src/bastion.rs:656-725`), not personality-modulated; the whole death-spiral mitigation needs this. (2) the actual `--autonomy-death-spiral-scenario` gate. **OPUS RULINGS (2026-07-13): APPROVED, scope confirmed tight.** (1) trait-stagger + (2) the death-spiral gate build as scoped. Graceful-degrade past the band = NO freeze/crash/deadlock/spam + Despond keeps re-firing correctly — death/emigration explicitly DEFERRED to a future BODIES/MIG block, not built here. NO farm self-designation generator — v1 tries pure trait-stagger + pre-painted capacity FIRST (labor-reallocation by subtraction), a minimal food-urgency Work-scorer multiplier is only a fallback if the scenario proves stagger-alone insufficient (report which was needed). **★ NEW LOAD-BEARING GUARD: the stagger needs an explicit SAFETY FLOOR** — even the hardiest/dutiful colonist must still preempt-to-eat before any terminal state, asserted directly (not assumed); the stagger widens the recoverable band, it must never disable B7-2's backstop. **★ OPUS FINAL SIGN-OFF ON THE MECHANISM: CLEARED (BUILD_REVIEW_LOG §R17, 2026-07-13)** — safety floor confirmed construction-enforced, colony-level acceptance correctly applied; this part stands regardless of the flake below (verified directly in code, independent of it). **★★ REAL DETERMINISTIC REGRESSION FOUND AND RESOLVED (2026-07-14) — TAG NOW FULLY CLOSED.** A quiet-machine PREEMPT rerun (requested to replace weak 1/3 flake-classification evidence) surfaced a genuine, byte-identical deterministic FAIL on `preempt_hover_silent` — a DIFFERENT assert than the known `endure_dug` flake, previously masked by the loaded box's timing noise. Diagnosed (read-only, both findings confirmed by measurement not theory): (1) the assert is B7-2's anti-thrash hysteresis companion, a pure attempts-counter check with ZERO stuck-economy/safety surface (a name-collision with the unrelated R3/FR15 physics-hover class, not a shared one — `no_embeds`/CENTER_NET stayed green throughout); (2) the fixture hardcoded `0.21` against the OLD flat 0.2 interrupt, which AUTON-2's own trait-stagger made per-colonist — an anxious-rolled seed's real threshold sits above 0.21, so the preempt firing was CORRECT stagger behavior, not a bug; the fixture's assumption was stale. Architect ruled: test-only, Sonnet-tier (no full Opus re-pass needed), approved a small harness-only fix (`bastion-harness/src/main.rs`, +21/−2) making the hover level threshold-aware via the mechanism's own public `stagger_interrupt()` — diff reviewed by both Sonnet and the architect. Closure evidence, all satisfied: PREEMPT quiet ×3 PASS/PASS/PASS with the fix; UNIT 30/30; SPIRAL confirmed clean via TWO separate ×2 draws (the first had one field-flip correctly attributed to a pre-existing, untouched-code, already-documented ARCH-003 sensitivity per the newly-banked flake-by-field discipline; a second confirmatory ×2 requested to raise the bar anyway came back fully green both runs). Fix committed solo (`b0b7016d89`, own-commit discipline) before HIST-1 unparked on top. **Also filed: a real B22 misclassification lesson** — the builder's own earlier gate-time flake call matched the FAIL to a known-flaky LEG by name without diffing the failing FIELD, missing that the same leg carried both a genuine flake and a genuine regression simultaneously; general rule now registered. (tag `bastion-block-AUTON2` → `01151c61c1`, THE E1 GATE. `stagger_interrupt` [care_factor's pattern]: h from Craft/Tradition values ±0.5 + Conscientious +0.5/Neurotic −0.5; eff = base×(1−0.4h), clamped [0.05 floor, base×1.5 ceiling] — hardiest possible h=1.5 → eff=0.08, safely above the 0.05 floor, UNIT-pinned exact. `FOOD_DEFS` gained wheat as approved [data-only, B7-3 machinery untouched]. **Acceptance-criteria ruling (architect, after a disclosed builder over-gating correction):** E1's "recoverable-band auto-recovers without input" reads COLONY-level [stock≥start, ≥5/6 fed, all alive, straggler's meter positive-and-retrying — reported not gated], not an every-individual-deadline reading, since individual mechanism properties [floor, stagger discipline] are already independently asserted and the stricter reading would gate E1 on 3 already-filed non-stagger concerns [B38, idle-home-leash, ARCH-003 scheduling]. Gate: 32/33 first draw + PREEMPT flake-classified per B22 [pre-existing signature, identical to AUTON-1's — ★ WEAKER evidence this time, 1/3 not 3/3, rerun requested]; UNIT 30/30; SPIRAL green; ×2 aggregate-identical. Ten-draw forensics chain surfaced 4 registry classes: trait-surface ownership, window-as-decay-clock, idle deep-wander + teleport-vs-brain-target [banked to Design backlog as IDLE-HOME-LEASH], and B38 [merged-pile eat serialization, filed]. PURE-STAGGER SUFFICED — the packet's food-urgency fallback scorer was never needed.) | DONE | NEW | [Autonomy §AUTON-2](AUTONOMY-ARBITRATION-SPEC.md) |
| 50.5 | FLAT-TEST-ARENA | **Ben-decision (2026-07-13): jumps to NEXT priority after AUTON-2** — Ben wants his playtest arena soon; also gates the ARCH-003 integration step below. A SMALL dev tool, not an epic — DIRECT builder packet, no full designer pass. Scope: a chooseable flat empty spawn (flat terrain patch + N colonists, nothing around), selectable ALONGSIDE the normal world (a launch flag / mode toggle). Placeholder-first (no new art, per the standing asset-lane rule). **★ SUPERSEDED ORDERING (2026-07-14): AUTON-3 (row 51) then B-AG4/UI-4 (row 62) now execute BEFORE this row** — the earlier "AUTON-3 moves after the arena" call was itself resequenced twice since; this row is now next in line AFTER both of those tag. **At tag: notify the Play-Tester lane to build a fresh playable exe for Ben.** Prior-art: `server/src/test_world.rs` already proves a minimal flat-ish `World` with the real crate's exact public API, but it's swapped in at COMPILE-TIME via a Cargo feature flag — the wrong granularity for a runtime launch-toggle Ben can pick at will. Packet flags the builder to resolve the cheapest runtime-selectable mechanism at craft (leaning toward a bounded `generate_chunk` override near spawn over a full world-selection abstraction, but left to the builder's judgment once in the code). **★★ TAGGED (`bastion-block-ARENA` → `17e62e1efe`, gate 35/36) BUT CONFIRMED INERT IN REAL PLAY (Play-Tester, 2026-07-14) — REOPENED, PRIORITY FIX.** The env var reaches the process but a fresh world still generates normal stock terrain (town/bridge/river) and spawn isn't slab-centered; full writeup `PLAYTEST_REPORTS.md #BUG:FLAT-TEST-ARENA`, env/save issues already ruled out. **The real lesson: the gate tested the SCENARIO/HARNESS path, not the live singleplayer path this feature was actually built for** — a green gate proved nothing about real-world behavior for this class of feature. Fix must include a genuine live-path check this time, not just a re-green scenario gate. Routed to the builder as priority, ahead of B6HAUL-WIDEN/CHOP-FELLING. **ROOT CAUSE DIAGNOSED (2026-07-14): the server-side override code was always correct; the bug was FLAG TRANSPORT** — normal singleplayer launch never propagates `BASTION_FLAT_ARENA` to the spawned server thread (unlike the proven `--bastion-overseer` pattern, which explicitly `set_var`s in the voxygen main thread before any thread spawns). **FIX (adopts that exact proven pattern):** a new `--bastion-flat-arena` CLI flag (env fallback), resolved single-threaded in `main()` before thread spawn; a boot diagnostic log line (`flat_arena_enabled=`) so the next real launch gives a decisive yes/no signal instead of relying on visual inference. **STATUS: fix committing, PROVISIONAL — genuinely unverified in real play** (VOXCHECK compile only proves it builds, per B41's own lesson applied here deliberately). Will not close until the Play-Tester rebuilds, Ben launches with the new flag, and confirms flat terrain + slab spawn + the log line reading `true`. **★★ CHARTER EXPANDED (Ben, via architect, 2026-07-14) — supersedes the original "small dev tool" framing: the flat arena is now the FLEET'S PERMANENT STANDING TESTBED, not a one-off.** Goal: stand up a FULLY FUNCTIONAL, SELF-SUFFICIENT, AUTONOMOUS COLONY inside it end-to-end for real (mining, farming, building, needs, the full autonomy stack actually running, not spot-checked). Two standing rules from here on: **(1) GROWTH LOOP** — whenever building that colony reveals something MISSING from the arena's own toolset (a tool, a resource, a spawner capability, anything blocking "fully functional colony"), ADD it rather than work around it; this is how the arena becomes genuinely feature-complete over time (the Farm-tool bug is a preview of this class). **(2) USE THE NEW TEST TOOLS to build/verify it**, not manual play alone: harness scenario flags, the regression/metamorphic corpus + save-fuzz once row 50.6 merges, the UI-4 inspector (verify colonist state directly), and Grok's bot-client once merged (scripted interaction). The arena is where the fleet's OWN new tooling gets exercised for real. Row 50.53 (ARENA-TEST-STAGE, resource stock + spawner) and row 50.65 (the regression sweep) both now read as pieces of this larger standing charter, not separate one-off asks. **EXE DELIVERED (2026-07-14): the Play-Tester's combined ARENA+Farm-palette rebuild finished and reached Ben BEFORE the test-infra-merge priority hold landed** — includes UI-4.1's highlight ring too (same window). Ben's live-test results for all three can land independent of the merge hold whenever he gets to it; all OTHER build/test activity fleet-wide is paused per the priority override until the architect confirms the merge. | CURRENT | CHEAP | (exe delivered to Ben, live-test pending; all other builder/PT activity paused for the test-infra merge) |
| 50.51 | FARM-PALETTE-BUG | **PRIORITY BUG (Play-Tester find, 2026-07-14) — routed ahead of the ARENA fix if sequencing allows, directly blocks Ben's live-testing.** Farm has been UNSELECTABLE in the client UI since it shipped: `voxygen/src/bastion/tools.rs:25-39`'s `ToolMode::ALL` is missing `ToolMode::Designate(DesignationKind::Farm)` — 9 entries where there should be 10; the zone-color legend (`:126`) already reserves Farm's color, never wired to a palette button. Trivial one-entry fix. Full writeup `PLAYTEST_REPORTS.md #BUG:Farm-tool-MISSING`. Routed to the builder. **DONE** (tag `bastion-block-FARMTOOL` → `23087dbd68` — the one missing `ALL` entry added, `[ToolMode; 9]`→10, VOXCHECK green. Row was stale-marked CURRENT after this landed; corrected here retroactively. Root-cause-of-class fix shipped separately as EXHAUSTIVENESS-ASSERTS, row 51.52.) | DONE | CHEAP | (fixed, `23087dbd68`) |
| 50.6 | ARCH-003-INTEGRATE | Rebase the Bug-Tester's Opus-CLEARED clean tree (8 fixes, `codex/arch003`) onto fleet HEAD, resolving the known PATH-0 conflict; also the Grok Phase-1+2 test-infra bundle merge point. **★ RE-ACCELERATED (Ben-directive, "implement the testing framework," 2026-07-14, SUPERSEDES the UI-4.1 resequence below): moves to RIGHT AFTER the ARENA fix + Farm-palette-bug (50.5/50.51) — ahead of CHOP-FELLING/UI-4.1/the regression-audit checkpoint.** Rationale: the ARENA-inert + Farm-tool-missing bugs are the exact proof-of-need for this — the test-infra bundle is already built + Opus-cleared, just needs the box + a stable base; don't let polish items delay it further. New order: ARENA fix → Farm-palette fix → **THIS ROW** → CHOP-FELLING → UI-4.1 → 50.65 (regression audit). *(Superseded note, 2026-07-14 earlier: had been pushed after UI-4.1 since Ben wanted the highlight ring first — overridden by the testing-framework directive.)* Architect directs the actual integration step when reached; Bug-Tester holds its verified tree until then, don't merge early. **DONE — MERGE COMPLETE, FREEZE LIFTED (architect, 2026-07-14).** Tag `bastion-block-ARCH003` → `9dfff6ec7e`. Verified on the actual merged tree by the architect: clean release build, 3x seed-21 byte-identical (only the known benign wall-clock field differs), seed-22 sanity PASS. A full priority-halt was held fleet-wide (builder + Play-Tester paused all other work) from the freeze declaration through this merge landing — the halt is now lifted, normal work resumes. Sonnet cleaned the tree ahead of the merge: a bookkeeping commit (`84f269b0c9`, 17 readme/run-log/ledger/master-list files) + the builder discarding 2 paused uncommitted source files (`common/src/bastion.rs`, `voxygen/src/bastion/tools.rs` — pre-verified patch-recoverable via `git apply --reverse --check`, HEAD unchanged at `f6ac4c8bc7` through the discard). Next: EXHAUSTIVENESS-ASSERTS/TOOLBAR-ICONS/Bed-fix (row 51.52, already CURRENT/unblocked), then the 8 banked Ben findings (51.61-51.65 + progress bar/z-level-scope/tree-felling-repro/stockpile/gather/UI-5) per existing sequencing. Bug-Tester starts the full functional test catalog separately, now that the merged tree is the stable base it needed.) | DONE | MIXED | (architect-directed integration, not a standard packet) |
| 50.65 | FULL-REGRESSION-AUDIT | **STANDING DIRECTIVE (Ben, via architect, 2026-07-14) — future checkpoint, queued not urgent.** Once test-infra (Grok Phase-1+2 + ARCH-003 determinism, row 50.6) is merged and live: walk EVERY DONE row in the master build list and verify it's STILL actually working — real functional checks, not "it compiled once" or "the tag/history says so" (the direct lesson of B41/FLAT-TEST-ARENA's inert-despite-green-gate finding). Use the new regression corpus + metamorphic/save-fuzz tools once available as the mechanism. Flag anything found silently regressed to the architect for triage. Sequenced AFTER 50.6, and after the arena bug fix + B6HAUL-WIDEN/CHOP-FELLING/UI-4.1 currently ahead of it. **★ STAGING ENVIRONMENT (architect, 2026-07-14, once Ben live-confirms the ARENA fix): the sweep runs IN FLAT-TEST-ARENA, not a normal world** — a clean stage, no town noise, controlled spawn, exactly the reason Ben wanted the arena as a standing test tool in the first place. The pending arena resource-stock/item-spawner follow-up (row 50.53, ARENA-TEST-STAGE) becomes directly relevant here too — the sweep will want quick access to per-feature test-inputs without a full economy grind each time. **★ FOLDED INTO THE ARENA'S EXPANDED STANDING CHARTER (Ben, 2026-07-14, row 50.5)** — this sweep is now one piece of the arena's larger permanent-testbed purpose (a self-sufficient autonomous colony running end-to-end, verified with the fleet's own new tooling: harness scenario flags, the regression/metamorphic/save-fuzz corpus, the UI-4 inspector, Grok's bot-client), not a standalone one-off checkpoint. | TODO | MIXED | [Common Issues §B41](BASTION_COMMON_ISSUES.md) |
| 50.8 | WINDOWED-ASSERT-SWEEP | Housekeeping (builder proposal, AUTON-3 tag, 2026-07-14; registry B40): a consistency-sweep across the harness's OTHER deadline-shaped/windowed asserts (N-ticks/polls completion checks), auditing each for the same near-zero-margin risk that hit SPIRAL's recovery window, SPIRAL's floor window, and HAULPIN's emissions window (four confirmed instances of the same class before this sweep). Widen any found with <2× headroom to the established fix shape. Low-pri, queue for a natural gap. | TODO | CHEAP | [Common Issues §B40](BASTION_COMMON_ISSUES.md) |
| 50.9 | FLAT-ARENA-TOGGLE | Ben-direct follow-up (2026-07-14): replace FLAT-TEST-ARENA's `BASTION_FLAT_ARENA` env-var toggle with a proper in-game selectable option (a checkbox/toggle on the launch or world-select screen). Non-blocking — the env var works fine for now. Same tier/priority as UI-4.1, no urgent resequencing, ship whenever a natural slot opens. | TODO | CHEAP | (packet to be crafted at handoff) |
| 50.7 | MERGED-PILE-EAT-CONCURRENCY | Follow-up (builder finding, AUTON-2 tag, 2026-07-13; registry B38, data-proven live): a merged food pile is one entity/Uid/one reservation, so concurrent eating under mass hunger throttles to ~1 fed per preempt-cooldown window regardless of stock size — exactly the death-spiral moment AUTON-2 exists to prevent, a genuine live-play risk not just a scenario artifact. Fix candidates: per-unit reservation semantics for multi-stacks, or reservation-free eats (the existing sniped-moot arm already precedents this on the last unit). Touches B7-2/B6 reservation semantics — its own scoped block, NOT built now. | TODO | MIXED | [Build report §B7](veloren-colony-rts-build-report.md) |
| 51 | AUTON-3 | Trait urgency, policy tilt, and decision legibility. **RESEQUENCED AGAIN (architect, 2026-07-14, supersedes the 2026-07-13 note below): moved back BEFORE FLAT-TEST-ARENA — closes the AUTON arc while the builder's primed, rather than context-switching to the arena mid-arc.** *(Superseded note, 2026-07-13: had been pushed after FLAT-TEST-ARENA + ARCH-003-INTEGRATE as tuning/polish that doesn't block Ben's arena — no longer the order.)* **NARROWED (Sonnet, 2026-07-14):** three of the spec's four done-when clauses have real dependency gaps — UI-4/B-AG4 (row 62, unbuilt, nothing to DISPLAY `last_scores`), DF-POLICY (row 86, unbuilt, nothing to bias weights from), "Ben-tuned curves" (needs live playtesting, not a build task). This packet builds ONLY: trait-modulated drive-urgency SCORING (reusing AUTON-2's `stagger_interrupt` read pattern) + `last_scores` as server-side data (probe-readable, no UI yet). Self-verify+tag (architect-confirmed) with an explicit Flee-floor guard baked in as a done-when — lower stakes than AUTON-2's floor since Flee can't fire for real without B8 yet, but worth getting right now so B8 inherits a correct invariant rather than a landmine; flag for Opus scrutiny again once B8 makes Flee live. **DONE** (tag `bastion-block-AUTON3` → `5e9ed6385f`. Gate storm mid-development: 5 legs [CK/ZONE/BED/B73/HAULPIN] went red at once on the first 35-leg draw — root-caused, not classified, to a scope-hoist bug in the builder's own diff [the rtsim read-guard acquisition was hoisted to fire every arbiter tick instead of the original zero, a lock-cadence perturbation of exactly the ARCH-003-sensitive class]; zero scoring/urgency/pick asserts failed anywhere, confirming the trait-modulation mechanism itself was never at fault. Fixed [guard acquired once per selection tick, matching the mood-pass cadence precedent] + one separate latent fixture issue found in the same storm [HAULPIN's 240-poll window had zero headroom for its 3 strike-drop cycles — timing-marginal not nondeterministic, widened to 480 polls, registry B40]. Root cause itself filed as B39. `modulated_urgencies` (pure, RNG-free): one value + one personality-trio per axis — Wealth→Work `[0.4,0.6]`; Glory/Adventurous/Worried→Flee `[0.85,1.15]` with an `0.8` floor; Kin/Sociable-or-Extroverted/Introverted→Idle `[0.07,0.13]` — input-swapped at the arbiter's two `last_scores` write sites, selection/commitment/hysteresis machinery untouched. Axes (Glory/Wealth/Kin) deliberately DISJOINT from the stagger's (Craft/Tradition) — the two trait systems compose without interaction. Drive-order safety guard UNIT-pinned exact: bravest-possible Flee (0.85) > greediest-possible Work ceiling (0.6), margin 0.25; `.min(base)` zero-preservation (a no-signal 0.0 baseline can never be modulated UP into inventing a flee — the 3rd application of the recreation-zero discipline); idle ceiling (0.13) < work floor (0.4), preserving AUTON-0's liveness contract for every possible roll. Scenario proved E2 legibility directly, without any UI: two colonists with designed-opposite rolls recorded `last_scores` matching the mechanism's own prediction to the exact f32 bit. Gate: 33/35 on the closure draw, all five storm legs verified green in-suite; LOD1/BED classified per B22 (LOD1 = its own pre-existing registry class, BED = the pre-existing B7-1 kill-race field, both reconfirmed not new) → effective 35/35; UNIT 31/31; scenario ×2 byte-identical. **THE AUTONOMY ARC (rows 48-51) IS COMPLETE.** Deferred riders, all already tracked: UI-4 [next, row 62], DF-POLICY, Ben-tuning, B8's eventual live Flee, 49.1/50.7/50.8.) | DONE | MIXED | [Autonomy §AUTON-3](AUTONOMY-ARBITRATION-SPEC.md) |
| 51.5 | TOOLBAR-ICONS | Low-pri UI polish (Ben-confirmed via Play-Tester, queued by architect 2026-07-13): swap the overseer ToolMode palette's text `.label()` calls (`voxygen/src/hud/mod.rs` ~4848-4872 — Pan/Inspect/Mine/Chop/Gather/Farm/Build/Stockpile/Ladder/Erase/God-toggle) to Conrod Image widgets using delivered icon assets, 1:1 per the asset builder's marker/path map. UI-ONLY, NO sim/server touch, VOXCHECK-gated (a voxygen edit — B30 class). **SUPERSEDED (Ben, 2026-07-13, standing rule): asset lane stood down for token preservation — never gate a build on it.** The existing text `.label()` calls ARE the placeholder (no icon-asset dependency at all now). **★ CORRECTED, UN-PARKED (architect, 2026-07-14): all 11 icon PNGs already exist on disk** (`asset-lab/ui/overseer/tool_{pan,inspect,mine,chop,gather,farm,build,stockpile,ladder,erase,god}.png`, confirmed) — the "nothing buildable" call above was wrong, the assets were already delivered before the asset-lane stand-down and just never got wired. Same bug CLASS as the Farm-palette miss (B41-adjacent: asset/mechanism built, UI wiring skipped). Scope: swap `.label(tool.label())` → Conrod `Image` widget, texture id `tool_<name>.png` per tool — verify the icon set is still current against the LIVE `ToolMode::ALL` (just went 9→10 with the Farm-palette fix, confirm icons cover Farm too) before wiring. Routed to the builder as a small fill, no rush. **OWNERSHIP RULED (architect, 2026-07-14): builder does the copy+wire-in themselves** (asset-lab→tester→integration is dormant under the stand-down, no reason to spin it up for a mechanical file copy — this is code/integration work, not new asset creation). **★ KNOWN ISSUE, ship anyway, don't fix now:** the Asset Integration tester's partial pre-screen (pre-stand-down) found readability issues on this exact icon set — mine reads as mattock/T not pickaxe, chop reads as hook/scythe not axe, pan reads crown/comb-ish, and there's a gather↔farm look-alike collision. Icons integrated AS-IS (better than text labels, placeholder-first spirit); a readability pass is explicitly deferred to whenever the asset lane resumes. **DONE** (tag `bastion-block-ICONS` → `4f3fe6aa10`. The overseer palette now renders 34x34 `Button::image` icon buttons [active=bright/others=dimmed] instead of text `.label()` calls; God/Free stays text [shows the mode]. 11 asset-lab icons copied into `assets/voxygen/element/ui/bastion/` + declared in `img_ids.rs` + the palette loop rewired. Voxygen-only, VOXCHECK green [all 11 specifiers confirmed resolving to real committed files]. The known readability issues [mine/chop/pan misreads + gather↔farm collision] carry forward unchanged, still deferred to the asset-lane resume. **NEW GAP found by this pass itself:** Bed has NO icon — it was added to the palette by EXHAUSTIVENESS-ASSERTS AFTER this 11-icon set was made, so it falls back to a transparent image + text label; `tool_bed.png` is a new 12th-icon asset-lane backlog item, logged to `ASSET_REQUESTS.md`. VISUAL-UNVERIFIED until Ben eyeballs it [VOXCHECK only proves compile + specifier-resolution, not render/no-crash, per B41's standing lesson] — Ben-checklist item routed via the architect.) | DONE | CHEAP | [Build report](veloren-colony-rts-build-report.md) |
| 51.52 | EXHAUSTIVENESS-ASSERTS | **UNBLOCKED NOW (architect, 2026-07-14) — Item M from the Designer's TEST-INFRASTRUCTURE-AUDIT.md update, ahead of Phase-1's own hold (it's a cargo-check fix, not a framework, zero infra).** Root cause of the Farm-palette bug precisely: a hand-written literal array (`ToolMode::ALL`) silently bypasses Rust's own exhaustiveness checker (which only engages on a `match`/derive, not a literal). Scope: (a) convert `ToolMode::ALL` and similar hand-mirrored enum lists to a non-wildcard exhaustive `match` over the source enum where feasible (compile-time enforced); (b) where a literal array must stay, add a parity-assert unit test (`ALL.len() == EnumType::COUNT`); (c) grep the "append-only-enum family" (`WorkType`/`ZoneKind`/`Need`/etc.) for the same exposure pattern, fix opportunistically. Sequence: after ARENA/Farm-palette/icon fixes, before or alongside CHOP-FELLING. **★ IMPLEMENTATION REFINED (builder scoping, 2026-07-14): NOT a strum/EnumIter parity assert — `DesignationKind::Zone(ZoneKind)` is data-carrying, strum doesn't apply. Real guard = an exhaustive `is_tool_paintable(self) -> bool` match with NO wildcard, so a future new `DesignationKind` variant fails to COMPILE until categorized (genuine Farm-bug prevention, not just a count check) + a voxygen parity test linking it to `ToolMode::ALL`.** **★ A 3RD INSTANCE FOUND BY THIS WORK ITSELF, RULED (architect, verified directly in code): `DesignationKind::Bed` ALSO has zero `ToolMode::Designate` entry (color reserved at `:128`, same signature as Farm) — CONFIRMED BUG, not intentional auto-placement. Fold Bed's fix directly into this pass** (categorize it paintable in the new exhaustive match, gets a real palette button) — not a separate decision item. **DONE** (tag `bastion-block-EXHAUST` → `92ec5eabf1`, on the merged ARCH-003 tree. Guard shipped: `DesignationKind::is_tool_paintable()` — an exhaustive match, no wildcard, so a new variant fails to COMPILE until categorized — plus a bidirectional voxygen parity test asserting every paintable kind has a button and vice versa; both would have caught Farm AND Bed. Bed folded in: `is_tool_paintable(Bed)=true` + a real `ToolMode::Designate(Bed)` button (palette 10→11) — all its supporting infra [label/color/footprint/worktype] already existed, only the wiring was missing, same class as Farm. **Verification scope disclosed by the builder (same precedent as the Farm/UI-4.1 client-only tags):** this pass is SIM-INERT — the common-side additions are additive, touched by no sim code path, so the builder ran common UNIT [31/31] + the new voxygen parity test [1/1] + harness BUILD + VOXCHECK (all green) rather than the full 37-scenario gate, reasoning the scenarios test the sim (which this can't affect) while the Bug-Tester's isolated-worktree catalog run is separately validating the full suite under ARCH-003. Accepted — consistent with prior client-only-tag precedent, not bounced back for a redundant full gate.) | DONE | CHEAP | [Test Infra Audit §Item M](TEST-INFRASTRUCTURE-AUDIT.md) |
| 51.53 | ARENA-TEST-STAGE | Ben directive via Play-Tester (2026-07-14), folds into or fast-follows the ARENA fix: (a) FLAT-TEST-ARENA needs a starting stock of core testable resources (stone/wood/food/tools) so testers don't grind the economy just to reach the feature they want to check; (b) a debug item add/remove capability (god-mode spawner / console command) for isolated feature testing, scoped DEBUG-ONLY (`BASTION_FLAT_ARENA`-gated or similar, not a live-game feature). Makes the arena actually useful as the standing test stage per the "test every feature" directive — sequence soon, exact placement (bundled with ARENA vs. a fast follow-up) is the builder's call. | TODO | CHEAP | (packet to be crafted) |
| 51.6 | CHOP-FELLING | Ben-direct design (packet ready), queued by architect 2026-07-13, behind the AUTON cluster: a WORK-MODEL refactor, fell-set MATH untouched. Core: `place_chop_cells` → `place_chop_fell` (`server/src/bastion_jobs.rs:1466`) = ONE base-cut job + the fell-set stored in a JobBoard side-table (the B6-HAUL container-store/B7 BedSlot precedent); the completion arm (~:4405/:4561) fells the stored whole-set at once (drop per Wood + XP). Callers to update: `in_game.rs:1037` (player-paint) + `lib.rs:1564` (harness-hook). REUSE, don't touch: `tree_fell_set`, `detect_trees` (only add: return the base `seed_z`), the FR10 caps. Visual: v1.5 = top-down staggered removal (~0.4s, canopy-first/base-last, no physics, base stays ground-rooted so no-float-safe by construction) — v2 physics-topple deferred. Economy: base-cut threshold scales with Wood-count so total labor ≈ old per-block sum (a granularity refactor, NOT a rebalance — preserve yield + labor, no tuning drift). Bonus: fixes the FR10 floating-canopy residual for free. VOXCHECK-gated (removal + client visual). **TIER:** Sonnet-routine for the swap/side-table/whole-set completion; a TARGETED Opus eye AT THE TAG on three safety points only — (a) no-float invariant, (b) removal-ordering determinism (gate-relevant), (c) the side-table's B22-class scheduling perturbation. **RELEASED (2026-07-14): the AUTON cluster's completion IS the natural gap** — architect confirmed, no file collision with FLAT-TEST-ARENA in flight. Packet delivered as the builder's fill during FLAT-TEST-ARENA's gate window (may run past the window into the actual next-block slot, that's fine). **DONE** (tag `bastion-block-CHOPFELL` → `50aff8808a`, gate 37/37 CLEAN. Base-cut → whole-tree timber [DF/TreeCapitator genre standard]: ONE base-cut job at the ground-rooted base [always reachable — FR10's floating-canopy residual gone by construction], the fell-set frozen in `chop_fell_sets` under the job id [B6-HAUL/BedSlot co-located-table shape], completion = XP for the whole tree + top-down drain. Yield + per-Wood labor conserved exactly [threshold = `CHOP_WORK_PER_BLOCK`×Wood, proven: 9-Wood/3-Wood threshold ratio 3.0 measured at 2.95x via telemetry]. **Design-vs-code discrepancy found and disclosed:** the original design doc's "leaves clear free" reasoning was factually wrong about the CURRENT code [leaves already cost labor/XP as jobs today] — but the design's actual intended economic outcome still holds under the new model for a different, correct reason [canopy is most of a tree's cells, so trees fell faster overall — same practical effect, different mechanism]. **Targeted Opus-at-tag requested** on the 3 flagged safety points [no-float by construction, ordering determinism, side-table B22-scheduling] — pending. **B6HAUL-WIDEN landed in the same bundle** (`a6de03b44d`): b6haul's poll windows widened (240→480 class), fixing a marginal-window flake exposed by suite growth, unrelated to CHOP-FELLING's own logic. **BED leg resolved** — a pre-existing arbiter-vs-assign race (B42, fixed harness-side) plus a rarer residual CASE-003 physics-embed (B43, filed separately via Bug-Tester, not a blocker).) | DONE | MIXED | [CHOP-FELLING-REFINEMENT-design.md](CHOP-FELLING-REFINEMENT-design.md) |
| 51.61 | CHOP-PROGRESS-INDICATOR | Ben find (from the delivered exe, 2026-07-14) — banked, NOT built yet, fleet paused for the test-infra merge. A visible progress bar/indicator for tree-cutting: the base-cut job now has a size-scaled duration (CHOP-FELLING) but no visual feedback, so the player can't tell it's progressing. Small UI addition, placeholder-first fine. Queue for the builder once the merge freeze lifts. **DONE** (tag `bastion-block-PROGRESS` → `7f087da317`. Sim-inert display field `Arbiter.activity: Option<(WorkType,f32)>` [None default, NEVER read by scoring/selection/hysteresis]; written at the job-progress path, cleared at the existing `to_release`/`last_scores` sites — no new borrow. Re-packaged into the existing `BastionInspectPayload` [tail-appended, no new wire message, B30 discipline]; panel shows "Doing: Chop 74%" / "(idle)" reusing UI-4's fetch cache. New probe `bastion_colonist_activity`. Verified via the testing tools per the architect's standard, not code-read alone: chopfell scenario ×2 byte-identical, activity line populates and climbs to ~99.9% pre-fell on both a small and big tree, all pre-existing felled/topdown/no_orphan/drops asserts UNCHANGED confirming sim-inert; UNIT 31/31; VOXCHECK green; BUILD green.) | DONE | CHEAP | (packet to be crafted at handoff) |
| 51.62 | Z-LEVEL-CONTROLS-SCOPE | Ben observation (from the delivered exe, 2026-07-14) — banked, NOT built yet. Z-level/slice controls should arguably only matter for the Mine tool, not others — UNCLEAR yet whether this is a BUG (z-controls incorrectly affecting/showing for non-mining tools) or a UX suggestion (hide/disable them except when Mine is active). Needs investigation FIRST, don't assume which before scoping a fix. Queue for the builder once the merge freeze lifts. **INVESTIGATED (builder, 2026-07-14): VERDICT = NOT A BUG, no code changed.** Two distinct z-controls, both correctly scoped already: (1) the designation depth stepper (`hud/mod.rs:4929`) is gated on `footprint_mode()==Volume` → only Mine/Build/Stockpile/Ladder/Bed, correctly hidden for Area2D tools (Chop/Gather/Farm) and non-designate tools; (2) the camera z-slice PgUp/PgDn (`session/mod.rs:2093`) is gated on `bastion_overseer_active()` → works regardless of tool by design (a camera control, not a designation control). Neither is `ToolMode::Mine`-gated specifically, but Ben's intuition is already ~honored for the depth stepper (the one that actually matters per-designation). Only real gap found: the PgUp/PgDn slice has no on-screen affordance (pure discoverability) — an optional follow-up, not a bug fix. | TODO | CHEAP | (investigated, no fix needed — optional discoverability follow-up only) |
| 51.63 | ★ CHOP-FELLING-VISUAL-CHECK | **PRIORITY once the merge freeze lifts — Ben find, possible real bug, 2026-07-14.** Trees don't appear to actually get felled when chopped in the delivered exe. Ben suspected timing (the base-cut duration + top-down stagger), but the architect explicitly flagged: CHOP-FELLING has NEVER had a live eyeball-check — compile/gate-green ≠ feature-works, the SAME lesson ARENA and Farm-palette both taught this session, twice already. Do NOT assume it's just timing. Needs a real repro: designate Chop, time the actual base-cut duration against the size-scaled formula, confirm the tree visually fells top-down afterward. Treat with the same seriousness as the ARENA/Farm finds until proven otherwise. **INVESTIGATED (builder, 2026-07-14, via harness scenario + UI-4 inspector per the architect's tooling standard, not code-read alone): VERDICT = felling mechanism CODE-SOUND + SERVER-SIDE-EXECUTION-CONFIRMED; likely real-play cause = duration+legibility (addressed by 51.61); Ben's live eyeball still required to fully close.** Felling removes blocks via `block_change.set(cell, Block::empty())` at `bastion_jobs.rs:5420/5430` — the IDENTICAL client-synced path mining already uses (`block_change`→`TerrainChanges`→`terrain_sync`→client mesh), no raw-terrain bypass; `tree_fell_set` keys on `BlockKind::Wood|Leaves`, exactly what live worldgen trees are made of. Chopfell scenario ×2: top-down removal + no-orphan + timber-drop all PASS. Likely real culprit: the base-cut DURATION itself (measured `cut_polls` ≈13.6s small tree / ≈31s big tree of real work at 30tps) with zero prior feedback reading as "nothing is happening" — exactly what 51.61's progress indicator now fixes. Per the standing gate-must-test-live-path lesson (B41), a live eyeball (designate Chop on a real worldgen tree, watch it fell after the cut completes) is still required to fully close this row — Ben-checklist item recommended. | TODO | CHEAP | [Common Issues §B41](BASTION_COMMON_ISSUES.md) |
| 51.64 | STOCKPILE-INVESTIGATE | Ben find ("never seems to work"), 2026-07-14 — banked, NOT built yet, queued behind 51.61-51.63. Stockpile IS already a wired palette button (confirmed in the original `ToolMode::ALL`, unlike Farm/Bed) — likely NOT the missing-button bug class. Needs real investigation once testing resumes: does designating a stockpile actually get items hauled there? Does it visually show contents? Is claim/haul logic broken, or is this a UX-legibility issue (works but invisible)? Repro first, don't assume cause. **INVESTIGATED (builder, 2026-07-14, via the b6haul harness scenario re-run on the current tree): VERDICT = mechanic WORKS; likely real-play cause = live-path/legibility, not broken.** `b6_zonea_sum=5, b6_pad_total=5, b6_conserved=true, b6_delivered=true` — 5 mined stones auto-haul to a painted Stockpile as 5 REAL item entities (`bastion_sum_items_near` counts actual entities, so hauled items ARE physically present/visible, not abstract counter storage). Stockpile designation is a pure destination marker with zero jobs of its own. The likely "never works" root: a stockpile is INERT until loose items on the ground + a painted destination + a free hauler ALL exist simultaneously — paint one with nothing nearby to haul and correctly nothing moves, reading as broken. Fix direction: row 62.2 UI-5 (click a stockpile → contents panel) plus optionally a "waiting for N items" affordance. Ben-checklist item recommended to confirm which failure mode he actually hit. | TODO | CHEAP | [Build report §B6](veloren-colony-rts-build-report.md) |
| 51.65 | GATHER-FLORA-INVESTIGATE | Ben find (no food/berries to test Gather with), 2026-07-14 — banked, NOT built yet, queued behind 51.61-51.64. `DesignationKind::Gather` (row 38) is an old, shipped mechanic presumably targeting existing world flora. Open question: does gatherable flora actually spawn/exist near the colony (worldgen placement), is it just rare/sparse, or is there a Farm-class wiring gap? Check whether any gatherable flora exists near typical spawn points before concluding bug vs. placement-density issue. **★ SHARPENED (architect, 2026-07-14): Ben expects Gather to work as a select/click-designation on INDIVIDUAL plants/bushes — the SAME interaction pattern as selecting a tree for Chop.** Repro scope now covers BOTH: (a) confirm Gather's designation interaction actually matches the tree-select UX, AND (b) check whether gatherable flora exists/spawns near the colony at all. **INVESTIGATED (builder, 2026-07-14): VERDICT = UX-CONSISTENCY GAP, not a bug.** Client-side Chop and Gather are handled IDENTICALLY (`FootprintMode::Area2D`, `session/mod.rs:1086`) — the asymmetry is server-side: Chop's `detect_trees` snaps to the whole rooted tree object near the painted region (forgiving — click NEAR a tree fells it), while Gather's `job_wanted(Gather)` requires `is_directly_collectible()` on the exact painted CELL (precise — a plant is a single sprite, must paint ON it, no nearest-object snap). Both work as designed; Gather is just fiddlier than Chop, which is almost certainly what Ben felt. Fix direction if parity is wanted: a click-tolerance/nearest-collectible snap for Gather mirroring `detect_trees`' forgiveness — scoped as a UX-parity fill, not a bug fix, pending Ben's confirmation he wants it. No code changed. | TODO | CHEAP | [Build report §B4](veloren-colony-rts-build-report.md) |
| 52 | B8 | Autonomous muster/fight/flee and bounded draft. | TODO | MIXED | [Build report §B8](veloren-colony-rts-build-report.md) |
| 53 | HAZ-0..3 | Shared acute hazard, attribution, and mind reaction. | TODO | MIXED | [Hazard Events](HAZARD-EVENTS-design.md) |
| 54 | HIST-1..2 | Event capture and live Chronicle/announcement feed. **Linked dependent note (from row 41.1's closure, 2026-07-13): as each new emitter lands here, expand `assets/common/bastion_value_affinities.ron` (ChronicleKind→Value) + `assets/common/bastion_thoughts.ron` (ChronicleKind→thought weight) to cover it.** **HIST-1 DONE** (tag `bastion-block-HIST1` → `574f401132`, self-verify+tag, gate 34/34 all green first draw, zero flakes/reruns — includes the fixed PREEMPT fixture + a clean SPIRAL + the CHRON-baseline recheck confirming HIST-0's leg stays green with `ChronicleEvents` live. `ChronicleEvents` [`rtsim/src/rule/chronicle_events.rs`] is `ReportEvents`' sibling — same shape, same registration site, binding `OnDeath`+`OnTheft` to `record()`. Death: `actors=[victim,killer?]` [lands in BOTH figures' histories], `Notable`/`World`, NO witness gate [gossip needs witnesses, history doesn't]; Theft: `[thief]`, site+pos. `Reports` byte-untouched — same event, two sinks. Verified via the REAL pipelines end-to-end [a real kill → server death event → rtsim `OnDeath`; a real emission hook, not a synthetic one], one event → exactly one record, conservation held across a 300-tick settle, ×2 identical.) **Coverage update: Death now has BOTH a live emitter and existing thought/affinity rows [fully covered]; Theft has a NEW live emitter but no thought/affinity rows yet [a fresh gap, same class as this row's own linked note].** **HIST-2 stays blocked** (needs the unbuilt B9 HUD + client sync). | DONE | CHEAP | [DF-HIST §HIST-1/2](DF-HIST-design.md) |
| 55 | GH-A | Renderable hand, navigation, and selection. | TODO | MIXED | [God Hand Integration §showpiece](GOD-HAND-INTEGRATION-design.md) |
| 56 | B13-W0 / POWER-0 | Favor economy, targeting, and authoritative cast pipeline. | TODO | NEW | [Powers Wave 0](GOD-POWERS-CATALOG.md) |
| 57 | GH-B | Costed grab/carry/drop/throw/stroke/slap/tap. | TODO | MIXED | [God Hand §GH-B](GOD-HAND-design.md) |
| 58 | B13-W1 / POWER-1 | Smite, Bless, Weather, and resource-seed powers. | TODO | CHEAP | [Powers Wave 1](GOD-POWERS-CATALOG.md) |
| 59 | GH-C | Cast, paint, sculpt, and divine VFX. | TODO | MIXED | [God Hand §GH-C](GOD-HAND-design.md) |
| 60 | GH-D | Deed-earned alignment morph and aura. | TODO | MIXED | [God Hand §GH-D](GOD-HAND-design.md) |
| 61 | B9 / UI-PLATFORMS | Overlay, dashboard, Chronicle reader, alerts, and policy/meta surfaces. | TODO | MIXED | [Build report §B9](veloren-colony-rts-build-report.md) |
| 62 | B-AG4 / UI-4 | Unit inspector, event box, and per-agent history. **★ PRIORITY-BUMPED (Ben-direct, via architect, 2026-07-14): executes right after AUTON-3, BEFORE FLAT-TEST-ARENA (row 50.5)** — the missing piece for verifying AUTON-0/2 by inspection instead of inference, and unblocks AUTON-3's own deferred `last_scores` display. **Scope (narrowed for this pass, not the full row):** click/select a colonist → a panel showing needs meters, mood, personality traits, current drive, and `last_scores` (Work/Flee/Idle numbers). UI-only, placeholder-first (no new art, per the standing asset-lane rule), CHEAP-leaning. Prior-art: entity selection already exists (`bastion_pick_entity`/`bastion_select_set`, `voxygen/src/session/mod.rs:422`, since B2a) — reused, not rebuilt. Packet delivered (AUTON-3 tagged `5e9ed6385f`, row 51 → DONE — the autonomy arc 48-51 is complete). **CRAFT-TIME FINDING, confirmed and approved:** none of the inspector's data (needs/mood/personality/drive/`last_scores`) reaches the client today — only vanilla comps (Stats/Health/Pos) are in the comp-sync registry. Minimal fix: a request/response wire pair (`ClientGeneral::BastionInspect{target:Uid}` + `ServerGeneral::BastionInspectInfo{payload}`, appended at the enum tail, one commit) rather than registering 4 comps for full continuous sync — the inspector is a single-target on-demand query, not a broadcast-to-everyone need, so polling is the right architectural fit. Server handler re-packages the ALREADY-EXISTING probe reads keyed by Uid. **DONE** (tag `bastion-block-UI4` → `e28cc2b7d0`, gate 34/35 first draw + BED field-classified per B22 [pre-existing B7-1 kill-race class, 3/3 rerun] → effective 35/35. THE ARC BECOMES VISIBLE: click a colonist → needs/mood/personality/current-Drive/`last_scores` [W/F/I], live at ~1Hz — the first display surface for everything rows 44-51 built. Server: the `bastion_spawn` deferral pattern [requests gathered in-join, resolved in the post-join drain, the rtsim guard at REQUEST cadence — B39's lesson applied at birth, not repeated]; non-colonist/stale target → `None` payload [no-crash invariant]. Client: latest-wins reply cache. Voxygen: reuses `bastion_pick_entity`/`bastion_select_set` exactly [no second selection mechanism], one plain-text block, placeholder-first, zero art. Read-only end to end. **Ben-eyeball item flagged to the Play-Tester lane** [click→panel, ground/multi→clears, watch W/F/I move on a drive switch] for the next BEN-TEST-CHECKLIST entry.) | DONE | MIXED | [UI Dialogue/Selection](UI-DIALOGUE-SELECTION-design.md) |
| 62.1 | UI-4.1 | Ben-direct follow-up (2026-07-14): a world-space visual ring/highlight on the selected colonist (not just the dashboard panel) — confirmed genuinely missing (`bastion_select_set` has no entity highlight; the existing region-outline is zone-painting only, `session/mod.rs:817`). Small, self-contained, voxygen-only, placeholder-first (a simple ring/outline, no new art). **QUEUED, do not release until FLAT-TEST-ARENA + CHOP-FELLING both land** — non-colliding but sequenced behind them. **★ RESEQUENCED AGAIN (Ben-directive, "implement the testing framework," 2026-07-14, SUPERSEDES the note above): ARCH-003-INTEGRATE (row 50.6) now moves BEFORE this row** — the testing-framework directive re-accelerated it ahead of remaining polish. Order: ARENA/Farm fixes → ARCH-003-INTEGRATE → CHOP-FELLING → **THIS ROW**. **In practice CHOP-FELLING (already in flight when the resequence landed) finished first and is now DONE** — ARCH-003-INTEGRATE is architect-directed, not a builder-self-initiated packet, so the builder is proceeding to this row in parallel while the architect handles the integration step separately. **DONE** (tag `bastion-block-UI41` → `f6ac4c8bc7`, VOXCHECK-gated, no server/wire/harness change — pure voxygen render, self-verify+tag. Mirrors `bastion_sync_colonist_markers`'s exact per-entity `DebugShape` sync pattern, keyed on `bastion_selected`: a wide thin ground disc [Cylinder r0.7/h0.05] in warm gold `[1.0,0.85,0.3,0.85]` at the selected colonist's feet [+0.05], distinct from the cyan overhead marker. Tracks the colonist each frame; deselect/multi-select/vanish drops it; leaving overseer drains markers+rings together. Placeholder-first — visual feel confirmed by Sonnet, VISUAL-UNVERIFIED until Ben eyeballs it in the same combined rebuild as ARENA+Farm. Ben-checklist item routed via the architect to the Play-Tester.) | DONE | CHEAP | [UI Dialogue/Selection](UI-DIALOGUE-SELECTION-design.md) |
| 62.2 | UI-5 (UNIVERSAL DEBUG INSPECTOR) | **NEW STANDING REQUEST (Ben, via architect, 2026-07-14) — banked, NOT built yet, queued behind the current merge freeze + the 7 already-banked Ben findings (51.61-51.65 + item 1/2 above).** A genuinely valuable standing playtesting tool, treat with real design weight, not a quick hack — the natural growth of UI-4's own "growable GUI" charter (already noted in UI-DIALOGUE-SELECTION-design.md), not a fresh feature. Goal: every system-driven object in the world (a growing crop/farm plot, a tree mid-fell, a job/designation in progress, a stockpile + contents, a unit — anything with Bastion-tracked state) should be selectable and show its full internal state in a GUI panel. Scope: generalize UI-4's existing `BastionInspect`/`BastionInspectInfo` request/response wire (currently colonist-Uid-only) to accept a target that's EITHER an entity `Uid` OR a job/designation id, dispatch to the right probe-read per target kind (a farm plot's till/sow/growth-stage, a fell-set's remaining cells, a stockpile's contents list, a job's progress/claimant), same click→show/click-elsewhere→clear-or-switch panel pattern UI-4 already established. Reuse-heavy by design — the wire mechanism and the panel pattern both already exist, this widens WHAT can be a target, not how targeting works. **DONE** (tag `bastion-block-UI5` → `b5e4755336`, self-crafted in the UI-4 pattern per Sonnet's green-light — Sonnet-routine tier, no new dynamic mechanism, this widens WHAT a target can be, not how targeting works. `BastionInspect` wire generalized: `target: BastionInspectTarget::{Entity(Uid)|Cell(Vec3<i32>)}`, `payload: Option<BastionInspectKind> = Colonist(UI-4's payload verbatim, zero churn) | Job | Stockpile(contents — closes 51.64's legibility gap) | Farm | FellSet`. Server resolves a clicked cell XY-column-first [job→stockpile→farm→fell-set→None] in the existing post-join drain; an empty-handed click inspects, a colonist click still selects [UI-4's original behavior unchanged]. READ-ONLY end to end. New `--inspect-scenario` = ladder leg 38. Gate: full 38-leg ladder at HEAD `42f7c464a0` = 37/38 [all PASS incl. INSPECT + CK; BED red field-classified as the registered CASE-003 `bed_occupied_mid` signature, architect-accepted]; earlier block-level verification: inspect ×2 bit-identical, server/voxygen checks rc=0.) | DONE | MIXED | [UI Dialogue/Selection](UI-DIALOGUE-SELECTION-design.md) |
| 63 | OBJ-0..2 | Object live data, lore/history, and context actions. | TODO | CHEAP | [Object Inspection](UI-OBJECT-INSPECTION-design.md) |
| 63.1 | HEADLESS-RENDER (S1) | **Banked (Grok, via architect, 2026-07-14) — no action needed now, future Phase-3-capstone row.** ★ **LOWEST PRIORITY, EXPLICITLY LAST (Ben, 2026-07-14): ranks below Grok's entire text-view/interactive-play/recorded-playback/live-watch line (`TEST-INFRASTRUCTURE-AUDIT.md` §5 Phase 2 backlog) — must NOT compete for attention/resources against that work.** The DF-style text simulation is THE key testing-infra priority; this 3D graphical render item comes only after it. Grok's Phase-3 spike PROVED headless GPU rendering is viable on this machine (offscreen wgpu, drivers fine) but full live-proof screenshots are blocked on voxygen's renderer having no headless entry point — it assumes a real OS window. Real fleet-side engineering task, de-risked but not built: needs a new `Renderer::new_headless(extent, mode, runtime)` constructor + a parallel boot path reusing the existing `TakeScreenshot` capture. This is the Phase-3 capstone (S1) from `TEST-INFRASTRUCTURE-AUDIT.md` §5 — unlocks deterministic visual-regression (golden-image perceptual-diff) and finally answers the class of "does it actually render" question (CHOP-FELLING-VISUAL-CHECK, ARENA, Farm-palette, UI-4.1's ring) without needing Ben's eyeball every time. Full spike report: `bastion-bot/docs/PHASE3-HEADLESS-RENDER-SPIKE.md` in Grok's clone. | TODO | MIXED | [Test Infra Audit §5 Phase 3](TEST-INFRASTRUCTURE-AUDIT.md) |
| 64 | B10 | Save/load colony state, work, stock, and blueprints. | TODO | MIXED | [Build report §B10](veloren-colony-rts-build-report.md) |
| 65 | LOD-2 | Conservative abstract colony resolve while unwatched. | TODO | NEW | [LOD §LOD-2](LOD-PERSISTENCE-SPEC.md) |
| 66 | LOD-3 | Partial-load reconciliation and no-dupe/no-loss soak. | TODO | NEW | [LOD §LOD-3](LOD-PERSISTENCE-SPEC.md) |
| 67 | WORLDGEN-TUNE | Starter veins and cave/ore density tuning before embark. | TODO | CHEAP | [Caves/Ore Investigation](WORLDGEN-CAVES-ORE-density-investigation.md) |
| 68 | B11 | World survey, embark selection, and starting colony. | TODO | MIXED | [Founding §7](FOUNDING-EMBARK-DESIGN.md) |
| 69 | B12 | Embody/release through Veloren possession with one controller. | TODO | CHEAP | [Build report §B12](veloren-colony-rts-build-report.md) |
| 70 | PLAYER-MODES | Sovereign/Watcher/Incarnate faces over one control sink. | TODO | CHEAP | [Player Modes](PLAYER-MODES-design.md) |
| 71 | GOD-DOMAIN | Invoked domain with deed-derived moral face. | TODO | MIXED | [God Domain §§6–8](GOD-DOMAIN-design.md) |
| 72 | GH-E | Hand/control-spectrum transition into Embody. | TODO | CHEAP | [God Hand §GH-E](GOD-HAND-design.md) |
| 73 | B13-W2 / POWER-2 | Panic, curses, wards, and field blessing/blight. | TODO | MIXED | [Powers Wave 2](GOD-POWERS-CATALOG.md) |
| 74 | DIG-0..4 | Channel/ramp/stairwell/up-stair verbs over one safe decomposer. | TODO | MIXED | [DF Dig Verbs](DF-DIG-VERBS-design.md) |
| 75 | BUILD-0..3 | Construction spine, placement types, vertical build, and volume UI. | TODO | NEW | [Build Framework §BUILD-0..3](BUILD-FRAMEWORK-design.md) |
| 76 | BUILD-4..6 | Parameterized/composed structures and autonomous plans. | TODO | MIXED | [Build Framework §BUILD-4..6](BUILD-FRAMEWORK-design.md) |
| 77 | PROD-0 | Station-based Produce job over existing recipes. | TODO | MIXED | [Production §PROD-0](DF-PRODUCTION-design.md) |
| 78 | PROD-1 + POL-0 | One quota/order engine and stock census. | TODO | NEW | [Production §PROD-1](DF-PRODUCTION-design.md) |
| 79 | PROD-3 / COOK | Cooking, brewing, and seed-protection policy. | TODO | MIXED | [Production §PROD-3](DF-PRODUCTION-design.md) |
| 80 | QUAL-0..1 | Craft-quality stamp, value, and thought payoff. | TODO | MIXED | [DF Quality](DF-QUALITY-design.md) |
| 81 | TOOL-1..2 | Tool material tiers, hard-material gates, quality, auto-equip. | TODO | MIXED | [Tools Upgrade](TOOLS-UPGRADE-design.md) |
| 82 | PROD-4..5 | Economy legibility, aggregate LOD, and animations. | TODO | MIXED | [Production §PROD-4/5](DF-PRODUCTION-design.md) |
| 83 | QUAL-2..3 / ARTIFACT | Strange mood, artifact, and faithful failure tragedy. | TODO | NEW | [DF Quality §QUAL-2/3](DF-QUALITY-design.md) |
| 84 | ROT-0..3 | Conserved freshness/rot, miasma, hygiene, and vermin. | TODO | NEW | [DF Rot](DF-ROT-design.md) |
| 85 | ZONE-1-REST | Finish Refuse behavior; Gather already proves its wire. | TODO | CHEAP | [DF Zones §ZONE-1](DF-ZONES-design.md) |
| 86 | POL-1..4 | Standing rules, policy panel, LOD, and rule adapters. | TODO | MIXED | [DF Policy](DF-POLICY-design.md) |
| 87 | BURROW-0..3 | Hard restriction, survival escape, and shelter alert. | TODO | MIXED | [DF Burrow](DF-BURROW-design.md) |
| 88 | ROOM-0..3 | Enclosure, room role/value, ownership, and mind payoff. | TODO | NEW | [DF Rooms](DF-ROOMS-design.md) |
| 89 | FOCUS-1..3 | Personal-need jobs and focus-driven performance. | TODO | NEW | [DF Focus](DF-FOCUS-design.md) |
| 90 | REL-0..3 | Temples, worship need, priests, and devotion. | TODO | MIXED | [DF Religion](DF-RELIGION-design.md) |
| 91 | DF-TAVERN | Shared recreation/congregation venue loop. | TODO | SPEC | [DF Tavern stub](DF-TAVERN-design.md) |
| 92 | FEST-0..2 | Scheduled/event-driven festivals and feasts. | TODO | CHEAP | [DF Festival](DF-FESTIVAL-design.md) |
| 93 | STOCK-0..3 | Pasture, products, breeding, and bounded herd population. | TODO | MIXED | [DF Livestock](DF-LIVESTOCK-design.md) |
| 94 | TRADE-0..6 | Colony economy node, depot, caravan, and autonomous trade. | TODO | MIXED | [DF Trade](DF-TRADE-design.md) |
| 95 | MIG-0..3 | Prestige migration/emigration plus paired threat pressure. | TODO | MIXED | [DF Migration](DF-MIGRATION-design.md) |
| 96 | JUST-0..3 | Hierarchy, mandates, crime/justice, and divine reach-ins. | TODO | MIXED | [DF Justice](DF-JUSTICE-design.md) |
| 97 | B-AG6 | Settlement growth, reproduction, and genealogy. | TODO | NEW | [Build report §B-AG6](veloren-colony-rts-build-report.md) |
| 98 | PATH-1..3 | Hierarchical paths, shared flow fields, and repair. | TODO | NEW | [Pathfinding §PATH-1..3](PATHFINDING-SCALE-SPEC.md) |
| 99 | CAVERN-0..4 / GEO-1 | Danger tiers, prospecting, ore veins, breach, and deep life. | TODO | MIXED | [Cavern/Geology](DF-CAVERN-GEOLOGY-design.md) |
| 100 | LIGHT-0..3 | Carried/placed light and bounded gameplay darkness. | TODO | MIXED | [Underground Lighting](UNDERGROUND-LIGHTING-design.md) |
| 101 | UNDERGROUND-UX | Camera targets, atmosphere, and cursor-hand light. | TODO | CHEAP | [Underground Experience](UNDERGROUND-EXPERIENCE-design.md) |
| 102 | STRUCT-1 | Mine supports as cave-in prevention. | TODO | CHEAP | [DF Struct](DF-STRUCT-design.md) |
| 103 | MECH-0..3 | Trigger→link→effect for traps and operable terrain. | TODO | NEW | [DF Mech](DF-MECH-design.md) |
| 104 | POW-0..1 | Mechanical power graph and powered machines. | TODO | NEW | [DF Power](DF-POWER-design.md) |
| 105 | FLUID-0..3 | Bounded experimental flow with static fallback. | TODO | NEW | [DF Fluid](DF-FLUID-design.md) |
| 106 | TEMP-0..2 | Felt climate, winter answers, and biome effects. | TODO | MIXED | [Temperature/Biome](DF-TEMP-BIOME-FX-design.md) |
| 107 | SYN-0..3 | Data-driven syndromes and bounded epidemiology. | TODO | MIXED | [DF Syndrome](DF-SYNDROME-design.md) |
| 108 | DF-WOUND / MEDICAL | Persistent injury, infection, healer jobs, and recovery. | TODO | SPEC | [DF Wound stub](DF-WOUND-design.md) |
| 109 | DF-MILITARY / RANGED | Policy squads, equipment, training, patrols, and ranged defense. | TODO | SPEC | [Gap ledger §G](df-feature-gap-ledger.md) |
| 110 | NIGHT-0..2 | Systemic night pressure, light wards, and shelter/defense response. | TODO | MIXED | [DF Night](DF-NIGHT-design.md) |
| 111 | VILLAIN-0..2 | Persistent named nemeses with bounded escalation and closure. | TODO | MIXED | [DF Villain](DF-VILLAIN-design.md) |
| 112 | MISS-0..2 | Off-map missions resolved through rtsim quests. | TODO | MIXED | [DF Mission](DF-MISSION-design.md) |
| 113 | REP-0..2 | Chronicle deeds become live decaying social reputation. | TODO | CHEAP | [Reputation](REPUTATION-design.md) |
| 114 | AGENT-CULTURE | Race/culture-keyed mind, language, history, and relations. | TODO | MIXED | [Agent Culture](AGENT-CULTURE-CHARACTERIZATION-design.md) |
| 115 | HIST-3..6 | Legends browser, world retention, attribution, and epochs. | TODO | MIXED | [DF Hist](DF-HIST-design.md) |
| 116 | ART-0..2 | Art depicts real history and affects minds/rooms/culture. | TODO | MIXED | [DF Art](DF-ART-design.md) |
| 117 | KNOW-0..2 | Discover, teach, record, and potentially lose techniques. | TODO | NEW | [DF Knowledge](DF-KNOWLEDGE-design.md) |
| 118 | HALLOW-0..2 | Significant places become sacred or haunted. | TODO | MIXED | [Sacred Sites](SACRED-SITES-design.md) |
| 119 | EPITHET-0..2 | Colony names its god from recorded deeds and alignment. | TODO | CHEAP | [God Epithet](GOD-EPITHET-design.md) |
| 120 | OMEN-0..2 | Signs receive faith/culture/epithet interpretation. | TODO | MIXED | [DF Omen](DF-OMEN-design.md) |
| 121 | CHAMP-0..2 | Anointed champion with sainthood or fall. | TODO | MIXED | [Divine Champion](DIVINE-CHAMPION-design.md) |
| 122 | DEAD-0..2 | Ancestors and honored/restless dead. | TODO | MIXED | [DF Ancestors](DF-ANCESTORS-design.md) |
| 123 | CURSE-0..2 | Liftable divine syndromes and geasa. | TODO | MIXED | [DF Curse](DF-CURSE-design.md) |
| 124 | BEAST-0..2 | Persistent legendary megafauna and great hunts. | TODO | MIXED | [DF Beast](DF-BEAST-design.md) |
| 125 | RENOWN-0..2 | Derived colony legend/byname and bounded world response. | TODO | CHEAP | [Collective Renown](COLLECTIVE-RENOWN-design.md) |
| 126 | RECLAIM-0..2 | Fallen colonies persist as storied reclaimable ruins. | TODO | MIXED | [DF Reclaim](DF-RECLAIM-design.md) |
| 127 | GH-F | Remembering-colony hand enrichment. | TODO | MIXED | [God Hand §GH-F](GOD-HAND-design.md) |
| 128 | B13-W3 / POWER-3 / REL-4 | Late engines plug into the full god-power catalog. | TODO | MIXED | [Powers Wave 3](GOD-POWERS-CATALOG.md) |
| 129 | DP1..DP5 | Faction interests, faith politics, and competing gods. | TODO | NEW | [Divine Politics §6](divine-politics-bible.md) |
| 130 | NAVAL-0..3 | Sailing AI, vessels, harbors, and abstract trade routes. | TODO | MIXED | [Ships/Naval](SHIPS-NAVAL-design.md) |
| 131 | DF-PUMP / MAGMA / HYDRO | Fluid machinery, lava, aquifer, and flood engineering. | TODO | SPEC | [Fluid §FLUID-2/3](DF-FLUID-design.md) |
| 132 | DF-ECON / GUILD | Deep wealth/property and profession guilds. | TODO | SPEC | [Gap ledger §D](df-feature-gap-ledger.md) |
| 133 | DF-MINECART | Advanced hauling routes, carts, and rollers. | TODO | SPEC | [Gap ledger §E](df-feature-gap-ledger.md) |
| 134 | DF-PREF | Individual likes/dislikes feeding mind and culture. | TODO | SPEC | [Gap ledger §B](df-feature-gap-ledger.md) |
| 135 | DF-BOOKS / DF-NOTES | Accounting accuracy, annotations, and patrol routes. | TODO | SPEC | [Gap ledger §K](df-feature-gap-ledger.md) |
| 136 | AUTON-FULL-SOAK | Full founded colony survives, grows, acts, and remembers with zero input. | TODO | NEW | [Autonomous Colony §6 AUTO-3](AUTONOMOUS-COLONY-OPERATION-design.md) |

## Approved ordering notes

- B6’s tagged soft-collision work and its unbuilt hauling objective are explicitly split.
- NIGHTHORROR proves creature integration; systemic DF-NIGHT remains later.
- `LOD-0/1 → B6-HAUL → B-AG5 → ZONE-0 → GATHER` closes integrity and physical food acquisition before autonomy.
- `B7 → FARM → AUTON-0..3` supplies demand and renewable food before shortage-recovery claims.
- HIST-0 and FOCUS-0 lock shared schemas before downstream emitters/consumers fork them.
- PROD-1 and POL-0 co-own one order evaluator and stock census.
- GH-A is independent; GH-B onward follows the B13 cast/favor substrate; GH-E waits for B12; GH-F waits for depth.
- SPEC rows retain their approved position but receive a builder-complete JIT design only when reached.

**Approved Gather-vs-AUTON call:** acquire food first, make it renewable second, then claim autonomy.

## Design backlog (SEED — **TO BE DESIGNED**, not build-ready)

These are **concept seeds from Ben**, NOT specced blocks. Each needs a full **designer pass** (prior-art-first,
references in the doc) BEFORE it earns numbered build rows above. Status = **DESIGN-PENDING**. Do not supply to a
builder. Sequence the designer passes post-AUTON, when the noted systems deepen.

| Seed | What it is | Status | Design doc (references inside) | Reconcile with |
|------|-----------|--------|-------------------------------|----------------|
| IDLE-HOME-LEASH | Implicit home-radius soft-magnet / auto-zone around the first stockpile as the DEFAULT idle bound (painted zones override). Prior-art-friendly — DF/RimWorld both leash idle colonists to home. **Found live (AUTON-2 tag, 2026-07-13):** idle colonists wander unbounded and can drift 80+ blocks into raw worldgen; a later shortage-phase preempt-to-eat then releases as unreachable — starving amid plenty while stock sits untouched at the stockpile. AUTON-2's own scenario fixed itself with a painted ZONE-0 Meeting magnet (a fixture fix), but the underlying gap is real for any live colony. | DESIGN-PENDING | AUTONOMY-ARBITRATION-SPEC.md (AUTON-2 finding) | ZONE-0 (Meeting magnet, the reuse candidate), AUTON-2 (need-preempt is the trigger), B7-3 (EatFrom unreachable-release) |
| FOREST-ECONOMY | Renewable forest economy: trees drop seeds → ambient regrowth + collection; a **forestry/planting job** (reuses FARM plot+growth); **tree types incl. fruit/nut** (new food); **leaves → compost/forage/bedding** (was waste). | DESIGN-PENDING | [Forest Economy](FOREST-ECONOMY-design.md) | FARM (46), CHOP/CHOP-FELLING (FR10 + 51.6), GATHER, AUTON-2 |
| BODIES-DEATH-RITES | Corpse lifecycle: removal/haul, **disposal & funeral types** (bury/pyre/dump/leave, culture-chosen), **decomposition/rot stages** (the timer driving disease+mood+urgency) + **preservation** (freeze-power/spell, cold-storage "medieval fridges" — shared food+corpse mechanic), **grieving**, **cannibalism** (famine food + taboo), **disease/miasma** from bodies, **mood** from corpses/graves, **body-type** (colonist/other-colony/animal/monster) driving each reaction. | DESIGN-PENDING | [Bodies & Death Rites](BODIES-DEATH-RITES-design.md) | B7-1 death gate, HAUL, **122 DEAD** (DF-ANCESTORS), 107 SYN (syndromes), 106 TEMP (rot rate), mind/mood, EMERGENT-AGENCY (funeral-by-faith, cannibal-taboo), AUTON-2 + FOREST (famine cannibalism) |
| WEATHER-EFFECTS | Dynamic weather (rain/snow/storm/wind/fog/heat) as systemic colony pressure: effects on **work, mood, crops, decomposition, disease, movement, fire/hazard, shelter/visibility**; **snow accumulation + rain/flooding** as a later voxel-accumulation tier. Reuses vanilla Veloren's weather sim; the FAST event layer atop TEMP's slow climate. | DESIGN-PENDING | [Weather Effects](WEATHER-EFFECTS-design.md) | vanilla weather sim, **106 TEMP** (likely one env-FX pass), SEASON (done), FARM (46), BODIES (rot), 107 SYN, PATH-0 (movement), 58 Weather power, 110 NIGHT/shelter |
| EMERGENT-AGENCY | Agency at every scale: god-quest → avatar → NPC → **rival gods** → **colony-as-character**; **possession** (incl. against-nature); **laws/crime/punishment/prison/favor**; **theology types** (mono/poly/atheist/deist/animist, emergent); **political systems** (democracy/monarchy/theocracy). | DESIGN-PENDING | [Emergent Agency Layers](EMERGENT-AGENCY-LAYERS-design.md) | 129 DP (divine politics), 112 MISS (quests), 118–125 faith/omen/champion/renown cluster — UNIFY, don't duplicate |

**Rule:** a design-backlog seed only becomes a numbered build row (with a build-ready design + gate class) after
its designer pass completes and the architect schedules it. Until then it stays here as design-pending.

### Near-term dev tools (small, buildable — not epics)

- **FLAT-TEST-ARENA** (Ben, 2026-07-13) — a **chooseable flat, empty sandbox** to spawn colonists into for clean
  observation/testing (flat land, nothing around — no town noise), selectable *alongside* the normal world so Ben
  can test BOTH. SMALL dev tool (a launch option / debug spawn + a flat terrain patch + N colonists), NOT the full
  Founding/Embark (B11) — complements it. High value: the cleanest way to eyeball autonomy behavior + doubles as a
  determinism-backed scale testbed (50-agent digs, etc.). Slot as a small near-term block post-AUTON. Distinct
  from B11 (full in-game site-selection, downstream).

| 51.7 | REENGAGE-BOUND-UNIT | Follow-on from BACKSTOP-OPT (architect-ruled at the N1C race finding): extract the emergency-route RELEASE DECISION (three outcomes: verified-exit / exhausted-replan / keep-driving, bastion_jobs.rs) + the EMERGENCY_REENGAGE_BOUND counter into a pure testable function and UNIT-TEST the exhaustion leg (5 consecutive exhausted-replans → net-bound release) — the leg is live on tape (counter=1) but race-dominated in sims (three independent net terminators, fastest wins; the positional watch always beats outcome-cycling). Behavior-preserving refactor AFTER the BACKSTOP-OPT tag (don't churn the just-green path); verify with fixture N1B/N1C/N7 + stuckjob corpus before/after. Chip spawned. | TODO | CHEAP | (BACKSTOP-OPT follow-on; standing pin-bounded-logic-as-unit-test rule) |
