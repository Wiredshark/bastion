# BASTION COMMON ISSUES — the SHARED game-side registry + pre-flight checklist

**Ben's mandate (2026-07-10):** keep ONE common list of the issues that recur — **bugs, design issues,
performance issues, gameplay-loop issues** — plus the **common questions that should always be asked** — curated
by the **Build-Reviewer** and contributed to by **every agent**. This is the game-code/design counterpart to the
asset team's `ASSET_COMMON_MISTAKES.md` (which stays the authority for asset/voxel issues). A living checklist so
the same class of problem stops recurring.

## Protocol (all agents)
- **CONSULT before/while working** — builder pre-implementation + at gate; designer while spec'ing; reviewer as
  the code-review AND feasibility checklist; play-tester while driving.
- **APPEND when a new class appears or an old one recurs** — with HOW TO DETECT it and the FIX/RULE. Every real
  bug Ben, the play-tester, or the reviewer finds should leave a class here so it can't hide next time.
- **REVIEWER curates** (dedupes, keeps it scannable, promotes recurring one-offs into classes). Architect folds
  in Ben's findings + cross-agent patterns.
- Companion: `ASSET_COMMON_MISTAKES.md` (assets), `BASTION_ARCHITECTURE.md` (system map), `BUILD_REVIEW_LOG.md`
  (per-review findings), `RESEARCH-IMPROVEMENT-LOG.md` (method upgrades).

---
## A. BUGS (correctness) — seeded from real finds
| B30 | **CRITICAL, HIGH-SEVERITY — the harness gate does NOT cover the `voxygen` client crate, so an exhaustive wire-enum append can break the CLIENT BUILD while sailing through a full 26-leg gate** (FARM/PROD-2 tag find, 2026-07-13: `DesignationKind::Bed` was added in B7-1 [`bastion-block-B71`] but never added to `voxygen/src/bastion/tools.rs:105`'s exhaustive `zone_rgb` match — `cargo check -p veloren-voxygen` has failed with E0004 [non-exhaustive match] on every tag SINCE, six tags running: B71/B72/B73/BAG3V/FOCUS0DERIVE/PATH0. The harness gate only builds `common`+`server`+the harness — it has zero visibility into whether the CLIENT even compiles. Any play-tester/Ben voxygen build attempted off any of those six tags CANNOT have succeeded; if a "live-tested" exe exists from that window, it predates B71) | after ANY append to a wire-shared enum consumed by an exhaustive match in `voxygen` (`DesignationKind`, `JobKind`, `ChronicleKind` if voxygen ever matches it, etc.): does `cargo check -p veloren-voxygen` actually pass? The server/common gate passing is NOT sufficient evidence — voxygen is a separate crate the gate never touches | **RESOLVED (architect ruling, 2026-07-13): option (a) — `cargo check -p veloren-voxygen` is now a PERMANENT standing gate leg** (documented in `BASTION_PIPELINE.md`, leg 3 of the ladder, right after the BUILD integrity leg for fail-fast placement — confirmed wired before FARM tagged). Discipline-only (b) was explicitly rejected given the six-in-a-row failure before this was caught. **Fixed and confirmed green in `bastion-block-FARM` (`682211eac9`)** — the first client-buildable tag since B7-1, VOXCHECK ran and passed as part of FARM's own 28/28 gate. Play-Tester lane + Ben notified directly by the architect re: any stale pre-B71 exe |
| # | Class | How to detect | Fix / rule |
|---|---|---|---|
| B1 | **Off-by-one in a reach/boundary predicate** (annulus "standable = rise ≤ reach" was really ≤ reach−1 — entombed a novice for WEEKS as a "flake") | boundary unit test on the PURE function at exactly reach==rise; scenario flakes that come/go | Pin every ±1 predicate with a direct `#[cfg(test)]` boundary test; a pure function must not be tested only through a flaky scenario |
| B2 | **Guard short-circuits BEFORE the real check** (churn "anchored" guard used pure PROXIMITY ≤8, skipped egress_scan → colonist walled off from a near-but-unreachable anchor never rescued) | ask "does this guard run the authoritative check, or a cheap proxy that can lie?" | Run the authoritative check (egress_scan/reachability) before trusting a proximity/count proxy |
| B3 | **One stuck item freezes a global one-at-a-time economy** (a single unreachable is_access job froze colony-wide rescue; unreachable jobs never pruned) | any global `any(...)`/one-plan gate + no staleness prune; two simultaneous instances compound | Staleness timeout (abandon+clear if no progress in N) or region-scope the gate; never let one failure wedge the whole economy |
| B4 | **Admin/debug-gated command inert in singleplayer** (time controls wired to admin `/time_scale` → buttons did nothing) | test the feature as a NON-admin singleplayer player, not just via the console | Wire player-facing verbs to the real setter/resource, not an admin command path |
| B5 | **Sprite-adjacency violation → silent one-tick vacate** (placed sprite vanishes; engine runs an adjacency sweep every terrain change) | consult `common/src/terrain/sprite/mod.rs::adjacency_requirement`; runtime persistence assert | Respect each sprite's attachment rule (standing needs solid below, wall/ceiling variants, ±z) — also fires on LATER mining near the sprite |
| B6 | **A*-walkable but not capsule-passable** (2-high opening → colonist wedges in window) | pathing succeeds but the agent physically stalls at the gap | Openings must fit the colonist capsule (height + width), not just the path graph; bar 2-high slits |
| B7 | **Serde-default / wire / save-compat break on a new field** | load an OLD save / cross-version; missing `#[serde(default)]` | New fields serde-default + note the wire/save caveat in the ledger; ship the pair |
| B8 | **Determinism break** (timing-dependent assert; A* reset-on-move; RNG order) | run the gate on a QUIET machine + repeat; flake under load | Assert on deterministic state, not wall-clock; seed + fix iteration order |
| B9 | **Panic/unwrap/overflow path** on unexpected state | grep unwrap/expect/indexing on runtime-derived data | Handle the None/err/empty + bounds; a colony sim runs millions of ticks — rare = certain |
| B17 | **A "proven" invariant tested on a PARALLEL code path or UNREPRESENTATIVE geometry → the real shipping path is unverified** (the cave-in "entombment invariant GREEN" scenario drove `bastion_force_collapse_check` — a ~90-line COPY of the collapse+eject orchestration in lib.rs — NOT the shipping `Sys::run` eject loop, AND on a SHALLOW above-ground pad where the deep-mine window-top burial [R8 F-CAVE-1] is STRUCTURALLY UNREACHABLE; so "proven" meant "proven for a shallow parallel copy," while the real deep-mine path entombed) | does the test call the SAME fn/system production runs, or a duplicate? is the geometry REPRESENTATIVE of the real use (deep mine, natural slope — not a flattened/shallow fixture that terraforms the failure away)? can the test geometry even REACH the failure branch? | Assert on the ACTUAL shipping path (the real `Sys::run`/production entry, e.g. place+complete a Mine), with geometry that structurally reaches the failure (deep-mine floor >120 below surface, natural slope). BEST remedy (R8b, cave-in): DELETE the copy — extract ONE shared fn both the harness and production call, so identity is by CONSTRUCTION not assertion (`cavein_eject_and_injure` is now the single impl for Sys::run + `force_collapse_check`). THEN verify the shared fn's INPUTS match across callers — a fn reading only caller-invariant state (e.g. terrain OUTSIDE the mutated set: `eject_dest` skips `crush_xy`) is timing/caller-agnostic, so an immediate-`set_block` harness and a deferred-`BlockChange` Sys::run PROVABLY agree. If a copy MUST persist, assert it stays behaviorally IDENTICAL to production. Sibling of B15 (natural-slope-not-flattened) + the F4a Testing question (mechanism-not-motivator) |
| B18 | **Server/HEADLESS gate GREEN ≠ CLIENT (voxygen) figure-render VERIFIED** — a new figure/body/species/`.ron`-manifest can pass the common+harness gate (120/120) while its client render path is NEVER exercised. The headless harness is explicitly "no voxygen, no GPU, no network clients" (`bastion-harness/src/main.rs:4`), so it never calls `FigureModelCache::new`'s `<Skel::Body as BodySpec>::load_spec().unwrap()` (`voxygen/src/scene/figure/cache.rs`) — the RUNTIME `.ron`-manifest→enum deserialize. The COMPILE checks the exhaustive Rust match; it does NOT run the deserialize, so a `.ron` manifest that disagrees with the `#[repr(u32)]` biped_large `Species` enum panics ONLY client-side. PROOF (live): the night_horror interim exe panicked at `cache.rs:325` `NoSuchEnumVariant "NightHorror"` loading `biped_large_central_manifest` — a version-mismatch artifact here, but it exercises the EXACT path the headless gate skips. **RESOLVED for night_horror (2026-07-12):** Ben's canonical `/spawn enemy night_horror` smoke on the merged NIGHTHORROR build RENDERS CLEAN (correct model, active combat, no client panic) — manifest ↔ `#[repr(u32)]` `Species::NightHorror` agree at RUNTIME, confirming the interim panic was purely the version-mismatch artifact. The RULE STANDS for every FUTURE new figure/body/species (each still owes this launch+spawn-render smoke in its done-when) | for any new figure/body/species/`.ron`-manifest change: does the done-when include a VOXYGEN launch + `/spawn`-render smoke, or ONLY a server/headless gate? a green common/harness run does NOT touch `load_spec()` — ask "do the `.ron` manifest and the `Species` enum agree at RUNTIME, not just compile?" | Any new figure/body/species block needs a voxygen-launch + `/spawn`-render smoke in its done-when (play-tester's canonical `/spawn`-render is the current stand-in). Also consider bubbling the `load_spec()` error instead of `.unwrap()` (the code's OWN NOTE at `cache.rs`: "might be better to bubble this error up rather than panicking") so a bad manifest degrades gracefully, not a hard client panic. Sibling of B17 (gate-green ≠ shipping-path-verified): B17 = tested a PARALLEL code path / unrepresentative geometry; B18 = tested the WRONG PROCESS (headless server ≠ client render) |
| B19 | **An EXTERNAL writer places a colonist pos INSIDE solid terrain (no air-check at the dest) → `box_voxel_collision` LOCKS it there (SAFETY / entombment invariant break)** — ★ CORRECTED 2026-07-12: the CASE-003 seed-21 DETERMINISTIC repro REFUTED my original soft-collision-pinch framing (0 pair-trips in 24 seeds, victim 84 blocks from any other colonist). REAL mechanism (R10): the `surface_teleport_dest` ULTIMATE fail-safe returned `s+1` INSIDE a tree trunk — `column_surface_z` scans via `is_surface_terrain` which EXCLUDES Wood/Leaves (`bastion_jobs.rs:341`, "canopies never read as the surface"), so it sees THROUGH the trunk to the ground UNDER it and `surface+1` lands in the Wood — with NO air-check. `box_voxel_collision` then can't separate (16 attempts) and its exhaustion path **REVERTS to the tick-start pos** (`vel=0; pos.0=old_pos`, `collision.rs:275`) — so a pos that STARTED the tick embedded is REVERTED-TO and locked (NOT "exits still penetrating" — my earlier claim was WRONG). Corollary: a pos that started VALID is reverted to that valid pos, so a mid-tick soft-collision/pinch overlap CANNOT embed — the resolver reverting is why the soft-collision pinch was a red herring | for ANY code that WRITES a colonist pos (teleport, relocate, fail-safe, spawn, cave-in eject): does it AIR-CHECK the dest — feet AND head cells LOADED + not-filled (`terrain.get(c).is_ok_and(!is_filled)`, errored read rejects)? A surface-scan helper that sees THROUGH non-surface solids (Wood/Leaves — `is_surface_terrain` excludes them) returns the ground UNDER a tree, so `surface+1` is inside the trunk. Remember `box_voxel_collision` does NOT fix an externally-written embed — it REVERTS to (LOCKS) the tick-start pos on exhaustion; the resolver is NOT a safety net | (a) AIR-CHECK every pos-write dest: feet+head loaded-air, errored-read rejects (never write into unloaded/solid). (b) PER-TICK center-safety-net (the general belt): a colonist whose torso-center cell is filled after integration → relocate via the ONE shared `eject_dest` (`common::bastion`, true-standable `!solid(c)&&!solid(c+ẑ)&&solid(c−ẑ)`); None→leave in place; own-write + terrain-read only = rayon-deterministic — guarantees the invariant regardless of the WRITER ("entombment impossible by construction"). Both landed in the CASE-003 fix (R10). Sibling of B17 (one shared `eject_dest`) + the `is_filled()`-gotcha (canopies aren't surface) |
| B20 | **A single-tick detector/belt/assert fire ≠ a PERSISTENT bug — gate on N-consecutive-tick persistence before naming a writer** (the CASE-004 misattribution: chokepoint `CENTER_NET_FIRES` on seeds 7/8/1337 read as a persistent ladder-magnet embed + "SETTLED" — but they were **dig-pocket TRANSIENTS** [diggers settling into a fresh 1-deep pocket / boundary straddle, core-in-solid a few ticks then recovering] that the OLD **single-tick** belt over-fired on). The discriminator is B19's revert-lock: a REAL external-write embed is `box_voxel` revert-LOCKED (persistent) → it STILL fires under a persistence-gated detector; a transient recovers and does NOT | before attributing a detector/net/assert trip to a specific writer/bug: does the SAME entity stay tripped across N CONSECUTIVE ticks, or recover on its own? A single-tick fire CANNOT distinguish persistent-bug from transient-over-fire. **Proof pattern:** re-gate the detector on N-consecutive-tick persistence (belt-v2: relocate only after 30 consecutive ticks core-in-solid) → if `net=0`, they were transients (CASE-004: net 0/0/0 confirmed transient); if it still fires, it's a real writer bug. Note zero-delta / 1-block-headroom / fractional-XY signatures match the TRANSIENT class too — a correct MEASUREMENT can carry a wrong ATTRIBUTION | Gate the detector/net on N-consecutive-tick persistence so it fires only on real (revert-locked) embeds, not transients. In FORENSICS: run the PERSIST-vs-RECOVER test (Opus's CASE-003 discriminator) BEFORE naming a cause; separate the (correct) DATA from the (premature) attribution. Pairs with B19 (revert-lock ⇒ persistent) and the box_voxel-reverts fact |
| B16 | **Signed float → unsigned `Duration` (`from_secs_f*` on an unbounded-below value)** — the panic archetype: a dt/delta/countdown/nudge that CAN go negative is fed straight to `Duration::from_secs_f32/f64`, which panics `"cannot convert float seconds to Duration: value is negative"`. Real crash: `Clock::game_dt`'s `last_game_dt = (average_dt + (real−game)·NUDGE_RATE).min(MAX_GAME_DT)` has an UPPER clamp only; after alt-tab the game clock overshoots real time and, at high fps / a fast-frame burst, the negative nudge exceeds `average_dt` → negative dt → panic (BUG-LOG CASE-001; **VANILLA/upstream**, not ours) | grep `from_secs_f32`/`from_secs_f64` (and `Duration::mul_f*`) and ask: can the arg go negative? trace every subtraction / `(a−b)` / countdown / clock-delta feeding it — is there a clamp on BOTH sides before the conversion? `from_secs_f*` panics on the FULL set {negative, NaN, ±inf, OVERFLOW (a too-big FINITE value, >~1.8e19)} — not just negative. ANY single-sided guard is the red flag: the clock case guarded only the UPPER bound (missed negative); the `/aura` fix (ARCH-001) guarded only the LOWER+finite (`!is_finite()||<0.0`, missed OVERFLOW → `from_secs_f32(1e20)` still panics). | Floor at the SOURCE: `.clamp(0.0, hi)` not `.min(hi)` (BOTH bounds — a lone `.max(0.0)` still overflows), so the stored value AND every reader are safe; or guard the reader `Duration::try_from_secs_f64(x).unwrap_or(Duration::ZERO)`. If REJECTING malformed input (not silently zeroing), key the reject off `try_from_secs_f*(x).is_err()` — it matches the panic set EXACTLY; do NOT hand-roll `!is_finite()||<0.0` (misses overflow — ARCH-001, empirically confirmed). Prefer the source clamp (protects internal accumulation too). Specialization of B9; the charter's named proactive-hunt target — sweep the tree for it |
| B15 | **Claimability/exposure gate admits UNSTANDABLE work** (Mine exposure = "any non-filled neighbor" but NOT "a colonist can STAND adjacent" → floating blocks + hillside `+1`-arrival-gap cells get claimed, never workable → cycle claim→unreachable; slope-mining "gives up" with blocks left, historically 8/200) | count completed vs `unreachable`-marked vs claimed-never-worked on a NATURAL slope (not a flattened fixture — fixtures terraform this away, so gates miss it); floating/arrival-gap remnants = unreachable cluster | Gate claimability on STANDABILITY (a reachable standable adjacent cell), not just a non-filled neighbor; for produced floaters, mine-in-place-from-adjacent or collapse/fall. Sibling of B6 (A*-walkable ≠ capsule-passable). Natural-slope geometry must be in the test matrix, not flattened away |
| B14 | **Reset-prone accumulator — the reset races/starves the threshold** (an accumulator that gates an action at threshold T is zeroed by SUB-threshold events, so it never reaches T and the action never fires: `stuck_time` zeroed by sub-block jitter → the watchdog never fires and every net below it goes blind [R3, confirmed THE root cause of the 20+-iter hover]; `churn.1` reset to 0 at cycle 8 → the teleport at cycle 16 is UNREACHABLE dead code [R5-F5]) | for any accumulator gating an action at a threshold: what RESETS it, and can the reset fire faster than it accumulates below T (or between a lower and a higher threshold)? trace the sawtooth | Reset ONLY on genuine progress/success (hysteresis: `stuck_time` zeros on ≥1 block NET progress, not any wiggle; a lower-threshold action resets the counter ONLY when it actually dispatched, so a higher threshold stays reachable). A dead net that comments present as active is worse than none — it reads as covered |
| B13 | **Implicit loaded-chunk assumption broken by a top-down/remote trigger** (an edit/op that's safe when a PRESENT player triggers it — player presence guarantees the target chunk is resident — silently drops when a top-down god-place / remote command / LOD op targets an UNLOADED chunk: `BlockChange::try_set` on a non-resident chunk is discarded on apply, no error) | who triggers this — a present actor (chunk loaded) or a top-down/remote one (chunk maybe unloaded)? does the handler verify the target chunk is loaded? | Any top-down/remote/god op on world state must verify the target chunk is LOADED (or load-then-edit), not inherit the native path's implicit "the player is standing there" locality. Ties the LOD seam (only loaded regions are editable/pathable; unloaded → the abstract tier) |
| B12 | **Real-time-anchored rate silently coupled to the timescale/day-length knob; TimeOfDay↔DeltaTime desync under the frame clamp** (a `dt.0`-based rate like `WORK_DURATION_BASE` is anchored to REAL seconds, so tuning `day_length` silently retunes "per game-day"; and `MAX_DELTA_TIME` clamps DeltaTime but NOT TimeOfDay, so at high TimeScale + low fps the calendar leads the sim → day-anchored vs dt-anchored rates diverge) | grep `dt.0 *`/`* dt`/`_SECS`/`_BASE` for rates; ask "is this SIM-PACING (per game-day) or ENGINE-timer (responsiveness)?"; does it stay put when `day_length` changes? | SIM-PACING rates: derive the dt-constant from a game-time spec × `day_cycle_coefficient` at load (stays physics-lockstep AND day-length-invariant) — do NOT rekey the mechanism to TimeOfDay-delta (it then leads physics under the clamp). ENGINE/watchdog timers (STUCK_TIMEOUT/grace): leave dt-based — they already scale with TimeScale and must not be day-length-anchored. Both clocks already ×time_scale (state.rs:884/892) |
| B11 | **Partial save-back loses loaded-tier mutations at the LOD seam** (the classic colony-sim persistence death: state mutated while an entity is Loaded is never written to its persistent home before unload/save — the rtsim sync writes only `npc.wpos`, so skill-XP/soft_until gained while loaded is lost on unload/save; promote restores from the now-stale record) | for EVERY field mutated while loaded: is there a save-back path to the persistent home before demote/save? unload→re-promote (or save→reload) and diff the state | Every loaded-tier mutation needs a save-back to the authoritative persistent record; drive it off change-tracking (FlaggedStorage) so it's cheap+complete, and flush before-save + on-demote. Conservation across the seam is the hard invariant (no dupe/loss) — distinct from B7 (that's serde/wire-compat; this is the sync being incomplete) |
| B10 | **Shared mutable state added under a PARALLEL join → silent determinism break** (a determinism that survives today ONLY because per-agent work is independent — e.g. the agent system's `.par_join()` at `server/src/sys/agent/mod.rs:76`; a future shared PathBudget/cache/counter the agents COMPETE for makes the outcome order-dependent under a nondeterministic join → two runs diverge) | ask "is new shared read/written INSIDE a par_join? was the parallelism safe only because entities were independent?" | Keep parallel work independent; if agents must share a budget/resource, allocate it order-INDEPENDENTLY — a sequential deterministic pre-pass (entity-id order) or lift the contended work OUT of the par_join into a sequential scheduler. Distinct from B8 (that's timing/RNG/assert-order; this is shared-state-under-parallelism) |
| B22 | **COMPLETION-WITHIN-WINDOW / position-timing-sensitive telemetry probes flake on scheduling-seam draws — a same-binary rerun, not a rebuild, is the classification test** (THREE instances filed — but instance (1) was MISFILED and is now RE-CLASSIFIED, see correction at end of this row: (1) ~~CK `ck_failsafe_out` seam-flip [`bastion-block-BAG5CORE`]~~ **CORRECTED 2026-07-17: NOT B22-eligible and never was** — `ck_failsafe_out` is an INVARIANT-class assert per this row's own rule ("entombment impossible by construction": the B6 ultimate fail-safe guarantees escape at 60s against CK's 240s window, 4x headroom — a backstop with 4x headroom missing its window is never scheduling noise, something SUPPRESSED the backstop). The "flake" was a real deterministic regression: Stage-1's CarvedStair route-ownership (a route kind with NO executor) parked the trapped colonist in RouteOwnedWaiting, hiding him from the stuck-watchdog until the rescue overran the budget. RESOLVED by the STAIR-LADDER Phase-1 split (stairs walk plain/un-owned, ladders climb route-owned). Founding-instance count for this class is therefore TWO, plus the later instances below; (2) FOCUS0-ENUM tag's `lod1_stones=2` [expected 3] miscount; (3) SEASONHUD tag's B6HAUL `b6_built=1/2` + `b6_mined=false` — x3 rerun on the identical binary came back FAIL/PASS/PASS, and the SHORTFALL MOVED BETWEEN PHASES across runs [built=0/mined=true vs built=1/mined=false] — the signature of a completion racing a measurement WINDOW, not a broken writer. **The generalized rule: an assert that checks something completed/counted/positioned WITHIN A WINDOW (a tick deadline, a spawn radius, a timing-sensitive completion race) can flake on scheduling noise; an INVARIANT assert [conservation, no-leak, no-orphan, exactly-one-claimant] NEVER has** — every one of these 3 instances had every invariant probe green in the SAME failing run) | a gate leg fails but every OTHER safety/correctness probe in the SAME run is green (no leaked jobs, no stuck colonists, no conservation break) — especially a COUNT, POSITION, or COMPLETION-DEADLINE probe (stones/claims/net_fires/built-within-N-ticks-style), not an invariant assert. Ask: (a) does the tagged commit's diff have ANY execution-path surface the failing probe could plausibly correlate with — schema-only/read-nothing (FOCUS-0-ENUM) or provably-unrelated-lane (SEASONHUD's voxygen-only diff failing a b6haul probe) is the tell; (b) is the failing assert a WINDOW/completion-race check or a true invariant — only the former is B22-eligible | Rerun the SAME tagged binary (not a rebuild) ×2-3. Consistent PASS, or a shortfall that MOVES BETWEEN PHASES across reruns (not a fixed wrong value), on the identical binary = scheduling-seam noise, classify and tag per this precedent (log the signature: which probe, expected vs. observed per run, what changed vs. didn't in the diff). Consistent or majority FAIL on the SAME phase/value = a real regression, do NOT tag, investigate the actual writer. Never apply this classification to an invariant-class assert (conservation/no-leak/no-orphan) — those flaking would be a real bug, not B22. Ties B8 (determinism/scheduling-noise) and the FR17 paired-A/B rationale (same underlying scheduling-seam class, [5k..46k] locomotion telemetry) — this is the GATE-LEG-CLASSIFICATION specialization of that general class. **Fourth instance + a causal mechanism finally named (RUN-0 tag, 2026-07-13):** B5's completion-window leg re-drew its historically-documented flake after RUN-0 added `WriteStorage<Energy>` to `bastion_jobs`' `SystemData` — zero logic change to B5's own path, but the new storage access changes the ECS DISPATCHER's dependency graph (`bastion_jobs` now serializes against vanilla's stats system, which also writes `Energy`), which shifts tick scheduling. **General rule: a SystemData storage addition IS a scheduling change**, even when it adds no new logic — expect completion-window legs to re-draw their noise whenever a block adds a new component read/write to an existing system's SystemData, and check via the same rerun-the-identical-binary protocol above before assuming a regression. **Fifth instance — a MISCLASSIFICATION caught by the very protocol meant to catch it (AUTON-2 tag, 2026-07-14): the classifier matched a gate-time FAIL to a KNOWN flaky leg BY LEG NAME, without diffing which FIELD actually failed — and the leg was flaking on TWO DIFFERENT assertions.** The PREEMPT leg's historically-known flake is `preempt_endured`/`endure_dug` racing a timing window (genuinely B22-eligible); at the AUTON-2 gate the SAME leg ALSO started failing `preempt_hover_silent` — a real, fully deterministic regression (byte-identical across 3 quiet-machine reruns, including float tails) caused by a stale scenario fixture (tuned against a now-superseded global constant a later block made per-colonist). The noisy gate-box's load intermittently masked the deterministic failure behind the OTHER field's genuine flakiness (a PASS on the noisy box came from the endure phase's cooldown drifting late enough that no preempt attempt could even fire, which vacuously passed `hover_silent` too) — so "FAIL/PASS/FAIL, matches the known leg" read as B22 noise when one specific field was actually always-wrong. **General rule: B22 classification must diff the FAILING FIELD across reruns, not just match the LEG NAME to a known-flaky leg — the same leg can carry both a genuinely flaky assert and a genuinely broken one at the same time**, and leg-name pattern-matching alone will misclassify the broken one as noise |
| B31 | **`job_wanted` serves TWO independent masters (paint-time eligibility AND mid-travel validity re-check) — a new designation kind must decide BOTH explicitly, or one silently churns jobs into a create/claim/drop loop invisible to job-count probes** (FARM tag, run-1 fail: Farm's paint-eligibility trigger correctly set `job_wanted=false` once a cell's target state was reached, but the SAME flag is also read mid-travel as "is this job still worth pursuing" — for a state-driven kind [where validity should live in its own completion arm, not this shared flag], the trigger's correct paint-time write got read as an incorrect mid-travel invalidation, mooting every job right as colonists reached it. Result: 6729 job creations, 6720 claims, ZERO completions — a complete silent churn — and `jobs_bounded` stayed GREEN throughout [the board never held more than 9 jobs at once, so a job-count invariant sees nothing wrong; only the creations:claims:completions RATIO in the log reveals it]) | for any NEW `DesignationKind`: does it read/write `job_wanted`? If so, does it handle BOTH the paint-time-eligibility use AND the mid-travel-validity use, or only one? A kind whose validity is state-driven [owns its own completion arm] should opt OUT of the mid-travel `job_wanted` re-check entirely — sharing it unexamined is the trap. Symptom signature: job-count/bounded invariants stay green, but completions stay nonzero-target-yet-actually-zero and creation:claim counts run far higher than the visible job count would suggest | Explicitly decide, per new designation kind, whether it participates in the mid-travel `job_wanted` re-check or opts out in favor of self-owned state-driven validity (Farm's fix: opt out). Add a creations:claims:completions RATIO assert to any new scenario touching a designation kind with a lifecycle beyond simple claim-and-complete — a bounded job-COUNT invariant alone cannot see a churn loop where the count never grows past its ceiling. **MONITORING (architect, 2026-07-13):** Farm's own instance is fixed (opted out), but WATCH whether this class shows up as real churn/perf cost under live food-loop play — if it bites for real (not just this one caught-pre-tag instance), route to the Bug-Tester queue; non-urgent while the Bug-Tester is heads-down on ARCH-003 |
| B32 | **An empty/vacant terrain cell can read `Some(SpriteKind::Empty)`, never bare `None` — a `Some(_) => foreign/skip` arm silently swallows every vacant cell it was meant to claim** (FARM tag: the till/sow/harvest state-detection logic matched `None` as "vacant, mine to work with" and fell everyone else into a foreign-state skip arm; but plain air actually reads as `Some(SpriteKind::Empty)`, not `None` — every vacant field cell silently matched the FOREIGN arm and produced zero jobs, diagnosed only by a pass-head probe logging raw sprite reads) | for any match on a terrain/sprite read meant to detect VACANCY ("nothing here yet"): does it match bare `None` only, or does it ALSO cover `Some(SpriteKind::Empty)`? Zero jobs generated from an apparently-correct vacancy check, with no error anywhere, is the tell | Vacancy checks must match `None | Some(SpriteKind::Empty)` (or whatever the engine's actual "nothing here" representation is) — never assume the absence-sentinel is bare `None`. Verify against a live read (a probe logging the raw value) rather than the type signature alone before trusting a vacancy match. Sibling of B7-3's B26 (a silent self-destruct from an unexamined field) — this is the silent-skip mirror on the READ side |
| B33 | **A material-fetch machinery hardcoded to ONE specific item kind (`BUILD_MATERIAL`) across THREE separate sites (availability signal, claim-side carry, fetch-commit) silently mis-signals for any second required-item kind — generalize per-def before a second consumer arrives, not after it breaks** (FARM tag: Sow's `required_item = wheat_seeds` needed the SAME fetch/carry machinery Build already used for `BUILD_MATERIAL`, but the machinery read/wrote that one hardcoded kind at three independent call sites; fixed by generalizing all three to read the job's own `required_item` def rather than the hardcoded constant — Build is unchanged by construction since it still resolves to the same def it always did, but the fetch machinery is now genuinely per-def instead of single-kind) | before wiring ANY new required-item job kind onto the existing fetch/carry machinery: grep every site that currently hardcodes the ORIGINAL material kind (`BUILD_MATERIAL` or equivalent) — availability checks, claim-side carry state, fetch-commit logic are the three shapes this hit. A kind-specific hardcode compiles fine and passes every existing scenario since the original kind never changes; it only breaks the NEW kind, silently (a stall that looks like "no materials available" even when the stockpile has them) | Generalize the fetch/carry machinery to read the job's own `required_item`/def rather than a hardcoded kind constant BEFORE (or as part of) shipping the first new required-item job kind — don't ship kind-specific and generalize reactively later. This tag's generalization is now the standing shape every future material-carrying job kind (recipes, DF-COOK, any future produce-job) inherits for free |
| B34 | **A Bastion drain competing against a vanilla resource's UNCONDITIONAL, ACCELERATING regen must beat the regen's CAP RATE, not its base/average rate — losing this race fails SILENTLY (the floor is simply never reached, nothing errors)** (RUN-0 tag, run-1 fail: vanilla's `Energy::regen` is unconditional and accelerates up to a 10/s cap [`energy.rs::regen`, `min(10.0)`] — the first drain attempt used 6/s, which lost the race: energy dipped to ~88 then the accelerating regen outpaced the drain and pulled it back to full WITH THE RUN FLAG STILL SET, so the low-energy floor was structurally unreachable and the force-revert-to-walk governor never fired. Telemetry showed it precisely: the floor value was never observed, energy ended near-max, the run flag stayed true — no crash, no error, just a mechanism that silently never engages. Fixed: 15/s drain, a comfortable net −5/s against a maxed regen, giving the intended ~7-18s reserved sprint) | for any NEW Bastion mechanic that drains a resource ALSO written by an existing vanilla system on the SAME entity (Energy, Health, any accelerating/regenerating stat): what is the OPPOSING system's regen shape — flat, or accelerating to a cap? A drain rate compared only against the regen's STARTING/average rate can still structurally lose once the vanilla side accelerates. Symptom signature: the intended low-resource threshold/floor is never observed in telemetry, the mechanism silently never triggers, nothing errors | The drain rate must beat the OPPOSING regen's CAP rate (not its base rate) with real margin — read the actual regen implementation's ceiling, don't estimate from its starting value. Document WHY the chosen constant beats the cap directly in the code comment (this fix's constant states its margin explicitly) so a future tuning pass doesn't accidentally drop it back below the cap without realizing why that's unsafe. Sibling of B26 (a mechanism that silently self-destructs) — this is the silently-never-engages mirror on the RESOURCE-COMPETITION side rather than the job-lifecycle side |
| B35 | **A GLOBAL telemetry counter (fires-across-the-whole-scenario) asserted as "must stay zero" gives a false negative if the danger window is only PART of the scenario — scope the assert to the WINDOW the claim is actually about** (AUTON-0 tag: `CENTER_NET_FIRES` [the embed-watch teleport counter] is a scenario-wide global; the block's real claim was "zero false-trips during a specific 480-tick storm window, where the 60s stuck-watch timer structurally CANNOT complete, so ANY teleport in that window is provably false" — but a naive "assert the global counter is zero for the WHOLE scenario" would either miss legitimate OUTSIDE-the-window teleports [masking a real regression elsewhere] or, worse, get invalidated by them [flagging a correct OUTSIDE-window rescue as a failure]. The fix: scope the assert to the storm window specifically, not the whole run) | for any global/cumulative counter used as a safety assert (fires, teleports, embeds, any "should stay zero/bounded" telemetry): is the counter's DANGER WINDOW the whole scenario, or a specific sub-window (a storm, a preemption burst, a specific phase)? A global assert over a scenario that ALSO contains legitimate uses of the same counter outside the window under test will either false-pass or false-fail | Scope safety-counter asserts to the SPECIFIC WINDOW the claim is about (a tick range, a scenario phase), not the whole scenario's cumulative total — especially when the counter is a shared backstop (embed-watch, stuck-watch) that legitimately fires in OTHER parts of the same scenario for OTHER correct reasons. A counter that's "zero for the whole run" is a stronger and often WRONG claim than "zero during the window where a fire would prove a bug." Sibling of B22 (window/completion-race asserts flake on scheduling) — this is the assert-SCOPING mirror, about WHAT window to check rather than whether a window-check can flake |
| B36 | **A health/threat-based test condition must be HELD (re-asserted every tick/second like a real hostile would), not SET ONCE — vanilla can silently restore state behind a one-shot write** (AUTON-0 tag: a scenario set a colonist's health to 0.1 once to simulate a persistent threat, expecting the Flee drive to stay engaged; instead a working colonist's health silently returned to exactly 100.0 within ~180 ticks — vanilla runs its own health-restore logic [a max-update-class heal] that the one-shot test write never accounted for. The MECHANISM under test responded CORRECTLY the whole time [Flee dropped exactly when the signal genuinely dropped, Work correctly resumed] — the FIXTURE was dishonest, simulating a threat that vanishes on its own, which no real hostile does) | for any test condition that simulates an ONGOING state (a threat, a shortage, a hazard) via a single write to a value vanilla ALSO manages (health, energy, any regen/heal-capable stat): does vanilla have its OWN logic that could silently revert the one-shot write before the scenario's assert window closes? A "the mechanism failed to stay engaged" result might actually be "the mechanism correctly disengaged because the simulated condition vanished on its own" | Re-assert/re-apply the simulated condition every tick or every second the scenario needs it held (matching how a REAL sustained threat/shortage would actually behave — continuously present, not a single frozen write) rather than setting it once and assuming it persists. Sibling of B34 (a Bastion mechanic racing vanilla's regen) — this is the TEST-FIXTURE-side mirror: B34 is about a GAME mechanism racing vanilla silently, this is about a FIXTURE racing vanilla silently and mistaking vanilla's win for the mechanism's failure |
| B47 | **A DETERMINISTIC seed-dependent fixture precondition that recurs red at every gate is NOT B22 scheduling-noise (which varies run-to-run) — settle it ONCE (fixture-timing vs. latent game bug), file the class, and stop per-gate re-diagnosing it** (the `bed_occupied_mid` recurrence, filed 2026-07-17 by Bug-Tester at architect direction after it re-cost a diagnosis at essentially every gate this session, incl. the Phase-B validation of integration checkpoint `2244ce8d71`). Signature: `bed_scenario` FAILs with `bed_occupied_mid` the SOLE false pass-gate field, and it is DETERMINISTIC per seed — false@1337 (×3 byte-consistent), false@42, but **true@seed21, true@777** (777 datapoint added by the 2026-07-18/19 adversarial bug-hunt) — i.e. seed-dependent, not the run-to-run flap that defines B22 (do NOT reach for B22's rerun-identical-binary protocol; reruns are identical here). Every OTHER bed invariant passes in the same run (beds_built, all ownership, sleep, collision-arbitration exactly_one/winner, occupancy_clear, kill, released_on_death, demote/promote persistence), and the bed-occupancy GAME logic is proven working (A sleeps bed1; B occupies bed2 @seed21) — so occupied_mid=false is NOT a universal bed-usage break. The harness authors themselves document it (`main.rs:10302-15`) as an "arbiter-vs-assign timing RACE, bed-occupancy logic untouched," with a 480-iter re-assert mitigation whose premise (5-tick re-assert out-paces the 15-tick arbiter selection so B is reliably steered to bed2) is deterministically FALSE at seed 1337. Grok integration READMEs corroborate ("Bed still deferred from H"). NOT the CASE-006 claim-churn wedge — that is SEPARATELY fixed by STUCKJOB-α (`stuckjob_scenario` PASS, teleport-rescue ~59s, 1681-sample precondition) | `bed_scenario` FAIL whose ONLY false pass-gate field is `bed_occupied_mid`, recurring across gates, DETERMINISTIC per seed (identical on rerun — the anti-B22 tell). If instead the failing field MOVES or flaps run-to-run on the same binary, that IS B22, not this. If MULTIPLE bed fields go false (e.g. seed21's `bed_built:false` cascade), that's a seed-specific fixture-terrain miss (bed1 can't place at that seed's site), a different fixture-fragility, not this class | TWO parts. (a) HARNESS FIX (lands at branch-reconcile): the bed-fixture fix that force-teleports B onto bed2 to satisfy the precondition deterministically at any seed exists in the catalog worktree — land it to integration so the leg stops false-reddening. (b) DEDICATED LATER SETTLE (LOW priority, B-rests-fine-elsewhere): confirm whether occupied_mid=false@1337 is PURE fixture-timing (the work-arbiter's 15-tick selection out-racing the 5-tick rest re-assert, steering B to work not bed — a test artifact) OR a latent GAME bug (a rest-assigned colonist genuinely arbiter-STARVED from its own bed under work contention — a real gameplay defect). Needs a bespoke work-contention repro (measure whether a rest-assigned, needs-tired colonist ever reaches its assigned bed within N ticks while jobs compete). Until settled, treat a lone `bed_occupied_mid` red as THIS known class — do not re-diagnose from scratch per gate. Sibling of B22 (window/completion probe) but the DETERMINISTIC-seed-dependent counterpart: B22 = varies-on-rerun scheduling noise; B47 = stable-per-seed precondition that may hide a real starvation bug |
| B37 | **✅ FIX VERIFIED LIVE (adversarial bug-hunt, 2026-07-18/19, `bughunt/BUGHUNT-REPORT.md`) — the row below was stale as "REPORTED, NOT FIXED"; the row-49.2 HAULPIN strike-cap fix HOLDS: `haulpin` scenario PASSES at both 1337 and 777 (drops the unreachable haul, frees the reservation, re-emits by rescan, conserves exactly). Original finding preserved below for the historical mechanism description.** REPORTED, NOT FIXED — a permanently-unreachable haul job never drops, so its item stays reserved forever and starves fetch of the whole pinned pile** (AUTON-1 tag: a haul job reserves its target item AT GENERATION time; if the target later becomes unreachable [a pit, a sealed-off pocket] the job churns `unreachable` indefinitely rather than ever dropping — and since it never drops, it never releases its reservation either. `should_merge` amplifies this: a merged pile shares ONE reservation across N physical stones, so ONE bad unreachable-haul job pins the ENTIRE merged stack behind it, starving every OTHER consumer waiting on that pile even though most of the stack is perfectly fetchable. Surfaced by AUTON-1's demand-driven mine generator: a churning haul on one pit-trapped stone caused a 2-of-4 build starvation even though 3 other stones were sitting fetchable in the same pile) | does any haul-style job reserve its target at CREATION time rather than at a point where reachability is confirmed? does an `unreachable`-churning job have any strike-cap/give-up path that also RELEASES its reservation, or does it churn forever holding the reservation hostage? Symptom: a consumer (fetch/build) stalls on `needs_materials` even though a stockpile visibly has enough of the item — check whether the visible stock is actually FREE or silently pinned behind an unreachable claim | Fix candidate (not yet built — changes B6HAUL's existing unreachable-retry behavior, so it's its own follow-up block, not a rider): give unreachable-haul jobs a strike-cap that DROPS the job AND releases its reservation on expiry (haul-gen can re-emit a fresh job for the same item if it's still a valid target, now with a clean reservation). Ties B25 (a different reservation-never-releases class — bed slots on destroyed blocks) — this is the STRIKE-CAP-MISSING sibling: B25 has no unregister hook at all, this one has an unreachable-retry loop but the loop never thinks to also drop the reservation |
| B38 | **A merged multi-stack pile collapses to ONE entity/Uid/reservation, so CONCURRENT eating throttles to the preempt-cooldown cadence regardless of stock size — under mass hunger, a colony feeds ~1 colonist per ~25-30s no matter how much food is actually in the pile** (AUTON-2 tag, data-proven: `should_merge` merges stockpiled food into one entity; a failed eat-attempt [losing the single reservation to another colonist] burns the FULL preempt cooldown before that colonist can try again. Observed live: stock=57 in one merged pile, 4 hungry eaters, exactly 1 fed per split window — one colonist starved through a full 200s recovery window purely on queue position, not on actual scarcity. Distinct from B37 [a churning unreachable-haul job pinning a reservation forever] — this needs NO unreachable job at all; the serialization is structural in the one-entity-one-reservation shape plus the failure-cooldown design) | for any scenario/live-play situation with MULTIPLE hungry colonists and food that has merged into large piles (`should_merge` stacks): does eating throughput scale with stock SIZE, or is it capped near ~1 successful eat per preempt-cooldown window regardless of how much food is available? Symptom: colonists visibly starve/wait despite a large nearby stock, and the wait interval matches the cooldown constant almost exactly | Follow-up block (own scope, touches B7-2/B6 reservation semantics — NOT a quick fix): either per-UNIT reservation semantics for multi-stacks (an amount>1 stack can serve N concurrent eaters, not just one), or reservation-FREE eats on stacks (the existing sniped-moot arm already handles the race correctly on the LAST unit, so this pattern is precedented). This is exactly the failure mode a real famine/feast event would hit — the death-spiral moment AUTON-2 exists to prevent — so it's a genuine live-play risk, not just a scenario artifact, even though AUTON-2's own gate avoided it honestly (separated single-wheat starters instead of one merged pile, testing the real threshold behavior without the serialization masking it) |
| B39 | **A hoisted lock/guard ACQUISITION is a behavioral change even when the code it guards stays lazy — guard cadence must match the pass's SCORING cadence, not its ITERATION cadence** (AUTON-3 tag: a diff deferred an rtsim NPC lookup lazily (correct) but left the READ-GUARD acquisition itself hoisted to the top of the per-colonist pass, so it fired every tick (~30/s) instead of the pass's historic ZERO. Zero logic change, zero new data read on most ticks — but the lock-acquisition CADENCE against the rtsim thread changed, which is exactly the ARCH-003-sensitive scheduling class. Result: a correlated 5-leg gate-storm [CK/ZONE/BED/B73/HAULPIN] on the very first draw, every one a pre-existing timing-sensitive leg, zero of them a new scoring/logic failure — confirmed by field-diffing, not assumed) | for any diff that adds a lock/guard/read-storage acquisition inside a hot per-tick pass, even if the CODE it protects is deferred/lazy/conditional: does the ACQUISITION itself still fire unconditionally every tick, or only when actually needed? A "lazy lookup, eager guard" split still changes lock cadence. Symptom: a correlated storm across MULTIPLE pre-existing timing-sensitive legs on the same gate draw, with zero scoring/logic/pick asserts failing anywhere — that pattern points at a cadence/scheduling perturbation, not N independent new bugs | Acquire the guard/lock at the SAME conditional point as the logic that needs it (e.g. gate the acquisition itself behind the same `select_tick.then(...)`-style condition the rare-branch logic already uses), matching the pass's own scoring cadence rather than its raw iteration cadence. Sibling of [[no-cargo-during-own-gate]] (the same lesson at the BUILD-lock level, this is its RUNTIME-lock counterpart) and B22's SystemData-addition instance (RUN-0) — three now-confirmed distinct ways a "no logic change" diff can still be a scheduling change: SystemData access, cargo invocation, and lock/guard acquisition cadence |
| B40 | **A "complete within N ticks/polls" deadline-shaped assert needs STRUCTURAL headroom sized against the mechanism's own real-world cadence, not a number that happens to just barely fit — four confirmed instances now, this is its own class, not one-off tuning** (SPIRAL's recovery window, SPIRAL's floor window, and now HAULPIN's `--haulpin-scenario` emissions window all independently hit the same shape: a fixed poll/tick budget that fits the EXPECTED case with zero or near-zero margin, so ANY scheduling breath [the ARCH-003 residual, a cadence change, ordinary timing jitter] can push the real completion just past the deadline and flip a genuinely-working mechanism's assert to FAIL — HAULPIN specifically: a 240-poll window fit exactly 3 strike-drop cycles at ~25s each with ZERO headroom; a tag-time draw happened to land exactly on 3, but any future draw could legitimately land on 2) | for any NEW windowed/deadline assert ("X completes within N ticks/polls/seconds"): compute the mechanism's actual expected completion time under normal conditions, then check the window's margin — is it comfortably ABOVE that (2× or more, matching this class's now-established fix shape), or does it fit with near-zero slack? A near-zero-slack window will eventually flip on nothing but ordinary scheduling variance, not a real regression | Size deadline-shaped assert windows with explicit headroom (≥2× the expected completion time, the pattern this class has now converged on four times independently) rather than the tightest value that happened to pass once. **Standing follow-up proposed (builder, AUTON-3 tag): a consistency-sweep pass across the harness's OTHER windowed/deadline asserts, auditing each for the same near-zero-margin risk before it becomes a fifth instance** — queue as a small housekeeping row when there's a natural gap, not urgent. Ties B22 (scheduling-noise classification) — this is the WINDOW-SIZING prevention half of that class, not the after-the-fact classification half |
| B41 | **A green scenario/harness gate proves NOTHING about a gen/visual/terrain-class feature's real behavior if the gate never exercises the LIVE code path the feature actually runs on** (FLAT-TEST-ARENA tag, 2026-07-14: `bastion-block-ARENA` tagged clean at 35/36, but the Play-Tester found the feature TOTALLY INERT in real singleplayer — a fresh world still generated normal stock terrain, spawn wasn't slab-centered, despite the env var confirmed reaching the process. The scenario harness's world-gen path apparently diverges from singleplayer's actual initial-gen call sites, so the gate's "PASS" only proved the harness's OWN path worked, never touched the real path the feature was built for) | for any gen/visual/terrain/world-shape-affecting feature: does the scenario harness's world-gen entry point actually match the REAL client/singleplayer entry point, or could they diverge (different call sites, different init order, a harness-only shortcut)? A gate leg passing for this class of feature is NOT sufficient evidence of real behavior — "boots-confirm" (the binary launches, the harness scenario passes) proves launch-succeeds, not feature-correctness, for terrain/visual-shape changes specifically | Any gen/visual/terrain-class feature needs an explicit "unverified until eyeballed" framing until a human (or a genuine live-path check, not just the scenario harness) confirms it — don't treat a green scenario gate as equivalent proof for this class the way it legitimately is for pure-logic/data mechanisms. When building this class of feature, identify and verify BOTH the harness's own gen path AND the real client's gen path explicitly, don't assume they're the same call sites just because the API looks unified (the `World`/`test_world::World` duality is exactly this kind of divergence risk, already known from B0 — this is a live instance of it biting a specific feature) |
| B42 | **A test scenario's single assignment call can silently no-op against a live arbiter that's already claimed the target — `let _ =` on a busy-returns-false assign swallows the failure with no retry** (CHOP-FELLING tag, BED leg attribution: the scenario's `assign_rest(&bn, bed2)` fires once; by the kill-while-sleeping phase, colonist B has usually already run live through a long sleep + a collision + a 3600-tick loop and already holds an `ActiveJob`, so the single assign call returns `false` [busy] and is silently ignored — bed2 never fills, `occupied_mid` flaps ~40-75% depending on timing) | for any test-harness assignment/assign-to-slot call whose target could ALREADY be busy by the time the scenario reaches it (a live arbiter/claim system running underneath): does the harness check the return value, or is it discarded (`let _ =`)? A single-shot assign against a live, independently-ticking system is a race by construction | Re-assert the assignment EVERY iteration (safe no-op while busy, takes the moment the target idles) rather than a single-shot call — out-paces the underlying system's own re-decide cadence (here: 5-tick harness reassert vs. the arbiter's 15-tick selection). Widen the completion window alongside if the fix shifts timing. Confirmed via parent-commit isolation this was PRE-EXISTING (not introduced by the tag under investigation) before fixing — attribute before fixing, same discipline as everything else this class |
| B44 | **A capability cap keyed to a skill LEVELABLE BY THE CAPPED ACTIVITY ITSELF is farmable through itself** (FREE-CLIMB-CAP corpus leak: the free-climb cap `3·(level+1)` sat beside an assist granting 1.5 climb-xp/s; a trapped colonist bounce-farming legally below the cap earned level 1 in 13.3s, grew his own cap 3→6, and escaped a shaft the cap was proven to contain on other seeds — plus a second-order hole, vanilla scramble launches from wherever the cap let him climb, NOT from the anchor, so "scramble ≤ cap(0) no interaction" reasoning silently assumes the wrong reference frame whenever a cap-legal position sits within scramble reach of a purchase lip. Found only because the corpus ran 6 seeds — the single-seed green was rim-geometry luck) | for any new capability cap keyed to a skill/stat: can the CAPPED activity itself raise the skill the cap is keyed to? if so, is the grant path exploitable from inside the capped state (e.g. free/assisted practice, not just real apparatus/work)? Also: does any "by construction, no interaction" argument about two mechanics quietly assume a shared reference frame (anchor vs. current position) that a rare geometry can violate? | Grant progression XP from REAL apparatus/work only, never from free/assisted practice that the cap exists to gate. Audit every "by construction" edge-case note for an implicit reference-frame assumption. Superseded in practice by B45's frozen-episode-snapshot fix (the actual shipped closure), but the general capability-cap class stands on its own for future caps |
| B45 | **A falsifier whose premise depends on a SPAWN ROLL (or any other lottery-shaped random initial condition) must STAGE or ASSERT that roll — "green on the seed you tried" is the lottery variant of starvation-class flakiness** (FREE-CLIMB-CAP corpus leak's PRIMARY cause, superseding B44's farming theory as the dominant one: colonists roll climbing 0..=1 at spawn: a level-1 roll legitimately free-climbs deeper than a level-0 roll, so a falsifier that assumes "the colonist" is level-0 without staging it is testing a coin flip, not the mechanism. Ben's design ruling on the underlying farm: climb XP stays on free wall-climbing [ladders teach nothing — the grant is deliberately inverted], and the actual farm-closure fix is a FROZEN CAP-SKILL episode snapshot [`or_insert`-lazy at cap-consult, cleared only at genuine-surface exit sites] — a cap reads the skill the colonist brought INTO the predicament, never one minted inside it) | for any new falsifier/scenario whose pass/fail depends on a value that's randomized at spawn (skill rolls, stat rolls, any RNG-seeded initial state the mechanism under test is sensitive to): does the scenario STAGE that value explicitly (a setup-only override hook), or does it just hope the seed cooperates? "Passed on seed 1337" is not evidence if a different seed's spawn roll would flip the verdict | Add a setup-only staging hook for any spawn-randomized value a falsifier's premise depends on (house precedent: `bastion_set_colonist_climb_level`, recorder-marked, in-JSON staged flag) and use it explicitly rather than relying on a favorable roll. Run the seed corpus (≥6 seeds) specifically to catch this class — a single-seed green cannot distinguish "the mechanism works" from "this seed's lottery happened to cooperate" |
| B46 | **`ActiveJobState::Arrived` can trigger on a job whose cell sits within work radius THROUGH A SOLID WALL, wiping any watchdog keyed on the Arrived transition** (corpus v2.1 seed-8 residual: a vaulted colonist arrived 4× at shell-zone haul dropoffs embedded in his vault's walls — each arrival wiped his stuck-backstop clock, measured 1/10/21/55s values. Live-play surface: any colonist adjacent to a wall with a job on the far side can Arrived-wipe watchdogs and possibly stall "working" a cell it can never actually reach) | does the Arrived-transition check use a pure RADIUS test against the job's target cell, or does it also verify line-of-sight/reachability (no solid geometry between colonist and target)? any watchdog/backstop that resets on Arrived is exposed to this class if the radius check alone can fire through a wall | Production fix (not yet built): add an adjacency/reachability check at the Arrived transition (or at the watchdog's Arrived-reset sites specifically) so a through-wall radius-only match cannot silently wipe rescue timers. Scenario-side exposure is bounded (within-budget after the shell fixture fix); this is a genuine live-play risk pending the production fix — follow-up row suggested |
| B43 | **REPORTED, NOT FIXED — a rare pre-existing CASE-003-class physics embed (colonist clips below grade) surfaced by FIXING a masking flake, not caused by the fix itself** (BED-STUCK-EMBED, surfaced during B42's fix verification: with `occupied_mid` now reliably true, a rarer [~15% alone, 0/5 at the pre-fix parent — meaning the OLD flake was masking this one, not that the fix introduced it] flake appeared — the ULTIMATE FAIL-SAFE correctly teleports a below-grade colonist [z=392 vs 393] TWICE during bed-build, and the fail-safe's own 60s delay eats into the scenario's early timed phases. The safety backstop DOES fire and DOES rescue the colonist — this is a timing/test-flake cost, not an entombment failure) | when fixing a dominant flake exposes a second, rarer one on the same leg: is the newly-visible flake a symptom of the FIX, or was it always there, just masked by the first flake failing first/more often? Check via the SAME parent-commit-isolation discipline before assuming causation | Filed as its own task via the standing Bug-Tester bug-routing workflow (CASE-003 class — a physics-embed under a specific placement sequence), not bundled into whichever tag happened to expose it. The 60s fail-safe delay costing an early-phase test window is itself worth a look if this recurs (either speed up detection for test-relevant windows, or accept it as a rare/low-priority residual) |
| B24 | **A TEST FIXTURE produces the exact failure mode it exists to detect (fixture geometry, not a game bug) — a flattened pad below/above surrounding ground is a de-facto PIT/RIM that traps wanderers and false-triggers the stuck-teleport** (B7-1's bed-scenario fixture: a pad flattened AT the site center's own `gz` [398] sat in a PIT — surrounding terrain ran z 400-401, so the ±12×±9 pad had 2-3-block walls all the way round; idle wanderers hugged the walls for 60s and the ULTIMATE fail-safe [jobless stuck-watch tier, `secs=60`] teleported them OUT to z=401, off-fixture, jobs released — a real rescue firing on a FIXTURE ARTIFACT, not a game-code bug. Gen-2 attempt [a 3-high containment rim at ±20×±14] recreated the SAME class INSIDE the rim — rescues fired at the rim's inner face, teleporting colonists ONTO the rim. Distinct from B22: B22 is a real assert flaking on scheduling noise around a genuine mechanism; this is the FIXTURE ITSELF manufacturing the trapped-colonist condition the scenario is supposed to exercise cleanly) | a scenario/fixture that flattens a pad: is the pad's Z chosen from the SITE's own single sample point (center `gz`), or from the surrounding AREA's actual ground profile? does the pad sit flush with what's around it, or does it create a step (a pit if lower, a rim/wall if a containment border is added at a fixed height)? stuck-watch/teleport rescues firing at the pad's EDGE (not scattered through its interior) is the tell | Build fixtures as a FLUSH PLATEAU: sample the area's ground height on a grid (B7-1 used 8-block spacing) and flatten to the MAX of that sample, not a single center point and not a containment rim at an arbitrary height — a flush plateau has no step for wanderers to hug or fail-safes to trip on. If a scenario needs a bounded arena, prefer flush-to-surrounding + an invisible logical boundary (a distance check) over a physical wall/rim. Sibling of B15 (natural-slope-not-flattened) and B17 (unrepresentative fixture geometry hides/manufactures failures) — this is the manufacturing-a-failure mirror of B17's hiding-a-failure. **Second instance (B7-2 tag):** a sealed-box fixture for testing "unreachable target" had the colonist's arrive-radius reach THROUGH the box's 1-block wall — the colonist ended up sleeping against the OUTSIDE of the box, not genuinely blocked. The underlying arrive-distance check was correct (honest construction, not a bug); the fixture's wall was too thin to actually test enclosure. General enclosure-fixture rule: verify the target is unreachable by the SAME distance/arrive logic the mechanism under test uses, not by visual/geometric enclosure alone — a wall thinner than the arrive radius doesn't enclose anything. **Third instance (B7-3 tag):** a gz-1 strip fixture built under a partially-undesignated overhang dead-ended at the lip — a genuine 1-high gap with no standable stance, which B15's stance gate CORRECTLY refused (claims stopped 3s before the scenario even reached its intended breakdown-resume assert). Not a bug — the fixture's geometry itself couldn't reach the behavior it was built to exercise. General rule reinforced: a resume/claim-after-recovery fixture must use TOP-EXPOSED surface cells, not an under-overhang strip, so a failed resume is diagnosed as BEHAVIORAL (the mechanism didn't resume) rather than GEOMETRIC (there was never a valid stance to resume to). **Fourth instance (AUTON-1 tag):** a stockpile anchor sat 4 cells south of the strip's center, so the mine-generator scan's first candidate row landed on the strip's south EDGE against raw undesignated worldgen — a colonist hauling a pit-trapped stone through the rough terrain band churned `unreachable` ×6. Fixed by moving the anchor to strip center so the generator's scan circle sits entirely inside controlled flat rock. Same class every time: an off-center or edge-adjacent fixture anchor lets the scenario's OWN geometry manufacture the failure it's trying to observe cleanly |
| B27 | **A harness rename-then-demote with ZERO ticks between them silently misses — the rtsim RECORD's name only updates on a SYNC TICK, but ECS-side name components update immediately** (FOCUS0DERIVE tag: `bastion_force_demote` looks the colonist up by matching against the rtsim RECORD's name; `bastion_rename_colonists_unique` renames the ECS component instantly, but the record mirror only picks up the new name on the next sync tick. Rename-then-immediately-demote with no tick between = the record still holds the OLD name = the demote's name-lookup silently misses = BED/NEEDS hooks read nothing downstream, with no error anywhere) | after any test-harness rename helper, before any hook that looks a colonist up by NAME through the rtsim record (not the raw ECS component): has at least one sync tick elapsed? ECS-component reads are immediate; record-mirror reads are NOT the same tick | Tick ≥1 sync after `bastion_rename_colonists_unique` before any RECORD-name lookup hook. General harness-authoring rule: know which of your test hooks read the LIVE ECS component (immediate) vs. the PERSISTED record mirror (next-sync) — they are not interchangeable and a rename is the sharpest case where the gap shows. Make outcomes that depend on this their OWN standalone assert bool (not folded into a composite) so a silent miss can't hide inside an unrelated failing/passing aggregate — this tag made `derive_demoted` its own bool specifically for that reason. Sibling of B7 (a different loaded↔persisted-record gap) |
| B28 | **A FIXED post-action wait can race a despawn/respawn window — poll until ready, don't sleep a fixed guess** (FOCUS0DERIVE tag: a fixed-duration wait after `bastion_force_demote` occasionally landed inside the demote→respawn gap, so the next getter read empty/stale rather than the settled post-respawn state — not a game bug, a harness timing assumption) | does any post-action harness wait use a FIXED sleep/tick-count where the underlying operation's completion time can vary (despawn/respawn, async promote/demote, any multi-tick teardown-rebuild)? a fixed wait that's "usually enough" will occasionally race the exact window it's trying to wait out | Poll-until-ready instead of a fixed wait wherever the harness crosses a despawn/respawn or promote/demote boundary — the BED/NEEDS scenarios' poll-until-condition pattern is the house precedent (already used successfully for occupancy/mood settling); apply it to any new boundary-crossing wait rather than reinventing a fixed sleep per scenario. Sibling of B8 (determinism/timing) and B22 (scheduling-sensitive asserts) — this is the harness-side mirror of both: the SIM can be timing-sensitive (B22) and so can the HARNESS's own wait logic around it |
| B26 | **A reservation created WITHOUT `required_item` gets fetched then SILENTLY released — the fetch-contract must travel as a pair, never just the reservation alone** (B7-3 tag, `JobKind::EatFrom` v1: the B6 material-fetch arm activates for ANY non-Haul job holding a reservation, but derives its `carrying` flip from `job.required_item` — `EatFrom` supplied the reservation but left `required_item: None`. The fetch steered to the item and picked it up [ground count dropped to 0 — the pickup genuinely happened], but `carrying` never flipped because there was no `required_item` to match against; the NEXT tick's reserved-uid-vanished arm read this as "the reserved item disappeared out from under the claim" and released the WHOLE job with no log; the claim loop then re-employed the now-idle colonist ~0.2s later; the orphan sweep silently reaped the released board job. End state: every probe green except the hunger meter, and the colonist ended up physically holding (hoarding) an item whose job had already been silently torn down around it. Diagnosis footnote: item Uids and JobIds are different namespaces — a log line reading "item=2... job claimed job=2" is coincidence, not a link; don't cross-reference IDs across namespaces without checking they're actually the same space) | for any NEW job kind that carries a board reservation (mirrors `Haul`'s fetch-then-carry shape): does it ALSO set `required_item`, or does it leave it `None`? if a reserved-item job's `carrying`/fetch-completion state derives from `required_item` matching, a job that reserves without setting it will silently misread "item picked up" as "item vanished" and self-destruct its own claim with no error. Symptom signature: every OTHER invariant probe green, only the specific meter/effect the new job was supposed to produce is missing or wrong | Fix: the reservation and `required_item` must travel TOGETHER at job-creation time — never insert one without the other for any job kind that rides the fetch arm. General registry rule for future reserved-item job kinds (future Produce/Cook/Craft jobs will hit this exact shape): treat "reservation + required_item" as ONE inseparable contract, not two independently-settable fields — consider a constructor-level invariant (a single `new_reserved_item_job(reservation, required_item)`-style helper) rather than trusting every future call site to remember both fields. Sibling of B7 (serde/wire-compat on new fields) and B17 (a shipped mechanism silently not doing what it claims) — this is the silent-self-destruct mirror of B3's stuck-forever class |
| B25 | **REPORTED, NOT FIXED — a side-table slot keyed to a terrain block never unregisters when that block is destroyed (stale-target class)** (B7-2 tag finding: `BedSlot`'s side-table is keyed by block-pos at placement; nothing hooks bed-block DESTRUCTION [mining it out, or any future terrain edit] to remove the corresponding slot — a mined-out bed stays targetable, so the NEED-CHECK pass or a future FOCUS/venue-need job could still assign a colonist to travel to a bed that no longer physically exists. Filed for B7-3/designer-lane triage, not blocking B7-2's tag — no scenario currently exercises destroying an owned/occupied bed) | for any side-table keyed to a terrain block position (BedSlot, and any FUTURE per-block registry — stockpile containers, mechanisms, POW-0 machines): is there a symmetric UNREGISTER hook wherever that block can be destroyed (mining, cave-in, god-hand demolition), or only a REGISTER hook at placement? grep the block's completion/placement site for its registration call, then check whether the corresponding Mine/destroy path has the mirror-image removal | Add the unregister hook at the same site(s) a Mine job (or any other block-removal path) clears that block — mirror of the registration call at placement. A slot pointing at empty air should be swept the same tick or the same arbitration cadence as the destruction, before anything can claim/target it. Consider a general pattern for future per-block side-tables (BedSlot, future container/mechanism slots): a shared "on block cleared, drop any side-table entry at this pos" hook rather than re-deriving it per feature |
| B23 | **CLOSED — claims-ratio right-tail widening was scheduling noise, NOT a B7-0 regression (attribution falsified by the follow-up A/B)** (B7-0 tag: `b58_d_claims_ratio` drew {1.879, 1.327, 1.858}, vs. the then-established {1.21-1.35} band — flagged as a first-signal watch, non-blocking. The follow-up run had a path flaw [a worktree `cd` bug made BOTH interleave legs resolve to the PRE-B7-0 parent binary], which answered the question more directly than the intended pair: FIVE clean quiet-machine draws on the parent ALONE — `{1.268, 1.273, 1.353, 1.220, 1.866}` — already contain a 1.866, matching the post-B7-0 1.88/1.86 draws that raised the watch. The elevated tail PREDATES the B7 diff entirely; the original {1.21-1.35} band was a lucky streak, not the metric's true spread) | if `b58_d_claims_ratio` (or any B22-class scheduling-sensitive metric) draws an outlier again: is it actually outside the metric's TRUE spread, or just outside a SMALL-SAMPLE band that hasn't seen the tail yet? ~10 prior gates was not enough draws to characterize this metric's real distribution — a single elevated draw is not evidence of a regression by itself | The corrected observed band is **~1.2-1.9**, not 1.21-1.35 — don't re-raise a watch entry for draws inside that wider range on their own; a real regression needs either a draw clearly outside 1.2-1.9 or a paired A/B (SAME seed, SAME binary except the diff under test) showing a systematic shift, not just a high single draw. General lesson for future paired-A/B setups: verify BOTH interleave legs actually built/ran the binary you think they did (a worktree/cd/build-cache bug can silently make both legs the same commit) — check this BEFORE trusting a "no difference" or "difference confirmed" verdict from any A/B. Filed at B7-0's tag, closed at B7-1→B7-2 transition (builder A/B, `docs/BASTION_RUN_LOG.md`) |
| B21 | **WATCH (rtsim vendor-bump question) — LOD-1's dupe-guard soundness assumes vanilla rtsim never self-mutates `SimulationMode`** (verified TRUE today — grepped the `rtsim/` crate: the only `SimulationMode` write anywhere in vanilla is the struct's default value, `rtsim/src/data/npc.rs:401`; every REAL mode transition is Bastion-owned, both demote triggers (`hook_rtsim_entity_unload`, `bastion_force_demote`) fire as synchronous imperative calls OUTSIDE the ECS dispatch cycle, so the flip never races `bastion_jobs::Sys`'s `is_loaded` gate within a live tick — Sonnet R-review, 2026-07-12, LOD-cluster rollup) | on any FUTURE Veloren/rtsim vendor upgrade: re-grep `rtsim/src/` for `SimulationMode` writes / any internal mode-transition logic inside `RtSimState::tick()`. If vanilla ever starts mutating mode ITSELF (not just Bastion's hooks), the "flip always lands between dispatch cycles" guarantee breaks — a same-dispatch race becomes possible (`bastion_jobs::Sys` runs before rtsim's `tick::Sys` per registration order, so a same-tick internal vanilla flip could slip a stale-Loaded read past the gate for one tick) | Re-verify this specific assumption as a standing pre-flight check on every rtsim/Veloren upstream merge/vendor-bump, not just a general regression run — it's a narrow, easy-to-miss invariant (a race window with no compile error and no obvious symptom short of a rare dupe). If vanilla does add internal mode transitions, either (a) add an explicit dispatch dependency ordering `bastion_jobs::Sys` after rtsim's tick system, or (b) re-derive the gate's "impossible by construction" argument for the new shape. Ties LOD-1 (row 33) + the general D12 "vanilla stays byte-identical" isolation invariant — this is the read-side mirror of that: Bastion's CORRECTNESS also depends on vanilla staying inside its OWN current boundaries |

| B29 | **WATCH (vanilla Architect-rule respawn uses thread-rng, not tick-seeded rng) — irrelevant to today's short scenarios, a real determinism risk for any FUTURE long-soak crossing a game-day** (PATH-0 premise investigation, 2026-07-13: vanilla's Architect rule imports `rand::rng` [OS entropy] for respawn timing/placement, not the `tick_rng(seed, ...)` pattern `migrate.rs` already uses — B8's discipline. The 24-in-game-hour `MIN_SPAWN_DELAY` is far longer than any current gate/scenario, so this doesn't bite today) | before writing any scenario/gate that runs long enough to cross a game-day (a real long-soak, B-AG6/settlement-growth territory, or anything exercising the Architect respawn path): does the Architect rule still use `rand::rng` (OS entropy, non-deterministic across runs), or has it been migrated to `tick_rng`? | If a long-soak scenario needs deterministic respawn timing/placement (two runs must agree), the Architect rule needs the same `tick_rng(ctx.index.seed, 0, 0x316A)`-style treatment `migrate.rs` already got — don't discover this by chasing a flaky long-soak gate, check it BEFORE writing one. Sibling of B8 (determinism/scheduling-noise) and B21 (a different "vanilla assumption that could break on future work" watch item) |
| B48 | **An EXTERNAL-authored adversarial conservation-soak leg with NO prior-tag pass history → a deterministic small total-GROWTH (a dupe, not a loss) during a QUIESCENT soak is INHERENT to the leg (never exactly-conserving), NOT a checkpoint regression — mark known + defer the instrumentation, don't burn a bisect/rebuild to re-confirm what the run-log absence already says** (`b55-deep` conservation soak, filed 2026-07-17 by Bug-Tester at architect direction; Phase-B validation of integration checkpoint 2244ce8d71). Signature: the adversarial `--b55-deep-scenario` (external ChatGPT/Grok B5.8 Stage-1 delivery, commit 871a9157d9 — "1000-drop persistence soak past 300s, multi-wave pile consolidation, erase/repaint races") fails `mine_conserved` because `authoritative_mined_after_soak` GREW past its designated total (observed 1000→1011, +11/+1.1%) during a soak with NO new designations — DETERMINISTIC per seed (×2 identical @1337), no panic, ALL loss-buckets zero (removal_class_merge_loss/unattributed=0 → nothing vanished, N stone are EXTRA). **EMPIRICALLY CONFIRMED INHERENT (2026-07-18):** the IDENTICAL +11 reproduces at the pre-integration ff2874b4b6 baseline (provenance-verified exe, pre-Grok-merge/pre-#4) → not a checkpoint regression; triangulated by empirical-baseline + run-log-absence + game-code-delta-doesn't-touch-the-path. Internal inconsistency: the leg's OWN `merge_exact=true` yet the authoritative total grew → an UNACCOUNTED consolidation/ambient re-drop path OR a telemetry double-count in the leg's elaborate multi-bucket accounting (persistent/timed/ambient/merge/removal-class). | a conservation-soak leg fails a "total == designated" gate by a small deterministic surplus (GROWTH, not shortfall), with every loss/removal bucket zero and the leg's own merge/sub-accounting reporting exact. FIRST ask: (a) is the leg EXTERNAL-authored / not fleet-vetted, and (b) does the fleet run-log show it EVER passing this gate at any prior tag? No prior pass + external origin + deterministic-bounded-dupe = INHERENT, not a regression introduced by the current checkpoint. Confirm the game-code delta since the leg's last-known state doesn't touch the conserved economy's mutate path (here: the ff28→checkpoint delta was a food-gather refactor + an item-STRIP test helper + a test assert — none touches stone pile-consolidation/ambient) | Mark KNOWN (this row) + file to BUG-INVESTIGATION-LOG; do NOT burn a bisect/rebuild to re-confirm inherency the run-log absence already establishes. DEFERRED DEEP-DIVE (commit it, tie to the owning capstone — here B5.5/M7, since exact conservation is a B5.5 acceptance criterion): instrument the merge/ambient conservation path to disambiguate a real-sim pile-consolidation/ambient DUPE (a genuine B-class conservation bug worth fixing) vs. a telemetry double-count in the leg's own multi-bucket accounting (a test-only fix). Sibling of B22/B47 (test-artifact-vs-real-bug classification) — this is the EXTERNAL-adversarial-leg / no-baseline-history counterpart: the run-log absence is the cheap disambiguator, not a rerun protocol. **★ EXE-GENERATION CAUTION (adversarial bug-hunt, 2026-07-18/19):** an OLDER exe (`bastion-harness.exe 7f087da317+dirty`, built 2026-07-16, an ANCESTOR of both this row's `ff2874b4b6` baseline and the `2244ce8d71` checkpoint) fails `b55-deep` at BOTH 1337 and 777 in a **DIFFERENT failure mode** than this row's registered one: `cycle_exact=false` + `cycle_work_progressed=false` (96 initial jobs, 192 repaint-created = 6 cycles × 32), while `mine_conserved=TRUE` (exact 1000→1000 — the +11 growth this row documents does NOT appear on this older exe) — different field names entirely, not a variant of the same signature. Confirmed deterministic (same-seed ×2 byte-identical, both seeds share the same failing values) and confirmed genuinely OLDER via git ancestry, not a fluke build. **Do NOT conflate the two modes when the deferred instrumentation lands** — the +11 conservation-growth mode is NOT universal across harness builds; the cycle-leg-red mode is this older build's own signature, with zero prior fleet run-log history in either shape (`cycle_exact`/`cycle_work_progressed`: zero prior hits) |
| B54 | **B16-family (wall-clock flavor): `SystemTime::duration_since(UNIX_EPOCH).expect(...)` at server startup — a pre-1970 host clock (dead RTC / misconfigured VM) panics the boot** (`server/src/metrics.rs:404-406` `.expect("Time went backwards")` at metrics registration; LOW, VANILLA, startup-only, host-clock-pathological — the sole finding of the 2026-07-18 secondary time-math sweep, CASE-009, which otherwise gave server+common+rtsim a clean bill: zero bare Duration-subtraction underflows, all Instant::duration_since sites saturating on the fleet toolchain, rtsim wall-clock-free) | grep `duration_since(UNIX_EPOCH)` followed by unwrap/expect — SystemTime::duration_since returns Result and a skewed host clock is the reachable Err | Chip: fall back instead of panicking (e.g. `.map_or(0, \|d\| d.as_secs())`; the codebase's own exemplary idiom is `bastion_flight_recorder.rs:637-640`'s `.ok()`). Specialization of B16 (same panic-on-unvalidated-time-input archetype, wall-clock flavor); evidence pointer = Bug-Tester's CASE-009 sweep report |
| B55 | **An emergency-only claim-exclusion gate silently also starves a non-emergency job sharing the same flag, when a distinguishing registry already exists but isn't consulted** (DPA leg-C stall, 2026-07-19, root-caused from `da5-1337.log`: `bastion_jobs.rs:12883`'s `if job.is_access && dist > 16.0 { continue; }` was built for the real-rescue deadlock case [run-12: a distant colonist hogging a trapped colonist's own escape rung] — correct there. DIG-PROVISIONED-ACCESS's proactive infrastructure ladders [`:12683-12695`, emitted with `emergency_owner: None`] carry the SAME `is_access` flag and get caught by the SAME 16-block cap; in a wide dig [6×6×12, 4 crew] nobody may be within 16 blocks of the next rung, so it's never claimable → the F3 pruner [`:10693-10722`, 20s colony-global idle timeout] kills it as stale → the descent-gate immediately re-emits the same need → infinite prune/re-emit churn, tape-confirmed [321 emissions, same 2 job IDs, for the full 6.5-min leg-C window], zero net dig progress past the point needing that rung) | when a claim-eligibility gate is commented as protecting a SPECIFIC scenario [here: real rescue-only]: does it check WHICH scenario it's actually in, or does it fire on every job sharing the same broad flag [`is_access`] regardless of origin? If a broader flag covers both an emergency case and a routine/proactive case, and a distinguishing registry already exists elsewhere in the same file [here: `board.emergency_access_jobs`, already used by the F3 pruner for exactly this split], the gate is very likely missing the same check | Fix: gate on the SAME distinguishing registry the sibling code already uses — `board.emergency_access_jobs.contains_key(id)` splits real-rescue (keep the 16-block cap, do not weaken) from DPA-proactive (no cap, or a much larger tunable one — it's not rescue-critical, any colonist can walk over). No F3 change needed; the pruner is correctly reacting to a genuine "nobody eligible" state, the eligibility rule upstream is the bug. Status: FIX IMPLEMENTED (commit `f187ec4e2a`, `emergency_owner.is_some() && dist > 16.0` — the rescue arm keeps the cap verbatim, proactive rungs uncapped), gate re-run PENDING (v6, both seeds, dig-access corpus). |
| B56 | **A scenario red at a NON-canonical seed is UNCLASSIFIED until the canonical gate seed is checked — a seed-premise gap, not automatically a code regression** (b4 gate finding, 2026-07-19: `--seed 1` reds with `arrived_enabled=0` at EVERY commit checked, including the pre-session fleet base `7b59588376` — i.e. it was NEVER green, at any point in history — while `--seed 1337` [the canonical b4 gate seed] is GREEN on full HEAD. Root: seed 1's worldgen site rolls ring-job terrain nobody can stand/path to — a seed-dependent terrain-placement issue, not a mechanism bug) | when a scenario reds on a seed you happened to try: is that seed the CANONICAL gate seed for this scenario, or an off-corpus/ad-hoc one? Check the canonical seed FIRST — if it's green there and the red seed has NEVER passed at any prior commit (run-log/history check), the red is very likely a seed-premise gap [pathological worldgen roll], not a regression. Sibling of B45 (spawn-lottery falsifier premises — same "stage/assert the roll" lesson, different mechanism: worldgen terrain vs. colonist stat rolls) and B47 (seed-dependent fixture preconditions recurring across gates) — this is the CANONICAL-SEED-FIRST triage step for the same general class | Validation runs should PIN per-scenario canonical gate seeds and check them first before triaging an off-corpus-seed red as a regression. Follow-up (small, unassigned): add a b4 spawn-premise assert so a pathological seed FAILS LOUD as precondition-unmet (the standing falsifier-asserts-own-precondition discipline) instead of reading as a mechanism red. Evidence: scratchpad `b4-*.log`, cross-commit-history check |
| B57 | **OWN-PREFIX SELF-HIT: a validator/preflight sweep against a route's OWN installed apparatus (its own already-built rungs) reads as a persistent blocked-approach, not as "arrived" — 4 confirmed sites, same signature every time** (M3A escalation, 2026-07-19: corridor-STEP validation, corridor-COMMIT validation, the mount-approach PREFLIGHT, and [site 4, same day, commit `cf837245da`] the corridor STEPPER's own runtime re-validation all independently hit this — a colonist approaching its own route's lane sweeps a capsule/cylinder that clips the route's OWN Ladder-sprite cell [confirmed instance: 16/17 preflight hits landed on exactly `{15860,16240,392}`, the route's own first rung; site 4's instance: a planned segment legitimately hugging the prefix produced a 1770-cycle commit→invalidate livelock — `committed`/`invalidated` counters BOTH at 1770, `corridor_present=false` at every sample, until the fix dropped it to `committed 1 / invalidated 0 / advancing 59`], and the validator treats that hit as a real obstruction rather than recognizing "this is MY OWN apparatus, not an obstacle") | for any NEW sweep-consuming validator/preflight added near emergency-route or constructed-apparatus code: does it distinguish a hit on the route's OWN installed cells [rungs/apparatus this exact route placed] from a hit on genuinely foreign terrain? Signature to watch for: a persistent reject OR a commit→invalidate livelock (matched counters cycling in lockstep) where the hit target is an access/apparatus cell OWNED by the same route the colonist is trying to use — that's this class, not a real blockage | Fix pattern (applied at all 4 known sites, site 4 via a shared `emergency_own_prefix_hit` helper — owner's Ladder-sprite access cells, snapshotted pre-borrow, checked at BOTH the stepper's endpoint-ready and edge-stale-invalidation touch points): a preflight/validator hit on THIS route's own access-cell registry [`emergency_access_cells`/equivalent, keyed by route ownership] should be treated as clear — physics/squeeze the colonist's own actual contact with its own apparatus, never treat "I'm touching my own rung" as a foreign obstruction; a hit on a NON-own-prefix cell still invalidates normally (real terrain-change detection stays intact). Audit any future sweep-consuming validator against this class at review time — four sites now, all found by the identical signature, strong precedent for a fifth existing somewhere unaudited |
| B58 | **TRACKED OPEN, tag-acceptable (not fix-before-tag): frontier-approach net-reliance at organic geometries (M3 seeds 21/42 class) is INHERITED M2-era behavior, not an M3-queue defect** (M3 24-run matrix, 2026-07-19: after B57 site-4 landed, seeds 21/42 still red on M3A's own zero-teleport fixture bar — 3-4 net deliveries per run — while fair order, zero same-tick double-ownership, and zero conflicts hold on all 24 matrix runs. **Discriminator (decisive, checkable):** `N2` — the pre-existing M2-era single-member contract, run at these SAME seeds — also nets 1-2 members each, and N2's own acceptance bar was always report-only on teleports, never zero. So the approach-chain gap M3A's stricter bar exposes already existed before M3's queue work; M3 didn't introduce or worsen it. The 3-4 M3A teleports vs N2's 1-2 reads as proportional [each of 3 members independently hitting the same per-member gap N2 already has], not contention-amplified) | when a new/stricter fixture bar reds on a mechanism that an OLDER, already-shipped sibling contract also exercises: does the older contract hold that same bar, or is it a NEW aspiration the current work is being unfairly held to? Check the older contract at the SAME seeds/geometry before assuming the newer work introduced a regression — a stricter self-imposed fixture target quietly becoming the tag bar is a scope-creep trap | Per the M3 packet's own acceptance criteria (`M3-CONTENTION-BUILD-PACKET.md`), the teleport net is an explicit, valid backstop for "a queued member whose turn never comes," not a failure condition — M3A's internal zero-teleport target was a fixture aspiration, not the packet's actual contract. Fixing the underlying M2-era approach-chain gap is real, valid follow-up work (candidate for the same corridor-unification treatment the promoted-head path got) but belongs in its OWN scoped block, not folded into M3's tag — same discipline as B37 ("its own follow-up block, not a rider") |
| B59 | **PRE-EXISTING RED (data-side, not Bastion code): `veloren-common`'s `ensure_item_localization` lib test fails at pristine HEAD — one or more items were added without a matching i18n manifest translation-id, and no gate currently catches it** (found in passing by Builder 4, 2026-07-20, while working an unrelated block; verified independent via `git stash` at bastion/builder `8e0e3bc03d` with zero working-tree changes — genuinely pre-existing, not caused by any of tonight's work). The test (`common/src/comp/inventory/item/mod.rs:2186`) iterates every real item via `all_items_expect()`, checks `ItemI18n`'s manifest has a text entry for each, and panics listing every item missing one — a real, legitimate completeness check, not a fragile/flaky test. Root cause not yet localized to specific item(s) — the failing item list prints in the test's own panic output on repro. **Why it went unnoticed:** recent gates (incl. the CRATESPLIT tag) only ran `cargo check --all-targets` + the `veloren-server`/`bastion-server` test targets — nobody ran `cargo test -p veloren-common --lib` recently enough to catch a manifest gap that data content (not code) introduced | after adding/editing ANY item definition: does its i18n manifest entry exist? `cargo test -p veloren-common --lib ensure_item_localization` is the direct check — run it, don't assume a new item ships with its translation row just because the item itself compiles. More broadly: a crate-split or any refactor gate should include `cargo test -p veloren-common --lib` in its scope, not just `check`, or content-only regressions like this one sail through invisibly | Repro: `cargo test -p veloren-common --lib ensure_item_localization` (the panic output names every offending item). Fix is small and low-risk (add the missing translation-id row[s] to the i18n manifest asset) but not yet localized to specific items — chip spawned for someone to run the repro, read the panic list, and land the manifest fix. Does not block any in-flight bastion work (data-only, `common` crate, zero overlap with the bastion-server/traversal tree) |
| B49 | **A CLI flag with no clap-level validation can reach the FULL boot sequence before panicking on a degenerate value — waste the whole boot cost before failing, instead of failing fast at parse time** (adversarial bug-hunt, 2026-07-18/19: `bastion-harness --tps 0` [or any tps ≤0/non-finite] boots the FULL world first [~70-74s worldgen], THEN panics at the first tick on `Duration::from_secs_f64(1.0/args.tps)` — 1/0=+inf → "cannot convert float seconds to Duration" [B16's Duration-conversion archetype, at the harness front door this time, not in-sim]. Contrast: `--seed 18446744073709551615` is REJECTED cleanly at parse time by clap's own range check, exit 2, 0.1s — the seed flag has exactly the validation shape tps lacks. Silver lining: exit code IS nonzero, so no false-PASS shape; the cost is pure wasted time, not a wrong result) | for any new CLI numeric flag that feeds a `Duration`/rate/divisor computation downstream: does clap validate it at PARSE time [range/positive/finite], or does invalid input only fail later at first USE, after however much boot/setup cost the harness pays before reaching that use site? A flag that can reach `Duration::from_secs_f64` or similar unguarded is a live B16 instance regardless of where in the pipeline it sits | clap-validate `tps` (positive, finite, sane range) and/or compute-and-validate `dt` BEFORE server boot, so bad flags fail fast instead of after a full worldgen boot. General rule for future numeric CLI flags: validate at parse time to match the `--seed` precedent already in the codebase, don't rely on the eventual panic to catch bad input |
| B50 | **`--data-dir` reuse across a DIFFERENT seed is silently accepted with no seed-stamp check, producing a franken-state that LOOKS coherent via reconciliation — the exact "silent corrupt-state acceptance" shape, laundered** (adversarial bug-hunt, 2026-07-18/19: `--seed 1337 --data-dir D` then `--seed 999 --data-dir D` — `data.dat` carries no seed validation; the run reports seed 999 with rtsim_tick 600 [continuing 1337's history] and a plausibly-reconciled world [sites 204→191, npcs 2355→2279, 1337's rtsim culled against the 999 world]. Exit 0, no error, no warning. Same-seed reuse IS at least detectable [`contam-b`: rtsim_tick(400)≠tick_count(200)]; cross-seed reuse is not. WAI-caveat: the help text already warns fresh dirs are "required for reproducibility," and scenario runs always create fresh internal dirs so they're immune — this is a manual/CLI-misuse-only exposure) | for any harness/tool that persists seeded state to a reusable directory: does the persisted state carry a seed-stamp that gets checked against the CLI's requested seed on load, or can a mismatched seed silently load and "reconcile" against stale data? A silent franken-state that still LOOKS plausible (via automatic reconciliation) is worse than an obvious crash — it can pass casual inspection | Cheap hardening for a determinism-gating test tool: stamp the seed into `data.dat` on write, hard-error on load if the CLI's `--seed` doesn't match the stamp. Low-medium priority given the existing help-text warning and scenario immunity, but a determinism tool silently tolerating cross-seed reuse undercuts its own credibility if a tester ever reuses a dir by mistake |
| B51 | **A test suite's OWN cumulative state across many sequential sub-tests can silently void an entire assertion class in "run everything" mode while the SAME assertion passes fine in single-item mode — always cross-check suite-mode failures against a single-item control before concluding the feature itself is broken** (adversarial bug-hunt, 2026-07-18/19, asset dynamic-test suite: `--asset-test all` → 76/80, with 3 structure failures [boathouse, dungeon_quarry_hall, housing_human_cottage] sharing ONE identical signature — marker fidelity and single-colonist reach/egress PASS, then `multi-occupancy-in`/`-out` FAIL with the identical detail "goto refused for Jena the Quiet," the SAME second colonist every time. `--asset-test structure_boathouse` ALONE = 1/1, all assertions green INCLUDING multi-occupancy. So the defect is SUITE-CUMULATIVE state [needs decay / leftover fixture state / a drive-arbiter claim surviving across ~40 prior asset tests are the candidate mechanisms], not a per-asset defect — yet suite mode is exactly the mode `ASSET_INTEGRATION_LOG.md` is written from, so this silently voids the whole multi-occupancy assertion class in the log tests actually rely on) | when a multi-item test SUITE fails an assertion that a SINGLE-item invocation of the same test passes cleanly: is the failure suite-cumulative (state leaking/degrading across prior sub-tests in the sequence) rather than a defect in the specific failing item? Always run the single-item control before filing the failure against the item itself — the fix target is "reset/re-stage state between sub-tests," not "fix these 3 assets" | Reset or re-stage colonists between asset sub-tests in suite mode (or investigate specifically why a colonist refuses `goto` after a long fixture sequence — needs-decay and leftover-claim are the leading candidates). Severity LOW given the asset lane is stood down, but flag: this silently voids a whole assertion class in exactly the mode the integration log is generated from, so it should be fixed before the asset lane resumes, not discovered again the hard way |
| B52 | **A placed asset's footprint can create a ONE-WAY pathing trap — the outbound route around it succeeds while the return route genuinely sticks, and the stuck POSITION is stable even though the watchdog TIMING varies with machine load** (adversarial bug-hunt, 2026-07-18/19: `sprite_orevein_velorite` asset dynamic test — outbound path around the placement succeeds [7.1s]; the return leg hits the stuck watchdog, best-remaining-distance 36.9, confirmed in BOTH suite mode [20.8s] and single-asset mode [12.3s, dist 36.8] — unlike B51, this one is NOT suite-cumulative, it reproduces standalone. The one genuine dynamic pathing failure found across 80 assets tested) | for a placed-asset dynamic pathing test: does the OUTBOUND route succeed while the RETURN route (same asset, same colonist, opposite direction) stalls? A stable stuck-position signature across repeated runs [even with varying watchdog timing] points at the asset's own footprint geometry creating an asymmetric/one-way pathing trap, not a scenario-timing flake | Asset-geometry/pathing fix, queued for whenever the asset lane resumes (currently stood down) — investigate `sprite_orevein_velorite`'s placement footprint for a return-path chokepoint the outbound route doesn't hit. Worth keeping precisely because it's the suite doing its job: the one real dynamic failure the 80-asset suite actually caught |
| B53 | **SUSPECTED, static-only: an unguarded `Duration::from_secs_f64` reader on an EVENT path, downstream of a command path that IS guarded — the guard on the well-trodden entry point doesn't protect every caller of the underlying event** (adversarial bug-hunt, 2026-07-18/19, static pass: `server/src/events/entity_creation.rs:728` `handle_create_aura_entity` calls `Duration::from_secs_f64(dur.0)` unguarded — the SAME B16-class panic on negative/NaN/±inf/overflow. The `/aura` command path IS guarded upstream [`cmd.rs:5255`, the ARCH-001 fix, keyed on `try_from_secs_f32(..).is_err()`], but the event READER itself is bare, so any OTHER emitter [ability paths, future callers, a malformed ability RON] reaching this event with a bad f64 crashes the server. No harness CLI surface reaches it with hostile input today — static/latent, not runtime-triggered) | when a B16-class Duration-conversion panic gets guarded at ONE call site [a command handler, a specific entry point]: does the underlying EVENT/reader itself also get guarded, or only that one caller? A guard on the well-known entry point doesn't protect a bare event reader that other, less-obvious emitters can still reach | Guard the reader directly [same `try_from_secs_f32`-style pattern the ARCH-001 command-path fix already established], not just the one command path that happens to call it today — defense-in-depth at the event boundary, not just the command boundary. Small, self-contained server fix, disjoint from other in-flight lanes; good fill-candidate for whenever a slot opens |

## B. DESIGN issues
| # | Class | How to detect | Fix / rule |
|---|---|---|---|
| D1 | **Re-implements a shared primitive** (a new dig/build/fell path that should call `carve_ramp`; a new search that should reuse `astar.rs`/`Chaser`) | code survey FIRST — does a primitive already do this? | Reuse-first; the fleet's best fixes were "it's carve_ramp again." One decomposer, N callers |
| D2 | **Two authorities write the same state / same id** (asset19 tool-gen clobbered asset36; duplicated invariant logic drifts) | two code paths mutating one resource/id | One authoritative owner per piece of state/id |
| D3 | **Mechanism designed too far ahead of its substrate** (speculative epics) | is the substrate at the frontier? | FRONTIER+1 — design just-ahead; feasibility-gate the hard ones |
| D4 | **Mechanic with no player-legible UI / feedback** (a verb the player can't see or trigger) | can a player SEE it work + invoke it? | Every mechanic needs a clear UI/feedback surface |
| D5 | **Escape-hatch / power with no cost** (emergency run with no penalty; a god-power with no favor cost) | is there a trade-off, or is it strictly-dominant? | Every strong option pays a cost; no free strictly-dominant verbs |
| D6 | **Drift from the architecture doc** (built ≠ documented invariant) | diff against BASTION_ARCHITECTURE + ledger consistency notes | Update the doc or fix the drift; flag consistency notes per tag |
| D7 | **Shared VOCABULARY not locked before emitters harden** (ChronicleEvent enum had ~10 kinds, corpus emits ~35; god-power catalog missing ~10 verbs — found in the gap-audit) | is this an enum/field that MANY systems write into? are all intended values enumerated? | Lock the FULL vocabulary early (like `Quality`/`Need`); a glyph/handler set follows the ONE lock, never forks per-system. Distinct from D1 (D1=reuse a primitive; D7=lock a schema) |
| D8 | **Asset-request skipped** (a design pass "done" without filing its assets → ~12 systems needed a gap-audit backfill) | does the pass have an ASSET_REQUESTS entry OR an explicit "asset-free" note? | Assets are a MANDATORY pass step — a pass isn't DONE until filed-or-noted-asset-free (and placed in the prioritized queue by build-proximity) |
| D9 | **Unbuilt dependency: crash vs graceful-degrade** (a system references a dep not yet built — DF-WOUND/DF-TAVERN — consumers must no-op gracefully, not crash/softlock) | what does this depend on that isn't built? do consumers degrade to a bounded self-resolving state? | DESIGNED-downstream tag + a graceful-degradation note for every unbuilt dep (a stub resolves the dangling ref); frontier+1 says write the full dep when ITS frontier nears |
| D10 | **4X / management-drift** (a system becomes MANDATORY management, or lets the god RUN the colony instead of influence it) | is zero-input / zero-policy STILL a complete healthy game? does the god TILT or COMMAND? | Autonomous-not-4X (the optional-thermostat guard) + influence-not-command (god tilts/blesses, never runs trades/moves units); the pillar guardrail |
| D11 | **OVERSTATED REUSE — a cited "reuse" symbol is a bare stub / unused / has no machinery** (feasibility class; the inverse of D1: a spec leans on `SharedChaser` for path-sharing, but it's `{nodes, goal}` with zero call sites, not even a registered Component — the NAME fits, the SUBSTANCE isn't there) | for every "reuse X" claim: grep X's call sites + read its body — does it have the machinery the plan needs, or just a fitting name? | Verify claimed reuse has real substance (call sites, logic, wiring) before pricing it as reuse; a fitting-named stub = build-from-scratch, re-rate the effort. The reviewer's job: confirm the reuse dividend is real |
| D12 | **Bastion feature MODIFIES a shared/vanilla struct instead of gating on `Colonist`** (adding a `portal_route` field to the shared `comp::Agent.chaser`, or any change to a struct all NPCs use, silently touches vanilla movement + wire/save for every entity) | does this edit a struct/system vanilla NPCs also use? is the new behavior gated on `comp::Colonist`? | Bastion behavior composes as Colonist-GATED wrappers/parallel components, never by mutating shared/vanilla structs — the ladder-waiver, soft-collision, tool_factor all gate on Colonist; vanilla stays byte-identical (the isolation invariant) |
| D15 | **Overloaded-BlockKind conflation — a naive kind-match hits semantically-different structures sharing the kind** (`BlockKind::Wood` is BOTH trees AND worldgen buildings/gnarling, so "chop every Wood" fells house walls; `Leaves` is mostly trees but also gnarling-moss/giant_tree — a kind-only selection can't tell them apart) | does the selection key off a BlockKind that worldgen reuses for unrelated structures? grep worldgen for the kind | Discriminate STRUCTURALLY, not by kind: seed from an authoritative oracle (`tree_valid_at`), connected-component with a must-contain-companion-kind gate + size-cap, and/or exclude cells inside a site's building PLOT. The colony's own builds are a distinct kind (Rock) — lean on that. Ties material-semantics (ASSET #16) |
| D14 | **Type-level "impossible by construction" invariant with a LEAKY variant** (the guarantee rests on the enum SHAPE — e.g. `PowerEffect` has no `Command` variant so a god-power "can't command a unit" — but another variant can express the forbidden semantics: `MindWrite`/`RtsimAct` writing `rtsim_controller.activity`/an ActiveJob IS a command in influence's clothing) | for every "X is impossible because the type has no Y variant" claim: audit each REMAINING variant's latitude — can any of them express Y's forbidden semantics? | The shape-invariant is necessary but not sufficient; enforce the semantic constraint INSIDE the permissive variants (MindWrite writes only mood/urgency the arbiter weighs, never activity/destination/job). Impossible-by-construction must cover every branch, not just the absent one |
| D13 | **Abstraction/LOD/culling FLATTENS the vertical deep** (our defining axis — a chunk-portal hierarchy that models only horizontal chunk-border portals mis-abstracts a ladder/shaft INSIDE one column; occlusion/camera/pathfinding all must respect z) | does this abstraction preserve intra-column vertical connectivity (ladder z10↔z20 in ONE chunk = cluster-internal, not a border portal)? does the deep survive it? | The vertical deep is load-bearing for a mining colony — every abstraction (nav hierarchy, occlusion, camera slice, LOD) must model vertical connectivity as a first-class edge, not collapse it to the surface plane |
| D16 | **Disabling/removing an access-PROVIDER silently starves a GATE that depended on it** (flagging `AUTO_LADDER_ACCESS` off removed the auto-access that ACCESS-BEFORE-DESCENT waits for → the gate then held every depth>2 cell UNMINEABLE wherever stairs don't fit → tight deep digs stalled at depth 2, 75/150 blocks; the provider and its consumer-gate were coupled but the flag touched only the provider) | when you flag-off / remove a PROVIDER (an access builder, a resource source, a job emitter), grep its CONSUMERS — who waits on / gates behind what it produced? does any gate now wait forever for something that can no longer be produced? | Audit a provider's consumers when you disable it: co-disable the dependent gate (tie both to the same flag) OR give the gate a fallback. Here: when no access is buildable AND the auto-provider is off, RELEASE the descent gate and rely on the universal teleport for egress (entombment stays impossible by construction — the redundant protective purpose is covered by a stronger backstop). A flag that silently half-breaks a downstream mechanism is worse than the bug it fixed |
| D18 | **A shared coordination/allocation field calibrated for ONE distribution pattern (sparse, one-worker-per-cell) gets overrun when composed with a DIFFERENT job generator that clusters densely (many same-target jobs in one coarse cell, or spread across Z if the field is XY-only)** (COORDINATION's saturation field (`coord_cell`, XY-only, ignores Z) calibrated its DEPOSIT/DECAY/WEIGHT constants against a single steady worker's equilibrium (~20 → ~15 score-penalty, "never crush the top-down ordering"); CHOP's whole-tree fell-set naturally puts a multi-colonist crew's jobs into the SAME 1-2 coarse cells (canopy spans up to 40 Z, all bucketed to one XY cell), so an N-colonist crew saturates 2-3× past the design ceiling and risks getting steered off a partially-felled tree — reviewer R10-F1) | when a new job-generator produces MANY same-target jobs clustered in one coarse allocation cell (a tree, a large stockpile pile, any multi-block single-structure target): does the existing coordination/dispersion field's calibration assume ONE worker per cell, or does it hold under N workers cooperating on one target? | Exempt jobs sharing one designation/target-id from the crowding penalty (a committed crew finishing ONE target is cooperating, not crowding), key the coarse cell off the target/designation id rather than raw position, or extend the cell to 3D so a tall dense cluster doesn't collapse into one XY bucket. Recalibrate against a dense-cluster test case, not only a sparse-frontier one |
| D20 | **A GENERATOR's own output can become its NEXT input, mined/consumed/re-triggered by the same generator that made it, unless the generator explicitly excludes its own past products** (AUTON-1 tag: the mine generator digs any exposed Rock near home; a RETIRED build plan's finished platform is now exposed rock too, so the generator would happily dig through the LAST plan's building to satisfy the NEXT plan's material demand. Worked around with a skip-column list [plans+stockpiles+farms+beds+built_xy], but `built_xy` for player-PAINTED constructions has no real registry yet — an honest edge, not fully closed) | for any self-designation/autonomous generator that scans world state for candidates (mine exposed rock, haul loose items, harvest ready crops): can the generator's OWN completed output re-satisfy its OWN candidate predicate on a later pass? If a generator both CREATES a structure and SCANS for raw material by a purely physical/kind-based test (exposed + rock-class), it will eventually eat its own prior work unless something excludes it | Any generator whose output could pass its own candidate filter needs an explicit exclusion registry (a footprint/skip-set) — don't rely on the candidate predicate alone to stay disjoint from past output. This one's still an OPEN edge for player-painted constructions specifically (only generator-built structures are tracked in `built_xy` today) — a general built-structure footprint registry is the real fix, filed as a future row rather than solved here. Sibling of D2 (two authorities on one state) — this is a generator becoming an unwitting SECOND authority over its own prior output |
| D19 | **An UNDESIGNED interaction between a vanilla homeostat and a Bastion mechanic silently drains Bastion's population into vanilla's over long soaks** (PATH-0 premise investigation, 2026-07-13: vanilla's Architect rule [`rtsim/src/rule/architect.rs`] respawns dead roles toward a worldgen-frozen `wanted_population` ceiling ~24 in-game-hours after death — real, already-shipped replenishment machinery. But colonists are architect-TRACKED citizens [`Role::Civilised`, home = nearest site], so a dead colonist's role gets respawned as a PLAIN VILLAGER [`Npc::new` without `with_bastion_colonist`] — colonist-N monotonically DECREASES over time while total site population homeostats back to the ceiling. Invisible today [harness scenarios run minutes, live colonies haven't died much], but real for any long soak or eventual live colony] | for any Bastion entity type that's also tracked by a VANILLA population/homeostat/respawn system: does dying route back through that system in a way that loses the Bastion-specific marker/component? does the vanilla system have its OWN replenishment logic that doesn't know about Bastion's wrapper? | Either (a) hook the Architect respawn to re-attach `with_bastion_colonist` when replenishing a role that was colonist-held (keeps colonist-N stable), or (b) treat this as an intentional attrition mechanic and design around it explicitly (a colony that loses colonists to the world over time is itself a DF-flavored consequence — a designer call, not an accident to silently ship). Either way, it needs a deliberate decision once colony lifetimes get long enough to matter (B-AG6/settlement-growth's natural landing point, since that block already touches the Architect-rule seam) |
| D17 | **OVERSEER-mode reuse of a FIRST-PERSON / screen-center picking primitive gives SCREEN-CENTER, not the cursor** (the god-hand plan [FR16 Q3] anchored the hand to `targets_under_cursor(cam_pos, cam_dir).position` — but that shoots ONE camera-FORWARD ray = screen center; the bastion overseer runs a FREE cursor DECOUPLED from cam_dir AND an ORTHOGRAPHIC camera where even the ray ORIGIN varies per pixel, so a single cam_pos+cam_dir can't represent the cursor → the hand would peg to screen-center, visibly detached from the mouse) | for ANY overseer feature that reads "what's under the cursor" (hand anchor, hover-highlight, tooltip, god-power aim/target, context menu): does the primitive shoot a SINGLE `cam_pos + cam_dir*t` ray (first-person = cursor locked to screen-center under perspective), or unproject the MOUSE screen-pos through the ortho inv-view*proj? The overseer is ORTHO + free-cursor — a cam-forward ray is the WRONG picker | Use the overseer's OWN cursor helpers — `bastion_point_under_cursor`→`unproject_to_world_plane` (`session/mod.rs:369`, terrain, sky-safe via the plane) / `bastion_cursor_block` (`:464`, surface snap) / `bastion_pick_entity`→`cursor_ray` (`:422`, `bastion/mod.rs:236`, entity) — per-pixel ortho unproject of the mouse, O(1). NOT the first-person `targets_under_cursor` (which is also range-gated ≤5.0 to the player eye, so Build/Mine/Collectable rarely resolve in the overseer anyway). Sibling of the derive-from-camera-mode rule (`bastion_context` `:313`): overseer reuse must be OVERSEER-NATIVE, not a first-person primitive |

## C. PERFORMANCE issues
| # | Class | How to detect | Fix / rule |
|---|---|---|---|
| P1 | **Unbounded per-agent compute, no per-tick budget** (per-agent A* that chokes as population grows) | cost scales super-linearly in N; frame time grows with colonists | Global per-tick budget + scheduler; reuse the incremental poll (PATH-0) |
| P2 | **Full recompute where incremental repair suffices** (re-search a whole path on any terrain change) | recompute cost on a small local mutation | Dirty-cluster + re-refine only the broken leg (hierarchy localizes the damage) |
| P3 | **Many-to-one hot goal not shared** (every colonist paths to the depot/dig-face independently) | N searches to 1 goal | Shared route / flow-field for hot common goals |
| P4 | **Watchdog/timeout burns cycles on a no-benefit path** (STUCK_TIMEOUT grace spent on TERRAIN-blocked stalls where soft-collision can't help; re-arms per re-claim) | timeout fires where the mechanism can't act; latency compounds | Gate the grace to cases it can actually resolve (density/cause-gated) |
| P5 | **Build/iteration cost** (28-min cold build; exe-relink lock on Windows) | RUSTC_WRAPPER unset; relinking a running voxygen | sccache wired; never relink the running exe; play-tester owns builds |
| P6 | **Wide regression window hides a perf regression** (500-tick part-(e) window would pass a 2× rescue-latency regression silently) | a lenient budget/window that a slowdown fits inside | Tighten windows to the real budget so a 2× regression fails loudly |

## D. GAMEPLAY-LOOP issues
| # | Class | How to detect | Fix / rule |
|---|---|---|---|
| G1 | **Loop doesn't close** (a verb with no payoff; a resource with no sink; produce with nothing to consume it) | trace the loop end-to-end — does output feed back into input? | Every loop closes: action → consequence → new choice |
| G2 | **Tedious friction vs fun friction** (micromanagement the autonomy should handle) | while playing, is the friction interesting or busywork? | Automate tedious friction; keep the interesting decisions |
| G3 | **Pace wrong** (instant dig felt cheap → needed deliberate pace + tool progression) | does the action feel weighty/rewarding, or trivial? | Deliberate pacing + progression (tools/skill) gives weight |
| G4 | **Autonomy-breaking stall** (a stuck/entombed colonist shatters the "it plays itself" fantasy — Ben's #1) | watch colonists over a long run; any stuck/idle-forever | The autonomy fantasy is sacred — a colonist must never be permanently stuck (ties B2/B3/G4) |
| G5 | **No feedback that an action worked** (time controls that don't visibly change anything) | does the player get immediate legible confirmation? | Visible, immediate feedback for every player action |
| G6 | **Missing emergent verb** ("I wish I could…" moments the rendered game begs for) | play-tester creative pass — what does the game WANT? | Surface as a suggestion → design lane; don't force, but notice |
| G7 | **Re-target churn without commitment hysteresis** (an agent re-selects its best target every arbitration cycle before completing the current one → visible in/out bob; mine-oscillation: 219 claims/150 jobs, claim cadence ~0.4s ≪ completion ~1.9s, reads as "running in and out of the mine") | count claims-vs-completions in a work window (ratio ≫1 = churn); claim cadence == arbitration interval; agent never commits long enough to finish | COMMITMENT HYSTERESIS — once claimed + making progress, don't re-select until Arrive+act or genuine fail; make staged access-anchor STICKY; dispersion/scoring biases INITIAL pick only, never re-targets a committed worker (the R3 hover-tail hysteresis applied to CLAIMS; ties the pathfinding-bob B-class) |
| G7 | **Write-only sink / no read-back** (a system RECORDS/emits but nothing CONSUMES the record — the chronicle before the reputation read-back; memory that affects nothing) | is the recorded/emitted data ever READ BACK into behaviour, or is it a dead log? | Every record needs a reader; close the write→read loop (deeds→standing→treatment — the 4-faces lesson). The sharpest, most common variant of G1 |
| G8 | **Keystone-dark** (a designed system can't close its loop because its keystone half is unbuilt — B7 needs gates the payoff of ~6 systems; the producer ships but nothing consumes) | what keystone (B7 needs / B8 defense) must exist for THIS loop to actually DO something? | Sequence the keystone first, or flag DARK-until-keystone; don't ship the producer half alone expecting a live loop |
| G9 | **Unbounded pressure / soak-law break** (a threat/night/climate/curse not scaled-to-colony + capped → attrition, not drama; a tended colony can't survive it) | does a TENDED, defended colony survive this UNTOUCHED (the Tier-1b soak law)? is it capped? | Bound every pressure to colony scale + cap it (prestige threat-pairing); drama not a nightly tax; the zero-input colony must endure |
| G10 | **Permanent softlock / unrecoverable loss** (a knowledge lost forever, a curse unliftable, a colony-death dead-end, a colonist un-stickable) | is there a REACHABLE recovery/relief for every loss/pressure? | Every loss is recoverable (re-learn / lift / appease / reclaim / un-stick); no permanent softlock. Composes with G4 (stuck) — the same rule at the loss layer |
| G11 | **Earned vs granted** (standing/alignment/reputation/knowledge set by a TOGGLE instead of earned by recorded deeds → kills the emergence + the B&W soul) | is this earned by PLAY (recorded deeds) or set by a switch? | Earned-by-deeds, never chosen; the drift IS the story (the hand's alignment, the epithet, reputation — all earned, reversible, legible) |
| G12 | **Self-correcting feedback that CONSUMES its own recovery labour** (the death-spiral trap: food-shortage raises Survive-urgency so colonists go EAT — but past a depth, EVERYONE spikes and nobody works the FARM jobs, so the "labour auto-shifts to food" recovery starves itself) | does the corrective feedback depend on a resource (labour/attention) the crisis itself drains? at what depth does recovery stop being possible? | Stagger the response (trait-modulation makes the hungriest/greediest spike at different thresholds so SOME keep producing) + gate must assert recovery in the RECOVERABLE band and graceful-degrade (not freeze) only PAST it — test a DEEP shortage, not a mild one. The E1 death-spiral criterion made concrete (composes with E1/G4/G10) |

---
## E. GENRE LESSONS — our genre = COLONY SIM × GOD GAME (from studying the field, 2026-07-10)
Reference titles: Dwarf Fortress, RimWorld, Oxygen Not Included, Going Medieval, Songs of Syx, Timberborn
(colony sim); Populous, Black & White, From Dust, Reus, WorldBox (god game).
| # | Lesson | Source | Apply to us |
|---|---|---|---|
| E1 | **Prevent DEATH SPIRALS — the player PLANS, doesn't FIREFIGHT** (autonomy that lets a colony recover; forced micromanagement kills the fantasy) | libcolony; DF/RimWorld | Autonomy-arbitration (drive scoring) must avoid unrecoverable spirals + minimize forced micro — the "it plays itself" promise IS this lesson. Ties G4/G10/D10 |
| E2 | **Emergent story via APOPHENIA — let the player's brain generate the narrative** (agents get freedom in HOW they execute; legible traits/moods/relations let players read stories in) | DF vs RimWorld storytelling | Agent-culture (stats/behaviour/history/relations/language) surfaces LEGIBLE traits + lets agents choose HOW, doesn't over-script outcomes — feeds object-inspection/dialogue UI + G11-earned drift |
| E3 | **INDIRECT CONTROL is the god-game pillar** (influence via environment/incentives/miracles; never command individual units) | Populous, B&W, From Dust | Never add a select-unit-and-order crutch; god-verbs shape terrain/incentives (hand, powers). Reinforces D10 (tilt-not-command) |
| E4 | **"Good enough" pathing is CORRECT, not a bug** (RimWorld deliberately picks good-enough paths to save CPU) | RimWorld routing | Validates PATH-0's budget: don't chase optimal; the invariant is "never stuck/entombed" (G4/B1-B3), NOT "shortest path." A suboptimal route is not a defect |
| E5 | **FLUID is a GOD VERB, not just a sim** (From Dust: manipulate water/lava/soil FLOW to guide people + avert disaster — the fluid IS the gameplay) | From Dust | DF-FLUID = a god-power (channel water to fields, divert lava, flood attackers), not just terrain flavor. Design the sim + the god-verb together |
| E6 | **CA fluid: choose the pressure model deliberately** (ONI/DF use cellular automata — Navier-Stokes too costly; one-material-per-tile; DF adds pressure so water climbs, DwarfCorp omits it so water only falls) | ONI, DF, DwarfCorp | The DF-FLUID spike's real fork = CA-WITH-pressure (richer/costlier, water equalizes+rises) vs WITHOUT (cheaper, falls/spreads only). Decide by the god-verb we want — confirms the designer's CA rec |
| E7 | **UI legibility is a first-class FEATURE, not polish** (DF's opacity vs RimWorld's streamlined UI = the accessibility gap that gates reach) | DF vs RimWorld | Object-inspection + dialogue + action-bar are genre-critical: sim depth is worthless if unreadable. Ties D4/G5 |
| E8 | **Work ALLOCATION ≠ job RESERVATION — a large task needs spatial PARTITION or you get the SWARM** (per-job claim stops two workers on one CELL but not the whole crew piling one big designation; RimWorld's swarm is a known complaint; DF BURROWS + RimWorld ZONES are the *manual* spatial-partition tool) | DF burrows, RimWorld zones/swarm | COLONIST-COORDINATION = auto-sector-partition (the automatic burrow): partition a large designation into sectors, one colonist per sector, top-down WITHIN the sector. **Pitfall: coordination-churn** — the partition/rebalance must be commitment-bounded (sector-commit until exhausted) or it reintroduces the B6/AR-2 claim-bob. Ties G4/D10 + the AR-2 anti-bob work |
| E9 | **EMERGENT beats CENTRAL for division of labour — reach for REAL-WORLD SIM (ant colonies) not just games** (ants organize with NO controller via STIGMERGY [decaying environmental marks] + RESPONSE-THRESHOLD [act when local stimulus > threshold; task-performance lowers the stimulus → auto-rebalance]; Bonabeau/Theraulaz, adapted to swarm robotics). Games mostly do central/manual (Factorio bot-dispatch, RimWorld zones); nature does emergent | ant stigmergy + response-threshold (real-world sim); Factorio/ONI | Our B6 dispersion-repel is already a crude one-shot stigmergy → generalize to a DECAYING SATURATION FIELD + threshold = emergent division, robust (no orphan-sectors), SMALLER than a central scheduler, and the dialogue NARRATES it. **Determinism-safe iff terrain/board-only + once-per-cycle** (like B15). The PRIOR-ART-FIRST rule (survey games AND sim/robotics/CS lit before designing) is now standing |
| E10 | **The colony brain is a LAYERED architecture — assemble the field-proven stack, don't invent** (blackboard → coordination → decision → execution → movement → memory). Shipped-AI consensus: UTILITY decides (multi-input survival gameplay) + BEHAVIOR TREE executes + optional GOAP/HTN sub-planners for deep sequences | The Sims (utility), Veloren/F.E.A.R. (BT), Anno (HTN chains), BDI (intention=commitment) | AGENT-SYSTEMS-RESEARCH.md: utility(AUTON) decides → Veloren's BT executes → on the job-board blackboard → stigmergy coordinates → chronicle remembers → ORCA moves. Every layer = an existing/planned system. **Adopt RimWorld's work-priority GRID as the readable allocation UI.** intention=commitment (BDI) = our claim-commitment anti-bob |
| E11 | **SCRIPTED beats LEARNED for the RUNTIME brain; bleeding-edge belongs in the AUTHORING room** (a deterministic real-time sim needs legible/tunable/cheap behaviour → utility+stigmergy+BT win; LLM/MARL are non-deterministic, expensive, black-box — they fight determinism + designer-control). BUT ★ adopt the generative-agent STRUCTURE (memory→reflection→plan) DETERMINISTICALLY | Stanford generative agents (Park 2023: "believability depends on the MEMORY architecture, not the model"); MARL | AGENT-SYSTEMS-RESEARCH.md §4: CHRONICLE=memory-stream, chronicle-derived standing/mood/domain=reflection, utility-arbiter=planning → generative-agent believability WITHOUT the LLM (cheap+deterministic). LLM/MARL = OFFLINE authoring/tuning only (baked, determinism-safe), NEVER the tick loop |

---
## COMMON QUESTIONS TO ALWAYS ASK (the pre-flight / review checklist)
**Correctness** — What's the boundary/±1 case (reach == rise)? Does this guard run the AUTHORITATIVE check or a
cheap proxy that can lie? Can this safety net WEDGE ITSELF? What happens with TWO simultaneous instances? Any
unwrap/panic/overflow on runtime state? Is it deterministic under load — and does any NEW shared state sit inside a
`par_join` whose determinism only held because the work was independent (B10)?
**Feasibility (spec pre-flight, before it reaches the builder)** — Are the spec's CODE claims actually true (verify
against source, don't trust the survey)? Does each claimed "reuse" have real machinery + call sites, or is it a
fitting-named stub (D11)? Does the approach touch a shared/vanilla struct instead of gating on Colonist (D12)? Does
the abstraction preserve the vertical deep (D13)? Is the effort estimate honest once the hard seam (determinism,
sync, decoupling) is priced in? What's the cheapest 80%-value alternative? Verdict: FEASIBLE / -WITH-CHANGES /
NOT-AS-SPECCED.
**Reuse / design** — Does a shared primitive already do this (`carve_ramp`, `astar.rs`, `Chaser`, `BlockChange`)?
One authority or duplicated? Does it drift from BASTION_ARCHITECTURE? Is this frontier+1 or speculative? Is a
shared VOCABULARY (enum/field N systems emit into) locked to its FULL range before emitters harden? Are its
ASSETS filed (or noted asset-free)? For every unbuilt dependency, do consumers DEGRADE gracefully? Is zero-input
still a complete healthy game (autonomous-not-4X; god tilts, never runs)?
**Testing** — Is there a UNIT test on the pure boundary function? A regression scenario for THIS exact bug? A
metamorphic property (more access ≤ entrapment; better tool ≤ dig time)? Did the fix ship WITH its test? Does the
scenario prove the MECHANISM, or does it pass via a scenario-specific motivator/workaround (continuously-fed jobs,
teleports, hand-placed anchors) that wouldn't hold in real play — and if so, is the masked gap logged + tracked?
**Performance** — Is there a per-tick budget? Full recompute or incremental repair? Does cost stay ~linear in N?
Is a hot many-to-one goal shareable? Does the regression window fail on a 2× slowdown?
**Persistence** — serde-default safe? wire/save-compat noted? rtsim promote/demote with no dupe/loss/desync?
**Gameplay** — Does the loop CLOSE? Is the recorded/emitted data READ BACK (no write-only sink)? Is the loop
DARK until a keystone (B7/B8) is built? Is the pace deliberate? Is there player-legible feedback? Does a stuck
agent break the autonomy fantasy? Does the strong option pay a cost? Is every pressure bounded + soak-survivable?
Is every loss RECOVERABLE (no permanent softlock)? Is standing/alignment EARNED by deeds, not granted by a toggle?
**Player legibility (UI)** — Can the player SEE it work and INVOKE it? Is there an action-bar/HUD surface for it?

*Seeded 2026-07-10 from real finds (annulus off-by-one, F2/F3 guard-wedges, time-controls-admin-gate,
window-wedge, pathfinding-scale, watchdog-grace). Append-only — every new class earns its row.*

*Curator appends — 2026-07-10 (Build-Reviewer): +B10 (shared-state-under-par_join determinism), +D11 (overstated
reuse / stub), +D12 (Bastion touches vanilla vs Colonist-gated), +D13 (abstraction flattens the vertical deep),
+Feasibility checklist row — all from the FR1 pathfinding + FR2 fluid feasibility reviews (BUILD_REVIEW_LOG §FR1/FR2).
+G12 (self-correcting feedback consumes its own recovery labour — the deep-shortage death-spiral trap) from FR3
autonomy-arbitration. +B11 (partial save-back loses loaded-tier mutations — the LOD-seam persistence death) from
FR4 lod-persistence. +D14 (type-level invariant with a leaky variant — the "impossible by construction" claim must
cover every branch) from FR5 god-powers-dispatch. +B12 (real-time-anchored rate coupled to the timescale/day-length knob + TimeOfDay/DeltaTime
clamp-desync) from FR6 timescale-design. +B13 (implicit loaded-chunk assumption broken by a top-down/remote
trigger) from FR7 build-mode. +B14 (reset-prone accumulator — the reset races/starves the threshold; R3 stuck_time +
R5-F5 churn.1, confirmed recurring) from the B6 SOFT-0 reviews. +D15 (overloaded-BlockKind conflation — Wood = trees
AND buildings; discriminate structurally) from FR10 chop-redesign.*

*Curator dedup — 2026-07-11 (Build-Reviewer): the architect-added "claimability/exposure gate admits UNSTANDABLE
work" class collided on the B10 number (B10 is already shared-state-under-par_join, referenced across the log/FR3/R7b)
→ renumbered the new class to B15, kept both. Numbers are stable once referenced; new classes take the next free slot.*

*Builder append — 2026-07-11 (LADDEROFF / Build 1): +D16 (disabling an access-PROVIDER silently starves a GATE
that depended on it) — surfaced while measuring the auto-ladder-off deep-dig throughput: the ACCESS-BEFORE-DESCENT
gate held depth>2 cells unmineable once the auto-ladder fallback was off (tight 5×5 stairs don't fit → plan_access
None → gate never releases → 75/150). Fix shipped in the LADDEROFF tag: release the descent gate when
AUTO_LADDER_ACCESS is off AND no access is buildable; the universal teleport is the egress. Architect-endorsed;
fully reversible with the flag. (Reviewer: curate/renumber as needed.)*

*Builder append — 2026-07-11 (SLOPE / Build 2): **B15 CLOSED** — bastion-block-SLOPE
gates Mine claimability on a TERRAIN-ONLY once-per-cycle STANDABLE stance
(has_standable_stance): prefers on-top, routes a wedged +1-slot to its reachable
downhill adjacent stance, clean-skips an isolated 1-wide floater (no reachable
stance → no churn). The exposed→standable swap is regression-safe (b58 150/150;
a first cut that preferred adjacent regressed to 87/150 — on-top-preferred + the
≥3-solid-side wedge check is the fix). Play-tester confirmed the class on a real
2:1 slope (65/75 give up pre-fix, 44 exposed-unstandable = the target; a solid
slab yields 0 floaters — floaters need undercut/overhang). B15's "natural-slope
in the test matrix, not flattened" rule now has b5 phase 7.9 (claim-level unit)
+ the play-tester's --slope-mine-scenario/--floating-block-scenario (SET-A/SET-B)
as the regression. (Reviewer: curate.)*

*Bug-Tester append — 2026-07-11 (BUG-LOG CASE-001, inaugural): +B16 (signed float → unsigned `Duration` —
`from_secs_f*` on an unbounded-below value) — the negative-`Duration` panic archetype the charter names, from
Ben's real alt-tab crash in `Clock::game_dt`. Root-caused to the missing LOWER clamp on the game-dt nudge
(`.min(MAX_GAME_DT)` should be `.clamp(0.0, MAX_GAME_DT)`); classified VANILLA/upstream (clock.rs unmodified from
the B0 baseline; bastion time-controls don't feed the client Clock). Routed to Reviewer (confirm) + Architect
(Build-3 bugfix batch). (Reviewer: curate/renumber the next free B-slot as needed.)*

*Builder append — 2026-07-12 (COORD tag, process class for curation): **VERIFY-TAG-TARGET-AFTER-PLACING** — a
`git merge -F -` misfire (stdin not supported) left HEAD unmoved while the subsequent `git tag` placed the tag on
the WRONG commit (the docs commit, no feature in it); caught because the post-tag verification re-read
`rev-parse` + the code-identity diff. RULE: a tag is only placed AFTER the merge commit is verified (`log -1`
shows the merge; identity diff vs the gated commit runs CLEAN), and the tag's target is re-read (`describe`/
`rev-parse <tag>`) before any announcement. Merge messages always come from a FILE (-F <path>), never stdin.
(Reviewer: promote/number as a process row.)*

*Builder append — 2026-07-12 (CASE-003 repro, for B19 curation): the wedge REPRO’d (seed 21, DETRNG
sweep) and the mechanism is NOT the B19 soft-collision pinch — nearest other colonist was 84 blocks away.
Confirmed chain: ULTIMATE FAIL-SAFE teleport → `surface_teleport_dest` accepted `column_surface_z+1` with
NO air check while the scan deliberately sees THROUGH non-surface solids (Wood) → colonist teleported INTO
the tree standing on the returned surface → phys locked it (see correction below) → stuck-watch re-fired to
the SAME deterministic cell (permanent teleport→wedge loop). TWO corrections for the row: (1) B19’s
"`box_voxel_collision` exits STILL PENETRATING on exhaustion" is inaccurate — at `collision.rs`
`attempts == MAX_ATTEMPTS` it REVERTS `pos` to tick-start and ZEROES velocity, i.e. it never CREATES an
embed mid-tick but it PERMANENTLY LOCKS an embed written by any non-phys writer (teleport/eject/spawn) —
the persistence mechanism, arguably its own class: "the resolver revert-lock preserves externally-written
invalid state forever." (2) The soft-collision pinch produced ZERO trips across 24 deterministic seeds —
downgrade to WATCH; the LIVE class is "a programmatic pos writer whose destination picker does not verify
TRUE STANDABILITY (feet+head air)" — the R8/F-CAVE-1 sibling, now fixed in `surface_teleport_dest` (skip
occupied columns) + belted by the phys per-tick CENTER-SAFETY-NET on the ONE shared `eject_dest`
(`common::bastion`, `CENTER_NET_FIRES` reported telemetry). Rule for the checklist: EVERY programmatic
position write verifies feet+head air at the destination or routes through `eject_dest`. (Reviewer: fold
into B19 / split a new row as you see fit.)*

*Bug-Tester append — 2026-07-12 (BUG-LOG CASE-004, architect-directed): +B20 (a single-tick detector/belt fire ≠ a
PERSISTENT bug — gate on N-consecutive-tick persistence before naming a writer). From my own CASE-004 misattribution:
I read the chokepoint `CENTER_NET_FIRES` (seeds 7/8/1337; correctly-MEASURED zero-delta / 1-block-headroom / fractional-XY
signature at the CASE-003 tag) as a persistent ladder-magnet embed and marked it SETTLED — but belt-v2 (relocate only
after 30 CONSECUTIVE ticks core-in-solid) → net=0/0/0 proved they were dig-pocket TRANSIENTS the old single-tick belt
over-fired on. The signature matched the transient class too → correct data, wrong attribution. Method lesson = run
Opus's CASE-003 PERSIST-vs-RECOVER discriminator BEFORE naming a cause. Pairs with B19 (revert-lock ⇒ a real embed is
persistent, so a persistence-gated detector separates them). The magnet `:1599-1610` code gap Opus confirmed is
real-but-LATENT (row 31.1 = Opus's defensive-gate call, part of belt-v2). (Reviewer: curate the B20 slot / merge with
the B19 persistence note as you see fit.)*

*Builder append — 2026-07-17 (FREE-CLIMB-CAP / F5 investigation, three classes for curation): (1)
BELOW-GRADE BOUNDARY OSCILLATION — a colonist stalled in the LAST 3 blocks of his exit (cap-denied
climber, half-dug tunnel) sits inside `!below_grade` (dz<3) and the watchdog WIPED his backstop clock on
every approach (measured: a 52s clock wiped at the boundary, 13 oscillations, 200s never rescued). Fixed:
near-surface wipes only when GROUNDED; airborne near-surface FREEZES. The general class: any
timer-guarding predicate evaluated at a spatial boundary needs the E2/churn treatment (freeze ≠ reset) —
sibling of "employed must not reset but must not accrue". (2) GLOBAL CLAIMING STARVES JOBLESS-ONLY
DETECTORS — the trapped-detector's still-loop joins `!&active_jobs`, and ANY claimable job anywhere keeps
a trapped colonist employed via claim-churn, so he never verdicts (F5 rev-1: the decoys never existed
because the bait meant to distract colonist A employed colonist B; the scenario tested α twice and A2
never). Scenario-design rule: anything requiring a VERDICT must sequence its jobless window BEFORE any
claimable job exists. Code-watch: any future jobless-only detector has this class by construction. (3)
SILENT-CONTINUE STARVATION — the egress request loop had two unlogged `continue`s (route-adopt,
one-plan-at-a-time swallow); a swallowed rescue is indistinguishable from a never-requested one in every
log. Both now trace under BASTION_EGRESS_DIAG, and ALL 11 `stuck_watch` wipe sites route through a
reason-tagged `watch_wipe()` shim (the investigation stalled precisely because ~a dozen writers were
observationally identical). Rule: multi-writer suppression state gets a reason-tagged single-point shim
the day it gains its third writer. (Reviewer: number/split as you see fit.)*

*(Builder's original 3 raw append-notes for this finding — skill-cap self-licensing, the spawn-variance
correction, and arrived-by-radius — reviewed and formalized as B44/B45/B46 in the table above; not
duplicated here.)*

*Builder append — 2026-07-18 (M2 fixture x2 comparator, class 7): NONDETERMINISTIC ITEM IDENTITY —
`item_hash` on inventory items differs across byte-identical runs (first-divergence: the post-damage
colonist eats food; UseItem's item_hash = 4110382425457152195 vs 2060603065296337836 on identical seeds
and tick streams). Any determinism comparator, replay/CRC regression (the Grok save-fuzz corpus), or
state-hash gate will trip on item identity even when behavior is identical. The M2 fixture comparator
normalizes item_hash out (named, justified); the ENGINE fix is deterministic item-instance identity
(seeded or content-addressed, not pointer/HashMap-order derived). (Reviewer: number as class 7.)*

*Builder widening on class 7 — 2026-07-18 round-4: the nondeterminism is item IDENTITY broadly,
not just the hash: the same x2 comparator also caught `inv_slot` differing (slot_idx 8 vs 6, same
tick, same Consumable(Food)) — the colonist draws the food from a DIFFERENT inventory slot across
byte-identical runs. One root (inventory ordering), two visible fields; comparators must
normalize both (item_hash + inv_slot) or gate on item KIND.*

*Builder second widening on class 7 — 2026-07-18 round-5: the hole PROPAGATES INTO BEHAVIOR.
After byte-identical runs eat food from different slots (same tick, same kind), their
trajectories FORK — 6990/9002 recorder samples diverge downstream (one run walks its move
target, the other stands: a real behavioral split, not an identity field). Comparator
normalization is therefore INSUFFICIENT for class 7; determinism-sensitive fixtures must
ISOLATE it by staging (canonicalize inventories to one asset-deterministic item —
`bastion_canonicalize_colonist_food`). Raises the engine-fix priority: item identity
nondeterminism = colony-simulation nondeterminism, not a cosmetic hash.*

*Class-7 root correction — 2026-07-18: `Item::item_hash` is a deterministic
hash of the item definition/content; differing hashes proved that different
items were generated, not that an instance pointer or hash table address leaked
into identity. The measured entropy was upstream: RTSim's stable NPC entity RNG
was dropped at lazy profession-loadout evaluation, `trader_loadout` used
`rand::rng()`, and `TradePricing::random_items_impl` used `rand::random()` to
index candidates sourced from hash-based storage. That changed definitions,
bag slots, the concrete healing `UseItem`, and finally trajectories. The engine
fix is a separate stable per-NPC lazy-loadout RNG plus full-definition candidate
ordering; it intentionally does not redesign ItemId/item_hash, persisted slot
schemas, or global inventory order. `item_hash` and `inv_slot` remain forbidden
normalizations because they expose the behavioral split.*

*Builder append — 2026-07-18 (architect-endorsed, class 8): STALE-BINARY FALSE GREEN — a
build+run chained with ';' lets a FAILED build silently run the previous exe, and the old
binary's results read as the new code's (M2 round-6: E0596 build failure + ';' chain + old exe
+ a same-slot lottery = a false det=True that the changed code never produced). Same family as
no-cargo-during-gate. Rule: any evidence pipeline VERIFIES build success AND exe mtime before
trusting a single result line; '&&' not ';' between build and run.*

*Builder append — 2026-07-18 (architect-endorsed, class 9): DEGENERATE FIXTURE GEOMETRY MASKS A
GENERAL CLASS — the M2 fixture's member stood IN the route entry, so the at-entry short-circuit
produced an EMPTY approach corridor and the fixture structurally could not exercise approach
movement; the corridor-movement deadlock (class instance: generic Chaser arrival tolerance
~1.5 > corridor cursor tolerance 0.75 → Goto declares arrival, cursor never advances, member
frozen → backstop 6/6 corpus-wide) stayed invisible through SEVEN green fixture rounds and was
caught only by the corpus. Rule: for every positional/geometric precondition a fixture stages,
include a GENERAL-POSITION variant (nonzero approach, nonzero path, non-adjacent start); a
degenerate case (at-target, zero-length, empty-set) exercises only the short-circuit, not the
mechanism. Kin to class 8 and gate-must-test-live-path.*

*Builder append — 2026-07-18 (architect-directed, class 10): TOLERANCE INVERSION — when the
MOVER's arrival radius is looser than the CURSOR's advance threshold, the system deadlocks
silently in the gap: the mover declares done and emits nothing, the cursor never advances, no
error fires anywhere (M2 instance: generic Chaser arrival ~1.5 blocks vs corridor REQ-0087
cursor 0.75 → member frozen 1.05 blocks out, forever, across 6/6 seeds). The class: ANY
producer/consumer pair where progress is measured by two different thresholds needs
threshold-ordering asserted (consumer's bar ≤ producer's bar) or a single shared constant.
Found only via a purpose-built live writer-diag — unit tests constructed the states directly
and structurally could not see the live-path gap (gate-must-test-live-path, kin to class 9).*

*Builder CORRECTION on the M2 named-open item (3) — 2026-07-18, BACKSTOP-OPT measurement: the
'AgentInbox interruption DEAD ON THE LIVE PATH' finding is WRONG AS FILED. corpus-v5 s1337
tape: a mid-climb owned traversal aborted with reason=agent-inbox-interruption at tick 1832 —
the mechanism FIRES LIVE on real inbox events. What was dead was the N6 FIXTURE STIMULUS
(sustained 0.5/tick bastion_emit_damage never produced an observed inbox event — emission
path or threshold; a third instance of the stimulus-aim-error class, folds into the
stimulus-window precondition-assert chip). The tag's named-open wording should read: 'the N6
fixture stimulus never engaged the live-proven interruption mechanism' — the mechanism itself
is vindicated by tape.*

*Builder append — 2026-07-18 (BACKSTOP-OPT, class 11): A BUG'S SIDE EFFECT MASKS A SECOND BUG —
fixing the first unmasks the second as a "regression." Instance: the broken 'verified surface
exit' release (class instance of premature-release) was ALSO accidentally wiping the stuck-watch
for members in the legitimate REQ-0071 energy-recovery wait; fixing the release correctly
(stable-dismount bar) exposed that NOTHING ELSE holds the watch through the ~90s designed wait
→ the failsafe teleported a member mid-recovery (N1B regression, deterministic). The class
rule: when a fix produces a regression in an ADJACENT behavior, ask what side effect the old
bug was providing before assuming the fix is wrong — the regression may be a SECOND latent
bug that was never separately handled. Fix the second bug on its own terms (here: a
reason-tagged, cumulative-bounded watch hold for the named designed-wait state), never by
restoring the first.*

*Builder append — 2026-07-18 (architect-directed, class 12): A BOUNDED-SAFETY PROOF MUST TEST
THE COUNTER-RESET/CYCLING CASE, not just a single engagement. Instance: N7 proved the
energy-wait hold's 120s bound on ONE CONTINUOUS wait and passed — while the corpus C-leg
cycled abort→reacquire→energy-wait with the counter CLEARING ON EACH TASK CREATION, so the
bound never accumulated and a colonist stranded forever (the reset bug slipped straight
through the single-wait proof). Same family as the stimulus-window aim errors (classes 9's
kin): the proof did not engage the mechanism's worst-case FORM. Rule: every bound over a
resettable counter needs its falsifier to exercise the RESET path (cycling/repetition), and
the clear-sites of sibling counters guarding the same episode should be aligned (the reengage
counter cleared on real progress; the energy-wait counter cleared on task creation — the
inconsistency was the hole).*

*Designer append — 2026-07-19 (DIG-PROVISIONED-ACCESS packet, DPA-3 known-class note): LATERAL-GALLERY RE-GATING — `Job.depth` is z-relative to the region's box-top, so a lateral gallery at constant depth extending beyond an access anchor's XY-8 radius re-triggers `descent_gated` and plans a wasteful fresh vertical shaft for a cell that isn't actually deeper. Known + deliberately deferred to Part-B/M5 (fix = widen the anchored-check for same-z-band cells to walkable reach, not a fresh vertical link — DIG-PROVISIONED-ACCESS-PACKET.md §7/DPA-3). If a long gallery shows mystery access-plan spam, this is that. (Reviewer: curate/class-number as needed.)*
