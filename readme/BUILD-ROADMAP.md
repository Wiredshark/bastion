# BUILD ROADMAP — the living list

**Maintained by Fable (orchestrator) — updated at EVERY item close, finding, or Ben directive.
Ben's standing order (2026-08-10): "you need to be constantly thinking about this stuff as we
finish features, you need to continually update this build list."**
Rulings live in `DECISIONS-FOR-BEN.md` (rows cited); this file is the current-state view.
Format: append changelog at bottom; statuses edited in place. Newest ruling wins.

## Standing design riders (apply to every item below)
- **Plot/plan data model** (#102): anything placing structures/zones designs against plots, never raw positions.
- **Live-emit declaration** (#88): every new harness accessor declares ported / harness-only-with-reason.
- **Diagnostics never verdict terms** (#70 pattern); **event-driven emits only** (diag-density budget).
- **Instrument + registry row land in the same commit**; **asset tuning touches compiled default same commit**.
- **Ruling acknowledged = packet edited, one operation** (#101). **Claims are not artifacts.**
- **A WITHDRAWAL is a ledger event too**: retraction and roadmap/packet edit are ONE operation — withdrawing has no natural trigger, which is exactly why stale entries outlive dead claims.
- **Every bar names its window AND its arena (who else can act)** (#95); success signature in every observable.

## ARC 1 — Close the survival loop (items 1–8)
| # | item | status |
|---|------|--------|
| 1 | Name the food thief | **DONE** (#95 — ambient NPCs; attribution complete, zero residual) |
| 2 | Stall counter (rescue cleanup counts progress, not claims) | **BUILT + corpus-exercised (wave33: prunes fire, both paths, attribution partitions perfectly).** ACCESS_STALL_SECS=120 PROVISIONAL — right-censored by its own reset (8 seeds at exactly 120; non-pruned 119 argues raise-never-lower); unblock row approved: env-tunable thresholds (+manifest rows same commit) + final-beside-peak emit, lands WITH item-6 witness, one fan scores both. Branch A retired (0/48 — option 3 dead by corpus, never built: measured-first paid). #38 CLOSED (prune fires, 14 events/11 seeds). |
| 3 | THE ARRIVAL FIX (sit trap) | **SCORED PARTIAL-AS-REGISTERED (Opus)**: 526 rescues/22 uids (precondition rich, fix self-witnessed), fail-safes 9→2 with both remainders attributed to a DIFFERENT mechanism (travel class) — sit class eliminated; residuals feed the travel row. Item 4 path RESOLVED: PATH 2 — the candidate log predates the fix (dated 08-04; its counter cannot exist there), so the 2-case re-score rides the farming confirm run as an observation rider; zero fail-safes there = stays open, never force-scored. Live-leg protocol gains the boot-time commit stamp (the last unstamped artifact class). |
| 4 | Egress planner (verdicts-without-plans) | **CLOSED — original suspicion NOT SUPPORTED** (Opus, 8d9b7d79c6): the clean post-fix specimen shows the planner CORRECT about an INVALID START STATE — the colonist was suspended mid-air (no ground, no wall, idle, z-velocity exactly 0, not falling); no_route ×10 was the right answer to a malformed question, and a destination WAS computed (the planner did its half). Limits held: n=1, chain inferred, route-existence unprovable. SUCCESSOR ROW spawned (travel-row orbit): the SUSPENSION state — does climb_free expiry leave entities without ground/wall contact? — plus the attached oddity: the fail-safe logged "teleporting to ground" while moving the colonist 5 blocks UP. |
| 5 | Server-authoritative test timing (Wait(n)) | **DONE** a35a98aaf7 — A/B PASS: sim-target matched (20.02 vs 20.01s) across unthrottled/contended with different spin counts (609/495), zero errors; failsafe corrected pre-land (mute wall-clock check → engine ServerTimeout signal, Opus catch); spin-direction surprise traced to producer (starved client's wall-dt grows) before reporting. Scope limit honestly held: whole-box contention arm, not isolated server-slowness. |
| 6 | Protected persistent provisioning + stockpile eating | **DONE — live acceptance PASSED** (field-placed pile protected the full 25-min run vs a loose drop taken in 37 s pre-fix; positive controls live). Corpus visibility: STRUCTURALLY IMPOSSIBLE under existing scenarios (5b premise-check at source: zero item-drop creation anywhere in ~40 harness scenarios) — witness fields stay as DORMANT SENTINELS with the premise documented; no bespoke fixture on spec. **CORPUS-INVISIBLE (wave33 gap): no field matches pile/protect/ambient/loot — witness row APPROVED (refusals-by-reason, pile-pickup attribution, reserved-units), lands with item-2's threshold changes, one fan scores both; WITNESS RANKS FIRST within the batch (wave33 addendum: build/material signature widened to ≥3 seeds incl. a clause-SWAP invisible to the fail count — the gate is the strongest unexplained-pattern candidate). REGISTERED PREDICTION for the witness fan: if "ambient-loot-disabled" refusals fire on COLONIST pickers, the movers are explained and the gate has a membership/timing bug — candidate, not claim.** |
| 7 | Farm-to-table self-sufficiency | **REAL ROOT CAUSE: SEED-SUPPLY DEADLOCK (structural, two code sites — 5b addendum 812757982c).** `FARM_SEED_ITEM`'s only producer anywhere is a successful HARVEST (bastion_jobs ~13692); harvest needs a mature crop needs a SOW needs a fetched stockpiled seed; worldgen volunteers explicitly unharvested ("a later nicety"). A fresh colony has NO possible first seed — sow jobs sit forever unclaimable. The earlier "phantom-retire = root cause" claim is WITHDRAWN in location/timing: terrain.get's ONLY failure is NoSuchChunk, (x,y)-dependent — z cannot discriminate (Chonk::get_unchecked infallible per z, read at source); all 48 retirements fired in ONE 1.1ms window <1s after driver disconnect = end-of-run chunk-unload sweep, not a during-run defect (job 703 survived ~4800 sweeps first). The sow/till asymmetry explained: till jobs complete and leave the board; seed-blocked sow jobs alone survive to be swept at disconnect. TWO INDEPENDENT FIXES: (1) seed bootstrap source (cheap confirm first: /give_item seeds run); (2) task #57 sweep splits read-failure from genuine mismatch (disconnect unload must not masquerade as designation change) — still worthwhile, no longer the cause. |
| 8 | Multi-day endurance run (TIER-1 AUTONOMY GATE) | waits on 2,3,6,7 — the "colony runs itself" milestone; Ben gets observer seat |

## ARC 1.5 — Infrastructure (runs alongside)
- **Entity event log** (#99/#100): designed (DESIGN-ENTITY-EVENT-LOG.md, premise-checked chassis, per-entity rings, promotion persistence). **PROMOTED — builds NEXT in 5b's queue.** Pilot vocabulary includes Released{job, reason, tick, actor}; **first acceptance customer = the mover investigation's registered claim-overlap prediction** (orphanings cluster near removals vs uniform — undecidable by run-total counts, decidable by the stream). The Voonoo query stays as the second acceptance.
- Reasoning dossiers (#98): FOOD pilot written; travel dossier at item 3's close; dossier per arc-close is ritual.
- Self-terminating run harness (#73 — server+driver teardown); collect_wave --baseline DONE (#67). Graduation note (Fable, 2026-08-10): when #73 lands, `stale_binary_preflight.sh` (scratch tooling, 5b) becomes its FIRST STEP, invoked by the run script itself rather than remembered by whoever launches a run — a tool someone must remember to run is a habit wearing a filename; wired into the runner it becomes law.
- Farming writeup (5b, DONE 2026-08-10): FARM-SOW-PHANTOM-RETIRE-FINDING.md + addendum -- root cause is a seed-supply chicken-and-egg deadlock (structural, code-verified), not the phantom-retire mechanism the original body centered on (that's real but incidental, a disconnect-triggered chunk-unload sweep). .engine-integration-wt target scoped cleanup (5b, DONE 2026-08-10): target/debug/incremental removed (54G, confirmed 2-days stale), target/no_overflow untouched.

## ARC 2 — Legibility (9–12)
9 Colonist inspector HUD · 10 Colony dashboard · 11 Recreation/idle life · 12 Chronicle UI (becomes the entity-log's player view).

## ARC 3 — Threats (13–15)
13 Hostiles near colony (Flee drive's live test) · 14 Guard job · 15 Fortification designations.

## ARC 4 — Economy & persistence (16–18)
16 Haul priorities + hauler-vs-eater contention (**plot-model rider applies**) · 17 Skills visible+felt · 18 Save/load integrity (entity-log promoted-set migration rides this).

## Capstones (19–20)
19 Renderer horizon retest (one-constant fixture fix — arena radius ≥ tested horizon) · 20 **Milestone playthrough v2** — LLM-player session scoring everything (#62 recurring gate).

## Band 21–40 (directional; design docs exist where noted)
21 Personalities visible · 22 Relationships · 23 Morale events · 24 Seasons that bite · 25 Shelter quality ·
26 Crafting chains · 27 Cooking · 28 Tool quality/wear · 29 Trade with vanilla world · 30 Stockpile zones by type (**plot rider**) ·
31 God-hand v1 (founding docs exist) · 32 Faith economy · 33 Miracles ·
34 Raids scaling with wealth + **THIEVERY as designed feature** (#97 — gate lift is line one; chronicle wiring pre-built per #100) ·
35 Injuries & medicine · 36 Death that matters ·
37 LLM-player harness v2 · 38 RL-tuning groundwork · 39 Performance row (sub-threshold tick degradation) · 40 Colony scale 16–32.
**THE COLONY MIND (#107, Ben direct)** slots beside the settlement arc: a colony-level drive arbiter (Sustain/Grow/Defend/Expand) weighted by board-computed colony needs (food-days, housing ratio, threat pressure, buffers), outputting WEIGHTS on the existing generators — never orders to individuals. The god-interface surface + the RL substrate. NOW-RIDER active: colony metrics accumulate as diagnostics in current work (the mind is born with senses).
**SETTLEMENT PLAN LAYER (#102)** slots as its own arc in this band, before 31: plot grammar reuse, district zoning, cost-driven form, desire-line roads, layout-quality corpus metrics.
KNOWN INHERITANCE (charter line, Opus 08-10): today's `stockpiles: Vec<(ZoneId, Region)>` painted-bounding-box zones ARE the raw-position model — the arc's FIRST MIGRATION TARGET, not a green field. Item 16 is stockpile-adjacent and carries the plot rider pre-spec.

## CHARTERED ROWS & PARKED QUESTIONS (the board beyond the numbered 40)
**Ben (2026-08-11): "I feel like we have more items than just 40" — correct; this section makes them countable.**
Open investigation/engineering rows:
- **4b** Grounded verdicts-without-plans (specimen uid=81; union counter split SHIPPED; scores from endurance/travel data)
- **#85** The suspension (a LOADED colonist that doesn't fall; lead: climb_free's x/y-only velocity writes; + the teleport-UP oddity attached)
- **uid=109 dispatch question** (fail-safe with zero egress consultation, mutex absent — chartered-on-second-specimen)
- **Mover mystery** — PARKED, 3 wake conditions (concentration real at p=0.021, storyless; stream exists for the next registered idea)
- **Multi-writer counter audit** (fill-sized; two union counters have each cost an investigation)
- **ItemInstanceId identity row** (identity survives pickup, notable-items-only per ruling; unblocks entity-log item retention)
- **Queue-position capture fix** (capture before the queue entry clears — seed-69's missing witness)
- **#73 self-terminating run harness** (server+driver teardown; stale-binary preflight graduates into it)
- **Farming fix 2** — task #57 sweep splits read-failure from mismatch (reporting honesty; no longer causal)
- **Colony Mind arc (#107)** — two-drive pilot (Sustain/Grow); design in progress, Architect-pattern spine; metrics rider ACTIVE
- **Needs-in-rtsim** (the (b) row — chartered at the multi-colony horizon)
- **Thievery** (designed feature, band 34 — gate lift is line one, chronicle wiring pre-built)
- **Founding kit** generalization (band 21-40)
- **External prior-art verification** (marked recalls to check before load-bearing use)
- **Ben's observed celebration session** (after the endurance gate passes)
Done-but-worth-counting process artifacts: live-leg commit stamps (protocol), dormant witness sentinels (item 6), dossier-per-arc ritual, the heartbeat loop.
**Honest count: ~40 numbered features + ~15 live chartered rows + parked questions with named wake conditions ≈ a 60-item board.** The 40 are the destination; the chartered rows are the road maintaining itself. Both tracked here; neither hidden.

## Changelog
- 2026-08-11 (30): **#82 CLOSED by restatement (Opus, read-only during the quiet window)**: the hung-Wait theory was impossible — the driver exits ~60s in BY DESIGN (the scored window is unattended, that's the row); the real gap was an OBSERVATION PROTOCOL with no listener ("liveness via the food-stock heartbeat" written in a log note nobody polls — a heartbeat with no listener is indistinguishable from no heartbeat; mute-channel law, operational layer). **The already-adopted early-crash beat IS the fix** — a gap filed as a code seam closed by a process proposal made for an unrelated reason. Side findings: #73 half-satisfied (driver self-terminates by design; the SERVER half is the real remainder — rescope when touched); item 5's both-clocks line visible working in production (600 requested / 587 spins / 20.04 sim-s side by side).
- 2026-08-11 (29): **ENDURANCE v3 IS FLYING** — veloren-server-cli up since 10:42:40 local (verified by process check, not message; launch chain on branch: 517cb50f6d → bd9e26fbbe pin-fill GREEN → f5267f15bb denominator + corrected-TPS refinement). **Early-crash check ADOPTED into the wake plan** (Opus's ask, accepted on cost asymmetry — one glance vs two burned hours; his own caveat noted as the model form for one-instance process fixes): ~30 min post-launch — process alive, log growing across two reads, b5_split_off_one_fired > 0 (trigger population arrived; also confirms measure 3's first rest-cycle crossing). Schedule: early check ~11:13, health ~13:12, run-end kick ~14:12, scoring per ITEM8-V3-SCORING-PROCEDURE.md.
- 2026-08-11 (28): **readme/PACKET-CRAFT-CHECKLIST.md landed** (four entries with provenance: #88 LIVE-EMIT + snapshot-vs-accumulator rider; #102 plot/plan model; where-is-its-row + calibration test + both clauses; scoring-refusals founding five) — Opus's memory copy DELETED down to a pointer ("an index that repeats its target's content is a fork waiting to drift" — the config-fallback law one level up). Maintenance clause: new entries only when skipping one has cost a rework, rework named. Authoring-vs-gating kind-split recorded not acted on (seam at 8 entries; tell = spec-time readers never reaching the gating half). Awaiting 5b's v3 launch record.
- 2026-08-11 (27): **"The packet template" turned out to be a PRIVATE artifact both parties referenced as shared** — no file exists; what exists is Opus's standing PACKET-CRAFT CHECKLIST (entries: #88 LIVE-EMIT declaration, #102 plot/plan data model, + today's two: where-is-its-row with calibration test/both clauses, and the scoring-refusals founding five). RULED: promoted to readme/PACKET-CRAFT-CHECKLIST.md, Opus authors, one page, each entry carries its provenance; his memory copy becomes a pointer, the file is the single source. Also banked with mechanics: a derived figure is a baseline only if every term in its derivation is nameable; cheap tell — a measured rate ABOVE a capped target indicts the measurement, not the system; default — prefer a declared constant for any bound (it carries its producer by construction).
- 2026-08-11 (26): **Scoring instrument written BEFORE the data (ITEM8-V3-SCORING-PROCEDURE.md)**: preflight VOID gates → fix claim scored FIRST (three-way rule, splits:eats denominator) → six measures → zero-window → health at 30 TPS. Its "WHAT I WILL NOT DO AT SCORING TIME" section (five pre-commitments against sunk-cost generosity, incl. no cross-carrying between co-resident results — "the fix can hold while the colony still starves, and that is legible, not mixed") is **ADOPTED PROGRAM-STANDARD**: every future scored gate's procedure carries one, written by its scorer before data exists. Also: the ~31.8 ticks/s v2 baseline Opus handed 5b was CORRECTED by its own producer — server target is `const TPS: u64 = 30` (read, not assumed); 31.8 embedded an unwritten start-point choice and would have read a healthy server as 6% degraded. Ledger line: a derived figure is a baseline only if you can name every term in its derivation.
- 2026-08-11 (25): **GREEN — fix chain cleared at 468fe8f07c (+517cb50f6d refinements), ENDURANCE v3 LAUNCHING on it.** All four gate items verified structurally (witness partition strictly inside the Some branch, vanilla door None-only; exit debug_assert checks the REAL struct invariant not a length proxy, with "file a row, don't patch around it" in the assert message — today's law embedded where the next person will be standing; precondition witness + three-way rule; merge-half named UNPROVEN; accessor-trap row filed). **Opus's own rate-condition gate item was UNSATISFIABLE** (no v2 splits counter ever existed) — caught by the calibration test's own question and refined to an internal check: splits commensurate with the run's own EatFrom completions, near-zero-splits-with-healthy-eats = path suppressed = VOID; no external baseline needed. Two clauses ratified into the law: (1) before issuing a no-row-no-pass flag, STATE THE OBTAINABLE OBSERVATION that would satisfy it — none obtainable = malformed flag, refine or drop, never hold the gate; (2) a gate item the builder cannot satisfy is a FINDING ABOUT THE GATE and must be flagged, not silently omitted. Arc-closer on record: the fix was ready three messages early — everything after bought what the green will MEAN. My heartbeat owns the run wakes; Opus scores on landing; #85 four bits next in 5b's queue; #107 behind the verdict.
- 2026-08-11 (24): **Prereg reviewed early — consolidated FOUR-item gate, one amend cycle** (Opus). Gap 1 (the catch): the v3 bar was crash-ABSENCE with no precondition witness — a silent run cannot distinguish "fix worked" from "trigger never occurred" (sit-trap law verbatim); required: counter on split_off_one returning Some, **splits==0 registered VOID not PASS**, and the rate condition in the prereg text (v2's 23.6-min fuse makes a silent 2.5h strong evidence ONLY at comparable split rates). Gap 2: the prereg's own delta table scoped out the mine-supply `.item().amount()` unsafe-accessor trap with no row — same shape that cost v2, caught in a table this time; law's final form: **a scoped-out failure mode is tracked in the LEDGER or it is not tracked** (doc comments and prereg tables alike cannot page anyone). Full gate: (1) witness exclusivity proof/unified writer, (2) split_off_one exit guard or row, (3) splits counter + zero-VOID, (4) accessor-trap row. Three of four are witnesses, the right ratio for a fix whose failure mode is silence. Ratio ruling on Opus's self-check: three no-row-no-pass hits in one review = the law working, not tiresome — calibration test set: a flag gates iff the scoped-out condition could silently cost a run or misread a result; cosmetic-worst-case = "row optional, noted."
- 2026-08-11 (23): **Opus commit review: fix APPROVED, chain AMBER on two gate items — both today's laws at first application.** Fix verified by read: len()==1 makes the assert's iterator empty, the violating shape is UNCONSTRUCTIBLE (over-satisfies the post-condition bar); planted test reconstructs the live crash operation; the ship-time comment now carries the lesson ("the comment that caused this now warns about comments that cause this"). GATES: (1) 5509dc95c3's restored witnesses each have TWO WRITERS (vanilla inventory_manip + new bastion_jobs sites) — the union-counter class MULTI-WRITER-COUNTER-AUDIT cleared from this exact file this week; falsifiability counters must not drift upward (over-counting fails in the direction that looks like success); 5b supplies a documented exclusivity proof (which door does the last-unit None consumption take?) or a single shared writer. (2) no-row-no-pass applied to the review queue's own first item: split_off_one is invariant-safe because multi-entry stackables cannot exist, not because the method guarantees it — debug_assert on exit or a row, silence not an option. Closer on record: **this fix's residual is the same shape as the bug it fixes — defended at review time by a guard instead of at ship time by a doc comment, which is the entire delta the day bought us.** v3 launches on the amended reviewed pin.
- 2026-08-11 (22): **Both fix-review requirements answered by CHECKING (5b; pins e14795700e fix → 5509dc95c3 witness restoration → 9530d34748 v3 prereg)**: (1) the timing delta DOESN'T EXIST — PickupItem::amount() was always total-invariant across a split, the physical decrement always happened at consumption in both designs; consumers (has_capacity/reserved_count, job_still_wanted Farm arm, food sampler) enumerated and cleared, table in the prereg. (2) the seam question WAS REAL: **two genuine witness regressions found** — b5_pile_pickup_by_member (item 6's B2 falsifiability pair) and ItemEventKind::PickedUp both fired only inside the vanilla InventoryManip::Pickup handler the consume-direct path bypasses; **both restored at the new site**. A benign-assertion would have shipped both losses silently. ITEM8-V3-PREREGISTRATION.md written (launch cites v2 record, delta table, unchanged bar, registered falsifiable prediction: the trigger population occurs live and the assert stays silent). All routed to Opus for commit review; review to confirm the restored PickedUp keeps its meaning/denominator at the new site.
- 2026-08-11 (21): **Fix BUILT (5b, awaiting Opus commit review): split_off_one returns Option<Item> — never grows the Vec, post-condition unconditional**; split moved reservation-time → arrival-time, split value CONSUMED DIRECTLY (no bag round-trip, no new entity/Uid — reservation and EatFrom keep keying the original pile Uid). Accepted as inside direction 1, stronger than born-single-entry. 3/3 common tests green incl. the planted crash-reproduction test (direct construction, not workload-dependent). Two review requirements added: (1) ENUMERATE THE DELTA of the timing move field-by-field before v3 (pile amount stays higher for the walk window — every consumer named: job_still_wanted Farm arm, food-days metrics, diag corpus fields); (2) NAME THE SEAMS the bag round-trip used to cross — no pickup event now, so the event-log eat attribution (#95 lineage) and pile-protection accounting (#96/#97) must each be shown witnessed through another instrumented door, or the loss named deliberately. 5b's fill = ITEM8-V3-PREREGISTRATION.md (launch procedure, fix pin, expected-delta table, unchanged bar, registered prediction: live splits-then-merge-checks occur and the assert stays silent).
- 2026-08-11 (20): **Finding review ACCEPTED + the deepest fact of the day: THE ASSERT HAS BEEN VACUOUS FOR THE LIFE OF THE CODEBASE** — non-stackables are trivially full (max 1), stackables never produce a remainder (overflow past u32::MAX never happens), so vanilla PickupItems are effectively ALWAYS single-entry and the invariant's iterator was empty until #89: **we didn't violate an invariant, we created the first population in which it has content** (status-quo-is-the-exercised-population, purest instance — the text existed, the exercised population was empty; "pre-existing in shape" dies for the stronger reason: we invented the shape). **FIX RULED: DIRECTION 1** — the eat path stops creating multi-entry stackable PickupItems; the vanilla invariant stands (an assert that has never been exercised is the one whose purpose nobody knows; direction 2 = renegotiating a contract with an absent author for unenumerated consumers). Natural shape: SPLIT AT THE SOURCE — decrement the pile's stack in place, construct a born-single-entry PickupItem; 5b confirms or refutes from call sites, alternatives stay inside direction 1. Precision: pick_up is UNMODIFIED (#89 added split_off_one so the untouched pick_up pops the pushed single) — finding doc corrected. **Opus's review-practice addition adopted PROGRAM-WIDE into the packet template: every "scoped out / deferred / acceptable for now" in a reviewed diff gets "where is its row?" — no row, no pass.**
- 2026-08-11 (19): **Opus's independent read CONVERGED with 5b's dig — root cause confirmed OURS, and the comment's own defence refuted by read**: vanilla try_merge always pushes the partial LAST (full self_last, then remainder), so a multi-entry stack built by normal means satisfies the invariant by construction — **split_off_one is the only thing in the codebase that creates a partial non-last entry**; "pre-existing in shape" is dead. Opus named his own miss: he scored #89 with the three-part dismissal (unenforceable · pre-existing · out of scope) standing unexamined — three individually checkable claims, checked by nobody, "the comment did my thinking for me." Reviewer-independence trade made correctly: sent his read to 5b with stake flagged ("do not ratify me") + an open question (len()==1 behaviour). **FIX RULED — 5b builds, Opus gates at commit**: (1) invariant as POST-CONDITION, never crash-absence; (2) one design-space-deciding read FIRST — if max_amount()==u32::MAX holds for stackables, the 1-amount single is ALSO partial ⇒ no ordering of two partials can satisfy the invariant ⇒ fix must EXIT the single-Vec design (remainder as separate pickup/entity); (3) ship-time comment rewritten — its prediction becomes the planted test ([Stack(39), Item(1)] merge-checked, red-then-green by name), comment cites the test; (4) pins green → review → endurance v3 on that pin.
- 2026-08-11 (18): **CRASH ROOT CAUSE FOUND (5b, pin 15e8884b8a): OURS, not vanilla — #89's split_off_one is the producer**: it pushes the new split as the LAST entry, leaving the decremented original stack NON-LAST; for stackables max_amount()==u32::MAX so that entry permanently violates try_merge's all-before-last-full invariant, detonating on the next merge-check against a fresh same-item drop — which sustained farm/haul cycling reliably produces. **split_off_one's own doc comment predicted this exact failure verbatim at ship time and scoped it out.** Law grows a clause (sufficiency-claims memory updated): A SCOPED-OUT FAILURE MODE IS A SCHEDULED CRASH UNLESS TRACKED — naming the case is necessary, not sufficient; a predicted-and-deferred failure mode must also become a ledger row with its trigger condition, because a comment cannot page anyone. 5b corrected his own "not bastion" call-site misread in the doc. Finding routed to Opus (first-line); NO fix before his ruling; 5b's fill = the #85 four-bit amendment; endurance v3 on a pinned reviewed fix only.
- 2026-08-11 (17): **Colony Mind measure 2 split into two legs after the identity default exposed a self-contradiction one line from shipping**: at identity 1000‰ the wiring is behaviour-preserving BY CONSTRUCTION, so "fixtures break at S3 = success" was wrong as an unconditional. Now: leg 2a A=A control (identity; fixtures MUST stay green — red = rounding/determinism bug in colony_cap itself, blocks S3, a failure mode with no witness before the control existed) + leg 2b absorption (forced non-identity; fixtures MUST break by name). Same fixture, opposite readings, one stage apart — the packet requires declaring WHICH LEG before reading the result. Mechanism noted: the identity default and the contradiction-catch were one event — a true control forces every reading in the measure to declare its leg. Opus's arc summary on record verbatim: "the five retractions were not the cost of the arc, they were the mechanism of it — every one died to a read that would have happened eventually; the only thing that changed was whether it happened before or after a build."
- 2026-08-11 (16): **ENDURANCE v2 CRASHED at ~23.6 min** (tick 45000, cycle 1; pin f8abfffecb; ITEM8-CRASH-FINDING.md): debug_assert in PickupItem::try_merge — "All items before the last in PickupItem should have a full amount" (inventory/item/mod.rs:1752). 5b report-first, no relaunch — the crash IS the result; the run stands as the deepest sustained load the loop has had (12 preempts, real eats, 23 min compounding farm churn). RULED: 5b's "vanilla pre-existing, not bastion" classification must clear the invalidating check first — **#89 SPLIT-PICKUP modified PickupItem::pick_up (pop-last + reinsert-remainder), i.e. WE changed the producer of exactly the state the assert polices**; his grep cleared the CALL site, not the CONSTRUCTION site, and the violation plants at construction, detonates at merge. 5b digs the producer (main task; #85 four-bit amendment yields), root cause written with the constructing site READ, both branches argued; NO fix before Opus review; endurance v3 relaunches only on a pinned reviewed fix. Reframe on record: the debug_assert firing is the instrument WORKING — release would have been silent inventory corruption. Item 8 stays OPEN; Arc 1 close waits.
- 2026-08-11 (15): **ROW-COLONY-MIND-PACKET.md written and APPROVED for a builder** (queues behind the endurance close; 5b finishes the #85 four-bit amendment first). Opus caught that my both-ends intensity clamp was necessary but NOT sufficient — integer division can floor a positive intensity to a ZERO cap (1×1×500‰=0; unreachable today only because BASE=2, reachable by the routine tuning this row enables) — law: **A CLAMP ON A FACTOR IS NOT A CLAMP ON THE PRODUCT; enforce the bound at the unit the consumer reads.** Packet ships colony_cap() with .max(1) + a planted test defending the guard. Arithmetic: integer permille, multiply-all-then-one-division-last (rounding rule and implementation are the same fact); measure 6 requires a bit-identical CAP SEQUENCE. Measure 2's inversion in bold (green fixture after wiring = weight absorbed = FAIL). Ruled at approval: intensity_min/max values DEFERRED to S2's trace (no provisional numbers — #103's lesson) + **default intensity = IDENTITY 1000‰ every kind** so unset/unloadable config = current behaviour by neutral element, S2 = true A=A control. ColonyDrive (not Drive) naming, corpus from S2, zero-window, Sustain lock-in planted test with seeds-49/50 provenance all in.
- 2026-08-11 (14): **#107 SHAPE SETTLED AND RATIFIED — the charter's sentence was literal**: the generators are three `pub const` per-kind caps (HAUL/MINE_GEN/BUILD_GEN_JOBS_PER_COLONIST, all colonist-scaled), and the Colony Mind = KindIntensity MULTIPLIERS ON THOSE CAPS. Allocator (lexicographic priority/score) LEFT ALONE — ordering becomes emergent from what exists to allocate, stigmergy intact; unread "how priority is produced" retired as a scoping decision, not a gap. **CHARTERED BOUNDARY: need jobs (EatFrom/RestAt/Despond) permanently out of scope — modulating need thresholds is orders-to-individuals through the back door.** Free absorption test: bastion-harness already asserts exact cap arithmetic, so a wired drive breaks fixtures BY NAME (green after wiring = the weight was absorbed = measure FAILS — inversion stated in bold in the packet). Two packet requirements added at ruling: intensity clamped BOTH ends in ColonyMindConfig (uncapped multiplier = unbounded job-board cost) + deterministic representation (fixed-point/permille, named rounding, fixed multiplication order). Opus proceeds to packet craft; row sits behind the endurance close. 5b's #85 discriminator commit 5d905a247d reviewed CLEAN.
- 2026-08-11 (13): **"NO DEMAND FORMULA" RETRACTED + THE FIND OF THE DAY: a drive arbiter with hysteresis ALREADY EXISTS in bastion_jobs.rs at per-colonist scale** (Drive{Work,Flee,Idle,Personal}, modulated_urgencies, incumbent + ARB_HYSTERESIS switch guard, BASTION_ARB_SWITCH_DIAG) — the Colony Mind design doc independently re-invented hysteresis (its principle 4) and anti-thrash (its measure 4), both shipped with a documented regression lesson attached (Personal-incumbent asymmetry, seeds 49/50). Retraction cause: fn-name greps cannot establish concept absence — scoring is INLINE and selection is LEXICOGRAPHIC (priority discrete, then score continuous), so my permutation ratification dissolves with its premise; final action-space shape awaits Opus's SINGLE consolidated position after reading the three named unreads together (how priority is produced; what modulated_urgencies computes; Drive's per-colonist scope). **#107 re-scoped: INSTANTIATE the existing arbiter pattern at colony scale, don't invent** — "Sustain incumbent, Grow can never win" becomes a named inherited failure mode with a planted test. Laws banked: SURVEY THE TARGET FILE BEFORE SURVEYING THE FIELD (prior-art order: target file → own tree → evidence tree → the field); a grep pattern is a claim about naming — absence-of-match proves absence only if the naming claim is verified.
- 2026-08-11 (12): **COLONY MIND DESIGN WRITTEN (COLONY-MIND-DESIGN.md) — and its §0 entry-gate voided its own central mechanism before the packet opened**: NO demand/priority formula exists in bastion_jobs.rs; work allocation is a hard boolean precedence ladder + nearest-first within kind, so the chartered DriveWeights multiplier had NOTHING TO ATTACH TO (would have compiled, logged plausible weights, changed nothing — #60's absorbed-fix shape, caught for the cost of four greps instead of three stages). **Corrected shape RATIFIED: the arbiter emits a PERMUTATION over job kinds (KindOrder), not a weight vector** — design law: the arbiter's action space must be the space the consumer actually reads. Safety improves (a permutation cannot starve a kind; reorder-at-the-branch is a cleaner absorption test) and the RL substrate improves (discrete action space). S0 next (Opus): enumerate BOTH organs — the full allocation ladder rung-by-rung AND the job-generation sites (charter said weights-on-GENERATORS; the no-purchase finding was on the allocator; intensity may still have numeric purchase at generation — if not, permutation-only ships with intensity as a named non-capability). Doc carries: 6 measures/6 planted failure modes, S2 = log-only whole stage, registered zero-window (early Sustain invalidates), the SLOWING blind spot named ("it can tell the colony is HUNGRY, it cannot tell the colony is SLOWING"), DF honesty (the reason is the god-interface and RL substrate, not that the ants need it).
- 2026-08-11 (11): **#85 gate 4 RETRACTED by its proposer** (~5 min after proposing — `StateExt::create_npc` inserts all physics components in one atomic builder chain; the tick.rs decoration is a bastion component, wrong storage; specimen had a PhysicsState). Fifth mechanism, fifth self-kill by producer-read. **Opus STOPS proposing mechanisms — endorsed and banked as law: when reading can no longer narrow the field, instrument; a story that fits the specimen is the cheapest artifact available.** Build ruling survives the retraction: membership BITS stay (their justification was the missing witness, not gate 4's liveness); final 5b commit = in_loaded_chunk + is_rider + 4 membership bits + d.z context + head_clear fix. Keeper: Vel(zero) is the CREATION value — velocity.z==0.0 also reads "never integrated since spawn". **ROW-COLONY-PRESENCE FORMALLY SCORED: 4/4 PASS** (ROW-COLONY-PRESENCE-SCORING.md; measure 3 cleared a higher bar — 16 completed unattended eats retire the count-vs-mechanism objection; measure 2 a real trajectory not endpoints). Coverage gaps recorded: disband-leak NOT exercised, cost-per-colony an ESTIMATE (item 40 inherits it as one). Banked: 50% preemption completion rate = first baseline; 40-events-vs-32-counter producer gap on the next-in-file list. 5b's unprompted self-downgrade of their own v1 claim ratified as the row's honest half.
- 2026-08-11 (10): **#85 GATE 4 found — JOIN MEMBERSHIP** (Opus): the `par_join()` component list is itself the first physics filter, stricter than any `if` — an entity missing Collider/Mass/Density/Body is dropped with no branch and no log, and rtsim tick.rs decorates colonists INCREMENTALLY after creation, so a missing-component window is live and matches the specimen exactly (also survives the dest=Some constraint that killed gate 1). Gate-2 supporting evidence corrected: every `dismount` in bastion_jobs.rs is a ladder-exit cell, not the mount component (name collision; join exclusion itself still stands). RULED: field set amended — the joinable AND is REJECTED per aggregate-late (a false AND can't name the missing component); 5b builds the four membership bits individually + in_loaded_chunk + Rider/VolumeRider + d.z context + head_clear fix, one commit, one firing decides all four gates. Endorsed regardless of outcome: silently-outside-the-join currently has no witness anywhere in the engine. Law banked: **a join() IS a filter — reading only the `if` inside it enumerates half the gate.**
- 2026-08-11 (9): **#85 relocation WITHDRAWN** (fourth inversion, all on reads): the fail-safe is GUARDED CORRECTLY by construction (fail-closed closures — found rigor; no destination from absent terrain, so the entry-8 candidate is impossible). dest=Some constrains the specimen: gate 1 disfavored (chunk-boundary residue only), GATE 2 (Rider exclusion from both physics joins) now LEADS by elimination. RULED: 5b's do-not-build LIFTS — three additions at the emit site in one commit (in_loaded_chunk decider bool, Rider/VolumeRider bool, d.z-beside-feet.z as downgraded-to-context) + the head_clear is_ok_and one-liner folded in (unreadable must not render as clear). Teleport-up oddity loses its defect story (8-radius scans legitimately return higher nearby surfaces).
- 2026-08-11 (8): **#85 RELOCATED (Opus, triple self-inversion, all on reads): the suspect is now THE ULTIMATE FAIL-SAFE ITSELF** — firing on colonists inside normal chunk-unload windows and teleporting them via coordinates derived from absent terrain (retro-explains the teleport-UP oddity: a ground search against unreadable terrain). Loaded is a LATCH (chunk can unload beneath it); the freeze is BOUNDED (anchor None ⇒ one delete-sweep cycle ≈ the observed 59s); the 59s gap was the mechanism's PREDICTED signature, not a refutation. RULED: build `b5_failsafe_in_loaded_chunk` + target-z-beside-feet-z NOW as 5b fill (in-tree only, zero contact with the running endurance) — presence passing would HIDE this defect, not fix it, so the field guards the guard regardless. Fix-shape pre-noted: a ground search against unreadable terrain must REFUSE.
- 2026-08-11 (7): **ITEM 8 v2 LAUNCHED** (pin 34256b8a2b, driver-disconnect 09:14:08 EDT) — founding verified: 8 promotions, ZERO demotions (presence holding where v1 died). 5-cycle score point ~11:44, 7-cycle end ~12:44; Fable's heartbeat owns the health check and run-end wake. #85 renamed THE UNTICKED COLONIST, demoted-pending to the presence row's regression witness. (Commit 66a09cb0f0 was Fable's SECOND script-edit mojibake churn — restored, real edit here, rule hard-banked.)
- 2026-08-11 (6): **COLONY PRESENCE ACCEPTANCE: CLEAN PASS, all 4 measures** (pin ea2cfa5192) — 16 real eats, 32 preempts, needs cycling, 0 demotions, zero clients in the scored window; v1 honestly downgraded to inconclusive (unset diag). THE WORLD LIVES UNWATCHED, VERIFIED. **ENDURANCE RELAUNCH RULED** — 5b launches now; Fable's heartbeat wakes them at run-end (~2.6h); Opus scores acceptance + fills (Colony Mind design / #85 physics / dossier §5) during the run. GAME-VISION.md written (Ben request). Chartered-rows section added (~60-item honest board).
- 2026-08-11 (5): COLONY PRESENCE LANDED (7b00cce894 + acceptance diagnostic fb9a740110) — server-owned presence keeps founded colonies Loaded with no client; acceptance run next, then the endurance gate re-runs. BEN DESIGN THREAD: the Colony Mind (ant-model: colonists=ants+job-board stigmergy today; a colony-level drive arbiter — Sustain/Grow/Defend/Expand — weighting the generators as the future executive layer; god-interface + RL substrate) — pending Ben's go to charter beside the settlement arc, with a cheap now-rider: colony-level metrics (food-days, housing ratio, threat pressure) accumulate as diagnostics so the mind is born with senses.
- 2026-08-11 (4): Item 4 RE-SCOPED — the suspended case's closure stands (uid=166, n=1 limit honored); ITEM 4b opens on the GROUNDED case (uid=81: valid start, no mutex, destination computed, 6 verdicts, 0 plans, ZERO no_route — the original suspicion in purest form); first step = the verdict-enum producer read; files in the travel row's orbit. uid=109 = genuinely-new dispatch question (mutex absent), chartered-on-second-specimen. Fourth failure mode added to item 8's table (verdicts>0 ∧ plans=0, grounded). Endurance launch proceeding.
- 2026-08-11 (3): **ITEM 7 DONE — the farm economy is SELF-SUSTAINING AND COMPOUNDING** (775a715e2c): 8 founding seeds → 56/56/56 through THREE generations in one ~670s run, zero commands, zero stalls; the live-path wiring bug the first acceptance honestly caught (0/0/0) is documented in the doc as the gate-tests-live-path law's payment. Item-4 successor population now 3 fail-safes across the runs (routes to the suspension row). ITEM 8 IS FULLY UNBLOCKED — the endurance run launches next per the ruled packet.
- 2026-08-11 (2): ITEM 4 CLOSED — third reading (planner correct about an INVALID START STATE: mid-air suspension); suspicion not supported; successor row = the suspension physics + the teleport-up oddity. Arc 1 scoring now complete except item 8; only the founding-stock build + reseed leg remain before the endurance gate.
- 2026-08-11: **THE FULL FARM CYCLE CLOSED LIVE FOR THE FIRST TIME** (9098089c51): 20 sown → 20 MATURE → 20 harvested, 100%, no stalls. Open (named, not claimed): second-wave reseed — 20 new SOW jobs opened, zero claimed in window; script's wait was clocked from first-sow not first-harvest (timing-design gap, not a code finding); follow-up leg clocks from first harvest. #105's founding-seed-stock build is next with that leg as acceptance. Item-4 rider fail-safe count still owed (rescue counts reported twice; the ULTIMATE FAIL-SAFE number is the scoring input).
- 2026-08-10 (17): Item 4 → PATH 2 (candidate log predates the fix — a date that lines up is a coincidence until the artifact names its producing code); live-leg boot commit stamp added to protocol. NOTE: commit 7eb260114c was an accidental encoding-churn no-op (PowerShell UTF-8 round-trip mojibake, caught and restored from ed30a23fb5) — this entry carries the real edit.
- 2026-08-10 (16): #103's derivation LANDED (58cd1e8bef) — threshold = one expression over UNIFIED shared constants (the two happened-to-agree local copies eliminated at root), derived default 930s, test pins the real function; 80/80 green. Pilot fully landed all three stages; item retention alone waits on the identity row. 5b ordered: read accessor (critical path) → farming bootstrap; item-4 log question pending their answer.
- 2026-08-10 (15): Item 3 SCORED (partial-as-registered: sit class eliminated, 9→2 fail-safes, residual = travel class) — item 4 un-parked with a live population. Arm B′ floor GREEN 48-proven zero-caveat (every seed self-attests env now). Pilot gap found: NO READ PATH to the corpus (run-total only — aggregate-late landing on its own escape instrument); accessor ordered first, then the acceptance fan, with #103 commit + farming prep as the fan-window fill.
- 2026-08-10 (14): Seed-69 collision SUPPORTED (1 prune ↔ +1 orphan at exactly 120.0; access→0) — supported-not-proven, no queue-position field exists; the pilot gains its SECOND customer (seed-69 position + mover clustering). Mover population cleans: 5 unexplained orphan-movers, no prunes involved. #103's derived-in-code threshold and re-scope stand. Entity-log stage 1 = the arc's critical path.
- 2026-08-10 (13): ARM 2 SCORED — prediction CONFIRMED (max self-resolved stall 141 ≥ 120): the pruner was cutting recoverable work. RULED: default → 180 (a bound, not a calibration; 287 = the genuine customer), ships with effective-config-to-stdout (third stderr casualty). UNREGISTERED PRIZE: six seeds peak at exactly 120.0 with the pruner OFF — a hidden ~120s releaser exists; open row (producer hunt, moderate priority) — may reframe item 2's value onto the >180 tail. "A data-side config proof only works where the config would have mattered" banked (24 proven / 24 unproven census).
- 2026-08-10 (12): Mover lead's meaning corrected at producer (orphaned-claim detector, not removal witness) — lead sharpened to a COMPOSITION fact (removal ∧ live claimant); #94 reopened on claim-timing footing; prediction registered with can-I-evaluate asked = NO ⇒ ENTITY EVENT LOG PROMOTED to next build, mover prediction = its first acceptance customer.
- 2026-08-10 (11): ITEM 6 DONE — live acceptance passed (pile held 25 min vs 37s pre-fix theft); corpus witness structurally impossible (no scenario creates item drops), dormant sentinels documented. #83 closed (count gate 43, ENVPREFIX, zone probe, effective-config banner — all rehearsed). ARM 2 RUNNING at 9999 with the effective-value-on-VMs check owed before any field is read; registered prior on record (max(R) ≥ 119 expected, over-read owned if wrong).
- 2026-08-10 (10): Specimen diffs name the mover mechanism as an ACCESS-PLAN REGIME change (seed 51: access planning stopped, 37→0 + region blocked; seed 50: doubled + 4x emergency calls — opposite directions = regime, not gate). Item 6 EXITS the candidate set. Next: bisect access-plan commits (free). PRE-REGISTERED reading: if the bisect names item 2's sweep, the movers ARE arm 2's calibration question (does pruning at 120s cost outcomes stalls used to salvage?). FREE HEADLINE: bit-reproducibility measured across zones/hosts/instances — 48 seeds, every scalar identical except stamp+clock; machine-type fallback licensed by data.
- 2026-08-10 (9): Wave34 arm-1 landed via rescue (mislabeled invocation retracted; paired_base = true matched control). Batch INERT on outcomes (12→12, zero clause drift) ⇒ movers are STABLE-TIP properties, not bundle-transient — next step = specimen-seed field diffs (50/51), free. Item-6 witness: correct but scenario-INERT (all counters 0 incl. member pickups) — prediction VOID not refuted; "gate never refuses ⇒ gate innocent" explicitly NOT licensed (upstream suppression); premise-check question to 5b, else fields stay dormant sentinels + live-path verification. stalled_final earned immediately (6 still-stalling seeds, invisible before). Run-invocation-in-header now a required template field. Arm 2 blocked only on #83.
- 2026-08-10 (8): Wave34 arm 1 RUNNING (defaults, pin 7f20a184, witness+stalled_final landed, checker rehearsed pre-data). TWO-ARM ruling: arm 1 = movers/witness/prediction (instrument-only delta); arm 2 = raised BASTION_ACCESS_STALL_SECS on the same pin for the uncensored calibration — corrects the one-fan conflation of landing-vs-answering.
- 2026-08-10 (7): Wave33 addendum — seed-66 tool refusal resolved (REG-1 vindicated on its own named specimen); build signature widened to ≥3 seeds via clause-SWAP; item-6 witness ranked first in batch; colonist-refusal prediction registered for the witness fan.
- 2026-08-10 (6): Item 5 DONE (A/B pass, a35a98aaf7) — driver timing is server-authoritative; the under-run class that voided driver-12 is closed.
- 2026-08-10 (5): Wave33 folded — #38 closed, branch A/option-3 retired by corpus, ACCESS_STALL_SECS provisional (self-censoring instrument), item-6 corpus witness + threshold row approved as one batch/one fan; 5 movers (11→12) PARKED-tracked pending the witness fan (bundle diff refused attribution).
- 2026-08-10 (4): 5b closed both owed items (farming writeup; 54G stale incremental cleanup — E: +54GB) and placed the #73 preflight-graduation note; item 5 in build.
- 2026-08-10 (3): Item 7 re-rooted — seed-supply deadlock (structural); phantom-retire demoted to end-of-run sweep noise (refuted at source + 1.1ms timing window); two independent fixes chartered.
- 2026-08-10 (2): Opus's #102 in-flight audit folded in — nothing exposed; item 16 flagged pre-spec; settlement arc's known inheritance (Region zones) chartered.
- 2026-08-10: Created (rows #93–#102 folded in). Items 1 done, 3 landed-pending-score, 6 in acceptance, 7 blocked-with-coordinate; settlement layer added with immediate rider; thievery moved to 34's family; entity log designed.
