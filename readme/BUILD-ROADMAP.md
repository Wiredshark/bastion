# BUILD ROADMAP — the living list

**Maintained by Fable (orchestrator) — updated at EVERY item close, finding, or Ben directive.
Ben's standing order (2026-08-10): "you need to be constantly thinking about this stuff as we
finish features, you need to continually update this build list."**
Rulings live in `DECISIONS-FOR-BEN.md` (rows cited); this file is the current-state view.
Format: append changelog at bottom; statuses edited in place. Newest ruling wins.

## Standing design riders (apply to every item below)
- **Plot/plan data model** (#102): anything placing structures/zones designs against plots, never raw positions.
- **Live-emit declaration** (#88): every new harness accessor declares ported / harness-only-with-reason.
- **Diagnostics never verdict terms** (#70 pattern); **event-driven emits only** (diag-density budget).
- **Instrument + registry row land in the same commit**; **asset tuning touches compiled default same commit**.
- **Ruling acknowledged = packet edited, one operation** (#101). **Claims are not artifacts.**
- **A WITHDRAWAL is a ledger event too**: retraction and roadmap/packet edit are ONE operation — withdrawing has no natural trigger, which is exactly why stale entries outlive dead claims.
- **Every bar names its window AND its arena (who else can act)** (#95); success signature in every observable.

## ARC 1 — Close the survival loop (items 1–8)
| # | item | status |
|---|------|--------|
| 1 | Name the food thief | **DONE** (#95 — ambient NPCs; attribution complete, zero residual) |
| 2 | Stall counter (rescue cleanup counts progress, not claims) | **BUILT + corpus-exercised (wave33: prunes fire, both paths, attribution partitions perfectly).** ACCESS_STALL_SECS=120 PROVISIONAL — right-censored by its own reset (8 seeds at exactly 120; non-pruned 119 argues raise-never-lower); unblock row approved: env-tunable thresholds (+manifest rows same commit) + final-beside-peak emit, lands WITH item-6 witness, one fan scores both. Branch A retired (0/48 — option 3 dead by corpus, never built: measured-first paid). #38 CLOSED (prune fires, 14 events/11 seeds). |
| 3 | THE ARRIVAL FIX (sit trap) | **SCORED PARTIAL-AS-REGISTERED (Opus)**: 526 rescues/22 uids (precondition rich, fix self-witnessed), fail-safes 9→2 with both remainders attributed to a DIFFERENT mechanism (travel class) — sit class eliminated; residuals feed the travel row. Item 4 path RESOLVED: PATH 2 — the candidate log predates the fix (dated 08-04; its counter cannot exist there), so the 2-case re-score rides the farming confirm run as an observation rider; zero fail-safes there = stays open, never force-scored. Live-leg protocol gains the boot-time commit stamp (the last unstamped artifact class). |
| 4 | Egress planner (verdicts-without-plans) | **CLOSED — original suspicion NOT SUPPORTED** (Opus, 8d9b7d79c6): the clean post-fix specimen shows the planner CORRECT about an INVALID START STATE — the colonist was suspended mid-air (no ground, no wall, idle, z-velocity exactly 0, not falling); no_route ×10 was the right answer to a malformed question, and a destination WAS computed (the planner did its half). Limits held: n=1, chain inferred, route-existence unprovable. SUCCESSOR ROW spawned (travel-row orbit): the SUSPENSION state — does climb_free expiry leave entities without ground/wall contact? — plus the attached oddity: the fail-safe logged "teleporting to ground" while moving the colonist 5 blocks UP. |
| 5 | Server-authoritative test timing (Wait(n)) | **DONE** a35a98aaf7 — A/B PASS: sim-target matched (20.02 vs 20.01s) across unthrottled/contended with different spin counts (609/495), zero errors; failsafe corrected pre-land (mute wall-clock check → engine ServerTimeout signal, Opus catch); spin-direction surprise traced to producer (starved client's wall-dt grows) before reporting. Scope limit honestly held: whole-box contention arm, not isolated server-slowness. |
| 6 | Protected persistent provisioning + stockpile eating | **DONE — live acceptance PASSED** (field-placed pile protected the full 25-min run vs a loose drop taken in 37 s pre-fix; positive controls live). Corpus visibility: STRUCTURALLY IMPOSSIBLE under existing scenarios (5b premise-check at source: zero item-drop creation anywhere in ~40 harness scenarios) — witness fields stay as DORMANT SENTINELS with the premise documented; no bespoke fixture on spec. **CORPUS-INVISIBLE (wave33 gap): no field matches pile/protect/ambient/loot — witness row APPROVED (refusals-by-reason, pile-pickup attribution, reserved-units), lands with item-2's threshold changes, one fan scores both; WITNESS RANKS FIRST within the batch (wave33 addendum: build/material signature widened to ≥3 seeds incl. a clause-SWAP invisible to the fail count — the gate is the strongest unexplained-pattern candidate). REGISTERED PREDICTION for the witness fan: if "ambient-loot-disabled" refusals fire on COLONIST pickers, the movers are explained and the gate has a membership/timing bug — candidate, not claim.** |
| 7 | Farm-to-table self-sufficiency | **REAL ROOT CAUSE: SEED-SUPPLY DEADLOCK (structural, two code sites — 5b addendum 812757982c).** `FARM_SEED_ITEM`'s only producer anywhere is a successful HARVEST (bastion_jobs ~13692); harvest needs a mature crop needs a SOW needs a fetched stockpiled seed; worldgen volunteers explicitly unharvested ("a later nicety"). A fresh colony has NO possible first seed — sow jobs sit forever unclaimable. The earlier "phantom-retire = root cause" claim is WITHDRAWN in location/timing: terrain.get's ONLY failure is NoSuchChunk, (x,y)-dependent — z cannot discriminate (Chonk::get_unchecked infallible per z, read at source); all 48 retirements fired in ONE 1.1ms window <1s after driver disconnect = end-of-run chunk-unload sweep, not a during-run defect (job 703 survived ~4800 sweeps first). The sow/till asymmetry explained: till jobs complete and leave the board; seed-blocked sow jobs alone survive to be swept at disconnect. TWO INDEPENDENT FIXES: (1) seed bootstrap source (cheap confirm first: /give_item seeds run); (2) task #57 sweep splits read-failure from genuine mismatch (disconnect unload must not masquerade as designation change) — still worthwhile, no longer the cause. |
| 8 | Multi-day endurance run (TIER-1 AUTONOMY GATE) | waits on 2,3,6,7 — the "colony runs itself" milestone; Ben gets observer seat |

## ARC 1.5 — Infrastructure (runs alongside)
- **Entity event log** (#99/#100): designed (DESIGN-ENTITY-EVENT-LOG.md, premise-checked chassis, per-entity rings, promotion persistence). **PROMOTED — builds NEXT in 5b's queue.** Pilot vocabulary includes Released{job, reason, tick, actor}; **first acceptance customer = the mover investigation's registered claim-overlap prediction** (orphanings cluster near removals vs uniform — undecidable by run-total counts, decidable by the stream). The Voonoo query stays as the second acceptance.
- Reasoning dossiers (#98): FOOD pilot written; travel dossier at item 3's close; dossier per arc-close is ritual.
- Self-terminating run harness (#73 — server+driver teardown); collect_wave --baseline DONE (#67). Graduation note (Fable, 2026-08-10): when #73 lands, `stale_binary_preflight.sh` (scratch tooling, 5b) becomes its FIRST STEP, invoked by the run script itself rather than remembered by whoever launches a run — a tool someone must remember to run is a habit wearing a filename; wired into the runner it becomes law.
- Farming writeup (5b, DONE 2026-08-10): FARM-SOW-PHANTOM-RETIRE-FINDING.md + addendum -- root cause is a seed-supply chicken-and-egg deadlock (structural, code-verified), not the phantom-retire mechanism the original body centered on (that's real but incidental, a disconnect-triggered chunk-unload sweep). .engine-integration-wt target scoped cleanup (5b, DONE 2026-08-10): target/debug/incremental removed (54G, confirmed 2-days stale), target/no_overflow untouched.

## ARC 2 — Legibility (9–12)
9 Colonist inspector HUD · 10 Colony dashboard · 11 Recreation/idle life · 12 Chronicle UI (becomes the entity-log's player view).

## ARC 3 — Threats (13–15)
13 Hostiles near colony (Flee drive's live test) · 14 Guard job · 15 Fortification designations.

## ARC 4 — Economy & persistence (16–18)
16 Haul priorities + hauler-vs-eater contention (**plot-model rider applies**) · 17 Skills visible+felt · 18 Save/load integrity (entity-log promoted-set migration rides this).

## Capstones (19–20)
19 Renderer horizon retest (one-constant fixture fix — arena radius ≥ tested horizon) · 20 **Milestone playthrough v2** — LLM-player session scoring everything (#62 recurring gate).

## Band 21–40 (directional; design docs exist where noted)
21 Personalities visible · 22 Relationships · 23 Morale events · 24 Seasons that bite · 25 Shelter quality ·
26 Crafting chains · 27 Cooking · 28 Tool quality/wear · 29 Trade with vanilla world · 30 Stockpile zones by type (**plot rider**) ·
31 God-hand v1 (founding docs exist) · 32 Faith economy · 33 Miracles ·
34 Raids scaling with wealth + **THIEVERY as designed feature** (#97 — gate lift is line one; chronicle wiring pre-built per #100) ·
35 Injuries & medicine · 36 Death that matters ·
37 LLM-player harness v2 · 38 RL-tuning groundwork · 39 Performance row (sub-threshold tick degradation) · 40 Colony scale 16–32.
**THE COLONY MIND (#107, Ben direct)** slots beside the settlement arc: a colony-level drive arbiter (Sustain/Grow/Defend/Expand) weighted by board-computed colony needs (food-days, housing ratio, threat pressure, buffers), outputting WEIGHTS on the existing generators — never orders to individuals. The god-interface surface + the RL substrate. NOW-RIDER active: colony metrics accumulate as diagnostics in current work (the mind is born with senses).
**SETTLEMENT PLAN LAYER (#102)** slots as its own arc in this band, before 31: plot grammar reuse, district zoning, cost-driven form, desire-line roads, layout-quality corpus metrics.
KNOWN INHERITANCE (charter line, Opus 08-10): today's `stockpiles: Vec<(ZoneId, Region)>` painted-bounding-box zones ARE the raw-position model — the arc's FIRST MIGRATION TARGET, not a green field. Item 16 is stockpile-adjacent and carries the plot rider pre-spec.

## CHARTERED ROWS & PARKED QUESTIONS (the board beyond the numbered 40)
**Ben (2026-08-11): "I feel like we have more items than just 40" — correct; this section makes them countable.**
Open investigation/engineering rows:
- **4b** Grounded verdicts-without-plans (specimen uid=81; union counter split SHIPPED; scores from endurance/travel data)
- **#85** The suspension (a LOADED colonist that doesn't fall; lead: climb_free's x/y-only velocity writes; + the teleport-UP oddity attached)
- **uid=109 dispatch question** (fail-safe with zero egress consultation, mutex absent — chartered-on-second-specimen)
- **Mover mystery** — PARKED, 3 wake conditions (concentration real at p=0.021, storyless; stream exists for the next registered idea)
- **Multi-writer counter audit** (fill-sized; two union counters have each cost an investigation)
- **ItemInstanceId identity row** (identity survives pickup, notable-items-only per ruling; unblocks entity-log item retention)
- **Queue-position capture fix** (capture before the queue entry clears — seed-69's missing witness)
- **#73 self-terminating run harness** (server+driver teardown; stale-binary preflight graduates into it)
- **Farming fix 2** — task #57 sweep splits read-failure from mismatch (reporting honesty; no longer causal)
- **Colony Mind arc (#107)** — two-drive pilot (Sustain/Grow); design in progress, Architect-pattern spine; metrics rider ACTIVE
- **Needs-in-rtsim** (the (b) row — chartered at the multi-colony horizon)
- **Thievery** (designed feature, band 34 — gate lift is line one, chronicle wiring pre-built)
- **Founding kit** generalization (band 21-40)
- **External prior-art verification** (marked recalls to check before load-bearing use)
- **Ben's observed celebration session** (after the endurance gate passes)
- **TIME-COMPRESSED ENDURANCE MODE (Ben, 2026-08-11: "why can't we run simulations at a higher rate")** — uncapped-TPS server mode (env-gated; the live loop sleeps to 30 TPS by design, the harness already spins ~9× headless). Validation BY DETERMINISM: same seed run compressed and real-time must match state fingerprints tick-for-tick — proven once, **compressed becomes the default for ALL unattended runs including certifying ones** (Ben's sharpening: if the math is identical, a real-time pass adds zero information — ritual, not rigor). Real-time survives as exactly two things: (1) a REVALIDATION TRIGGER — one fingerprint A/B whenever the shell changes (tick loop, IO layer, anything wall-adjacent), a regression check on the equivalence proof itself; (2) HUMAN-IN-THE-LOOP sessions (perceived pacing isn't in the fingerprint — real time matters exactly when someone is watching, and only then). Watch items: hidden wall-clock dependencies (the OS-entropy tick_rng class), IO/chunk-load timing, anything reading wall instead of tick. High-leverage infra; spec after Arc 1 closes.
- **COLONY-VIABILITY SENTINEL / TERMINAL-STATE FLAG (Ben, 2026-08-11: "when all colonists die, end the simulation")** — rides the unconditional food-stock heartbeat: all colonists in breakdown + zero food, sustained N windows ⇒ emit `COLONY TERMINAL` (loud, unconditional) + env-gated clean self-shutdown for test runs (BASTION_END_ON_TERMINAL). Conservative predicate v1 — recoverable-with-food-incoming must NEVER read terminal (recovery data is gold); upgrades to "all dead" when the death band ships. Completes #73's server half operationally and closes the #82 nobody-polls gap with a signal instead of a protocol. Game-form rider on the threats band: colony extinction = real game-over, Chronicle summarizes the colony's life. Evidence: v3's 75 famine minutes ran on an ad-hoc judgment call this flag makes systemic.
- **INTER-COLONY MARKET ECONOMICS (Ben, 2026-08-11: "certain colonies have markets within each other — wood sells for food which sold for stone, based on each individual goal")** — scarcity-derived exchange: each colony's days-remaining senses (food/wood/stone-days from the par-stock row) ARE its internal price vector; an exchange rate between two colonies = the ratio of their scarcities; trade fires when valuations diverge enough to cover caravan cost — comparative advantage by construction, no global price authority, deterministic, colony-goal-driven (a Grow-leaning colony bids up stone; a starving one pays anything in stone for grain — the drives become market forces). Trade executes as jobs via generators (never orders to individuals). Depends: par-stock row, multi-colony (needs-in-rtsim horizon), caravan/trade band item.
- **COLONY MIND STRATEGIC LAYER (Ben, 2026-08-11: "we also need that hive mind ai for big strategic and tactical planning")** — the third layer: STRATEGY (long-horizon: expansion sites, trade relationships, specialization, fortify-vs-grow posture) sets goals for DRIVES (the #107 tactical arbiter) which set weights for GENERATORS which make jobs which ants take — the no-orders-to-individuals chain extended one level up. The strategic layer is the natural RL end-target (long-horizon judgment, hard to hand-tune, ideal to learn); drives stay deterministic and legible beneath it. Depends: #107 two-drive pilot shipping first; senses maturing (SLOWING and MATERIALLY-POOR blind spots close before strategy can see enough to plan).
- **PAR-STOCK PULL ECONOMY (Ben, 2026-08-11: "why would colonists ever mine or chop wood — we never really considered that in full")** — the demand ORIGIN for materials. Today every gather chain roots in a hand-painted designation; food is autonomous only because hunger is an intrinsic renewable demand signal, and wood/stone have no hunger. Design shape: colony par-stock targets (keep ~N wood/M stone) ⇒ stock-below-par = standing deficit ⇒ auto-generated gather designations INSIDE god-painted zones (god owns WHERE, colony owns WHEN/HOW MUCH); gives the Colony Mind its missing wood-days/stone-days senses; Grow/Sustain intensities throttle deficit-chasing via the exact KindIntensity plumbing #107 just built. Real sinks arrive with their rows (tool wear = the renewable material sink, crafting chains, fuel, fortification, trade). Prerequisite for build/craft/trade autonomy; spec is cheap post-#107.
Done-but-worth-counting process artifacts: live-leg commit stamps (protocol), dormant witness sentinels (item 6), dossier-per-arc ritual, the heartbeat loop.
**Honest count: ~40 numbered features + ~15 live chartered rows + parked questions with named wake conditions ≈ a 60-item board.** The 40 are the destination; the chartered rows are the road maintaining itself. Both tracked here; neither hidden.

## Changelog
- 2026-08-11 (85): **BEN ESCALATION → STANDING LAW: EVERY TEST RUNS IN FAST MODE UNLESS IMPOSSIBLE** — impossibility NAMED in the packet (human-in-the-loop, proven wall-coupled subsystem, or the equivalence reference arm); tradition never qualifies. Banked to memory (fast-mode-default-for-all-tests) + checklist entry 6 chartered (clears admission by prior cost: N=10 crash validation ruled unaffordable on wall-clock alone; v3+v4 spent 5h proving what fast mode proves in 40 min). The equivalence spec is now the test program's constitution; revalidation trigger = standing gate (shell changes PAUSE fast runs until the proof re-passes — never silent continuation). All run disciplines carry into fast mode unchanged.
- 2026-08-11 (84): **BEN SEQUENCING OVERRIDE: "the fast simulation needs to be implemented before any more tests" — v5 LAUNCH HALTED mid-sequence** (a server boot at 17:52:04 caught seconds old; stop order issued; binary/gate-0/launch-prep keep their value). New critical path: uncapped-tick mode + fingerprint hook (5b, promoted from fill to primary) → equivalence short pair incl. the §3 planted wall-clock failure → Opus review → **v5 launches COMPRESSED if the proof passes (~20-min arc-closer)**, real-time only if the proof surfaces something unresolved. v4 scoring unaffected (scoring, not a test run). One-binary + gate-0 disciplines apply to compressed v5 unchanged.
- 2026-08-11 (83): **v4 WINDOW COMPLETE (~17:24, server alive throughout — zero panics on the churn-wracked colony; process stability now proven on both binaries).** Run-end kicks issued: 5b teardown/capture → v5 rebuild (approved fix tip, double gate-0) → v5 LAUNCH (expected config delta: the unclaimed_watch threshold line, named in advance); Opus scores v4 (determined-fail via confirmed churn; F5=2/F6=0; sentinel calibration #2; inverse-correlation + position-repetition reads). v5 = the arc-closer.
- 2026-08-11 (82): **Equivalence spec drafted (ROW-TIME-COMPRESSION-EQUIVALENCE-SPEC.md) — and it ruled AGAINST the convenient option**: v5 CANNOT be the real-time reference arm — a log-derived fingerprint proves the equivalence of a SHADOW (b5_ harness-only, event log in-memory, board state unlogged, farm lines kind-specific — the exact gaps behind today's three wrong findings); the fingerprint is a LIVE tick-loop hook and the reference is a DEDICATED SHORT PAIR (equivalence needs a match, not endurance — and the proof decouples from every scored run's schedule forever). §3 mandatory planted failure: inject a wall-clock dependency, comparison goes RED by name (the proof must not be vacuous — every fast run inherits it). Fleet consequence banked: **compression makes a ruled-out evidence class routine** — N=10 fix-validation vs the intermittent crash was unaffordable this afternoon, becomes an hour's work.
- 2026-08-11 (81): **BEN DIRECTIVE: compression row PROMOTED to tonight's parallel fill + FLEET CLAUSE added** ("let the current simulation run, then build the fast simulation that maxes out our VMs"): v5 still flies real-time tonight (proven mode carries the arc-closer), but the build starts DURING the waits — Opus: fingerprint/equivalence validation spec (coverage + resolution, wall-clock hunt list, A/B protocol); 5b (after teardown/rebuild/launch duties): the env-gated uncapped-tick mode, smallest possible diff. **Fleet clause: compression × the 8-VM pool ≈ 24 endurance-equivalents/hour — endurance graduates from anecdote to CORPUS-GRADE evidence (N-seed survival rates; the run-many law satisfied at the endurance tier; the RL substrate's rollout engine).** Constraint: no scored gate flies compressed until the equivalence proof passes review.
- 2026-08-11 (80): **Churn ignition registered pre-scoring**: 12.4 reaps/min (first 15 min) → 3,352/min (next 55) — ~270× acceleration; linear extrapolation predicts 868 by minute 70, observed 184,527. **Predicted mechanism: FAMINE-COUPLED** — fewer claims → more positions cross the stale threshold → more jobs born past the gate → destroyed before claim → fewer claims; the loop feeds itself. **The spiral rider is RESURRECTED with a new engine**: its shape was right, its engine (preemption) was wrong — the sweep supplies it; re-earned via a discriminating read (reaps-per-window vs claims-per-window inverse correlation), not revived by assertion. **v5's falsifier registered: reap count must stay FLAT as claim rate varies.** Sentinel: two calibration cases = where a threshold stops being a guess. F6's silence during another subsystem's catastrophe = demonstrated SPECIFICITY, a nameable witness property.
- 2026-08-11 (79): **v4 mid-run (~70 min): churn EXPLOSIVE — designated_sweep_reaps 186 → 184,527** (create-reap at arbitration speed; the post-mortem's reap-vs-creation read is now trivial and the mechanism has a quantified signature). **THE SENTINEL FIRED — 3 COLONY TERMINAL lines, log-only as designed: calibration case #2 acquired from a second independent famine.** F6 zero across 70 min of pathological churn (the leak witness staying quiet while a different subsystem rages = a clean negative). claim_expiry_releases=2, eats=11, zero panics, run flies to ~17:24 per standing ruling.
- 2026-08-11 (78): **v5 fix APPROVED (3cce15dd33) — and 5b's flagged deviation CORRECTED THE REVIEWER'S CORRECTION**: Opus's created_at spec measured job LIFETIME (a job claimed at T+10, worked to T+900, released = instantly 890s "old" and reaped after a legitimate release) — the same defect shape as the bug it fixed. Right quantity = CONTIGUOUS UNCLAIMED DURATION (unclaimed_watch: claimed jobs dropped, release re-stamps fresh). **Closing theorem: three consecutive measurements of the wrong interval (position-since-claim → job-lifetime → contiguous-unclaimed), same tell each time — the quantity's name didn't match the question the gate asks.** Disclosure rule paid TWICE in one day, second time against the reviewer's own spec. Review also: cycles→seconds signature change deletes a unit-error class; both-polarity planted tests (never-reaps-fresh + still-reaps-old, so the fix can't pass by disabling the feature); cycles_since_last_claim untouched (never wrong, only misread); "a design needing no enumeration cannot have an incomplete one" (5b). v5 cleared for rebuild on v4's teardown.
- 2026-08-11 (77): **v5 route-3 fix BUILT (5b, 3cce15dd33, 88/88 green) while v4 still flies** — per-job `unclaimed_watch` (F6's periodic-observation technique) as the sweep's age input, deviation from literal created_at flagged with reasoning: no touch on every creation site, and claimed-then-released RESTARTS the clock (creation-time would hand a released job its full lifetime as instant age — born-past-the-gate one level down). Planted test named to carry the lesson: `sweep_never_reaps_a_freshly_unclaimed_job_regardless_of_position_history`. Cycle time on this defect: hypothesis 15:10 → code-confirmed 15:25 → fixed+tested 15:45, evidence run untouched throughout. Awaiting Opus review; v5 rebuild rides his green + v4's teardown.
- 2026-08-11 (76): **CHURN CONFIRMED FROM CODE, 15 minutes after the hypothesis registered — the fastest cycle of the day, bought by the heartbeat instrumentation**: the sweep's AGE INPUT is the POSITION's cycles_since_last_claim (increments every cycle, resets only on claim), not the JOB's age — **a newborn job at a stale position is born past the gate and reaped on sight** → cell frees → replacement → reaped again (no famine needed to start it). The constant is INNOCENT (930s-worth, correct units, live-derived — nothing could age 930s in 15 min; the jobs inherited an age). Opus's miss named at the joint: **A PURE PREDICATE'S TEST PROVES THE PREDICATE, NOT ITS INPUTS** — threshold provenance was checked, quantity provenance wasn't, and the field's name carried the answer. RULED: v4 flies to its end (counter series + sentinel calibration #2 accruing); **5b builds the per-job-age fix NOW in parallel** (created_at_cycle stamped at creation; planted tests: fresh-job-at-stale-position must NOT reap — "the test nobody thought to plant" — old-job must still reap, churn signature); v5 launches on teardown's heels. v4 scores as determined-fail on F2/F3 via a confirmed pre-registered mechanism. Arc shape: famine → fix → new famine → fix, each cycle faster and better-instrumented; **v5 has no known reason left to fail.**
- 2026-08-11 (75): **v4 EARLY CHECK (~15 min): alive, zero panics, HEARTBEAT INSTRUMENTATION WORKING (all four counters live) — and showing a NEW bad picture in real time**: food 0 at ~16 min (faster than v3), farm silent since minute 3, **designated_sweep_reaps=186 in 15 min vs v3's 87 jobs created in 2.5h**. Pre-registered hypothesis: the age-gated sweep is CHURNING (create → age out unclaimed → reap → regenerate → reap; gate too short vs claim latency under famine load — #103's shape: the constant, not the design); alternative branch registered (legitimate reaps + third famine cause); discriminator = position repetition + creation-vs-reap counts + gate constant vs observed claim latency. F5 fired (claim_expiry_releases=1 — the fix mechanism is live); F6 clean zero. No VOID met; run flies its full window per standing law; sentinel gets a second calibration case. Compression-row additions folded (thresholds checked sim-clock-safe; fingerprint must be tick-indexed with resolution stated — "the fingerprint is the instrument certifying the mode, so its own resolution is the proof's resolution").
- 2026-08-11 (74): **TIME-COMPRESSED ENDURANCE MODE chartered (Ben)**: uncapped-TPS env-gated server mode, validated BY DETERMINISM (same-seed compressed vs real-time fingerprint match tick-for-tick) — proven faithful, fast runs carry all iteration (2.5h → ~20 min) and real-time reserves for the final certifying pass. Wall-clock-dependency watch list named (tick_rng class, IO timing). Spec after Arc 1.
- 2026-08-11 (73): **ENDURANCE v4 LAUNCHED (3b7055153a, ITEM8-LAUNCH-RECORD-V4.md)**: DOUBLE gate-0 (sacrificial boot then real boot, Server version==b96830d1 both), rebuild verified from the Compiling list, 8/0 founding, config diffed vs v3 (identical, as expected), effective-config line emits generic_claim_leak_secs=1860 (2× access_stall_secs, no override) + sentinel threshold. Scored window from 18:54:39Z ≈ 14:54:39 EDT; expected end ≈17:24 EDT. **All four counters ride the food-stock heartbeat per-300-tick** (splits, claim_expiry_releases, designated_sweep_reaps, generic_claim_leak_releases) — no end-of-run-only scalars. VOID/finding conditions in the record pre-data: F5==0 whole-window = VOID; F6>0 = finding-not-failure; mid-run stamp mismatch = VOID. Run-watch: early check ~15:25, health mid-run, run-end kick ~17:25. **v4 PASS closes Arc 1.**
- 2026-08-11 (72): **b96830d161 APPROVED — v4 CLEAR TO LAUNCH.** Review verified by read: F6's reachability test EXISTS (generic_claim_leak_fires_by_name_past_threshold — the b5-counter loop closed), route-2 test, sentinel both-polarities-in-one-test, four heartbeat counters, config emission. **The route-3 deviation was 5b CORRECTING THE PACKET**: `settle_invariant_violation`'s `is_none_or` is trivially true for Designated (always None) — the packet's literal kind-list extension would have swept every unclaimed Designated on sight, zero grace, reintroducing the churn loop; Opus's fifth caught error of the day, catchable ONLY because 5b flagged instead of silently substituting. Ledger: **a builder who substitutes silently deprives the reviewer of the correction.** 5b's third unprompted self-catch accepted (live access_stall_secs() vs a bare 930 — #103's exact trap). Derivation row honestly scoped (2× access_stall_secs = bound-of-a-bound, named as such). Launch order issued: rebuild → gate-0 → v4 with config-diff/one-binary/clock-qualifiers → run-watch resumes.
- 2026-08-11 (71): **v4 FIX SERIES BUILT (5b, b96830d161 + 48a6579f62)**: routes 1-3 + F5/F6 dual witness + sentinel S1 + b5 heartbeat port, built against the pushed packet FILE (not message content), 86/86 tests green, all-targets clean. Deviation flagged not hidden: route 3 = a NEW age-gated sweep instead of extending the orphan sweep's kind list (literal extension would reintroduce a documented churn-loop bug; reasoning at the site). Awaiting Opus commit review (backstop-reachability check + the churn-loop claim's read).
- 2026-08-11 (70): **Corrected packet + results + checklist entry 5 on readable pin 989caddfa1; F5/F6 RESOLVED**: F5 = targeted-release witness (releases on unreachable jobs > 0, zero VOID); **F6 = the generic expiry KEPT as an INVERTED LEAK WITNESS** (zero firings = expected PASS, any firing = recorded finding of a route outside the targeted fix — the day punished incomplete enumerations four times, so the backstop stays), with mandatory reachability planted test (its absence would be the b5-counter failure exactly — checked at commit) and a threshold that cannot race the fix (§1a-i). Two witnesses of opposite polarity, neither able to hide the other. 5b building the full series (now FOUR planted tests). Dossier's closing line banked: **it converged because both lanes were wrong in opposite directions and both published the wrongness — neither could have found the collision alone.**
- 2026-08-11 (69): **JOINT TABLE SETTLED (3c2f5c5547) + Opus's full retraction with propagation traced**: his 0/87 was correct-and-useless (structurally fixed at zero — Farm exits at 14234, can never reach the generic line; Gather likewise at 14236); worse, his 57-stuck set was computed WITH that line, so it was mostly COMPLETED jobs — "57/57 arrived" described the wrong population entirely. Real: 59 completed / 28 stuck, all SOW, CLAIMED-AND-NEVER-REACHED — which is exactly what 5b's read-2 mechanism predicts (correction strengthens the candidate). Closing law, fourth application: **A MEASUREMENT THAT CANNOT VARY WITH THE RUN IS NOT EVIDENCE ABOUT THE RUN** — new costume: an instrument never pointed at the population, zero indistinguishable from a finding; detector = arithmetic that fits three ways, noted as coincidence. **BUILD AUTHORIZED (5b, now)**: unconditional claim release + amnesty-clears-claims (or cited redundancy) + sweep extension + 3 planted tests red-then-green + sentinel S1 + b5 port, one series, Opus commit review; Opus's corrected packet (§1 around 5b's table, F5 choice) due before LAUNCH, not before build.
- 2026-08-11 (68): **RECONCILED — route 1 DELETED, route 2 PROMOTED TO PRIMARY with a precise target.** Opus's 0/87 was a single-channel error (his third of the day, self-counted): farm jobs exit via dedicated arms that never emit the generic "job completed" line. Real table: 19/19 TILL, 20/20 HARVEST, 20/48 SOW; structurally verified (15 cells cycled 3-5 times; 32+30+25=87). **The defect: 28 SOW jobs never engaged, 32 cells never recycled — mechanism (5b's read): `to_release` drains claimed_by only when `!job.unreachable`, and the amnesty resets unreachable but never claimed_by.** The ratified post-expiry-lifecycle-must-end requirement NAMED THE ACTUAL BUG (unconditional release + land where the is_none filter picks it up). Ledger: a number that fits three ways outranks a number that fits one. **F5 TENSION flagged for the corrected packet**: with a precise primary fix, generic-expiry-zero becomes the EXPECTED outcome — drop expiry for a targeted-release witness, or keep it as a leak witness with the bar term INVERTED (zero=good, firing=finding) + reachability planted test; Opus chooses and states in §1. Process: Opus's packet/results docs lived on an unreadable private tree, routed by description — corrected (readable pin before routing, always); registered candidate #3.
- 2026-08-11 (67): **CONTRADICTION BETWEEN THE TWO LANES' MEASURED TABLES — RECONCILIATION ORDERED before any fix design.** 5b's mandatory reads (96ab192717): 59/87 farm jobs COMPLETED (19/19 TILL, 20/20 HARVEST — 100%; 20/48 SOW — 42%), 28 stuck jobs ALL SOW and NEVER ENGAGED, completion predicate WORKS when reached — against Opus's 57-stuck-all-arrived-0/87-completed. The tell: 5b's 59-completed matches Opus's "59 arrived-at" exactly — different fields under colliding names ('job completed' log lines never fire for farm kinds: 7 all run, Mine/Bed only). Both lanes cite producers, one joint table; results-doc famine section + packet §1 PROVISIONAL until it lands (incl. the "completion seam" name itself). **New leading first-cause candidate (5b's read 2, survives either table): preempt-release is gated on !job.unreachable — a job marked unreachable during churn keeps its claim forever, and the periodic amnesty resets `unreachable` but never `claimed_by`.** Also: ITEM8-V4-PACKET.md was never pushed — Opus pushes before routing.
- 2026-08-11 (66): **v4 packet CONFIRMED COMPLETE AND ROUTED to 5b**: sentinel S1 + b5 port were in all along (Opus's summary omitted them — self-caught: a summary that drops sections is a label; the packet is the content). Expiry provenance became §1a-i with teeth: constant NAMES its derivation in code, emitted in effective-config, never calibrated from a field it resets, and — the #103 conversion — **the expiry must EXCEED every existing release/redirect budget (travel watchdog first), margins stated, budgets READ before the number is chosen** (two mechanisms racing on one condition make both counts meaningless); derivation basis = travel budget + work duration; a named conservative guess + row is acceptable, an observed median wearing a derivation's clothes is not. 5b's build sequence: completion-predicate read → watchdog-budget read → claim expiry (ending lifecycle) → seam repair → sweep extension → Opus commit review → rebuild on fix tip (validated procedure) → v4. Arc 1 closes on v4 PASS.
- 2026-08-11 (65): **5b's three tasks closed (ef4a88abf7)**: rebuild verified from the build's own Compiling lines (no --bin; stamp 5845b680 matches HEAD — GATE-0's first exercise, works as designed; binary is the procedure's validation run, superseded by the post-fix rebuild per the one-binary rule); reap-server.sh's sacrificial first exercise clean (SIGTERM path; SIGKILL/mid-save honestly unexercised); **anomaly root-caused as a mechanism, not just dissolved**: v2's launch record QUOTED the presence-leg's 8.25MB as a rationale and two readers independently absorbed it as v2's own size — law shade banked: **a quoted number needs its run-identity label at the quote site**; env/config identical, ~1.5× fully explained by famine, not a blocker. All lanes now wait on Opus's two packet confirmations → route to 5b → fix build.
- 2026-08-11 (64): **ITEM8-V4-PACKET.md APPROVED (one ruling)**: §0 states what the run is FOR (famine fix + endurance bar; NOT the crash fix — that rides the planted test); fix order claim-expiry-first; **post-expiry lifecycle MUST END, stated explicitly** (an expiry releasing into unclaimed-unreaped migrates the defect — #60 in a guard's clothes; F3 is the outcome witness); packet OPENS WITH A READ (the farm completion predicate, a named unread); bar F1-F5 with F5's zero-VOID on the guard itself; six scoring refusals incl. route-2-success-must-not-hide-route-1-failure; launch preconditions incl. gate-0, output-read compile list, config diff vs previous arm, one-binary rule. Two confirmations before routing to 5b: sentinel S1 + b5 heartbeat port in-or-deferred-with-reason; expiry threshold provenance (producer named, effective-config logged, not calibrated from a field it resets).
- 2026-08-11 (63): **FIRST CAUSE CONFIRMED: COMPLETION SEAM — 57/57 stuck-claimed farm jobs ARRIVED-AND-WORKED, 0 completed** (59/87 arrived overall, 0/87 completed). Preempt-abandonment REFUTED (its signature is claimed-never-arrived; the data shows arrived). **Method banked: A WITHIN-RUN CONTRAST NEEDS NO MATCHED CONTROL — the population is its own** (v3 contains its own discriminator; the unmatched cross-run diff was never needed — and raising the confound is what forced finding the internal evidence). Spiral rider DIES with its engine (cliff curve = fixed unfinishable job pool + linear drawdown; no feedback loop needed; Sustain keeps its general argument, loses this exhibit). Fix order measured final: completion seam (the fix) · claim expiry (ships first — survives being wrong) · sweep extension (closer). Five-candidate table: 2 confirmed for their populations, 2 refuted with refuters named, 1 reclassified cause→container. Opus drafting the v4 packet.
- 2026-08-11 (62): **Double refutation, both pre-build**: (1) phantom-retire-was-completion DEAD on one read (812757982c is doc-only; the addendum's own text: retires fire all at once at disconnect — end-of-run cleanup, never a completion route). (2) **Opus downgraded his own "TILL 100%→0% regression" — the control was UNMATCHED** (prior-art run was seed-deadlocked with no famine and no preempts; v3 was starving with 127 rest- + 42 hunger-preempts). Law completed: the tree gives the control; VERIFY IT'S MATCHED. New leading candidate: **preempt-without-release** (claim held when hunger/rest preempts pull the colonist; never returns) — deciding read named: the 57's claimants vs preempt lines at claim-quiet time. **RULED: CLAIM EXPIRY SHIPS FIRST — it repairs the famine under EITHER first cause; a fix robust to its own diagnosis being wrong is what you build while the diagnosis is open.** Untested rider for the read: if preempt-without-release confirms, the famine is SELF-AMPLIFYING (hunger-preempts abandon farm claims → less food → more preempts — a damper-less positive feedback that would explain the cliff curve) — check whether claim-quiet times cluster along the food decline; if yes, the Colony Mind's Sustain drive gains its founding argument (a measured spiral it would have damped).
- 2026-08-11 (61): **ITEM8-V3-RESULTS.md FINAL** (title reads "of the UNFIXED BUILD"; reweighting in the header; 5b's --bin mechanism quoted; TILL regression as §3 first cause). **50× anomaly RETRACTED — v4 BLOCKER LIFTED**: v2's log is 287KB/~26min (~11 KB/min vs v3's ~7 — same order, no mismatch); the 8.25MB belonged to the presence-acceptance leg (diag ON, 27,640 lines). One misattributed number caused two downstream errors (the 52MB projection under the raw-commit sizing ruling + the phantom blocker); the sizing ruling survives being wrong but was ratified on a number off by 50× — recorded as a coin that landed well. Derivation checklist gains the run-identity term ("which RUN produced this number"). **Checklist entry 5 FILED as worded: BINARY PROVENANCE** (stamp vs intended pin before scoring; build verification reads the compiled-crates OUTPUT; source-tree verification is not binary verification; cost: a full 2h34m run scored to VOID). Next: Opus's predicate-diff read → v4 packet; 5b's final rebuild rides AFTER the famine fix (one binary for v4, not two).
- 2026-08-11 (60): **STEP 2 CLOSED BY THE EVIDENCE TREE: TILL completion regressed 100% → 0% against FARM-SOW-PHANTOM-RETIRE-FINDING.md's row89-green baseline** (48/48 then, 0/39 today; sow 0-complete both eras; phantom-retire GONE — 48/48 then, 0 today — item 7's fix worked). Two regressions in opposite directions; 20 sown + 20 harvested with zero completions = work happens, completion never registers. **Measured causal order: completion seam (origin) → claim expiry (guard) → sweep extension (closer).** Registered candidate with deciding read named: the phantom-retire may have BEEN farm completion wearing a bug's name — diff the completion/retire predicate across item 7's fix window; one read confirms or exonerates. Both Opus watchdog predictions settled WRONG on the record. Law banked: **prior art in your own evidence tree is a matched control you have already paid for — grep it before you reason** (source gives mechanism, the tree gives the control, together they give causation).
- 2026-08-11 (59): **v3 SCORED (ITEM8-V3-RESULTS.md; ledger row #108)**: presence PASS (2nd cert under famine), endurance FAIL (87/87 farm jobs never completed, food collapse, 8/8 breakdowns), crash fix + #85 NOT SCOREABLE (wrong binary), famine root cause FOUND AND MEASURED. Four registered wrongs recorded (claimed_by prediction falsified; sweep fix would absorb; 87=87 coincidence; source-not-binary certification). **Sentinel calibration with numbers: all-8 breakdown at 16:02:49, run continued to ~17:22 — the sentinel would have fired 79 MINUTES EARLY and destroyed the only live famine record; S2-first vindicated.** Read-list keeps checked-and-cleared beside used (five zero-returning patterns were naming assumptions, not absences). Opus → v4 PACKET CRAFT: famine fix in causal order (completion seam vs FARM-DIAGNOSIS.md prior art first, claim expiry, sweep extension), sentinel S1, b5 heartbeat port, gate-0 + output-verified compile as preconditions, run design justified against known intermittency. v4 PASS closes Arc 1.
- 2026-08-11 (58): **FAMINE ROOT CAUSE MEASURED — both framings true for DISJOINT populations**: 87 farm jobs → 57 CLAIMED-never-completed-never-released (claim leak) + 27 NEVER-CLAIMED (immortal, unreaped) + 3 released, 0 completed in 2h34m (41 completions all run: 39 Mine, 2 Bed). Opus's claimed_by=None prediction FALSIFIED as registered; the travel-watchdog inference (step 3) also DEAD for the majority (claims never released). **His proposed sweep-extension fix would have been ABSORBED** (sweep removes only UNCLAIMED — fixes 27 of 84, famine intact): #60's shape caught by measurement pre-build. **The likely FIRST CAUSE: 20 sown + 20 harvested with ZERO job completions — work happened on jobs that cannot register done** — and FARM-DIAGNOSIS.md documents the designed mechanism (sow places sprite → predicate stops matching → job retires); zero retirements today = diff against written prior art. Fix taxonomy three-route, in causal order: completion seam repaired + claim expiry as guard + sweep extension as closer. Also banked: 87=87 was coincidence (5/43 released positions are farm cells) — a count adjacent to the thing is not the count of the thing.
- 2026-08-11 (57): **Unfixed-binary MECHANISM found (5b, 5845b68068): the pre-launch build command's --bin filter silently excluded veloren-server-cli despite -p** — only common/client crates ever compiled, confirmed from the captured build OUTPUT (the command is the label; the output is the content). Upstream fix: build verification reads the compiled-crates list; downstream catch: gate-0 stands. **AND the 50× anomaly rests on a wrong number**: v2's log is 287KB not 8.25MB (that figure belonged to a different run — a right number from the wrong run, the silent-producer pattern landing in Opus's own framing); ≈12 vs ≈7 KB/min = same order, anomaly likely DISSOLVES, v4 blocker lifts on 5b's one-paragraph env diff. Relaunch sequence ruled: corrected rebuild → output-verified compile list → gate-0 stamp dry-boot (doubling as reap-server.sh's sacrificial first exercise) → anomaly paragraph → HOLD for scoring + v4 packet.
- 2026-08-11 (56): **Crossed rulings reconciled — identical scope.** Opus additions accepted: (1) PRESENCE'S SECOND CERTIFICATION VALID AND EARNED — fb9a7401 IS the presence build, so 0 demotions through 2h34m of famine stress is real data on a harsher leg than its own acceptance; (2) epoch-snapshot recovery KILLED structurally (a snapshot cannot contain a field the build never had). Relay delta: Ben had already received the pre-void "fix proven" claim — corrected IMMEDIATELY rather than held for the full report (a delivered false claim outranks a tidy timeline). Scoring proceeds on the real half.
- 2026-08-11 (55): **🛑 SCORING HALTED AT PREFLIGHT: v3 RAN THE UNFIXED BINARY** (boot stamp `Server version: fb9a7401` = the presence-acceptance build = v2's binary; three independent confirmations incl. the old `-> bool` split_off_one in that commit's source and 13 fail-safe lines carrying head_clear but NO membership bits). Nobody rebuilt server-cli between the fix chain's green and launch — Opus verified SOURCE (docs-only diff), never the BINARY; the log's line two had the answer for three hours. **VOID: fix claim, v2/v3 before-after (SAME BINARY BOTH ARMS — no treatment, no control), the substitute-precondition salvage, #85's secondary read. INVERTS: the crash is INTERMITTENT on the unfixed binary** (23.6 min crash once, 154 min survival once — the one-sample fuse). SURVIVES: famine root cause (code read, both binaries), all endurance/famine data as unfixed-build findings, presence-held. RULED: score the real half labeled honestly; fix-claim evidence REWEIGHTED permanently to the planted test (with an intermittent trigger, the direct-construction test IS the fix's evidence; live runs prove famine/endurance); **PREFLIGHT GATE 0 MANDATORY — binary provenance, stamp vs intended pin before founding (checklist entry 5, earned by cost per the admission rule; source verification is not binary verification)**; the 50× log-rate anomaly (8.25MB/23.6min vs 1.12MB/154min, same binary ⇒ conditions differed) BLOCKS v4 until named. 5b: rebuild from fix tip + stamp-verify + anomaly diff.
- 2026-08-11 (54): **v3 CAPTURE COMPLETE (5b, 807297f2db, ITEM8-V3-CAPTURE-REPORT.md)**: manual procedure exact, log stable across three reads (1,123,528 bytes — the 52MB estimate inherited v2's write rate, which didn't hold; another right number with a silent producer), userdata 779MB/474 files committed untouched with the size honestly disclosed (raw ruling was scoped to the log; v4-era pre-decision chartered: big evidence may want a dedicated branch/store). Headlines: ZERO panics across 274,200 ticks (~2h34m, 3× v2's fuse); 0 Farm completions; food 19→0 by 15:42Z; 331 breakdowns; 40 eats/48 sleeps; **0 demotions — colony presence held the entire run under famine stress, a quiet second certification.** 5b independently re-derived the b5_ gap + added a possible precondition-recovery path (save_universe epoch snapshots, routed to Opus as OPTIONAL). Opus kicked to score; Ben gets the two-result outcome on the verdict.
- 2026-08-11 (53): **Sentinel charter hardened pre-build (Opus, ratified)**: A SENTINEL THAT ENDS RUNS DESTROYS EVIDENCE — INCLUDING THE EVIDENCE THAT IT IS WRONG (premature terminal is self-concealing; the counterfactual dies with the run — field-cannot-calibrate-its-own-bound in an executioner's hood). **S2-FIRST MANDATORY**: log-only COLONY TERMINAL until N runs check the predicate against what actually happened; BASTION_END_ON_TERMINAL off until calibrated. v3 = the free calibration case (run the predicate over the famine tail at scoring: 3/8 despondent at T+75 vs run-end state). Mirror requirement: a REGISTERED NON-TERMINAL case that must NOT trip (breakdown-with-food-incoming recovery) beside the forced-terminal planted test; gate-input standards (it's a control, not a diagnostic).
- 2026-08-11 (52): **COLONY-VIABILITY SENTINEL chartered (Ben: "when all colonists die, end the simulation")** — terminal-state flag riding the heartbeat (all-breakdown + zero food, sustained ⇒ COLONY TERMINAL line + env-gated self-shutdown for test runs); conservative predicate (recoverable must never read terminal); completes #73's server half and closes #82's nobody-polls gap with a signal; game-form = colony-extinction game-over, rider on the threats band. Natural v4-infra item alongside the immortal-job fix.
- 2026-08-11 (51): **ROOT CAUSE CHAIN COMPLETE from reads alone, pre-capture**: the orphan sweep is KIND-SCOPED to the four self-job kinds (DepositRun/RestAt/EatFrom/Despond) — `Designated(Farm)` is never swept, so an unclaimed farm job is IMMORTAL BY CONSTRUCTION. Chain: sow claimed → first failure (step 2, the one open question — only has to explain the FIRST failure now) → watchdog releases claim (inferred from item 3's arc) → no reaper for Designated → job persists unclaimed forever → cell permanently occupied → no regeneration → famine. **Falsifiable prediction registered: surviving sow jobs show claimed_by=None** (confirms immortal-job, refutes claim-leak; Some(uid) = Opus wrong, leak right — recorded in advance). Ledger exhibit: TWO mechanisms each reasoned carefully about liveness (the sweep's owner-liveness comment for its kinds; occupied's "live" prose) and the famine lives in the COMPOSITION of their gaps — **reviewed care at N sites doesn't sum to coverage; the gaps between scopes belong to nobody.** Honest scope: ~19 remove_job sites inferred-not-read, one sweep closes it at scoring.
- 2026-08-11 (50): **occupied-construction read DONE (pre-capture, no log needed): COLLAPSE CONFIRMED — and THE COMMENT CONTAINS THE DEFECT.** `occupied = board.jobs.values().map(|j| j.pos)` — existence, not liveness; the comment says "one LIVE job per cell," the code computes "one EXISTING job per cell"; the word carrying the distinction appears only in prose (read-the-content in the code's own comment, at exactly the famine's origin; kin to split_off_one's ship-time comment — both comments doing the thinking at the failure site). Reframe refined, Opus correcting his own registered discriminator pre-data: **AN IMMORTAL JOB wearing a farm costume** — the lock is the job's EXISTENCE; claim leak is one route among several (unclaimable / unsatisfiable-completion show claimed_by=None); §4c primary read now "why is this job still on the board at all" (claimed_by · progress · age as columns). Class: ALL SIX occupied sites use the identical construction — the sweep is confirmation not exploration, and the fix lands ONCE at the shared shape. Design north star: "live" must be a property the code COMPUTES, not one a comment asserts.
- 2026-08-11 (49): **v3 RAN ITS FULL WINDOW — server ALIVE at 13:20 after 150+ min (v2 died at 23.6 min on the same scenario): the fix's before/after result is maximal**, including 75+ min under famine-cascade stress. Run-end kicks issued: 5b tearing down (manual procedure, raw log, untouched userdata), Opus scoring on capture (everything pre-registered: fix claim first via salvage, six measures on their own terms, #85 six-field read, teleport-up candidate read, §4c post-mortem occupied-first, read-list). Arc 1 does not close on this run; the post-mortem's fix defines v4.
- 2026-08-11 (48): **Famine post-mortem §4c registered pre-capture**: four candidates each with the read that decides it (claim leak → claimed_by vs despondent/rescued uids; seed stock at sow-creation; affordance/standable; board starvation), occupied's-construction read FIRST. Falsifiable prediction registered: if claim leak confirms, the travel-watchdog family is implicated (item 3's kind-agnostic watchdog releases colonists that never arrived; a released colonist leaves its cell occupied) — specific enough to be wrong, recorded either way. Class implication pre-drawn: **a cell's occupancy must not outlive its claimant's liveness** — every board resource keyed to a claimant has the same exposure; one-read-per-kind occupancy sweep rides the post-mortem, turning a farm fix into a class fix if the leak confirms.
- 2026-08-11 (47): **Pre-scoring collapse (Opus): the two farm puzzles are ONE** — farm generation is not count-capped; it's gated on an `occupied` set, so a sow job that never completes leaves its cell occupied forever and no replacement generates (149 farm lines in 6 min = every cell jobbed once; silence after = every cell occupied). One remaining named read decides the story: occupied's CONSTRUCTION (live-jobs-derived ⇒ claim-leak-wearing-a-farm-costume, the shared-exclusive-state family with the CELL as the lock — fix conversation becomes job liveness/timeout, not farming; sprite-derived ⇒ different story). **#85 extinction CONFIRMED IN STRONG FORM**: 4 fail-safe firings = exposure occurred, zero frozen-signature specimens — precondition MET, not the vacuous zero (six-field read on all four at scoring pending). Teleport-up RE-OPENED as candidate: 4 new instances at d.z 5-17 above feet; 17 blocks strains the higher-nearby-surface story; discriminating read = is the returned surface within scan radius of the colonist's own column. Refusal #5 passed its first real test before scoring started.
- 2026-08-11 (46): **v3 HEALTH LOOK (T+75): FIX HOLDING, COLONY IN FAMINE CASCADE — the pre-registered two-result split made flesh.** Server alive, zero panics at 3× v2's fuse (fix half holding). But the FARM LOOP WENT SILENT AT SIX MINUTES: sow jobs 104/106/107 created 14:54Z, never completed; zero Farm-kind completions all run (7 total completions, Mine/Bed only); food 0→22 peak→monotonic decline→flat 0 for 30+ min; last eat 15:41; breakdowns cascading (71/72/73 despondent ~16:04). Fail-safes: 4 firings, all normal rescue economy (named terminal causes, on_ground after) — not the #85 signature; teleport-up pattern repeats. RULED: NO INTERVENTION — the famine-response data is the first live recording of the failure system under total famine; run flies to ~13:18, capture ~13:20 with the SAVE as first-class evidence (the why-no-sows question reads from board state: seeds? claims? standable gate?). Registered pre-landing with the scorer. Also on record: this is the SLOWING blind spot made flesh — the colony was slowing from minute six and nothing could see it; the production-rate sense is now the most-evidenced dependency on the board.
- 2026-08-11 (45): **Colony Mind §6b horizon section folded (doc-only)**: composite-score ban now STRUCTURAL — three independent consumers require per-kind un-collapsed metrics (arbiter weighs kinds; par-stock chases per-kind deficits; a collapsed score DESTROYS the market's price vector). Blind spots PROMOTED from honesty caveat to chartered DEPENDENCY (production rates + material senses gate the strategic layer — "a dependency filed as debt is a dependency nobody schedules"). Recompute-not-persist turns out price-correctness-critical (a persisted scarcity number cannot be a price — stale valuations vs actual goods). Meta-law banked: a rule that keeps being re-derived by consumers who didn't know about it found something real.
- 2026-08-11 (44): **Two arcs chartered (Ben directives)**: (1) INTER-COLONY MARKET ECONOMICS — the days-remaining senses ARE each colony's price vector; exchange rates = scarcity ratios between colonies; trade fires on valuation divergence exceeding caravan cost (comparative advantage by construction, deterministic, no global price authority; the drives become market forces). (2) COLONY MIND STRATEGIC LAYER — strategy → drives → generators → jobs → ants, no-orders-to-individuals extended one level up; long-horizon planning (expansion, trade relationships, specialization, posture) as the natural RL end-target over a deterministic drive layer. Both in chartered-rows with dependencies named (par stocks, multi-colony horizon, #107 pilot first).
- 2026-08-11 (43): **Par-stock charter elevated to principle (Opus, doc-only during the run): AN AUTONOMOUS ECONOMY NEEDS A RENEWABLE INTRINSIC DEMAND SIGNAL PER RESOURCE CLASS** — a par stock without a sink is satisfied forever (the designation-only economy one level up), so tool wear is load-bearing in the charter, not a nicety. Per-kind intensity confirmed from a second independent direction (a future consumer needs the same granularity the arbiter does). Colony Mind senses limitation line now: it can tell the colony is HUNGRY, it cannot tell it is SLOWING, and it cannot yet tell it is MATERIALLY POOR.
- 2026-08-11 (42): **PAR-STOCK PULL ECONOMY chartered (Ben directive)**: "why would colonists ever mine or chop" exposed the missing demand ORIGIN for materials — gather chains all root in hand-painted designations; hunger is why farming is autonomous and materials have no hunger. Row chartered (see chartered-rows): par-stock targets → deficit → auto-designations inside god zones; wood-days/stone-days become Colony Mind senses; #107's KindIntensity is the throttle; tool wear chartered as the renewable material sink. Prerequisite for build/craft/trade autonomy.
- 2026-08-11 (41): **#85's secondary read VERIFIED live** (warn! at the fail-safe site carries all six fields; v2's log holds 5 ULTIMATE FAIL-SAFE hits — both channels, live + harness, "what a ported witness looks like"). **The contrast is the lesson, fixed at SPEC time**: "add a field to this log line" is live by construction (inherits a named emit site's reachability); "add a counter" is harness-only unless ported — same reviewer used both framings in one review; 5b built exactly what each ask described, correctly both times, so the defect was in the asks. Law banked: **WHEN ASKING FOR A WITNESS, NAME THE LINE IT APPEARS ON, NOT THE NUMBER IT COUNTS.** Checklist registered-candidate #2 filed (fails admission — no re-run cost yet); linked to candidate #1, both promote together if either ever costs a re-run. Scoring impact final: six measures + health + #85 unaffected; fix-claim exercise witness LOST (argued from v2's detonation, as pre-registered); splits:eats denominator dead.
- 2026-08-11 (40): **CORRECTION — `b5_split_off_one_fired` IS HARNESS-ONLY: two sites in the tree (declaration + increment), no reader, no emitter, no log line; the entire b5_* class never reaches a live log** (empirical: v2 log has 0 "b5_" hits vs 150 preempt_attempts). Entry-39's two-branch fork was TWO READINGS OF AN UNWIRED CHANNEL — the zero was guaranteed before launch at any split rate. JOINT failure named: Opus gated the counter without verifying a consumer; Fable built an early-check step and a pre-registered fork on its silence. **SALVAGE RATIFIED, dated pre-landing in the scoring doc — THE PREREG FILE IS NEVER EDITED MID-RUN** (a prereg corrected in place is indistinguishable from one tuned to its result; a dated amendment beside it is an honest instrument repair): v2 supplies the precondition (observed detonation, 23.6 min, same scenario/config/food loop), v3 supplies the outcome (before/after, a matched control in the wild — the fix's blast radius provably doesn't touch the population driver); splits:eats denominator DEAD for this run, not computed; live-emit port = v4 row (Opus files, his gate shipped it unported). #85 secondary unaffected (fail-safe fields emit live). Law: **A WITNESS NEEDS A CONSUMER ON THE SURFACE THAT WILL BE READ — a counter with no emitter is a doc comment that compiles.**
- 2026-08-11 (39): **v3 definitive early check PASSED** (~14 min into scored window): alive, log growing, 5 eats, food 12→9 — run continues undisturbed. **Registered early for scoring: 5 eats / 0 splits**, both branches pre-written — (a) single-unit piles ⇒ None path legitimate, fix-claim evidence VOID-without-defect (fix stays proven only by the planted test); (b) multi-unit piles existed ⇒ suppressed-path VOID-with-teeth. Discriminator: pile stack sizes at eat times, readable from the log at scoring. Also: zero 'NeedCrossed' grep hits despite eats — the crossing-witness pattern to be recorded verbatim in the read-list.
- 2026-08-11 (38): **RULED pre-teardown (Opus's arithmetic: ~350 KB/min ⇒ 52–75 MB log): v3's full server log commits RAW** — no gzip (unreadable via git show), no filtered extract (a filter's exclusions are invisible exactly when they bite); first full-length endurance artifact = the baseline, paid once; v4-era optimization deferred WITH condition: the filter spec, if ever built, derives from the list of what scoring ACTUALLY read from v3. Timing law banked: the capture-format decision made AT teardown would be made by whoever holds the file under time pressure — pre-decide anything that would otherwise be decided while the evidence is being written.
- 2026-08-11 (37): **RULED (Opus flag): v3's teardown uses the PROVEN MANUAL SEQUENCE** — reap-server.sh does not touch v3; its first live exercise is the post-v3 sacrificial dry run. The script's untested failure modes are all evidence-direction (truncated tail, unflushed last cycle, wrong-PID live writer during capture) and none is detectable after the fact — a truncated log looks exactly like a log. Law banked: **a new tool inserted between the run and the capture is a change to the one procedure that protects the evidence — change it when the evidence is cheap, never when it is the deliverable.**
- 2026-08-11 (36): **Both 5b hold-window fills landed**: (1) 40-vs-32 preempt gap RESOLVED as two loosely-named populations (d0df44e590) — 24 shared hunger-preempts + 16 reclaim-suspended-EatFrom the counter never sees + 8 silent cooldown arms the log never names; no defect, naming addendum filed (store's-unit law; the 8 silent increments flagged as unauditable-from-log-alone, mute-channel family). (2) #73 server-half: reap-server.sh + PID-file convention (d98d5ea21e), idempotent, four synthetic cases green, real server untouched-verified; SIGTERM→SIGKILL escalation honestly named NOT-tested — dry run approved post-v3 against a sacrificial server. Opus's audit self-correction banked: "I counted presence; the question was provenance" — the producer law's audit form (a table must carry the column saying what produced each row).
- 2026-08-11 (35): **v3 early-check partial at ~10 min into the scored window: ALIVE and WORKING** — PID 51100 (process start corrected to 10:46:51; the 10:42:40 sighting was an aborted launch attempt — a third true clock, qualified per the two-clocks law), log growing (168,953→169,865 over 8s), mine jobs completing, food stock steady at 12, zero eats/splits AS EXPECTED pre-first-hunger-crossing (~15.5 min at the 930s threshold). Definitive splits>0 check at ~30 min (~11:18 EDT).
- 2026-08-11 (34): **Opus audited his own "structurally under-witnessed" remark against the live bar — THE BAR SURVIVES**: 7 of 8 claims carry their precondition witness at design time (measure 6's zero correctly ruled non-vacuous by the preflight doc's own line: a proven-reachable channel reading zero is DATA, an unreachable one is silence); the single gap was #85's late-attached extinction prediction, which never went through packet-craft. **Headline finding: gaps come from claims attached AFTER a run's bar was designed, not from bars written carelessly.** Checklist entry-5 candidate ("a claim attached to an existing run inherits none of its bar's witness guarantees; re-run the precondition check for the new claim alone") RULED NOT-YET by the checklist's own admission law — caught-before-cost fails the cost-a-rework criterion; registered here verbatim with promotion trigger: first actual rework/shipped-misread from a late-attached claim's missing witness. The calibration test gates reviews; the admission rule gates the checklist; the stricter governs the file.
- 2026-08-11 (33): **v3 IS #85's DISCRIMINATOR RUN FOR FREE** — the four membership bits (0891889d64) are an ancestor of the launch tip, so the running binary emits all six #85 fields at the fail-safe site; nobody noticed because the bits landed in a different session from the row that wanted them ("the binary knew more than the board did" — the roadmap's own failure class, caught by two people describing the same work and one checking). Scoring procedure amended: #85 scored SECONDARY under no-cross-carrying; one firing decides all four gates, a false membership bit NAMES the component; **zero fail-safes needs its precondition** (below-grade exposure existed — zero firings with zero exposure = VOID on the extinction prediction, not confirmation; sit-trap law, third instance today). Under-witnessed question answered by tense: all three of today's precondition witnesses were added at REVIEW time (v2 shipped with none) — the past was structurally under-witnessed, the law generalising is the correction, and the v4-era packet (witnesses present before any reviewer touches it) is where the question settles. 5b's second unprompted verified self-correction of the day noted.
- 2026-08-11 (32): **Two-clocks terminology law adopted**: PROCESS START (10:42:40) vs SCORED-WINDOW START (10:48:31, driver disconnect) are both true and ~6 min apart — the window between contains a connected client, so scoring measure 4 from process start fails a correct run SPURIOUSLY; "the run started at" is banned without a qualifier; scoring anchors to the disconnect. Producer-law day pattern on record: not wrong numbers — right numbers with silent provenance (31.8 ticks/s, the two-writer counters, the two launch times). 5b's #85 four bits confirmed already landed (0891889d64); his hold-window fills assigned: (1) read-only 40-vs-32 preempt-gap producer read, (2) #73 server-half teardown, in-tree, executed against nothing until v3 lands.
- 2026-08-11 (31): **v3 launch record committed (b6ae81470b, ITEM8-LAUNCH-RECORD-V3.md)**: official launch 2026-08-11T14:48:31Z (≈10:48:31 EDT), 8/0 founding, food heartbeat firing, one clean connect/disconnect, both late refinements in the prereg pre-launch; expected end ≈13:18 EDT at the corrected 30-TPS baseline. Three launch-mechanics defects hit+fixed+documented (positional args, --no-auth, a real cwd-resolution gap for spawned binaries — assets-resolve-by-CWD family). Wake plan on 5b's numbers: early-crash check ~11:18, health ~12:30, run-end kick ~13:20. 5b heads-down on the #85 four bits while the run flies.
- 2026-08-11 (30): **#82 CLOSED by restatement (Opus, read-only during the quiet window)**: the hung-Wait theory was impossible — the driver exits ~60s in BY DESIGN (the scored window is unattended, that's the row); the real gap was an OBSERVATION PROTOCOL with no listener ("liveness via the food-stock heartbeat" written in a log note nobody polls — a heartbeat with no listener is indistinguishable from no heartbeat; mute-channel law, operational layer). **The already-adopted early-crash beat IS the fix** — a gap filed as a code seam closed by a process proposal made for an unrelated reason. Side findings: #73 half-satisfied (driver self-terminates by design; the SERVER half is the real remainder — rescope when touched); item 5's both-clocks line visible working in production (600 requested / 587 spins / 20.04 sim-s side by side).
- 2026-08-11 (29): **ENDURANCE v3 IS FLYING** — veloren-server-cli up since 10:42:40 local (verified by process check, not message; launch chain on branch: 517cb50f6d → bd9e26fbbe pin-fill GREEN → f5267f15bb denominator + corrected-TPS refinement). **Early-crash check ADOPTED into the wake plan** (Opus's ask, accepted on cost asymmetry — one glance vs two burned hours; his own caveat noted as the model form for one-instance process fixes): ~30 min post-launch — process alive, log growing across two reads, b5_split_off_one_fired > 0 (trigger population arrived; also confirms measure 3's first rest-cycle crossing). Schedule: early check ~11:13, health ~13:12, run-end kick ~14:12, scoring per ITEM8-V3-SCORING-PROCEDURE.md.
- 2026-08-11 (28): **readme/PACKET-CRAFT-CHECKLIST.md landed** (four entries with provenance: #88 LIVE-EMIT + snapshot-vs-accumulator rider; #102 plot/plan model; where-is-its-row + calibration test + both clauses; scoring-refusals founding five) — Opus's memory copy DELETED down to a pointer ("an index that repeats its target's content is a fork waiting to drift" — the config-fallback law one level up). Maintenance clause: new entries only when skipping one has cost a rework, rework named. Authoring-vs-gating kind-split recorded not acted on (seam at 8 entries; tell = spec-time readers never reaching the gating half). Awaiting 5b's v3 launch record.
- 2026-08-11 (27): **"The packet template" turned out to be a PRIVATE artifact both parties referenced as shared** — no file exists; what exists is Opus's standing PACKET-CRAFT CHECKLIST (entries: #88 LIVE-EMIT declaration, #102 plot/plan data model, + today's two: where-is-its-row with calibration test/both clauses, and the scoring-refusals founding five). RULED: promoted to readme/PACKET-CRAFT-CHECKLIST.md, Opus authors, one page, each entry carries its provenance; his memory copy becomes a pointer, the file is the single source. Also banked with mechanics: a derived figure is a baseline only if every term in its derivation is nameable; cheap tell — a measured rate ABOVE a capped target indicts the measurement, not the system; default — prefer a declared constant for any bound (it carries its producer by construction).
- 2026-08-11 (26): **Scoring instrument written BEFORE the data (ITEM8-V3-SCORING-PROCEDURE.md)**: preflight VOID gates → fix claim scored FIRST (three-way rule, splits:eats denominator) → six measures → zero-window → health at 30 TPS. Its "WHAT I WILL NOT DO AT SCORING TIME" section (five pre-commitments against sunk-cost generosity, incl. no cross-carrying between co-resident results — "the fix can hold while the colony still starves, and that is legible, not mixed") is **ADOPTED PROGRAM-STANDARD**: every future scored gate's procedure carries one, written by its scorer before data exists. Also: the ~31.8 ticks/s v2 baseline Opus handed 5b was CORRECTED by its own producer — server target is `const TPS: u64 = 30` (read, not assumed); 31.8 embedded an unwritten start-point choice and would have read a healthy server as 6% degraded. Ledger line: a derived figure is a baseline only if you can name every term in its derivation.
- 2026-08-11 (25): **GREEN — fix chain cleared at 468fe8f07c (+517cb50f6d refinements), ENDURANCE v3 LAUNCHING on it.** All four gate items verified structurally (witness partition strictly inside the Some branch, vanilla door None-only; exit debug_assert checks the REAL struct invariant not a length proxy, with "file a row, don't patch around it" in the assert message — today's law embedded where the next person will be standing; precondition witness + three-way rule; merge-half named UNPROVEN; accessor-trap row filed). **Opus's own rate-condition gate item was UNSATISFIABLE** (no v2 splits counter ever existed) — caught by the calibration test's own question and refined to an internal check: splits commensurate with the run's own EatFrom completions, near-zero-splits-with-healthy-eats = path suppressed = VOID; no external baseline needed. Two clauses ratified into the law: (1) before issuing a no-row-no-pass flag, STATE THE OBTAINABLE OBSERVATION that would satisfy it — none obtainable = malformed flag, refine or drop, never hold the gate; (2) a gate item the builder cannot satisfy is a FINDING ABOUT THE GATE and must be flagged, not silently omitted. Arc-closer on record: the fix was ready three messages early — everything after bought what the green will MEAN. My heartbeat owns the run wakes; Opus scores on landing; #85 four bits next in 5b's queue; #107 behind the verdict.
- 2026-08-11 (24): **Prereg reviewed early — consolidated FOUR-item gate, one amend cycle** (Opus). Gap 1 (the catch): the v3 bar was crash-ABSENCE with no precondition witness — a silent run cannot distinguish "fix worked" from "trigger never occurred" (sit-trap law verbatim); required: counter on split_off_one returning Some, **splits==0 registered VOID not PASS**, and the rate condition in the prereg text (v2's 23.6-min fuse makes a silent 2.5h strong evidence ONLY at comparable split rates). Gap 2: the prereg's own delta table scoped out the mine-supply `.item().amount()` unsafe-accessor trap with no row — same shape that cost v2, caught in a table this time; law's final form: **a scoped-out failure mode is tracked in the LEDGER or it is not tracked** (doc comments and prereg tables alike cannot page anyone). Full gate: (1) witness exclusivity proof/unified writer, (2) split_off_one exit guard or row, (3) splits counter + zero-VOID, (4) accessor-trap row. Three of four are witnesses, the right ratio for a fix whose failure mode is silence. Ratio ruling on Opus's self-check: three no-row-no-pass hits in one review = the law working, not tiresome — calibration test set: a flag gates iff the scoped-out condition could silently cost a run or misread a result; cosmetic-worst-case = "row optional, noted."
- 2026-08-11 (23): **Opus commit review: fix APPROVED, chain AMBER on two gate items — both today's laws at first application.** Fix verified by read: len()==1 makes the assert's iterator empty, the violating shape is UNCONSTRUCTIBLE (over-satisfies the post-condition bar); planted test reconstructs the live crash operation; the ship-time comment now carries the lesson ("the comment that caused this now warns about comments that cause this"). GATES: (1) 5509dc95c3's restored witnesses each have TWO WRITERS (vanilla inventory_manip + new bastion_jobs sites) — the union-counter class MULTI-WRITER-COUNTER-AUDIT cleared from this exact file this week; falsifiability counters must not drift upward (over-counting fails in the direction that looks like success); 5b supplies a documented exclusivity proof (which door does the last-unit None consumption take?) or a single shared writer. (2) no-row-no-pass applied to the review queue's own first item: split_off_one is invariant-safe because multi-entry stackables cannot exist, not because the method guarantees it — debug_assert on exit or a row, silence not an option. Closer on record: **this fix's residual is the same shape as the bug it fixes — defended at review time by a guard instead of at ship time by a doc comment, which is the entire delta the day bought us.** v3 launches on the amended reviewed pin.
- 2026-08-11 (22): **Both fix-review requirements answered by CHECKING (5b; pins e14795700e fix → 5509dc95c3 witness restoration → 9530d34748 v3 prereg)**: (1) the timing delta DOESN'T EXIST — PickupItem::amount() was always total-invariant across a split, the physical decrement always happened at consumption in both designs; consumers (has_capacity/reserved_count, job_still_wanted Farm arm, food sampler) enumerated and cleared, table in the prereg. (2) the seam question WAS REAL: **two genuine witness regressions found** — b5_pile_pickup_by_member (item 6's B2 falsifiability pair) and ItemEventKind::PickedUp both fired only inside the vanilla InventoryManip::Pickup handler the consume-direct path bypasses; **both restored at the new site**. A benign-assertion would have shipped both losses silently. ITEM8-V3-PREREGISTRATION.md written (launch cites v2 record, delta table, unchanged bar, registered falsifiable prediction: the trigger population occurs live and the assert stays silent). All routed to Opus for commit review; review to confirm the restored PickedUp keeps its meaning/denominator at the new site.
- 2026-08-11 (21): **Fix BUILT (5b, awaiting Opus commit review): split_off_one returns Option<Item> — never grows the Vec, post-condition unconditional**; split moved reservation-time → arrival-time, split value CONSUMED DIRECTLY (no bag round-trip, no new entity/Uid — reservation and EatFrom keep keying the original pile Uid). Accepted as inside direction 1, stronger than born-single-entry. 3/3 common tests green incl. the planted crash-reproduction test (direct construction, not workload-dependent). Two review requirements added: (1) ENUMERATE THE DELTA of the timing move field-by-field before v3 (pile amount stays higher for the walk window — every consumer named: job_still_wanted Farm arm, food-days metrics, diag corpus fields); (2) NAME THE SEAMS the bag round-trip used to cross — no pickup event now, so the event-log eat attribution (#95 lineage) and pile-protection accounting (#96/#97) must each be shown witnessed through another instrumented door, or the loss named deliberately. 5b's fill = ITEM8-V3-PREREGISTRATION.md (launch procedure, fix pin, expected-delta table, unchanged bar, registered prediction: live splits-then-merge-checks occur and the assert stays silent).
- 2026-08-11 (20): **Finding review ACCEPTED + the deepest fact of the day: THE ASSERT HAS BEEN VACUOUS FOR THE LIFE OF THE CODEBASE** — non-stackables are trivially full (max 1), stackables never produce a remainder (overflow past u32::MAX never happens), so vanilla PickupItems are effectively ALWAYS single-entry and the invariant's iterator was empty until #89: **we didn't violate an invariant, we created the first population in which it has content** (status-quo-is-the-exercised-population, purest instance — the text existed, the exercised population was empty; "pre-existing in shape" dies for the stronger reason: we invented the shape). **FIX RULED: DIRECTION 1** — the eat path stops creating multi-entry stackable PickupItems; the vanilla invariant stands (an assert that has never been exercised is the one whose purpose nobody knows; direction 2 = renegotiating a contract with an absent author for unenumerated consumers). Natural shape: SPLIT AT THE SOURCE — decrement the pile's stack in place, construct a born-single-entry PickupItem; 5b confirms or refutes from call sites, alternatives stay inside direction 1. Precision: pick_up is UNMODIFIED (#89 added split_off_one so the untouched pick_up pops the pushed single) — finding doc corrected. **Opus's review-practice addition adopted PROGRAM-WIDE into the packet template: every "scoped out / deferred / acceptable for now" in a reviewed diff gets "where is its row?" — no row, no pass.**
- 2026-08-11 (19): **Opus's independent read CONVERGED with 5b's dig — root cause confirmed OURS, and the comment's own defence refuted by read**: vanilla try_merge always pushes the partial LAST (full self_last, then remainder), so a multi-entry stack built by normal means satisfies the invariant by construction — **split_off_one is the only thing in the codebase that creates a partial non-last entry**; "pre-existing in shape" is dead. Opus named his own miss: he scored #89 with the three-part dismissal (unenforceable · pre-existing · out of scope) standing unexamined — three individually checkable claims, checked by nobody, "the comment did my thinking for me." Reviewer-independence trade made correctly: sent his read to 5b with stake flagged ("do not ratify me") + an open question (len()==1 behaviour). **FIX RULED — 5b builds, Opus gates at commit**: (1) invariant as POST-CONDITION, never crash-absence; (2) one design-space-deciding read FIRST — if max_amount()==u32::MAX holds for stackables, the 1-amount single is ALSO partial ⇒ no ordering of two partials can satisfy the invariant ⇒ fix must EXIT the single-Vec design (remainder as separate pickup/entity); (3) ship-time comment rewritten — its prediction becomes the planted test ([Stack(39), Item(1)] merge-checked, red-then-green by name), comment cites the test; (4) pins green → review → endurance v3 on that pin.
- 2026-08-11 (18): **CRASH ROOT CAUSE FOUND (5b, pin 15e8884b8a): OURS, not vanilla — #89's split_off_one is the producer**: it pushes the new split as the LAST entry, leaving the decremented original stack NON-LAST; for stackables max_amount()==u32::MAX so that entry permanently violates try_merge's all-before-last-full invariant, detonating on the next merge-check against a fresh same-item drop — which sustained farm/haul cycling reliably produces. **split_off_one's own doc comment predicted this exact failure verbatim at ship time and scoped it out.** Law grows a clause (sufficiency-claims memory updated): A SCOPED-OUT FAILURE MODE IS A SCHEDULED CRASH UNLESS TRACKED — naming the case is necessary, not sufficient; a predicted-and-deferred failure mode must also become a ledger row with its trigger condition, because a comment cannot page anyone. 5b corrected his own "not bastion" call-site misread in the doc. Finding routed to Opus (first-line); NO fix before his ruling; 5b's fill = the #85 four-bit amendment; endurance v3 on a pinned reviewed fix only.
- 2026-08-11 (17): **Colony Mind measure 2 split into two legs after the identity default exposed a self-contradiction one line from shipping**: at identity 1000‰ the wiring is behaviour-preserving BY CONSTRUCTION, so "fixtures break at S3 = success" was wrong as an unconditional. Now: leg 2a A=A control (identity; fixtures MUST stay green — red = rounding/determinism bug in colony_cap itself, blocks S3, a failure mode with no witness before the control existed) + leg 2b absorption (forced non-identity; fixtures MUST break by name). Same fixture, opposite readings, one stage apart — the packet requires declaring WHICH LEG before reading the result. Mechanism noted: the identity default and the contradiction-catch were one event — a true control forces every reading in the measure to declare its leg. Opus's arc summary on record verbatim: "the five retractions were not the cost of the arc, they were the mechanism of it — every one died to a read that would have happened eventually; the only thing that changed was whether it happened before or after a build."
- 2026-08-11 (16): **ENDURANCE v2 CRASHED at ~23.6 min** (tick 45000, cycle 1; pin f8abfffecb; ITEM8-CRASH-FINDING.md): debug_assert in PickupItem::try_merge — "All items before the last in PickupItem should have a full amount" (inventory/item/mod.rs:1752). 5b report-first, no relaunch — the crash IS the result; the run stands as the deepest sustained load the loop has had (12 preempts, real eats, 23 min compounding farm churn). RULED: 5b's "vanilla pre-existing, not bastion" classification must clear the invalidating check first — **#89 SPLIT-PICKUP modified PickupItem::pick_up (pop-last + reinsert-remainder), i.e. WE changed the producer of exactly the state the assert polices**; his grep cleared the CALL site, not the CONSTRUCTION site, and the violation plants at construction, detonates at merge. 5b digs the producer (main task; #85 four-bit amendment yields), root cause written with the constructing site READ, both branches argued; NO fix before Opus review; endurance v3 relaunches only on a pinned reviewed fix. Reframe on record: the debug_assert firing is the instrument WORKING — release would have been silent inventory corruption. Item 8 stays OPEN; Arc 1 close waits.
- 2026-08-11 (15): **ROW-COLONY-MIND-PACKET.md written and APPROVED for a builder** (queues behind the endurance close; 5b finishes the #85 four-bit amendment first). Opus caught that my both-ends intensity clamp was necessary but NOT sufficient — integer division can floor a positive intensity to a ZERO cap (1×1×500‰=0; unreachable today only because BASE=2, reachable by the routine tuning this row enables) — law: **A CLAMP ON A FACTOR IS NOT A CLAMP ON THE PRODUCT; enforce the bound at the unit the consumer reads.** Packet ships colony_cap() with .max(1) + a planted test defending the guard. Arithmetic: integer permille, multiply-all-then-one-division-last (rounding rule and implementation are the same fact); measure 6 requires a bit-identical CAP SEQUENCE. Measure 2's inversion in bold (green fixture after wiring = weight absorbed = FAIL). Ruled at approval: intensity_min/max values DEFERRED to S2's trace (no provisional numbers — #103's lesson) + **default intensity = IDENTITY 1000‰ every kind** so unset/unloadable config = current behaviour by neutral element, S2 = true A=A control. ColonyDrive (not Drive) naming, corpus from S2, zero-window, Sustain lock-in planted test with seeds-49/50 provenance all in.
- 2026-08-11 (14): **#107 SHAPE SETTLED AND RATIFIED — the charter's sentence was literal**: the generators are three `pub const` per-kind caps (HAUL/MINE_GEN/BUILD_GEN_JOBS_PER_COLONIST, all colonist-scaled), and the Colony Mind = KindIntensity MULTIPLIERS ON THOSE CAPS. Allocator (lexicographic priority/score) LEFT ALONE — ordering becomes emergent from what exists to allocate, stigmergy intact; unread "how priority is produced" retired as a scoping decision, not a gap. **CHARTERED BOUNDARY: need jobs (EatFrom/RestAt/Despond) permanently out of scope — modulating need thresholds is orders-to-individuals through the back door.** Free absorption test: bastion-harness already asserts exact cap arithmetic, so a wired drive breaks fixtures BY NAME (green after wiring = the weight was absorbed = measure FAILS — inversion stated in bold in the packet). Two packet requirements added at ruling: intensity clamped BOTH ends in ColonyMindConfig (uncapped multiplier = unbounded job-board cost) + deterministic representation (fixed-point/permille, named rounding, fixed multiplication order). Opus proceeds to packet craft; row sits behind the endurance close. 5b's #85 discriminator commit 5d905a247d reviewed CLEAN.
- 2026-08-11 (13): **"NO DEMAND FORMULA" RETRACTED + THE FIND OF THE DAY: a drive arbiter with hysteresis ALREADY EXISTS in bastion_jobs.rs at per-colonist scale** (Drive{Work,Flee,Idle,Personal}, modulated_urgencies, incumbent + ARB_HYSTERESIS switch guard, BASTION_ARB_SWITCH_DIAG) — the Colony Mind design doc independently re-invented hysteresis (its principle 4) and anti-thrash (its measure 4), both shipped with a documented regression lesson attached (Personal-incumbent asymmetry, seeds 49/50). Retraction cause: fn-name greps cannot establish concept absence — scoring is INLINE and selection is LEXICOGRAPHIC (priority discrete, then score continuous), so my permutation ratification dissolves with its premise; final action-space shape awaits Opus's SINGLE consolidated position after reading the three named unreads together (how priority is produced; what modulated_urgencies computes; Drive's per-colonist scope). **#107 re-scoped: INSTANTIATE the existing arbiter pattern at colony scale, don't invent** — "Sustain incumbent, Grow can never win" becomes a named inherited failure mode with a planted test. Laws banked: SURVEY THE TARGET FILE BEFORE SURVEYING THE FIELD (prior-art order: target file → own tree → evidence tree → the field); a grep pattern is a claim about naming — absence-of-match proves absence only if the naming claim is verified.
- 2026-08-11 (12): **COLONY MIND DESIGN WRITTEN (COLONY-MIND-DESIGN.md) — and its §0 entry-gate voided its own central mechanism before the packet opened**: NO demand/priority formula exists in bastion_jobs.rs; work allocation is a hard boolean precedence ladder + nearest-first within kind, so the chartered DriveWeights multiplier had NOTHING TO ATTACH TO (would have compiled, logged plausible weights, changed nothing — #60's absorbed-fix shape, caught for the cost of four greps instead of three stages). **Corrected shape RATIFIED: the arbiter emits a PERMUTATION over job kinds (KindOrder), not a weight vector** — design law: the arbiter's action space must be the space the consumer actually reads. Safety improves (a permutation cannot starve a kind; reorder-at-the-branch is a cleaner absorption test) and the RL substrate improves (discrete action space). S0 next (Opus): enumerate BOTH organs — the full allocation ladder rung-by-rung AND the job-generation sites (charter said weights-on-GENERATORS; the no-purchase finding was on the allocator; intensity may still have numeric purchase at generation — if not, permutation-only ships with intensity as a named non-capability). Doc carries: 6 measures/6 planted failure modes, S2 = log-only whole stage, registered zero-window (early Sustain invalidates), the SLOWING blind spot named ("it can tell the colony is HUNGRY, it cannot tell the colony is SLOWING"), DF honesty (the reason is the god-interface and RL substrate, not that the ants need it).
- 2026-08-11 (11): **#85 gate 4 RETRACTED by its proposer** (~5 min after proposing — `StateExt::create_npc` inserts all physics components in one atomic builder chain; the tick.rs decoration is a bastion component, wrong storage; specimen had a PhysicsState). Fifth mechanism, fifth self-kill by producer-read. **Opus STOPS proposing mechanisms — endorsed and banked as law: when reading can no longer narrow the field, instrument; a story that fits the specimen is the cheapest artifact available.** Build ruling survives the retraction: membership BITS stay (their justification was the missing witness, not gate 4's liveness); final 5b commit = in_loaded_chunk + is_rider + 4 membership bits + d.z context + head_clear fix. Keeper: Vel(zero) is the CREATION value — velocity.z==0.0 also reads "never integrated since spawn". **ROW-COLONY-PRESENCE FORMALLY SCORED: 4/4 PASS** (ROW-COLONY-PRESENCE-SCORING.md; measure 3 cleared a higher bar — 16 completed unattended eats retire the count-vs-mechanism objection; measure 2 a real trajectory not endpoints). Coverage gaps recorded: disband-leak NOT exercised, cost-per-colony an ESTIMATE (item 40 inherits it as one). Banked: 50% preemption completion rate = first baseline; 40-events-vs-32-counter producer gap on the next-in-file list. 5b's unprompted self-downgrade of their own v1 claim ratified as the row's honest half.
- 2026-08-11 (10): **#85 GATE 4 found — JOIN MEMBERSHIP** (Opus): the `par_join()` component list is itself the first physics filter, stricter than any `if` — an entity missing Collider/Mass/Density/Body is dropped with no branch and no log, and rtsim tick.rs decorates colonists INCREMENTALLY after creation, so a missing-component window is live and matches the specimen exactly (also survives the dest=Some constraint that killed gate 1). Gate-2 supporting evidence corrected: every `dismount` in bastion_jobs.rs is a ladder-exit cell, not the mount component (name collision; join exclusion itself still stands). RULED: field set amended — the joinable AND is REJECTED per aggregate-late (a false AND can't name the missing component); 5b builds the four membership bits individually + in_loaded_chunk + Rider/VolumeRider + d.z context + head_clear fix, one commit, one firing decides all four gates. Endorsed regardless of outcome: silently-outside-the-join currently has no witness anywhere in the engine. Law banked: **a join() IS a filter — reading only the `if` inside it enumerates half the gate.**
- 2026-08-11 (9): **#85 relocation WITHDRAWN** (fourth inversion, all on reads): the fail-safe is GUARDED CORRECTLY by construction (fail-closed closures — found rigor; no destination from absent terrain, so the entry-8 candidate is impossible). dest=Some constrains the specimen: gate 1 disfavored (chunk-boundary residue only), GATE 2 (Rider exclusion from both physics joins) now LEADS by elimination. RULED: 5b's do-not-build LIFTS — three additions at the emit site in one commit (in_loaded_chunk decider bool, Rider/VolumeRider bool, d.z-beside-feet.z as downgraded-to-context) + the head_clear is_ok_and one-liner folded in (unreadable must not render as clear). Teleport-up oddity loses its defect story (8-radius scans legitimately return higher nearby surfaces).
- 2026-08-11 (8): **#85 RELOCATED (Opus, triple self-inversion, all on reads): the suspect is now THE ULTIMATE FAIL-SAFE ITSELF** — firing on colonists inside normal chunk-unload windows and teleporting them via coordinates derived from absent terrain (retro-explains the teleport-UP oddity: a ground search against unreadable terrain). Loaded is a LATCH (chunk can unload beneath it); the freeze is BOUNDED (anchor None ⇒ one delete-sweep cycle ≈ the observed 59s); the 59s gap was the mechanism's PREDICTED signature, not a refutation. RULED: build `b5_failsafe_in_loaded_chunk` + target-z-beside-feet-z NOW as 5b fill (in-tree only, zero contact with the running endurance) — presence passing would HIDE this defect, not fix it, so the field guards the guard regardless. Fix-shape pre-noted: a ground search against unreadable terrain must REFUSE.
- 2026-08-11 (7): **ITEM 8 v2 LAUNCHED** (pin 34256b8a2b, driver-disconnect 09:14:08 EDT) — founding verified: 8 promotions, ZERO demotions (presence holding where v1 died). 5-cycle score point ~11:44, 7-cycle end ~12:44; Fable's heartbeat owns the health check and run-end wake. #85 renamed THE UNTICKED COLONIST, demoted-pending to the presence row's regression witness. (Commit 66a09cb0f0 was Fable's SECOND script-edit mojibake churn — restored, real edit here, rule hard-banked.)
- 2026-08-11 (6): **COLONY PRESENCE ACCEPTANCE: CLEAN PASS, all 4 measures** (pin ea2cfa5192) — 16 real eats, 32 preempts, needs cycling, 0 demotions, zero clients in the scored window; v1 honestly downgraded to inconclusive (unset diag). THE WORLD LIVES UNWATCHED, VERIFIED. **ENDURANCE RELAUNCH RULED** — 5b launches now; Fable's heartbeat wakes them at run-end (~2.6h); Opus scores acceptance + fills (Colony Mind design / #85 physics / dossier §5) during the run. GAME-VISION.md written (Ben request). Chartered-rows section added (~60-item honest board).
- 2026-08-11 (5): COLONY PRESENCE LANDED (7b00cce894 + acceptance diagnostic fb9a740110) — server-owned presence keeps founded colonies Loaded with no client; acceptance run next, then the endurance gate re-runs. BEN DESIGN THREAD: the Colony Mind (ant-model: colonists=ants+job-board stigmergy today; a colony-level drive arbiter — Sustain/Grow/Defend/Expand — weighting the generators as the future executive layer; god-interface + RL substrate) — pending Ben's go to charter beside the settlement arc, with a cheap now-rider: colony-level metrics (food-days, housing ratio, threat pressure) accumulate as diagnostics so the mind is born with senses.
- 2026-08-11 (4): Item 4 RE-SCOPED — the suspended case's closure stands (uid=166, n=1 limit honored); ITEM 4b opens on the GROUNDED case (uid=81: valid start, no mutex, destination computed, 6 verdicts, 0 plans, ZERO no_route — the original suspicion in purest form); first step = the verdict-enum producer read; files in the travel row's orbit. uid=109 = genuinely-new dispatch question (mutex absent), chartered-on-second-specimen. Fourth failure mode added to item 8's table (verdicts>0 ∧ plans=0, grounded). Endurance launch proceeding.
- 2026-08-11 (3): **ITEM 7 DONE — the farm economy is SELF-SUSTAINING AND COMPOUNDING** (775a715e2c): 8 founding seeds → 56/56/56 through THREE generations in one ~670s run, zero commands, zero stalls; the live-path wiring bug the first acceptance honestly caught (0/0/0) is documented in the doc as the gate-tests-live-path law's payment. Item-4 successor population now 3 fail-safes across the runs (routes to the suspension row). ITEM 8 IS FULLY UNBLOCKED — the endurance run launches next per the ruled packet.
- 2026-08-11 (2): ITEM 4 CLOSED — third reading (planner correct about an INVALID START STATE: mid-air suspension); suspicion not supported; successor row = the suspension physics + the teleport-up oddity. Arc 1 scoring now complete except item 8; only the founding-stock build + reseed leg remain before the endurance gate.
- 2026-08-11: **THE FULL FARM CYCLE CLOSED LIVE FOR THE FIRST TIME** (9098089c51): 20 sown → 20 MATURE → 20 harvested, 100%, no stalls. Open (named, not claimed): second-wave reseed — 20 new SOW jobs opened, zero claimed in window; script's wait was clocked from first-sow not first-harvest (timing-design gap, not a code finding); follow-up leg clocks from first harvest. #105's founding-seed-stock build is next with that leg as acceptance. Item-4 rider fail-safe count still owed (rescue counts reported twice; the ULTIMATE FAIL-SAFE number is the scoring input).
- 2026-08-10 (17): Item 4 → PATH 2 (candidate log predates the fix — a date that lines up is a coincidence until the artifact names its producing code); live-leg boot commit stamp added to protocol. NOTE: commit 7eb260114c was an accidental encoding-churn no-op (PowerShell UTF-8 round-trip mojibake, caught and restored from ed30a23fb5) — this entry carries the real edit.
- 2026-08-10 (16): #103's derivation LANDED (58cd1e8bef) — threshold = one expression over UNIFIED shared constants (the two happened-to-agree local copies eliminated at root), derived default 930s, test pins the real function; 80/80 green. Pilot fully landed all three stages; item retention alone waits on the identity row. 5b ordered: read accessor (critical path) → farming bootstrap; item-4 log question pending their answer.
- 2026-08-10 (15): Item 3 SCORED (partial-as-registered: sit class eliminated, 9→2 fail-safes, residual = travel class) — item 4 un-parked with a live population. Arm B′ floor GREEN 48-proven zero-caveat (every seed self-attests env now). Pilot gap found: NO READ PATH to the corpus (run-total only — aggregate-late landing on its own escape instrument); accessor ordered first, then the acceptance fan, with #103 commit + farming prep as the fan-window fill.
- 2026-08-10 (14): Seed-69 collision SUPPORTED (1 prune ↔ +1 orphan at exactly 120.0; access→0) — supported-not-proven, no queue-position field exists; the pilot gains its SECOND customer (seed-69 position + mover clustering). Mover population cleans: 5 unexplained orphan-movers, no prunes involved. #103's derived-in-code threshold and re-scope stand. Entity-log stage 1 = the arc's critical path.
- 2026-08-10 (13): ARM 2 SCORED — prediction CONFIRMED (max self-resolved stall 141 ≥ 120): the pruner was cutting recoverable work. RULED: default → 180 (a bound, not a calibration; 287 = the genuine customer), ships with effective-config-to-stdout (third stderr casualty). UNREGISTERED PRIZE: six seeds peak at exactly 120.0 with the pruner OFF — a hidden ~120s releaser exists; open row (producer hunt, moderate priority) — may reframe item 2's value onto the >180 tail. "A data-side config proof only works where the config would have mattered" banked (24 proven / 24 unproven census).
- 2026-08-10 (12): Mover lead's meaning corrected at producer (orphaned-claim detector, not removal witness) — lead sharpened to a COMPOSITION fact (removal ∧ live claimant); #94 reopened on claim-timing footing; prediction registered with can-I-evaluate asked = NO ⇒ ENTITY EVENT LOG PROMOTED to next build, mover prediction = its first acceptance customer.
- 2026-08-10 (11): ITEM 6 DONE — live acceptance passed (pile held 25 min vs 37s pre-fix theft); corpus witness structurally impossible (no scenario creates item drops), dormant sentinels documented. #83 closed (count gate 43, ENVPREFIX, zone probe, effective-config banner — all rehearsed). ARM 2 RUNNING at 9999 with the effective-value-on-VMs check owed before any field is read; registered prior on record (max(R) ≥ 119 expected, over-read owned if wrong).
- 2026-08-10 (10): Specimen diffs name the mover mechanism as an ACCESS-PLAN REGIME change (seed 51: access planning stopped, 37→0 + region blocked; seed 50: doubled + 4x emergency calls — opposite directions = regime, not gate). Item 6 EXITS the candidate set. Next: bisect access-plan commits (free). PRE-REGISTERED reading: if the bisect names item 2's sweep, the movers ARE arm 2's calibration question (does pruning at 120s cost outcomes stalls used to salvage?). FREE HEADLINE: bit-reproducibility measured across zones/hosts/instances — 48 seeds, every scalar identical except stamp+clock; machine-type fallback licensed by data.
- 2026-08-10 (9): Wave34 arm-1 landed via rescue (mislabeled invocation retracted; paired_base = true matched control). Batch INERT on outcomes (12→12, zero clause drift) ⇒ movers are STABLE-TIP properties, not bundle-transient — next step = specimen-seed field diffs (50/51), free. Item-6 witness: correct but scenario-INERT (all counters 0 incl. member pickups) — prediction VOID not refuted; "gate never refuses ⇒ gate innocent" explicitly NOT licensed (upstream suppression); premise-check question to 5b, else fields stay dormant sentinels + live-path verification. stalled_final earned immediately (6 still-stalling seeds, invisible before). Run-invocation-in-header now a required template field. Arm 2 blocked only on #83.
- 2026-08-10 (8): Wave34 arm 1 RUNNING (defaults, pin 7f20a184, witness+stalled_final landed, checker rehearsed pre-data). TWO-ARM ruling: arm 1 = movers/witness/prediction (instrument-only delta); arm 2 = raised BASTION_ACCESS_STALL_SECS on the same pin for the uncensored calibration — corrects the one-fan conflation of landing-vs-answering.
- 2026-08-10 (7): Wave33 addendum — seed-66 tool refusal resolved (REG-1 vindicated on its own named specimen); build signature widened to ≥3 seeds via clause-SWAP; item-6 witness ranked first in batch; colonist-refusal prediction registered for the witness fan.
- 2026-08-10 (6): Item 5 DONE (A/B pass, a35a98aaf7) — driver timing is server-authoritative; the under-run class that voided driver-12 is closed.
- 2026-08-10 (5): Wave33 folded — #38 closed, branch A/option-3 retired by corpus, ACCESS_STALL_SECS provisional (self-censoring instrument), item-6 corpus witness + threshold row approved as one batch/one fan; 5 movers (11→12) PARKED-tracked pending the witness fan (bundle diff refused attribution).
- 2026-08-10 (4): 5b closed both owed items (farming writeup; 54G stale incremental cleanup — E: +54GB) and placed the #73 preflight-graduation note; item 5 in build.
- 2026-08-10 (3): Item 7 re-rooted — seed-supply deadlock (structural); phantom-retire demoted to end-of-run sweep noise (refuted at source + 1.1ms timing window); two independent fixes chartered.
- 2026-08-10 (2): Opus's #102 in-flight audit folded in — nothing exposed; item 16 flagged pre-spec; settlement arc's known inheritance (Region zones) chartered.
- 2026-08-10: Created (rows #93–#102 folded in). Items 1 done, 3 landed-pending-score, 6 in acceptance, 7 blocked-with-coordinate; settlement layer added with immediate rider; thievery moved to 34's family; entity log designed.
