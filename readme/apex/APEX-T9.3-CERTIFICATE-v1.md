# APEX Certificate v1

Generated by `common::apex::certificate::generate_certificate_v1` from real, cited attestations.

## Roots

Present: 2 / 8

- PRESENT `Content` = `sha256:c4df8f5f463df83ecdb95a3222c28d35ff4414f93ae1018c7cbd91cdfa5a2d93` — T3.3 (common/net/src/msg/envelope.rs::net_envelope_profile_descriptor_v1, live-computed)
- PRESENT `Fixture` = `sha256:01d280e7f215f8315b5b1d79c1229e0eaeed2ba591042cb5eca1d72a579a888e` — T3.5.20 (readme/apex/PROJECT-BASTION-APEX-T3.5-COMMAND-IDEMPOTENCY-CANARIES-v1.json, real checked-in fixture corpus)
- ABSENT `Build` (ROOT-BUILD): SourceClosureTreeV1::root() (T1.2, common/src/apex/source_closure.rs) exists and is tested against fixtures, but no invocation against the actual current source tree is checked into this tree
- ABSENT `Plugin` (ROOT-PLUGIN): PluginActivationPlanV1::activation_root() (T2.5, common/state/src/plugin/activation_plan.rs) exists and is tested, but every real instance found is a test fixture; no live activation plan exists to compute it from
- ABSENT `Manifest` (ROOT-MANIFEST): compute_save_universe_manifest_root_v1 (T4.6, common/src/apex/save_universe.rs) exists and is tested, but requires a live, running save-universe with a real committed epoch; saves are runtime-created and not checked into the tree
- ABSENT `Numeric` (ROOT-NUMERIC): NumericProfileV1::id_v1() (T6.4, common/src/apex/numeric_profile.rs) exists and is tested, but a genuine profile needs real rustc_version/llvm_version/dependency_set_root introspection this row did not perform -- fabricating plausible version strings was refused
- ABSENT `Schedule` (ROOT-SCHEDULE): no schedule-identity root computation was found anywhere in the tree
- ABSENT `Output` (ROOT-OUTPUT): no build output (a compiled binary) is checked into the tree or hashed anywhere; computing one requires an actual build invocation outside this row's scope

## Certified properties

Stated: 9 / 10

- `SameTargetReproducibility`: 4/4 covered — T1.3.11 (nix/apex/repro-canaries.nix)
- `PluginPermutations`: 269/270 covered — T2.3 (common/state/tests/apex_t2_3_catalog.rs), T2.4 (common/state/tests/apex_t2_4_catalog.rs), T2.5 (common/state/tests/apex_t2_5_catalog.rs)
- `SixStreamSchedules`: 171/176 covered — T3.4.22 (server/src/net_checkpoint_canaries.rs, live coverage map)
- `CommandRetryCrashReconnect`: 153/163 covered — T3.5.20 (server/src/net_command_canaries.rs, live coverage map), T9.1 premise-check (3cec01a713)
- `PredictionCorrectionRollback`: 27/27 covered — T5.3 (common/src/apex/input_receipt.rs, common/net/src/msg/input_receipt_wire.rs) -- coarse pass, no per-case catalog built, T7.4 (common/systems/tests/reconciliation.rs) -- coarse pass, no per-case catalog built
- `PhysicsWeatherNumericVectors`: 11/11 covered — T6.2 (common/src/apex/numeric_probe.rs) -- coarse pass, no per-case catalog built
- `WorldBaselineEconomyMismatch`: 38/38 covered — T4.3a/b (common/src/apex/world_baseline.rs), T8.1 (world/src/site/economy/context.rs), T8.3+T8.4 (world/src/site/economy/mod.rs)
- `MultiStoreCrashCutpoints`: 11/11 covered — T4.6 (server/src/save_universe.rs, SAVE-001..011)
- `HistoricalSaveMigrationBranching`: 76/78 covered — T4.5 (server/src/save_migration.rs), T9.2 (common/src/apex/save_universe.rs, server/src/save_universe.rs, commit 0f0d440acc)

Structurally absent (zero covered cases): 1
- `CrossTargetExecution`

## Open set (19 cases)

- `CrossTargetExecution` / `CROSS-TARGET-EXECUTION`: no artifact has been executed against two genuinely distinct compiler/target cells; T6.4's NumericProfileV1/verify_golden_vectors_v1 mechanism is proven correct by a same-process perturbation falsifier only, and T8.2 (the economy-specific cross-target lane) has never run -- Fable-deferred as an environment question. One underlying gap, cited from both rows, not two.
- `PluginPermutations` / `EARLY-ASSET-ACCESS`: the global cache remains usable pre-install for the ungoverned legacy path; a governed-process early-access guard is .13+ enforcement, deferred not silently dropped
- `SixStreamSchedules` / `CKPT-036`: resync under a new connection epoch has no code path yet -- the runtime is created per attachment and abandon_v1 is the only reset; a resync row belongs with the T3.5 session-control work
- `SixStreamSchedules` / `CKPT-121`: frontend events are emitted by the ordinary handlers at dispatch time, after commit; they are not separately staged, so there is nothing yet to assert about staged events
- `SixStreamSchedules` / `CKPT-123`: commit currently runs inside handle_messages; its ordering against the client's own State::tick is not yet pinned by a test
- `SixStreamSchedules` / `CKPT-124`: the inverse of CKPT-123, same missing ordering pin
- `SixStreamSchedules` / `CKPT-130`: no post-commit state root is computed or carried yet (bootstrap_manifest_root is always None), so there is nothing to compare
- `CommandRetryCrashReconnect` / `CMD-055`: the outbox has no reconnect handling yet — a boot change should drop pending commands, and nothing enforces that
- `CommandRetryCrashReconnect` / `CMD-056`: same missing reconnect handling for a SessionId change
- `CommandRetryCrashReconnect` / `CMD-061`: same-session reconnect must preserve the pending command; the outbox does not model reconnect at all yet
- `CommandRetryCrashReconnect` / `CMD-062`: an explicit logout should clear the outbox, which needs the same missing reconnect/lifecycle seam
- `CommandRetryCrashReconnect` / `CMD-067`: client-side durable retry state across a process restart is not built; the outbox is in-memory
- `CommandRetryCrashReconnect` / `CMD-116`: ordering the async terminal against the persistence response needs the live character-worker wiring, which is a later live-path row
- `CommandRetryCrashReconnect` / `CMD-125`: the real SQLite migration and its uniqueness constraint are a later live-path row; the reference store fixes what that migration must guarantee, and claims nothing about the schema itself
- `CommandRetryCrashReconnect` / `CMD-136`: results do not carry a SnapshotEpoch yet; the T3.3 causality field is still dormant, so there is nothing to check staleness against
- `CommandRetryCrashReconnect` / `CMD-152`: a restart cannot currently distinguish failed-before-effect from unknown; that needs the durable row to be consulted on boot, which is the live wiring row CMD-125 also waits on
- `CommandRetryCrashReconnect` / `T9.1-STEP2`: continuous-frame classification is genuinely absent -- SemanticStreamIdV1 names streams by role, nothing marks a stream as carrying continuous frames, so the replay rule has no SUBJECT and cannot be enforced or even stated against the present types (T9.1 premise-check, commit 3cec01a713, read-only, no code)
- `HistoricalSaveMigrationBranching` / `T9.2-CLI-WIRING`: restore_branch_v1 is a real, tested, callable mechanism but is not yet reachable from a running server -- no server-cli TUI/argv subcommand exists (banked follow-on, same mechanism/live-trigger split T4.6 itself used)
- `HistoricalSaveMigrationBranching` / `T9.2-STALE-CLIENT-WIRING`: decide_stale_branch_v1/StaleBranchRejectionV1 is a real, tested, standalone decision but is not wired into a live reconnect handler -- T9.1 has not yet built the refusal enum this would be the branch-aware arm of
