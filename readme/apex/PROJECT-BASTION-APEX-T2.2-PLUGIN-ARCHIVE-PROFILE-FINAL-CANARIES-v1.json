{
  "schema": "bastion.apex.t2.2-plugin-archive-profile-final-canaries/v1",
  "generated_at_utc": "2026-07-23T15:46:15-04:00",
  "live_commit": "f7b30de6d916930c96f181919160ff7839aa6d5b",
  "base_canary_drive_id": "1fZgZka8QHsWQUqZAfHN6D8_XteUyK0R8",
  "base_case_count": 50,
  "prior_correction_canary_drive_id": "1nuG54aTUVJ817ttVLkxU63KQAb9yS12h",
  "prior_correction_case_count": 14,
  "final_delta_case_count": 26,
  "total_logical_case_count": 90,
  "cases": [
    {"id":"PAR-C15","expected_terminal":"REJECT-RAW-BACKSLASH","mutation":"A raw UStar name field contains a backslash. StrictCanonicalV1 rejects the raw byte even though tar::Header::path_bytes would treat it as a separator; ObserveLegacy records the transformed-view mismatch."},
    {"id":"PAR-C16","expected_terminal":"ACCEPT-USTAR-PREFIX-NAME-VECTOR","mutation":"A frozen UStar prefix+name header reconstructs the expected canonical path bytes and the independently computed canonical split exactly."},
    {"id":"PAR-C17","expected_terminal":"REJECT-PARSER-VIEW-MISMATCH","mutation":"The repository raw decoder and pinned tar-rs processed path view disagree for the same header; strict admission fails before manifest parsing."},
    {"id":"PAR-C18","expected_terminal":"REJECT-DUPLICATE-RAW-MODULE-DECLARATION","mutation":"plugin.toml repeats the identical module string; Vec-backed legacy parsing preserves both declarations and rejects before execution-order conversion."},
    {"id":"PAR-C19","expected_terminal":"REJECT-DUPLICATE-CANONICAL-MODULE-DECLARATION","mutation":"Two different raw module strings map to one canonical path identity; the distinct alias terminal is emitted."},
    {"id":"PAR-C20","expected_terminal":"OBSERVE-DUPLICATE-RAW-DEPENDENCY-DECLARATION","mutation":"Duplicate dependency strings remain observable without resolving dependencies or silently deduplicating them."},
    {"id":"PAR-C21","expected_terminal":"REJECT-NONREPRESENTABLE-USTAR-PATH","mutation":"A 101-byte single path segment has total length below 255 but cannot fit the UStar name field and has no slash split; strict packing rejects before writing."},
    {"id":"PAR-C22","expected_terminal":"CANONICAL-PACKER-HOST-METADATA-INDEPENDENT","mutation":"Two source trees with different host mode, uid, gid, uname, gname, and mtime produce identical StrictCanonicalV1 tar bytes and roots."},
    {"id":"PAR-C23","expected_terminal":"REJECT-EXPLICIT-DIRECTORY-IN-STRICT-V1","mutation":"StrictCanonicalV1 receives an explicit directory entry. The canonical writer omits directories, so strict admission rejects while ObserveLegacy records it."},
    {"id":"PAR-C24","expected_terminal":"OBSERVE-GNU-NOT-STRICT","mutation":"An archive produced by the documented generic GNU tar workflow is inventoried successfully but cannot receive a StrictCanonicalV1 claim."},
    {"id":"PAR-C25","expected_terminal":"REJECT-OLD-HEADER-IN-STRICT-V1","mutation":"An old/V7-style header with a valid short path is ObserveLegacy-only; StrictCanonicalV1 accepts UStar regular-file records only."},
    {"id":"PAR-C26","expected_terminal":"ACCEPT-USTAR-NAME-100-BYTE-BOUNDARY","mutation":"A legal 100-byte ASCII path requiring only the UStar name field is accepted and round-trips byte-for-byte."},
    {"id":"PAR-C27","expected_terminal":"REJECT-USTAR-NAME-101-BYTE-BOUNDARY","mutation":"A 101-byte single segment is rejected with UstarPathNotRepresentable rather than causing a GNU long-name extension."},
    {"id":"PAR-C28","expected_terminal":"ACCEPT-CANONICAL-USTAR-SPLIT-BOUNDARY","mutation":"A path with a 155-byte prefix and 99-byte name is accepted when the rightmost valid slash produces the canonical UStar split."},
    {"id":"PAR-C29","expected_terminal":"REJECT-USTAR-PREFIX-OVERFLOW","mutation":"A path whose only possible split requires a prefix longer than 155 bytes rejects with UstarPrefixTooLong."},
    {"id":"PAR-C30","expected_terminal":"REJECT-NONCANONICAL-USTAR-SPLIT","mutation":"A representable path is encoded using a different valid prefix/name split than the profile's rightmost-valid-slash rule; strict admission rejects the noncanonical header bytes."},
    {"id":"PAR-C31","expected_terminal":"REJECT-WRITER-PATH-TRANSFORMATION","mutation":"A path containing a dot component would be rewritten by Header::set_path. The repository-owned encoder rejects before writing and never treats the transformed path as identity."},
    {"id":"PAR-C32","expected_terminal":"ACCEPT-FIXED-USTAR-METADATA","mutation":"A canonical regular-file header has mode 0644, uid/gid zero, empty uname/gname/linkname, mtime zero, UStar magic/version, zero device fields and zero reserved padding."},
    {"id":"PAR-C33","expected_terminal":"CANONICAL-PACKER-OMITS-DIRECTORY-RECORDS","mutation":"A nested source tree is packed as sorted regular-file paths only; parent directories are implicit and no explicit directory header is emitted."},
    {"id":"PAR-C34","expected_terminal":"REJECT-STRICT-EXPLICIT-DIRECTORY","mutation":"An otherwise valid UStar directory header is rejected in StrictCanonicalV1 because the v1 byte profile has no directory record form."},
    {"id":"PAR-C35","expected_terminal":"ACCEPT-EXACT-TWO-ZERO-BLOCK-TERMINATOR","mutation":"The archive ends immediately after exactly two 512-byte zero blocks."},
    {"id":"PAR-C36","expected_terminal":"REJECT-ONE-ZERO-BLOCK-TERMINATOR","mutation":"The archive contains only one terminal zero block and rejects with MissingSecondTerminator."},
    {"id":"PAR-C37","expected_terminal":"REJECT-TRAILING-ZERO-BLOCKS","mutation":"A third zero block or any bytes after the canonical two-block terminator reject with TrailingArchiveData; canonical bytes have one exact ending."},
    {"id":"PAR-C38","expected_terminal":"OBSERVE-GNU-LONGNAME-STRICT-REJECT","mutation":"GNU long-name extension provenance is retained in ObserveLegacy and StrictCanonicalV1 rejects before logical indexing."},
    {"id":"PAR-C39","expected_terminal":"OBSERVE-PAX-STRICT-REJECT","mutation":"PAX local/global records are bounded and inventoried in ObserveLegacy and rejected in StrictCanonicalV1."},
    {"id":"PAR-C40","expected_terminal":"SEPARATE-ARTIFACT-AND-SEMANTIC-IDENTITY","mutation":"Two safe legacy archives contain the same accepted regular-file namespace but differ in tar order/metadata. Artifact digests differ, semantic roots match, and neither gains a strict byte-profile claim until canonical repacking."}
  ]
}