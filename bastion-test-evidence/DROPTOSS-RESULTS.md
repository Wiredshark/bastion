# DROP-TOSS RESULTS — the yield fires; the loss is DOWNSTREAM. And my own gap claim falls.

Third attempt (two prior fans VOID: a missing `ENV=` prefix, then a missing
trailing space that glued the plant onto `BASTION_UNCAPPED_TPS`).

## Precondition — PASSES this time

| run | tosses | maturations | tosses / maturation |
|---|---|---|---|
| `k1/twin1` | 6,732 | 1,684 | **4.00** |
| `k1/twin2` | 96 | **24** | **4.00** |
| `k2/twin1` | 200 | **50** | **4.00** |
| `k2/twin2` | 6,088 | 1,539 | 3.95 |

## ★ Q2 ANSWERED — the yield is not the problem

`FARM_WHEAT_YIELD = 2` + `FARM_SEED_YIELD = 2` = **4 drops per harvest**, and the
ratio is **4.00 in the collapsed run too**. Directly on the mid-range run:
**50 harvests → 100 `wheat_seeds` drops. Exactly 2 per harvest.**

⇒ **The registered branch is decided: the conservation invariant fails at
DELIVERY, not at emission.** The seeds are created, in full, in exactly the
quantity the constant promises — and they never reach the stockpile. That is the
premise `SEED-DEADLOCK.md` needed and could not previously observe.

## ★★ AND MY OWN BIMODALITY CLAIM FALLS — the falsifier fires

`BIMODAL-COLONY-PREREG.md` registered: `THRIVE > 500`, `COLLAPSE < 50`, and
**"any run landing inside 50–500 ⇒ BIMODAL WITHDRAWN as a description of the
mechanism."**

**`k2/twin1` finished with exactly 50 maturations.** Not `< 50`, not `> 500` —
**inside the gap.**

| | |
|---|---|
| runs before this fan | 26, gap empty |
| this fan | **4 runs, one of them at 50** |

**So "bimodal" is withdrawn as a mechanism claim.** The gap survived n=14 → 20 →
26 and fell at n=30 — which is exactly why the threshold was registered *before*
the data rather than fitted to it.

★ And it fell to a fan I ran for a **different question**. The falsifier did not
need its own experiment; it needed only that the bar was written down first and
that I actually checked it against every run rather than the ones I was looking
at.

## What survives, and what this changes

**Survives:** the outcome is still sharply *split* — 24, 50, 1,539, 1,684 in this
fan — and the two clusters are real. What is withdrawn is the claim that the
**gap is empty**, i.e. that the mechanism is genuinely binary.

**Changes:** a run at 50 is consistent with the **constant-hazard** model — escape
happening late, leaving little time to produce. That is what `endurlong` (2×
length) was built to test, and it is now **predicted** rather than open: if
constant-hazard is right, the 570k-tick runs should show **more** mid-range
outcomes, not fewer.
