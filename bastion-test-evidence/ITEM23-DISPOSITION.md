# Item 23 (Morale events) — DISPOSITION: display bar **PASS**, live-emitter bar **BANKED**

Leg: `thoughts2` (chain9, 2026-08-20). Emitter = the `bastion_thought`
command driving `bastion_deposit_thought` (the hook's own doc blesses the
framing: synthetic EMITTER, shipping CONSUMPTION path — queue → chronicle →
mood recompute → inspect payload → MOODX).

**Display bar — PASS on every registered branch:**
- Osric (uid=3), SleptInBed deposited: `MOODX thoughts=1 [(54, 0.08)]`,
  total 0.6847 = base + the weighted term; next sample 0.6788 — DECAYING.
- Awen (uid=4), CaveIn deposited: `thoughts=1 [(50, -0.2)]`, total 0.3844,
  recovering to 0.3910 as the thought fades.
- Magnitudes are the RON table's exactly (0.08 / −0.2) — the same asset
  gameplay loads; no display-side constant exists to drift.
- The six un-deposited colonists: total=0.6000 flat, thoughts=0 — the
  in-leg control (20 samples).

**Live-emitter bar — BANKED with its blocker named:** organic thoughts sit
behind owned-bed sleep (bed builds starve on the colony-wide materials wall
— item 27's row) and cave-ins (needs a collapse fixture leg). Two failed
fixture shapes are on record: normal decay never dips rest in a short leg;
`BASTION_NEEDS_DECAY_MULT=20` collapses needs before any bed exists (388
preempt claims, 0 bed jobs claimed — the fixture defeated its own
precondition, disclosed in the queue). The bar re-runs on the first leg
where a bed completes or a collapse fires.
