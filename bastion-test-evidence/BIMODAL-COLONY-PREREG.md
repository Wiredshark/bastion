# BIMODAL COLONY OUTCOME + THE ANCHOR FIX'S DEMONSTRATION — pre-registered

> ## ★★ SUPERSEDED IN PART (2026-08-19): **the gap is NOT empty.**
> A later run finished at **exactly 50 maturations** — inside the registered
> 50–500 band — so the registered falsifier fired and **"bimodal" is withdrawn
> as a description of the MECHANISM**. The two clusters are still real; the claim
> that nothing lands between them is not. See `DROPTOSS-RESULTS.md`.
> Everything below was true of the corpus as it stood (n=26) and is kept intact.


Written before launch. Two independent questions on one 6-host fan.

## Arm `p1` — `anchorplant`: the red demonstration the fix still owes

The anchor-fix fan scored **VOID for demonstrating the fix**: bar A passed 6/6,
bar B (*"the fix was NEEDED"*) was satisfied by **none** — `Pos` arrived at tick
47-48 every time, inside the old fixed 60-tick spin. So the plant
(`BASTION_PLANT_POS_DELAY=90`) forces the case rather than waiting for it.

| outcome | verdict |
|---|---|
| driver logs `OLD BEHAVIOUR WOULD HAVE ANCHORED AT THE WORLD ORIGIN` **and** anchors at the colony (promoted class 304, no origin block) | **fix DEMONSTRATED** |
| driver anchors at `(0,0,0)` | **the fix does not work — I withdraw it** |
| no `PLANT ACTIVE` line | plant inert ⇒ arm **VOID**, not green |

## Arms `b1`–`b5` — the bimodal split, n=10 new + 4 banked

Four identically-seeded `endurseed` runs gave maturations **1,948 / 1,325 / 26 /
8**. That reads as two regimes rather than a spread, but it is n=4 and "bimodal"
is a strong word for four points.

**Registered threshold, chosen from the banked gap before any new data:**
`THRIVE` = maturations **> 500**; `COLLAPSE` = **< 50**. The banked runs leave
50–500 empty.

| outcome | verdict |
|---|---|
| all 14 runs land **outside** 50–500 | **BIMODAL CONFIRMED** — two regimes, and the gap is the finding |
| any run lands **inside** 50–500 | **BIMODAL WITHDRAWN** — it is a continuum and my four points were the tails of one distribution |

★ This is a real falsifier: a single mid-range run kills the claim, and with 10
new runs a continuum would very likely produce one.

## Exploratory, declared as exploratory

**Where does the regime get decided?** With ≥14 trajectories I will look for the
earliest tick at which THRIVE and COLLAPSE runs separate on `food_stock`. No bar
is registered for this — it is a *location* question, and I will report the
separation tick as an observation, not as a mechanism.

## Preconditions, printed above every verdict

1. Both twins boot and carry a terminator; `rc=124` on collection is **not**
   a failed run — the previous endurseed timed out *after* reaching its target.
2. Colonist name-list hash must be `d6a994cb04e6` on every run. A different hash
   means a different founding RNG and the run leaves the comparison.
3. `crop MATURE` must be countable in each log — an absent token is VOID, not 0.
