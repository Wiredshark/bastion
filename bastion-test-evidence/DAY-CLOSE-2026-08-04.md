# DAY CLOSE — 2026-08-04, Opus review/verification lane

**Read-only session. Zero VM minutes. Zero fan runs. One local build (auton) +
9 local scenario runs.** Every finding below came from evidence already on disk
or from reading code at the pinned tip `460626a6e2`.

**Filed here rather than in `readme/` per the tree split** (Opus owns
`bastion-test-evidence/`, Fable owns `readme/` + memory). Full working for every
line is in `SCENARIO-MAP.md`.

---

## 1. Statuses closed

| item | outcome |
|---|---|
| **Scenario-map re-count** | **COMPLETE — zero `NEEDS-REVERIFY` rows remain** |
| **auton** | **GREEN** at `460626a6e2`; 14/14 conjuncts; prerequisite `storm_baseline_captured` engaged; **3/3 run stability**; residual log variance named (boot UUID + temp datadir, non-sim) |
| **Task #34 (carve-cascade)** | **PERMANENTLY PARKED with a number** — cascade probe fires on **1 of 48 seeds (59), and 59 PASSES**; matched pair impossible in principle |
| **Seam-bar row** | **DOWNGRADED** (DECISIONS #52) on my own refuted prediction |
| **Farm's blocked A/B control** | **UNBLOCKED** — the mystery it waited on is solved |

## 2. Findings ledger

**Corpus reads (no runs):**
1. `offline_reachability_probe` is **sound for NEGATIVES only UNDER THE BODY-WIDTH MODEL** — bed's positive is void. ⚠️ **SUPERSEDED IN PART by #24: the asymmetry does NOT transfer to the column-collapse model, so "seed 80 strengthened" is WITHDRAWN and is now conditional on its site being single-layer.**
2. **Colony-global one-plan access bar** fixed at the descent caller, **still live at self-rescue**; codebase calls it a starvation bug at the sibling site; F3 pruner is its absorber
3. **Seeds 52/54** = free deterministic repro of the offline/live split (10 entries, identical across two independent fans)
4. **"12/48 failures" is ~5 modes**, not 12 defects
5. **Pair rule refined** — 3 of 4 pairs redundant; `mine_cleared`/`mine_blocks_mined` hides a **19–96% spectrum**
6. **Async-tail hypothesis REFUTED** — 61/90 bit-identical across 8 VMs ⇒ deterministic, therefore real
7. **Four mine failures, four distinct signatures** — all sitting unread in `mine_cell_diag`
8. **Four chop failures, four distinct signatures** — 80 unreachable **(conditional — see #24)**, 85 vantage-sensitive, 92 UNKNOWN (probe at cap), 78 unexplained
9. **Instrument depth by mode** — build is the **largest** mode (6/12) and has **zero** diagnostic fields; every build field is constant across 48 seeds
10. **Task #59 starvation hypothesis SUPPORTED 6/6** — ratio 1.000 ×5, 0.938 ×1, **registered kill case absent in every seed**
11. **Recursive schema**: 161 leaf paths, only 68 top-level — **93 invisible to a name scan, 69 of them varying**
12. **Seed 66 is instrument-suspect** — `tool 0.0` is a `.unwrap_or` sentinel **below the metric's own documented 1.0 floor**
13. **`b5_55_*` family inert** (constant across 48); **`drift_events` non-discriminating as documented**
14. **Instrument sensitivity measured** — the corpus HAS moved (14→16→12→11); waves 19/22/23/24 byte-identical across four commits is **neutrality at full resolution**, stronger than recorded

**Live re-run pass (8 of 9 reds, at the pinned tip):**
15. **`run`** — **14.07% vs a 15% bar**; not "running isn't faster"
16. **`auton3`** — **one score component reads 0.0**; components 2–3 match exactly, model proven
17. **`farm` SOLVED** — 8 of 9 cells tilled; `sown` downstream; **`growth_rose` UNSATISFIABLE** (`g1=15`, needs `>15`, loop breaks at `>=15`)
18. **`farm` till count = 9 → CLASS (a)**, job created / work never performed; missing cell **is a corner**, but **not** the probe corner
19. **`b55` UNINTERPRETABLE** — `remainder_progressed`'s baseline is never emitted
20. **`path` SATURATED** — `peak_tick_iters == cap == 3000`; `cap_held: true` reads as reassurance and means *pinned at ceiling*
21. **Seed-68 AABB** — **63× cells-per-tree outlier** (30.0 vs a 1891–2048 band); **splits** the two `ch_mixed` reds rather than uniting them
22. **Cross-scenario pattern** — colony work stalls **one unit short** in ≥3 places ⇒ **Ben's stop-bisecting directive fires**

**Infrastructure / provenance:**
23. **wave21 was mislabeled** — filename said `ed532c600e`, attests `1bf3ab2e` (docs-only ahead); sole mismatch of six; **renamed**
24. **`+dirty` across all waves CLEARED** — LFS noise, code clean via `reset --hard`; the guard's safety lives in its *environment*
25. **Corpus evidence was on ONE DISK** — `wave19_FULL.json` untracked, and **the raw attestation logs were gitignored** by a blanket `*.log`; fixed with a scoped negation (DECISIONS #50)

## 3. Deliverables authored

- **`readme/INSTRUMENT-PER-ATTEMPT-RECORD-spec.md`** — one grant site, seven release sites, pre-stated acceptance (**distinguishes 71 from 66, or the null is reportable**)
- **`readme/ARB-STARVATION-PRIOR-ART.md`** — gate (b); **splits shape A (lost the comparison) from shape B (never evaluated)** before surveying either

## 4. Three clean specimens — attested at the merged tip `d3235e5329`

| mode | seed | why clean |
|---|---|---|
| **build** | **62** | only build failed; `rescue_fired: false` — the sole unconfounded Mode-A seed |
| **mine** | **90** | 3 cells, all claimed by named colonists, nothing blocking, zero progress — **and its zero `unreachable` is structurally real** (claimed cells never enter the enclosure branch) |
| **chop** | **78** | path exists from spawn (complete, 228 cols), nothing blocking, `log_sum: 0` |

## 5. Self-corrections — SEVEN, all caught before they cost anything

| claim | fate |
|---|---|
| "`plan_access` models a capsule" | **WRONG** — construction planner |
| "zero `probe_incomplete` corpus-wide" | **WRONG** — a type guard dropped every chop probe |
| "corpus has zero access-plan visibility" | **WRONG** — nested under `cascade_probe` |
| "a seam-row green would be a FALSE GREEN" | **too broad** — the corpus demonstrably moves |
| "seed 71 improves if the bar is removed" | **REFUTED** — it emitted 3 plans; never starved |
| "`unreachable` discriminates 71 from 66" | **WITHDRAWN** — it means "six faces solid", downstream of dig progress |
| "the point-model STRENGTHENS seed 80" | **REVERSED** — true for body-width, false for column-collapse; a soundness direction belongs to the ERROR MODEL, not the instrument |

> **Three of six were the same error — substituting a NAME for its CONTENT —
> twice after writing the rule against it. The tell is SPEED: it happens on the
> promising thread, never the careful one. The countermeasure is ORDERING: read
> the definition BEFORE building on it.**

**And one that would have been a seventh:** `times_offered` is actually
`claims_by_pos` — **claims granted, not offers made.** Caught by reading first.

## 6. Claims CHECKED that HELD — recorded so the ledger has both columns

- Task #61's *"the corpus's only genuinely-unreachable chop case (seed 80)"* — **holds**; 85 is reachable-from-colonist, 92 unknown, 78 unblocked
- `vm-pool.sh`'s *"+dirty = LFS noise, code clean via reset --hard"* — **holds**
- `drift_events`' documented non-discrimination — **holds** (failing seeds span 5–17 against a 4–17 range)
- The server-cli isolation memory's scope line — **held 13 days later**, and stopped me "correcting" a memory that was right

## 7. Queued behind 5b's binary

**Single-seed diagnostics batch (5 items, no fan):** per-attempt claim traces on
**71 and 66**; extended-window runs for **61 and 90** (slow vs stalled); raised-cap
probe for **92** (UNKNOWN → known).

**Report-fix backlog:** `growth_rose` watch-precondition · the **unsatisfiable-watch
sweep** · `b55`'s unemitted baseline · seed 66's sentinel · `b5_55_diag` ·
constant build/b15 fields · probe container normalization.

## 7b. LATE ADDITIONS (post-ledger, same night)

**23. Unsatisfiable-watch sweep** — 5 baseline-relative watches in the harness
(667 loops scanned, 55 flag-and-break, 5 baseline-relative). **Only ONE emits
its baseline — and it is the only one we could diagnose.** `b55`, `b73` ×2 and
`chokepoint` emit nothing; two share b55's count-floors-at-zero shape.
**`b73` is a tracked EXPECTED-RED whose fingerprint rests on two of them** — not
wrong, but currently *unverifiable*, which is a worse status than expected-red.

**24. ★★ THE CAPSULE ASYMMETRY DOES NOT TRANSFER — my morning claim REVERSED
for a second error model.** `column_height_near` returns the **highest** solid
in ±60, so a passage under an overhang is invisible. **Body-width errs
permissive (negatives sound); column-collapse errs BOTH ways (negatives
UNSOUND).** In multi-layer terrain **neither direction is sound**, and I told
two lanes seed 80 was *strengthened*. **It is conditional on its site being
single-layer** — one more column scan on the batch. #61's parking survives
regardless on `n=1, wrong polarity`.

**25. Spec correction (5b caught it)** — "seven release sites, each stamping its
own reason" was a **structure I asserted without reading the consumer**: one of
the seven is a `to_release` drain fed by **26 producers**. Corrected to
carry the reason at the push, two steps, **step 1 alone answers the A/B split**.

**Two more process traps hit and recorded:** the **ANSI zero-count** (a literal
`sow=false` grep returns 0 because colour codes sit inside the field value — the
documented run-11 trap, hit anyway, caught by printing raw lines) and a
**`git commit` without `-C`** that ran in the wrong worktree and reported
"nothing to commit" for a repo I wasn't asking about. **Both go in the analysis
preamble: strip ANSI before any count-grep; `-C` on every git call in a
multi-worktree session.**

## 7c. SPECIES-DECIDING READS (architect-assigned, closed same night)

**26. `run` is a REAL movement gap** — `TRAVEL_SPEED 0.8` / `RUN_SPEED 1.0` ⇒
**design intent 25%**, clause bar 15%, **measured 14.07%**. The bar sits 10
points BELOW design, so it is not testing instrument overhead — the
structural-unclearability hypothesis is refuted, **and my own afternoon
"threshold conversation" reclassification is REVERSED.** N-run check: **4 runs,
every digit identical ⇒ deterministic, not noise.** Three eliminations, one open
question: real speed-factor gap vs deterministic measurement overhead —
**decided by lengthening the window**, which is the instrument seeds 61/90
already need. **Batch → 7 items.**

**27. `auton3` is a MODEL gap, not plumbing** — both `last_scores` write sites
read; the observed second component matches the ordinary path, where **all three
are written at the same site**. Row shrinks. **Corrected my own phrasing:**
component 2 is a constant here and carries no information; only component 3
discriminates. **NOT established** whether the engine's `0.0` work-urgency is
correct-for-situation or a real gap — fixture-vs-game triage first.

**28. Latent misread filed** — site 8641 **hardcodes `last_scores.0 = 0.0`** by
design (*"Flee fired on the signal, not on the score"*). Any consumer reading
that field as "the work urgency" is wrong on flee-preempt ticks. **The comment
names its case at the WRITE site; nothing names it at the READ site**, and UI-4
is a named future consumer. **A correctly-scoped comment does not protect a
reader who never reaches it.**

**29. ★★ `auton3` TRIAGE CLOSED — FIXTURE DEFECT, reversing my own "model gap"
call.** Harness `predict` hardcodes the work-urgency input to `0.5`; the engine
gates it on `work_sig = active_jobs.contains(entity) || work_available` with
`URGENCY_WORK = 0.5`. **The constant is right, the GATE is what the harness
omits** — so the engine recorded `0.0` correctly and the harness asserts 0.5
unconditionally. **Two of twelve corpus failures are now instrument-suspect
(66's sentinel, auton3's ungated prediction) — tracking the 10.4% prior almost
exactly.**

> **★ And the rule we both used was incomplete.** *"Same write site ⇒ model gap;
> different site ⇒ plumbing"* has a **third branch: same site, computation
> RIGHT, PREDICTION wrong = fixture.** The site-location test separates plumbing
> from computation; it cannot separate a wrong computation from a wrong
> expectation. **A two-branch rule is a sufficiency claim that two is all of
> them** — and marking a claim UNREAD catches unread *symbols*, not an
> unexamined *rule*.

## 7d. FIRST MEASUREMENTS FROM THE NEW INSTRUMENT (5b's counters, 3 seeds)

**30. Seam row's un-park condition NOT MET** — 52/54 made **17 self-rescue calls,
0 emissions, 0 starvation**; 71 never engaged the path at all. **Those rejections
are genuine `plan_access` refusals, not non-calls.** The row is now **parked on
evidence rather than on absence of evidence.** ★ And `access_pending_true_ticks`
488 / 379 with zero starvation gives a state nothing could previously express:
**armed but never fired — a latent hazard measured as latent.**

**31. The instrument is CROSS-VALIDATED, not merely present** — seed 71's new
`emergency_emissions: 3` reconciles exactly with the corpus's pre-existing
`cascade_probe.access_emissions_max: 3`. **Field-presence guard upgraded from
"do the fields appear" to "do they agree with what we already had."**

**32. Seed 71 `self_rescue_calls: 0` — access EXCLUDED by direct measurement.**
15 of 27 cells unclaimed all run, self-rescue never engaged, its 3 emissions
entirely emergency-side. **Independently corroborates #59's
arbitration-starvation reading from a different instrument with no shared
assumption.**

**33. `run`: I walked back my own hypothesis (b).** Both speed samples are
structurally identical and **each is preceded by a warm-up that exits after >2
blocks of movement** — so **acceleration ramp is already excluded and path
curvature cancels in the ratio.** Both mechanisms behind "deterministic
measurement overhead" are controlled for by the harness. **Prior moves strongly
toward a real speed shortfall.** Residual named: warm-up exits on a threshold
checked every 5 ticks, so sample start can differ by ≤5 ticks of phase.

**34. Wrong-window catch, sent before implementation** — `run_scenario` has **two
loops that both use 45**; the batch item needs the *displacement sample*, not the
CarvedStair settle loop. **And the flag must reach the `/ 45.0` divisor as well
as the `tick()`, on both sides**, or a longer window silently deflates the rate
and yields **a wrong number that looks fine.**

## 7e. MERGE — engine tip `460626a6e2` → `cf5757e31b`

**35.** Lean pass clean on four checks; **verified a TRUE fast-forward before
pushing and confirmed the remote after** (merge commits do not reliably
auto-push). `in_game.rs` — the only out-of-scope file — checked out as the
`benched_since_tick` wiring with unchanged filter semantics. **Field
completeness structurally guaranteed** (no `Default::default()` spread, so the
compiler rejects a missed site). **26 `ReleaseReason` push sites, zero bare
entities**, and 5b wrote the named-case rule into their own enum comment
unprompted.

**36. GAP: batch item 7 still has no instrument.** `--b5-settle-iters` and
`--ck-settle-iters` landed; **neither is the `run` displacement sample.** One
flag must drive **four** places — both `tick()` calls and both `/ 45.0`
divisors — or a longer window **silently deflates the rate.** Batch runs at six
until it lands.

**37. My eleventh correction, caught by the builder pre-build:** I placed both
45-loops in `run_scenario`; the CarvedStair one is in `chokepoint_scenario`.
**Substance held, location claim didn't.** *Second error of mine 5b caught by
reading rather than trusting — the READ/UNREAD protocol is doing more work
running UPWARD than downward.*

**38. Engine `cf5757e31b` → `a85dec2912` — batch item 7's instrument.** Gap
closed within one message of being flagged. **All four sites verified by reading,
zero literal `/ 45.0` divisors left.** ★ I specced *"one flag, not two"*; the
implementation is **ONE SHARED LOCAL**, so the walk and run windows **cannot
drift apart by construction.** Same shape as the absent `Default` spread —
**when a correctness property moves from convention into the control flow, it
stops being a review item forever.** Two branches, two instances, both
unprompted. Filed not blocking: `--run-sample-ticks 0` → `inf` → `ran_faster`
false, *a garbage result shaped like a clean fail*, in the very flag built to
avoid one.

**Batch: SEVEN items, all instrumented. Sequencing set — field-presence AND
agreement re-verify at the attested tip FIRST, then all seven. No measurement
spent on an unverified build.**

**39. `BATCH-RUNBOOK.md` written** — the seven items executable without
re-deriving anything: attest-first (`RUSTC_WRAPPER=""` with its reason
attached), presence **AND** agreement with the `members_seen` inequality placed
where it will be hit, all seven with both branches pre-stated, and the day's
reading discipline compressed into five operational lines. **Added on my own
judgement: item 7 must run the DEFAULT once in the same session** — "byte-
identical when absent" is a composition claim, we verified the *code* at four
sites but never the *built binary*, and one run stops it being a claim.

**40. Second-pass corpus screen — 114 scalar paths, three-part result.**
**(a)** The top signal was an **artifact**: `ch_rings_tried`'s FAIL 3.33 vs PASS
15.72 is four outliers on a skewed distribution; medians are near-identical.
*I checked only because the direction surprised me — had it pointed the expected
way I'd have banked it.* **(b)** The outliers are the real finding and it is a
**claim that HELD**: seeds 55 and 63 search all 121 rings, find **zero trees**,
and the harness correctly reports `ch_oracle_class: precondition_unmet` /
`ch_engaged: false` — the falsifier-asserts-own-precondition machinery working
as designed, on exactly 2 of 48. **(c)** But vacuity is **detected and not
propagated**: those seeds still count as full passes, so **the effective CHOP
denominator is 46, not 48** — which sharpens my own earlier "wave20 is Chop-only
in practice." Backlog fix: **propagate `ch_engaged` into per-clause denominators
so "passed" and "exercised" stop being the same number.**

**41. ★ EXERCISED DENOMINATORS computed — 5 of 12 families are NOT out of 48.**
`tool` 47 · `chop` 46 · **`access` 34** · **`cavein` 8** · **`cascade abort` 1** ·
`Gather` 0. **Every one had been quoted as a fraction of 48.**

- **`access` 34/48 is the seam row's own denominator** — fourteen seeds never
  emit an access plan, so "zero starvation on 3 seeds" is a sample from 34.
  Doesn't weaken the conclusion; **names the population**, which nothing did.
- **`cascade abort` 1/48** restates #34's parking as *a denominator of one* — a
  matched pair is impossible because the exercised set has one member.
- **★ `tool` 47/48 validated the method**: the 48th seed isn't a tool failure,
  its factor was **unreadable**. **A denominator computed from "is this
  readable" surfaces instrument defects for free**, re-discovering seed 66's
  sentinel from a different direction.

> **Not a wrong number anywhere — a denominator nobody stated, which made five
> correct numerators mean something other than they appeared to.**

## 7f. FIRST BATCH RESULTS + three corrections of mine

**42. Re-verify PASSED** — `emergency_emissions: 3 == access_emissions_max: 3`
exactly as predicted, `starvation_cycles` matched, all 9 fields present. **The
frozen table held on first contact.**

**43. ★ `unreachable` has THREE writers; I characterised it from ONE, twice.**
Churn-release (11280), self-rescue `plan_access`-None (12719), enclosure
(15418). **Surfaced as an impossibility: 7 of 9 cells on seed 71's TOP layer
flagged**, which cannot happen under the enclosure reading. 12719 is excluded by
5b's `self_rescue_calls: 0`. **If 15418 is the writer, seed 71's volume is
BURIED and it is a FIXTURE defect, not arbitration starvation — taking #59's
strongest evidence with it.** ★ **One terrain query decides it; item 1's
conclusion is HELD.** *Corrected rule: enumerate EVERY writer before
characterising a field — "read the definition" doesn't say "read them all."*

**44. ★ SHAPE C — my prior-art dichotomy was incomplete AGAIN.** 5b's trace:
frontier cells were **attempted 2–3 times each** — they won selection repeatedly
and failed anyway. **A: lost the comparison · B: never evaluated · C: selected,
attempted, rejected downstream — and neither fix family touches C**, because
both make a cell *win selection* and these already win. **Second two-branch rule
of mine today with a third branch.**

**45. ★★ UN-WITHDRAWN: phantom jobs on mined cells are REAL.** 5b re-derived it
from two independent measures (seed 66: terrain scan 27/27 cleared, board count
10 live jobs, same moment) and **task #57 documents the exact pattern.** I found
it, flagged it, then **withdrew the conclusion along with its flawed argument.**
> **A flawed argument for a true claim needs RE-DERIVATION, not retraction** —
> and this is the costliest error class here, because **a withdrawn true finding
> leaves no trace to come back to.**

**46.** Runbook corrected: `no_overflow` has a persistent `num-traits`
build-script failure → `dev` (profile doesn't affect hash attestation); and the
reconciliation table's starvation figures relabelled as **worst-cell**, not
per-cell constants. **Both flagged by 5b rather than silently worked around.**

**47. ★★ STEP 2 of ARB-ATTEMPT-01 became LOAD-BEARING — the spec was correct
when written and insufficient without changing.** Shape C's discriminator is the
**RELEASE REASON**, not the attempt count: **A carries timeout/preemption
(competition); C carries stance/material/work-start (gates).** Both show
attempts that fail. **So a trace with only `ReleaseReason::Other` cannot answer
the question it now exists to answer.** ★ **And the stakes are OWNERSHIP** — if
the shape is C, the fix lives at the rejection site and the row moves off
arbitration entirely. Scoped so it stays an afternoon: **classify only the
producers that actually fire on 71/66.**

> **A spec's sufficiency is DATED, like any other sufficiency claim.** This one
> didn't rot — the question moved underneath it.

**48. The shared error on seed 71's elevation, and the sharper half of it.** The
architect rated it *"the strongest single piece any row holds"* on a reading
whose foundation was my single-writer characterisation, with the dependency
unrecorded — **a conclusion's strength rating should carry its weakest
premise.** ★ **My half is worse in one specific way: the characterisation was
marked READ with lines cited.** **A one-site read of a three-writer field is an
UNREAD claim wearing a READ mark** — worse than an honest UNREAD, because it
*disarms the protocol that would have caught it.* **The mark must mean "all
writers enumerated," or it is a false credential.**

**49. ★★ SPECIMEN SELF-AUDIT — seed 90's zero `unreachable` downgraded from
structural to empirical, caught BEFORE anyone built on it.** Having corrected
`unreachable` twice, I had **stopped at the correction both times**; the third
step is sweeping back through every claim that used the old reading. It found
one: I'd justified a specimen's defining fact with *"claimed cells never enter
the enclosure branch"* — true of that branch, but **writer 12719 fires precisely
ON claimed jobs** (`carve_requests` loops over ACTIVE jobs).

> **A semantics correction has THREE steps: fix the reading, fix the record,
> re-audit every dependent claim.** Most people do the first two; the third is
> where a wrong specimen gets caught before it becomes a row.

★ **And the corrected reading SHARPENED the specimen:** writer 12719 needs a
*stuck* colonist, so *"claimed and unflagged"* now implies **"claimed, NOT
ascent-stuck, still not progressing"** — stronger than what it replaced. **Seed
90 stands as the cleanest mine specimen; only my justification was inflated.**

**★ The session's real cost curve, same error class throughout:** retraction
after publication → a paragraph when flagged-not-claimed → **a pre-emptive
self-audit.** *The error rate never improved. The containment did* — and only
because each stage was made mechanical rather than attentive.

**50. ★★★★ TERRAIN QUERY PARTITIONED the evidence — and the burial signature
screens to THREE seeds.** 5b's scan: of seed 71's 7 flagged top cells, **3 are
buried under a tree trunk** (enclosure writer) and **4 are genuinely
attempted-and-rejected** (churn writer). **#59's frontier-stall evidence
survives on the 4, loses the 3 — re-scoped, not voided.**

★ **Structurally, the query neither confirmed nor killed the hypothesis: it
PARTITIONED the evidence — and that was only possible because the flag's writers
had been enumerated first.** The all-writers discipline paid off **within an
hour of being adopted, on the field that motivated it.**

**Corpus screen (free, from disk) on `is_top_layer AND unreachable`:** hits on
**54 (3 cells), 61 (1), 71 (7)**; clean on 52 / 66 / 90. **A candidate marker,
not a verdict** — seed 71 proves the same flag has two writers.

★★★ **Seed 61 is one cell and one query from removing a row:** if
`[24516, 26191, 165]` is solid, its "one block short" is a **worldgen
obstruction, not a stall** — **61 drops out of the one-unit-short family
(→ two members), Ben's ≥3-places directive may no longer fire, and batch item 3
becomes pointless** because no window length mines a buried cell. **Query
ordered BEFORE the window run.**

★ **A fixture class, flagged not asserted: two fixtures in two days place
geometry without checking the terrain they place into** — bed's plateau missed a
cavity BELOW, b5's mine volume misses obstruction ABOVE. One confirmed instance
each.

**51. ★★★★ "Does seed 71's burial gate its interior?" — answered NO, from disk,
before the query it was queued behind.** Buried columns and open columns are
**identical** below the frontier (`z=602`: `[0,0,0]` vs `[0,0,0,0,0,0]`), so the
tree accounts for its three cells and no more. ★ **Two independent fields stayed
consistent:** the `unreachable` flag propagates down the buried columns (3/3
under the tree, 0/3 under open sky) while attempts are uniformly zero — *flag
tracks geometry, attempts track dig order*, and 5b's terrain scan agrees with
the corpus flags cell-for-cell.

★★★ **And it STRENGTHENS #59 rather than eroding it: all NINE frontier cells
were attempted 2–3× and the layer never cleared, including two never flagged at
all. Burial explains 3 of 7 FLAGS and none of the STALL.**

> **★ My own error inside a correction: I re-scoped #59 on the FLAG count when
> the flag was never its evidence — shape A is about ATTEMPTS, and there are
> nine.** Same family as building a mode map from pass/fail labels while the
> counts sat in the same JSON. **Narrowing honestly is right; narrowing on the
> wrong quantity is still wrong.**

**52. ★★★★ BURIAL QUERIES COMPLETE — seed 61 is a REAL STALL.**
`grid_solid_cells: []` at z=165, `real_ground_top_z: 164` — the mine cell IS the
exposed topmost natural block. **The family holds at three, Ben's ≥3-places
directive stands, and batch item 3 is a genuine measurement.** Seed 54: mixed
like 71 — of three flagged cells (all `x=26660`), **two open / one under two
extra rock layers.**

**Candidate arithmetic corrected: 11, not 14** (71:7, 54:3, 61:1) → **7 real,
4 buried, 7/11 = 64%.** *The ratio 5b reported was right and the counts were
not — 11 vs 14 is a 27% error in the base, which is exactly the denominator
lesson turned on our own screen.* **Conclusion survives: informative, not a
standalone filter — so screen-then-query carries "the query is not optional."**

★★ **Seed 54 independently reinforces shape A:** six top cells, **all attempted**
(2/3/2/2/2/2), only three flagged, layer never cleared at 16/27. **Two seeds now
show flags and stalls are INDEPENDENT** — second worked example of *a re-scope
operates on the quantity the claim rests on, not the quantity that moved.*

**53. ★★★★ FARM'S CORNER TILL — the convergent question answered for farm's
leg, from a log already on disk.** Job 6 was **created, CLAIMED FOUR TIMES by
colonists at FOUR DIFFERENT POSITIONS, and released `"job unreachable"` every
time.** Class (a) **with a named rejection reason** — the exact datum step 2 is
being built to capture, which farm has been emitting all along.

- **Same shape as the mine frontiers** — repeatedly claimed, repeatedly rejected.
  **The three family legs now share a SIGNATURE, not just a symptom.**
- **The rejection is about the TARGET, not the approach** — four spread-out
  vantages all failed.
- **`tilled pos` appears exactly 8×**, independently confirming `tilled=8`.
- **Farm jobs are `OnTopAlways` and the enclosure test is Mine-gated**, so a farm
  job's flag comes from writer 11280 or 12719 — *not* the enclosure test.
  *(READ: the Mine gate. UNREAD: which writer, and why a flat plot's corner
  reads unreachable from four vantages.)*
- **Transient vs persistent, third instance:** job 7 took one such release and
  **succeeded**; job 6 never recovered — the same split 5b drew between seeds 66
  and 71, now in a third subsystem.

> **★ The pipeline read no longer waits on three unknowns — it waits on ONE
> COMPARISON:** do the mine frontiers' release reasons match farm's
> `"unreachable"`? **Same ⇒ one defect in three costumes. Different ⇒ farm is a
> point bug and the mine stalls are shape-C variants.**

## 8. The day's method claim

> **The corpus is UNDER-READ, not under-collected.** Twenty-two findings came off
> disk and off the existing binary. The answer-machine for task #59 was built,
> ran in six fans, and was read for the first time tonight.
